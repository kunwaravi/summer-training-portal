// ============================================================================
// CADDED Software (Civil/Architecture) — Deep GfG-Style Curriculum (issue #93)
// ----------------------------------------------------------------------------
// Hand-written, original content replacing the machine-generated templates.
// Covers AutoCAD Civil, 3ds Max, SketchUp, and Revit (Structural + Architecture)
// over 20 weeks, each with 4 deep topics (substantive teaching text + a real,
// working tool/code example + an industry note) and 8 distinct chapter
// quizzes (4 options each, one correct, tool-specific). Ends with an
// 18-question cross-cutting final exam.
//
// Structure is intentionally plain so a reseed script can iterate it directly.
// ============================================================================

export interface CaddCivilTopic {
  title: string;
  text: string; // Markdown-style body (GfG-style explanation)
  code: string; // Working, copyable example (AutoLISP / MAXScript / Ruby / Revit steps)
  note: string; // Short real-world / industry takeaway
}

export interface CaddCivilQuiz {
  text: string;
  options: string[]; // exactly 4
  correctAnswer: string; // one of options
}

export interface CaddCivilSection {
  week: number;
  title: string;
  description: string;
  topics: CaddCivilTopic[];
  quizzes: CaddCivilQuiz[];
}

export interface CaddCivilFinalExamQuestion {
  text: string;
  options: string[];
  correctAnswer: string;
}

export const caddCivilSections: CaddCivilSection[] = [
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 1 — AutoCAD Civil: Units, Layers & Site Setup
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 1,
    title: 'AutoCAD Civil: Units, Layers & Site Setup',
    description: 'Set up a civil drawing the way a survey office does — units and scales first, then disciplined layers, then survey coordinate entry, all on a reliable drafting grid.',
    topics: [
      {
        title: 'Civil Drawing Setup: Units & Scales',
        text: 'A civil drawing is read by surveyors, planners and plan-checkers, so its units and scale must be unambiguous before the first line is drawn. Unlike a decorative sketch, a site plan is measured in real ground distances and must survive being overlaid on a survey. Set the units once and the entire file inherits them.\n\n## THE UNITS DIALOG\n\nThe UNITS command (menu Format > Units) sets the linear type. Civil work uses three conventions: **architectural** (feet and fractional inches) for US residential, **decimal feet** for many US municipal submittals, and **millimetres or metres** for metric. The pick is regional — never mix. The dialog also sets the angle type (surveyors use degrees/minutes/seconds) and the insertion scale.\n\n## DWGUNITS AND INSUNITS\n\nBeyond the display units, two system variables matter for interoperability. **INSUNITS** controls how the file is scaled when inserted into another drawing; **DWGUNITS** sets what AutoCAD assumes when units are missing. Setting both to the same value (4 = millimetres, 2 = feet, 6 = metres) stops the classic "everything is 25.4x too big" insertion error between offices.\n\n## SCALE AND LTSCALE\n\nCivil drawings are drawn **1:1 in model space** — a 100 m site is 100 drawing units wide. The plotted scale (1" = 20\', 1:100) lives in the viewport and in the annotation scale. **LTSCALE** must follow the plot scale so dashed lines (proposed easements, new roads) read correctly on paper. A 1:100 sheet needs LTSCALE near 100, not the default 1.\n\n## The Setup Sequence\n\nOpen a blank template, run UNITS and pick the convention, set INSUNITS/DWGUNITS, draw the site boundary at true coordinates, then assign the sheet scale in the layout. A drawing that declares its units in the title block and its scale in the viewport needs no guesswork downstream.',
        code: '; c:civsetup — configure units & scale for a metric civil sheet\n(defun c:civsetup ()\n  (setvar "INSUNITS" 4)      ; 4 = millimetres, 6 = metres\n  (setvar "DWGUNITS" 4)\n  (setvar "MEASUREMENT" 1)   ; metric hatches/linetypes\n  (setvar "LTSCALE" 100.0)   ; match 1:100 plotting\n  (setvar "DIMSCALE" 100.0)\n  (command "._UNITS")\n  (princ "\\nCivil units configured for 1:100.")\n  (princ)\n)',
        note: 'Municipal plan-check rejects drawings whose units do not match the submission standard — a metric site drawn with imperial units is an instant non-conformance.'
      },
      {
        title: 'Layers & Styles for Civil Drawings',
        text: 'A site plan contains dozens of disciplines — existing contours, proposed roads, water mains, gas lines, property lines, easements. If each lives on its own named layer, the whole drawing can be filtered, frozen and plotted by theme. If everything lands on one layer, nothing can be controlled. Civil offices therefore adopt a **layer naming standard**.\n\n## THE NCS LAYER STANDARD\n\nIn the US the common standard is the **National CAD Standard (NCS)**, whose layer names follow the pattern **Discipline-Major-Minor-Status**. The discipline is one letter: C for civil, V for survey/mapping, L for landscape, P for plumbing. Major groups: TOPO (topography), ROAD, UTIL (utilities), PROP (property), EASM (easement). Status suffixes: EX (existing) and PROP (proposed). A proposed road edge is **V-ROAD-E-PROP**; an existing contour is **C-TOPO-MAJR-EX**. Reading the name tells any drafter what it is.\n\n## LAYER PROPERTIES\n\nEach layer carries a colour, linetype, lineweight and plot style. Existing features are conventionally shown **dashed and in a light colour**; proposed work is **solid and bold**. Contours are often screened (light) so new construction stands out. Assign these in the Layer Properties Manager: pick the layer, set the colour index, the linetype (DASHED, CENTER, PHANTOM) and the lineweight (0.18, 0.25, 0.35 mm).\n\n## LAYER FILTERS AND STATES\n\nReal projects have dozens of layers. **Layer filters** (Property and Group filters) hide the noise; **layer states** save a named snapshot of visibility so you can flip between "existing only" and "proposed only" in one click. Both live in the Layer Properties Manager toolbar.\n\n## The Rule\n\nNever draw on layer 0 for sheet content. If a new feature has no layer, create one with the right name and properties before drawing. The layer list is the drawing\'s organisational contract.',
        code: '; c:civlayers — build an NCS-style civil layer set\n(defun c:civlayers ()\n  (command "._LAYER"\n    "_NEW" "C-TOPO-MAJR-EX,C-TOPO-MAJR-PROP,V-ROAD-E-PROP,V-ROAD-E-EX,V-UTIL-WAT-PROP,V-UTIL-GAS-PROP,V-PROP-LINE,C-EASM"\n    "_COLOR" "8" "C-TOPO-MAJR-EX"\n    "_COLOR" "1" "C-TOPO-MAJR-PROP"\n    "_COLOR" "3" "V-ROAD-E-PROP"\n    "_LINETYPE" "DASHED" "V-ROAD-E-EX"\n    "_LWEIGHT" "0.25" "C-TOPO-MAJR-PROP"\n    "")\n  (princ "\\nCivil layers created.")\n  (princ)\n)',
        note: 'AEC firms ship a company NCS profile so every drawing opens with the same layers — a drawing with "Layer 1" holds up a plan check and confuses the surveyor.'
      },
      {
        title: 'Site Data: Coordinate Entry & Survey Points',
        text: 'A site plan starts from **survey data** — measured ground points with a Northing, an Easting and an elevation. Civil CAD is built around placing those points exactly, then drawing between them. The surveyor delivers a point file (typically CSV or the LandXML format); the drafter brings it into the drawing and builds the site on real coordinates.\n\n## NORTHING/EASTING vs X/Y\n\nSurvey coordinates are geographic: **Northing** is the Y axis (north is up), **Easting** is the X axis. In AutoCAD, a survey point entered as N,E becomes the coordinate Y,X. Typing a point as Easting,Northing by mistake rotates and mirrors the whole site — always convert or note the order before the first point.\n\n## ENTERING POINTS BY COMMAND\n\nThe POINT command accepts absolute coordinates directly. To place a point at Northing 1000, Easting 2000, elevation 10.5, type **POINT 1000,2000,10.5** (AutoCAD order X,Y,Z, here Y=N, X=E). A **Script file (.scr)** automates dozens of points: each line is typed as if at the command prompt, so a point file becomes a script in seconds.\n\n## COGO POINTS AND IMPORT\n\nCivil 3D and many add-ons use **COGO points** — smart points that carry a point number, description and raw northing/easting. The **Survey** toolbar or the **Points > Import** command reads a CSV, maps the columns (number, northing, easting, elevation, description), and places styled markers. The marker style (circle, cross, plus) and its label (point number, elevation) come from point styles, which are reusable drawing settings.\n\n## Verify One Benchmark\n\nBefore trusting any import, check one known coordinate — the survey benchmark — against the drawing. If the benchmark lands where expected, the whole set is probably correct; if not, stop and fix the column mapping. One verified point is worth ten unchecked imports.',
        code: '; survey_points.scr — run with the SCRIPT command\nPOINT 1000.00,2000.00,10.50\nPOINT 1005.00,2000.00,10.55\nPOINT 1000.00,2005.00,10.60\nPOINT 1005.00,2005.00,10.58\nPOINT 1002.50,2002.50,10.62\nPOINT 1007.50,2002.50,10.61\n; each line = X(EASTING),Y(NORTHING),Z(ELEVATION)',
        note: 'Survey data is Northing/Easting (Y/X); a file typed as X/Y rotates the entire site 90 degrees — verify one benchmark coordinate before drafting anything.'
      },
      {
        title: 'Grids, Boundaries & Drawing Aids',
        text: 'Precision drafting on a site plan means the cursor lands exactly on the survey grid, exactly on the boundary, exactly on the point. AutoCAD provides a set of **drawing aids** that turn a mouse into a coordinate instrument. They are toggled from the status bar and stored in the drawing, so a good setup file is half the workflow.\n\n## THE GRID AND SNAP\n\nThe **GRID** is a visible lattice of dots or lines at a set spacing (F7 to toggle). The **SNAP** system forces the cursor to jump to grid increments (F9). For civil work the grid spacing usually matches the survey interval (5 m or 20 ft). Keep SNAP on for layout, off when tracing survey geometry — a snapping cursor cannot hit a point that is off-grid.\n\n## ORTHO AND POLAR TRACKING\n\n**ORTHO** (F8) locks movement to horizontal/vertical — essential for property lines and building edges. **Polar Tracking** (F10) locks to preset angles (45, 90, 15 degrees) and shows the exact distance in the tooltip, letting you type a length along an angle. With polar on, drag north and type 25 to draw exactly 25 m north.\n\n## OBJECT SNAPS AND OTRACK\n\n**Object Snaps (OSNAP)** snap to precise points on existing objects — Endpoint, Midpoint, Center, Intersection, Perpendicular. **OSMODE** stores the active set as a bit-coded number so a company can ship a standard snap set. **Object Snap Tracking (OTRACK)** (F11) reads a temporary alignment from an existing point: hover an endpoint, then move to see the projected horizontal/vertical, and click the intersection — no construction lines needed.\n\n## DRAWING BOUNDARIES\n\nThe **LIMITS** command defines the drawing extents; the grid fills them. Set limits comfortably larger than the site (plus room for the title area) so the grid and zoom behave predictably. A drawing whose limits match the survey extents never "loses" geometry off-screen.',
        code: '; Enable a civil drafting-aid profile\n(setvar "GRIDMODE" 1)     ; F7 grid on\n(setvar "SNAPMODE" 0)     ; F9 snap off (free tracing)\n(setvar "ORTHOMODE" 1)    ; F8 ortho on\n(setvar "POLARMODE" 2)    ; polar tracking on\n(setvar "OSMODE" 4133)    ; End+Mid+Cen+Int+Perp snap bits\n(command "._LIMITS" "0,0" "120,90")\n(command "._GRID" "_ON")\n(princ "\\nCivil drawing aids configured.")\n(princ)',
        note: 'A snapping cursor cannot hit an off-grid survey point — turn SNAP off when tracing survey geometry and rely on OSNAP instead.'
      },
    ],
    quizzes: [
      { text: 'Which UNITS convention is typical for a metric civil/site plan in most municipalities?', options: ['Millimetres or metres', 'Architectural feet and inches', 'Decimals of a foot', 'Nautical miles'], correctAnswer: 'Millimetres or metres' },
      { text: 'The system variable that controls how a drawing scales when inserted into another file is…', options: ['INSUNITS', 'LTSCALE', 'DIMSCALE', 'MEASUREMENT'], correctAnswer: 'INSUNITS' },
      { text: 'In the NCS layer name V-ROAD-E-PROP, the letters V and PROP stand for…', options: ['Survey discipline and proposed status', 'Village and property line', 'Vertical and profile', 'Vector and protected'], correctAnswer: 'Survey discipline and proposed status' },
      { text: 'Existing civil features are conventionally shown…', options: ['Dashed and in a light colour', 'Solid and bold in red', 'On layer 0', 'With no linetype'], correctAnswer: 'Dashed and in a light colour' },
      { text: 'A survey point delivered as Northing 1000, Easting 2000 should be typed into AutoCAD as…', options: ['2000,1000 (X=Easting, Y=Northing)', '1000,2000 (X=Northing)', '1000,2000,1000', '2000 only'], correctAnswer: '2000,1000 (X=Easting, Y=Northing)' },
      { text: 'The SCRIPT command plays back…', options: ['A .scr text file as command-prompt lines', 'A rendered animation', 'A macro in VBA', 'A PDF import'], correctAnswer: 'A .scr text file as command-prompt lines' },
      { text: 'Which function key toggles Ortho mode?', options: ['F8', 'F3', 'F7', 'F10'], correctAnswer: 'F8' },
      { text: 'OSMODE stores the active object-snap set as…', options: ['A bit-coded integer', 'A text string', 'A layer name', 'A plot style'], correctAnswer: 'A bit-coded integer' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 2 — Site Plan Drawing & Dimensioning
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 2,
    title: 'Site Plan Drawing & Dimensioning',
    description: 'Turn survey points into a real site plan — the boundary, the utilities and setbacks, the contours and spot elevations, and the dimensions that make the plan legally measurable.',
    topics: [
      {
        title: 'Drafting the Site Boundary',
        text: 'The property boundary is the legal frame of the site. Every proposed feature — building, driveway, easement — must be located relative to it. Getting the boundary exact is the drafter\'s first and most important task, because a shifted boundary shifts the whole permit.\n\n## START FROM THE SURVEY\n\nThe boundary is defined by the survey\'s bearing-and-distance legs: for example "North 45° East, 60.00 m". Surveyors deliver the boundary as coordinate points or as a legal description. The cleanest start is to place the boundary corners from the survey coordinates, then connect them with a closed polyline. Every corner is a known Northing/Easting, so the geometry is exact before any line is drawn.\n\n## DRAWING WITH THE LINE OR PLINE COMMAND\n\nType **PLINE** and enter each corner\'s coordinates in order; finish with C to close. For a boundary defined by bearings, use **@distance<angle** polar entry — @60.00<45 draws a leg 60 m at 45 degrees. Because the angles in a legal description are measured from north (survey convention), you may need to convert quadrant bearings to AutoCAD angles (which measure counter-clockwise from east).\n\n## CLOSING THE POLYGON\n\nThe final leg rarely lands exactly on the start point — a survey has small errors. Use the **Area** command or the polyline\'s closing to check the **closure error** (the gap between the last point and the first). A gap under a few millimetres at site scale is acceptable survey tolerance; a large gap means a data-entry error somewhere. The **LIST** command reports the polyline area, which you compare with the deed.\n\n## CHECK AGAINST THE DEED\n\nThe title sheet gives the official area. After closing the boundary, run LIST and compare. If the drawn area matches the deed within survey tolerance, the boundary is right; if not, revisit the corner coordinates before any design work. Every later measurement inherits this check.',
        code: '; c:civbdry — draw a boundary from corner coordinates\n(defun c:civbdry ()\n  (command "._PLINE"\n    "2000,1000" "2050,1000" "2050,1030"\n    "2000,1030" "_C")     ; C closes the polyline\n  (command "._LIST" (entlast) "")\n  (princ "\\nBoundary drawn; LIST shows area for deed check.")\n  (princ)\n)',
        note: 'A legal description uses quadrant bearings from north; converting to AutoCAD\'s counter-clockwise-from-east angles is a classic source of a flipped boundary.'
      },
      {
        title: 'Utilities, Setbacks & Zoning Lines',
        text: 'A site plan is more than the building footprint — it shows how the site connects to services and respects the rules. **Utilities** (water, sewer, gas, electric, storm), **setbacks** (the minimum distances a building must keep from the boundary), and **zoning lines** (the districts and overlay boundaries that govern use) all appear on the plan, each on its own layer and style.\n\n## DRAWING UTILITIES\n\nEach utility is a polyline on its own layer — V-UTIL-WAT for water, V-UTIL-SAN for sanitary sewer, V-UTIL-GAS for gas, V-UTIL-ELE for electric. A **legend** on the sheet maps each line style to the service. Existing services are dashed; proposed services are solid. Utility lines connect the building to the **public main** at the street, so they start at the building wall and run to the property line, then to the main. The drawing must show the **connection point** and the depth where relevant.\n\n## SETBACK LINES\n\nThe zoning ordinance sets **front, side and rear setbacks** — the minimum distance from the boundary to any building wall. Draw them as a dashed offset of the boundary using the **OFFSET** command (offset inward by the required distance), on a layer such as ZONING-SETBK. The building must sit entirely inside the setback envelope. Plan-checkers measure the gap; a building 0.2 m inside its setback fails.\n\n## ZONING LINES AND OVERLAYS\n\nA parcel may straddle two zoning districts or a floodplain overlay. Draw the **zoning boundary** as a distinct line (dashed, labeled with the district, e.g. "R-2"), and add the **flood zone** boundary (often "Zone AE") as a screened hatch. The label tells the reviewer which rules apply to which part of the site.\n\n## The Check\n\nVerify every utility reaches a service, every setback dimension is met, and every zone line is labeled. A complete utilities-and-zoning plan is what a planner actually approves.',
        code: '; Draw setback & utility lines in a few steps\n; 1. Offset the boundary inward by the front setback\nOFFSET  3.00  ; select boundary, pick inside point\n; 2. Put the new line on the zoning layer\nCHPROP  ; select the offset, Enter\n; Layer: ZONING-SETBK   Linetype: DASHED\n; 3. Draw the water service from wall to main\nPLINE  2002.0,1002.0  2002.0,998.0\n; 4. Assign utility layer\nCHPROP  ; Last, Enter  Layer: V-UTIL-WAT-PROP',
        note: 'Municipal plan-checkers measure setbacks against the ordinance — a building 0.2 m inside its required setback is a rejection, not a warning.'
      },
      {
        title: 'Spot Elevations & Contour Lines',
        text: 'A flat site plan hides the ground. **Spot elevations** and **contour lines** communicate the third dimension on a 2D drawing, letting the reviewer see slopes, drainage and cut/fill. They are the language of grading.\n\n## SPOT ELEVATIONS\n\nA **spot elevation** is a point with its ground height, usually at corners, at the top and bottom of slopes, at building entrances, and at the invert (bottom) of drainage structures. The symbol is a cross or dot with the elevation value (in metres or feet, two decimals). In AutoCAD, place a point at the location and add the number with a leader or a block that auto-fills the value. Spot elevations answer "how high is it here?"\n\n## CONTOUR LINES\n\nA **contour** connects points of equal elevation. The vertical spacing between contours is the **contour interval** — 1 m for a typical site plan, 0.5 m or less on flat sites, 2 m on steep ground. Existing contours are drawn on C-TOPO-MAJR-EX (dashed, light); proposed contours on C-TOPO-MAJR-PROP (solid). **Index contours** (every fifth) are drawn thicker and labelled with their elevation.\n\n## GENERATING CONTOURS\n\nIn plain AutoCAD, contours are **drawn** — a polyline traced through survey points of equal elevation using the spot elevations as guides. Civil 3D generates them from a **TIN surface**: import the survey points, build the surface, and the software computes and draws contours automatically, interpolating between measured points. The manual method teaches the concept; the TIN method is what an office actually uses.\n\n## READING SLOPE FROM CONTOURS\n\nWhere contours are **close together**, the ground is steep; where they spread, the ground is flat. Contours that point upstream form a valley (drainage); contours that bulge downhill form a ridge. Labelling a few index contours lets the reviewer read the whole site\'s slope at a glance.',
        code: '; Spot elevation block insertion (metric)\nINSERT  ; block name: SPOT_ELEV\n; insertion point: 2002.5,1002.5\n; X scale: 1  Y scale: 1  Rotation: 0\n; then edit the attribute to the value, e.g. 10.55\n; ---\n; Manual contour trace through equal-elevation points\nPLINE  ; trace through survey points at elevation 10.00\n; (points sampled from the spot-elevation grid)\nCHPROP  ; Last, Enter  Layer: C-TOPO-MAJR-EX  Linetype: DASHED',
        note: 'Contour spacing is the fastest slope read — contours that bunch together mean a steep grade, and storm drainage follows where those bunched contours point.'
      },
      {
        title: 'Dimensioning Site Plans',
        text: 'A site plan must be measurable by someone who never met the designer. Dimensions tie every feature to the boundary and to a datum, in units the reviewer expects. Poor dimensioning turns a correct plan into an unbuildable one.\n\n## WHAT TO DIMENSION\n\nDimension: the building\'s distance from the **front and side property lines** (the setbacks, again — reviewers check these first), the building footprint length and width, distances between buildings, the **width of driveways and walkways**, the location of utilities relative to the building and the property line, and the **spot elevation** of key points. Each dimension answers a question a contractor or inspector will ask.\n\n## LINEAR DIMENSIONS\n\nThe **DIMLINEAR (DLI)** command measures horizontal and vertical distances. Pick two points (two corners, a wall and a boundary) and place the dimension line. For an aligned distance (diagonal to the axes), use **DIMALIGNED (DAL)**. In civil work the dimension text should show the **ground distance**, matching the scale the plan is drawn at — a 1:1 model measured directly gives true ground distances automatically.\n\n## CIVIL DIMENSION STYLE\n\nCreate a dimension style (DIMSTYLE) for site plans: the **unit format** matches the drawing (decimal metres or feet), the **precision** is typically two decimals, the **arrowheads** are architectural ticks or dots, the **text height** is set so plotted text reads about 2.5 mm on the sheet, and the **scale factor** matches the viewport. A civil style is saved and re-used on every site sheet so dimension text is consistent.\n\n## CHAIN AND BASELINE\n\nFor a row of equal setbacks, use **DIMCONTINUE** (DCO) to chain dimensions end-to-end, or **DIMBASELINE** (DBA) to measure several points from one datum. Chaining from the property line makes the setback explicit and un-misreadable. Put dimensions on their own layer (DIM) so they can be frozen when the sheet is redlined.',
        code: '; Set a metric civil dimension style\nDIMSTYLE\n; New...  Name: CIVIL-METRIC\n;   Primary Units: Decimal, precision 0.00\n;   Scale Factor: 1  (model space = ground)\n;   Text height: 2.5  Arrows: Architectural tick\n;   OK, Set Current\n; ---\nDLI   ; dimension the front setback\n; First extension origin: building front wall\n; Second extension origin: front property line\n; Dimension line location: below the plan\nDCO   ; continue chaining the next setback',
        note: 'Plan reviewers measure the setback first — a front setback dimension that reads 6.00 m on the plan but measures 5.80 m in CAD is an instant red flag.'
      },
    ],
    quizzes: [
      { text: 'Which command draws a closed boundary from known corner coordinates most cleanly?', options: ['PLINE with corner coordinates and C to close', 'RECTANG from two points', 'SKETCH freehand', 'SPLINE through all corners'], correctAnswer: 'PLINE with corner coordinates and C to close' },
      { text: 'A legal boundary described as "North 45 East, 60.00 m" is drawn with which polar entry?', options: ['@60.00<45 after converting to AutoCAD\'s angle convention', '@45<60', '@60.00,45', '60<45 as absolute'], correctAnswer: '@60.00<45 after converting to AutoCAD\'s angle convention' },
      { text: 'A building must keep 3 m from the boundary. The fastest way to draw the setback line is…', options: ['OFFSET the boundary inward by 3.00 m', 'Draw a new line 3 m away by eye', 'Copy the boundary to a new layer', 'Use DIMBASELINE'], correctAnswer: 'OFFSET the boundary inward by 3.00 m' },
      { text: 'Existing utilities are shown on a site plan as…', options: ['Dashed lines on a V-UTIL-EX layer', 'Solid red lines on layer 0', 'Hidden polylines in the block', 'Text notes only'], correctAnswer: 'Dashed lines on a V-UTIL-EX layer' },
      { text: 'A contour line connects…', options: ['Points of equal elevation', 'Points of equal slope', 'Property corners', 'Utility valves'], correctAnswer: 'Points of equal elevation' },
      { text: 'Where contours are close together, the ground is…', options: ['Steep', 'Flat', 'Wet', 'At sea level'], correctAnswer: 'Steep' },
      { text: 'Which dimension command measures a distance that is not horizontal or vertical?', options: ['DIMALIGNED (DAL)', 'DIMLINEAR (DLI)', 'DIMRADIUS (DRA)', 'DIMANGULAR (DAN)'], correctAnswer: 'DIMALIGNED (DAL)' },
      { text: 'For a row of equal building setbacks measured from one property line, the fastest tool is…', options: ['DIMCONTINUE / DIMBASELINE', 'DIMLINEAR repeated', 'Manual text', 'DIMANGULAR'], correctAnswer: 'DIMCONTINUE / DIMBASELINE' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 3 — Residential Floor Plans
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 3,
    title: 'Residential Floor Plans',
    description: 'Draft a residential floor plan that a builder can price and build — wall layouts from the room programme, doors and windows sized to code, fixtures placed for real use, and coordination that catches conflicts before the permit.',
    topics: [
      {
        title: 'Drafting Wall Layouts & Room Planning',
        text: 'A floor plan starts not with a wall but with a **room programme** — the list of spaces and their sizes. The architect decides how the rooms relate (kitchen to dining, bedrooms to bath), then the drafter turns that into wall lines. Every wall you draw is a promise about structure, circulation and cost.\n\n## THE WALL LAYER AND LINETYPE\n\nWalls are drawn on a dedicated wall layer (A-WALL), using a **double line** — two parallel lines representing the wall faces, at the wall thickness (100 mm internal, 200 mm external in masonry; 90 or 140 mm timber stud). In AutoCAD use the **MLINE** command (set the scale to the wall thickness) or draw two parallel **LINES** / **OFFSET** a single centreline both ways. Most offices draw the wall as a closed polyline outline so it hatches and measures cleanly.\n\n## DRAWING ORDER\n\nDraw the **external walls first** (they define the building envelope), then the **internal load-bearing walls**, then the **non-load-bearing partitions**. Work from the plan\'s dimensioned module: set the outer walls at the building size, then add internal walls at their room sizes. Keeping walls on a grid or module (a 600 mm planning grid) keeps the plan disciplined.\n\n## ROOM PLANNING RULES\n\nCirculation is the hidden constraint: a bedroom needs a clear path past the bed, a kitchen needs work-triangle clearances, a corridor needs minimum 900 mm. As you place walls, check the resulting room width against the programme — a room that measures 100 mm short of the minimum fails a code check even if it looks right.\n\n## CLEANING UP INTERSECTIONS\n\nWhere walls meet, the lines must intersect cleanly — no overshoots, no gaps. Use **FILLET with radius 0** or **TRIM/EXTEND** to make T-junctions and corners exact. A plan with tidy junctions hatches correctly and reads professionally; a plan with overlapping lines looks amateur and can confuse the estimator.',
        code: '; Draw an external wall envelope as a closed outline\nPLINE\n; 0,0  to  12000,0  to  12000,9000  to  0,9000  to C\n; then offset the outline inward for the inner face\nOFFSET  200  ; external wall thickness (masonry)\n; select the outer outline, pick inside\n; result: a 200 mm thick external wall ring\nCHPROP  ; Last, Enter  Layer: A-WALL\n; ---\n; Join two wall lines that meet at a corner\nFILLET  ; set Radius 0, pick the two wall lines\nTRIM   ; clean the overshoot at the T-junction',
        note: 'A room that measures 100 mm short of the building code minimum fails inspection no matter how well it is drawn — check every room width against the programme as you place walls.'
      },
      {
        title: 'Doors, Windows & Openings',
        text: 'Openings turn a wall into a building. Doors and windows are the most re-used objects on a floor plan, so they come from a **library of blocks** — pre-drawn, correctly sized, with a swing arc for doors and a sill line for windows. Placing them is fast; sizing and placement follow code and common sense.\n\n## DOOR BLOCKS\n\nA door block shows the door leaf (a line), the swing arc (an arc showing the path of the door), and a label (e.g. "D1" or "864" for an 864 mm door). Common sizes: 860 mm singles, 1220 mm doubles. Insert the block at the wall opening, on the **A-DOOR** layer. The swing direction matters — a door that swings into a corridor or overlaps a light switch is a defect on paper.\n\n## WINDOW BLOCKS\n\nA window block shows the window symbol: three or four parallel lines with a centre break, or a single line with a label (W1, W2). Windows are measured by width and set by their **sill height** (the bottom-edge height above the finished floor). A bedroom needs a minimum window area (typically 8% of floor area for egress), and every **egress window** must meet the sill-height and clear-opening rules.\n\n## WINDOW AND DOOR SCHEDULES\n\nThe plan shows the block; the **schedule** holds the data. Each door and window gets a mark (D1, W1) that links to a row in the schedule: type, size, material, hardware, head height. The schedule is generated from the blocks via a **block attribute extraction** (the ATTEXT or the Data Extraction wizard), so the schedule never drifts from the plan.\n\n## PLACEMENT RULES\n\nDoors need **clear floor space** on the latch side. Windows need a clear **outside path** for light and egress. Corner windows need enough wall for the lintel. Check each opening as you place it — the block inserts in seconds, but the placement decisions are the design.',
        code: '; Insert a pre-made 860 mm door block\nINSERT  D-860  ; block name\n; Insertion point: end of the door opening on the wall\n; Scale: 1  Rotation: 0 (adjust swing with rotation)\n; ---\n; Insert a 1500 mm window block\nINSERT  W-1500\n; Insertion point: midpoint of the wall opening\n; ---\n; Extract door/window data to a schedule table\nATTEXT  ; choose the template file (doors.txt)\n; select the blocks, write the schedule data',
        note: 'An egress window in a bedroom must meet clear-opening and sill-height rules — a window that looks fine on the plan but fails egress is a code violation found at inspection.'
      },
      {
        title: 'Furniture & Appliance Layout',
        text: 'A floor plan becomes a home when it is furnished — on paper. Furniture and appliance blocks show that the rooms actually work: a bed fits, the kitchen work triangle flows, the living room can hold the sofa. Empty rooms on a permit plan invite questions; furnished rooms answer them.\n\n## FURNITURE BLOCKS\n\nThe furniture library holds **plan-view blocks** at true size: beds (single, double, queen), sofas, tables, chairs, wardrobes, toilets, basins, kitchen appliances. Insert them on the **A-FF** (furnishings) layer. Because they are blocks, one update (a new sofa block) re-fits every instance. Standard sizes: a queen bed is 1600 x 2000, a kitchen base cabinet 600 deep, a fridge 700-900 wide.\n\n## THE KITCHEN WORK TRIANGLE\n\nThe kitchen\'s three centres — **cooktop, sink, fridge** — form the work triangle. Good design keeps the three legs between 1.2 m and 2.7 m total, with no leg crossing the main circulation path. Place the sink under a window (light and view), the cooktop on a run with counters either side (landing space), the fridge near the door. The appliance blocks must leave a 600 mm clearance in front of each.\n\n## BEDROOMS AND BATHROOMS\n\nA bedroom must fit the bed, a wardrobe, and a clear path — the plan should show the bed and the wardrobe blocks in place. A bathroom needs the basin, WC, and shower/tub with clearances: 750 mm in front of the WC, a 600 mm basin zone. If the blocks overlap the door swing or each other, the room is too small — the programme must change.\n\n## THE CHECK\n\nWalk the furnished plan like a resident: can you open the door without hitting the bed? Is there counter space beside the cooktop? Does the sofa block the window? Every "yes, but..." found on paper is a change saved in construction.',
        code: '; Furnish a living area with standard blocks\nINSERT  SOFA-2400   ; 2.4 m sofa\n; at the living room wall, facing the TV wall\nINSERT  TV-55       ; wall-mounted TV\n; on the opposite wall\nINSERT  COFFEE-1200 ; coffee table\n; centred in front of the sofa, 450 mm clear\nINSERT  DINING-6    ; 6-seat dining table\n; in the dining zone, 900 mm clear around\n; ---\n; Check clearances with DIST\nDIST  ; sofa front edge to coffee table edge',
        note: 'The kitchen work triangle (cooktop-sink-fridge) is the fastest proxy for whether a kitchen actually works — a leg crossing the main path is a design error caught on paper.'
      },
      {
        title: 'Plan Coordination & Common Mistakes',
        text: 'A floor plan is one sheet in a set, but it carries the layout that every other discipline builds on. Coordination means the plan agrees with the structural, electrical and plumbing drawings, and that the common drafting mistakes — the ones reviewers see every day — never reach the sheet.\n\n## THE CHECKLIST\n\nBefore the plan is plotted, run a coordination pass: do the wall lengths match the dimensions? Do the door swings avoid the light-switch and the corridor? Are the window sizes consistent between the plan and the schedule? Does the plan show every room from the programme? A disciplined checklist catches 90% of permit-review comments.\n\n## COMMON MISTAKE: FURNITURE ON THE WRONG SCALE\n\nA bed block inserted at the wrong scale is a 500 mm bed or a 6 m sofa — the room suddenly looks fine or impossibly crowded. Check every block\'s insertion scale and unit; furniture blocks from different libraries are a common culprit.\n\n## COMMON MISTAKE: DIMENSIONS THAT DO NOT SUM\n\nChain dimensions that do not add to the overall length (e.g. two 3000 mm rooms plus a 200 mm wall plotted as 6000 overall) are a red flag. The dimensioned widths must sum to the building width. Run the numbers before you believe the plan.\n\n## COMMON MISTAKE: OPENINGS THAT OVERLAP STRUCTURE\n\nA window drawn across a structural column, or a door cut through a load-bearing wall with no lintel note, is a structural conflict. The plan must respect the grid and the structure — if the column is there, the opening moves.\n\n## THE COORDINATION REVISION\n\nWhen a change happens (a wall moves, a window grows), every related sheet changes: the plan, the schedule, the elevation, the section. A change made only on the plan is a coordination error waiting to be built. The professional rule: one change, one revision of the whole set.',
        code: '; Quick dimension-sum check for a room row\n; List each wall length, then sum manually\nDIST  ; room 1 clear width -> 3000\nDIST  ; wall thickness         -> 200\nDIST  ; room 2 clear width -> 3000\n; total must equal the outside dimension (6200)\n; ---\n; Verify a window block insertion scale\nINSERT  W-1500\n; if the block appears tiny, the scale was not 1\n; set INSUNITS / unit scale before inserting\nSCALE  ; re-scale the misplaced block to 1.0',
        note: 'Chain dimensions that do not sum to the building width are the most common plan-review comment — a 30-second arithmetic check saves a revision cycle.'
      },
    ],
    quizzes: [
      { text: 'A 200 mm masonry external wall is best drawn in plan as…', options: ['A closed outline offset 200 mm for the inner face', 'A single centreline', 'Two unrelated random lines', 'A hatched rectangle'], correctAnswer: 'A closed outline offset 200 mm for the inner face' },
      { text: 'Which command trims or joins wall lines that overshoot at a T-junction?', options: ['TRIM/EXTEND and FILLET with radius 0', 'MOVE and COPY', 'ROTATE', 'MIRROR'], correctAnswer: 'TRIM/EXTEND and FILLET with radius 0' },
      { text: 'A bedroom\'s minimum window area is typically about…', options: ['8% of the floor area (and must meet egress rules)', '1% of the floor area', 'No minimum', '50% of the wall length'], correctAnswer: '8% of the floor area (and must meet egress rules)' },
      { text: 'The door/window schedule is generated from the plan blocks so that…', options: ['The schedule never drifts from the plan', 'It looks professional', 'It prints faster', 'The block library updates'], correctAnswer: 'The schedule never drifts from the plan' },
      { text: 'The three centres of the kitchen work triangle are…', options: ['Cooktop, sink, fridge', 'Oven, pantry, window', 'Sink, dishwasher, table', 'Fridge, door, island'], correctAnswer: 'Cooktop, sink, fridge' },
      { text: 'A queen bed block is 1600 x 2000. Inserted at scale 0.5 it becomes…', options: ['An 800 x 1000 bed — wrong', 'A 3200 x 4000 bed', 'Correct, blocks auto-scale', 'Invisible'], correctAnswer: 'An 800 x 1000 bed — wrong' },
      { text: 'A door that swings into a corridor or overlaps a light switch is…', options: ['A design defect to catch on the plan', 'Fine, builders fix it', 'A structural issue', 'Unavoidable'], correctAnswer: 'A design defect to catch on the plan' },
      { text: 'When a wall moves, the professional coordination rule is…', options: ['Update the plan, schedule, elevations and sections together', 'Only update the plan', 'Only update the schedule', 'Redraw the whole set'], correctAnswer: 'Update the plan, schedule, elevations and sections together' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 4 — Sections, Elevations & Plotting
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 4,
    title: 'Sections, Elevations & Plotting',
    description: 'Give the floor plan a third dimension on paper — sections and elevations drawn from the model, hatched and annotated, placed on title-blocked sheets and plotted at a scale a municipality will accept.',
    topics: [
      {
        title: 'Creating Sections & Elevations',
        text: 'A floor plan shows a building from above; **sections and elevations** show it from the side. A **section** is a vertical cut through the building (a "slice" that reveals the floor-to-floor heights, the roof slope, the foundations); an **elevation** is an exterior face drawn as seen from outside. Together they communicate the third dimension.\n\n## SECTION MARK AND CUT LINE\n\nOn the plan, a **section mark** shows where the cut happens and the viewing direction: a line with two arrows, labelled A-A, B-B. The reviewer reads the mark on the plan, finds the section sheet, and sees the cut drawn from that direction. The mark must be on every plan that locates the cut.\n\n## DRAWING THE SECTION\n\nDraw the section by projecting: take the plan geometry along the cut line, carry the **heights** from the levels (floor finish height, ceiling height, roof ridge), and draw the wall/floor/roof outlines. Draw the cut elements (the parts the knife passes through) as **solid filled or heavy lines**; the background elements lighter. Foundation, slab, wall, floor, roof — each appears once with its thickness.\n\n## DRAWING THE ELEVATION\n\nAn **elevation** is the exterior face: the wall surface, the windows and doors in place, the roof edge, the ground line and the finished grade. Project the plan\'s wall length and openings horizontally, then draw the heights (sill, head, eave, ridge). Elevations are the architect\'s "front view" — they show proportion, materials and fenestration as the owner will see them.\n\n## FROM MODEL OR MANUAL\n\nIn a 2D workflow the section and elevation are drafted by projecting; in a BIM workflow (Revit) they are **derived from the model** — cut a section, and the walls, doors and floors appear automatically at true size. Either way, the section and elevation must agree with the plan: a window on the plan at the same place in the elevation is the basic coordination check.',
        code: '; Section mark block on the plan (A-A)\nINSERT  SEC-MARK\n; scale 1, rotation to point the viewing direction\n; ---\n; Project the section from the plan\n; 1. Copy the plan geometry at the cut line\nCOPY  ; select plan objects, base the copy below\n; 2. Draw the ground line\nLINE  ; across the full section width\n; 3. Draw wall outlines up to the roof height\nLINE  ; outer face up, inner face up, both floors\n; 4. Add the roof slope and ridge height\nLINE  ; from eave to ridge at the roof pitch',
        note: 'A section mark without a matching section sheet is a plan-review failure — every mark on the plan must resolve to a drawn section somewhere in the set.'
      },
      {
        title: 'Hatching, Materials & Annotations',
        text: 'A section or elevation is a set of outlines until it is hatched. **Hatching** identifies materials — the cut concrete, the masonry, the insulation, the ground — and annotation explains what the lines cannot. This is where a technical drawing becomes a buildable document.\n\n## HATCH COMMANDS AND PATTERNS\n\nThe **HATCH** command fills an enclosed area with a pattern. Common patterns: ANSI31 for general section cuts, **CONC/AR-CONC** for concrete, **EARTH/AR-HBONE** for ground, **AR-SAND** for insulation (or a continuous fill with a note), **BRICK** for masonry. In the Hatch dialog, pick the pattern, the scale (match the drawing scale — a hatch at scale 1 on a 1:100 drawing is a solid blur), and the angle. Pick the boundary by selecting the enclosed area; the hatch must sit inside a closed loop.\n\n## THE MATERIALS LEGEND\n\nEvery hatch used on the set appears in a **legend** on the sheet (on the A-ANNO or a dedicated legend layer): the pattern swatch, the material name, and where it is used. A reviewer who cannot tell the cut concrete from the ground has to guess — the legend removes the guess.\n\n## ANNOTATIONS: TEXT AND LEADERS\n\nThe notes live on an **A-ANNO-TEXT** layer: general notes, the "UNLESS OTHERWISE NOTED" block, material callouts, and **leaders** (the MLD multileader command) pointing from the text to the feature. Annotation text is plotted at a fixed height (about 2.5-3.5 mm on the sheet), so the drawing-scale text height = sheet height x scale factor. Keep annotations to the point: the drawing shows; the note tells what the drawing cannot.\n\n## HATCH LAYER DISCIPLINE\n\nHatches go on their own layer (A-Hatch or HATCH) so they can be frozen when plotting the linework only, and set to **plot in a light colour** so they never drown the geometry. A heavy hatch under thin walls makes the sheet illegible.',
        code: '; Hatch the cut concrete in a section\nHATCH\n; Pattern: ANSI31   Scale: 50   Angle: 0\n; Pick the enclosed concrete area, Enter\n; ---\n; Hatch the ground under the section\nHATCH\n; Pattern: AR-HBONE   Scale: 1.5   Angle: 0\n; Pick the area below the ground line\n; ---\n; Add a material leader note\nMLD   ; multileader\n; First point: the concrete face, then the text location\n; Type the note: "CONC. FOUNDATION, 25 MPa"',
        note: 'A hatch at the wrong scale is a solid black smear on a 1:100 sheet — match the hatch scale to the drawing scale, and keep hatches on a layer that can be frozen.'
      },
      {
        title: 'Title Blocks & Sheet Layouts',
        text: 'The sheet is the deliverable — the physical or digital page a reviewer reads. The **title block** is the legal identity of the drawing: who drew it, what it is, what scale, what revision, what sheet of what set. Sheet layout is the discipline of arranging views, notes and schedules so the page reads in a fixed order.\n\n## THE TITLE BLOCK\n\nThe title block (on the A-TTLB layer) is a border rectangle with zones: the **company name and logo**, the **project name and number**, the **sheet title** (e.g. "Site Plan", "Ground Floor Plan"), the **sheet number** (A-101, A-201), the **drawing scale** and the **date**. The **revision table** sits in the corner — each revision gets a letter, date, description and approval. On a municipal set, the title block carries the seal/space for the engineer or architect.\n\n## SHEET LAYOUT IN PAPER SPACE\n\nThe layout (paper space) holds the border and title block at **full sheet size** (A3, A1, 24x36). Inside the border, **viewports** frame the model views. Each viewport is scaled (1:100, 1:50) by setting its scale in the Properties or the viewport toolbar. The viewport scale is what makes a 1:100 annotation readable — the text height inside model space is drawn for that scale.\n\n## ARRANGING THE SHEET\n\nThe professional sheet reads top-to-bottom, left-to-right: the main plan or view largest and first, then sections and details, with notes and schedules along the bottom or right edge. Leave consistent margins between views; align the views on a grid. A sheet that is crowded tells the reviewer the scale or the layout is wrong.\n\n## THE SHEET INDEX\n\nThe first sheet of a set is the **index** (G-000 or cover), listing every sheet by number and title. The index and the actual sheet numbers must match exactly — a renumbered sheet without an index update is a classic transmittal error.',
        code: '; Build a title block in paper space (A3)\nLAYOUT  ; switch to Layout1\n; Draw the border (420 x 297 for A3)\nRECTANG  0,0  420,297\n; Title block zones as rectangles along the bottom\nRECTANG  0,0  420,40\n; Text in the zones\nTEXT  ; "PROJECT: WINDSONG RESIDENCE" at the zone\nTEXT  ; "SHEET: A-101  GROUND FLOOR PLAN"\nTEXT  ; "SCALE: 1:100   DATE: 2026-08-12"\n; Insert a viewport for the plan\nMVIEW  ; drag a viewport inside the border\n; set the viewport scale to 1:100 in Properties',
        note: 'A viewport scale that does not match the sheet\'s stated scale is the most common plotting rejection — always verify 1:100 in the viewport matches "SCALE 1:100" in the title block.'
      },
      {
        title: 'Plotting & Scaling for Municipal Submissions',
        text: 'A municipal submission is a legal document — the plan must plot at exactly the stated scale, at a readable size, in a file format the municipality accepts. Plotting is not "print this"; it is the final quality gate.\n\n## PLOT SETTINGS\n\nThe **PLOT** command opens the Plot dialog. Choose the printer/PDF driver, the **paper size** (A3, A1, or the municipal standard like 24x36), the **plot area** (Layout for paper space), the **plot scale** (1:1 when plotting a layout — the layout is already at sheet size), and the **plot style table** (CTB) that maps layer colours to pen widths. A CTB converts the drawing\'s colour-index lines to the correct plotted lineweights.\n\n## LINE WEIGHTS VIA CTB\n\nThe plotted drawing reads through **lineweight**: walls heavy, dimensions light, hatch lightest. The CTB assigns each AutoCAD colour a pen width (red = 0.5 mm, yellow = 0.25 mm, green = 0.18 mm). Set the CTB once and every sheet plots consistently. A drawing plotted with a missing CTB prints everything at the same weight and loses all hierarchy.\n\n## PDF FOR SUBMISSION\n\nMost municipalities accept **PDF** for plan-check, and increasingly the **native DWG**. Plot to PDF at the paper size with the CTB applied; verify the resulting PDF\'s text is selectable (vector, not raster) so the reviewer can measure. Name the file by the standard: PROJECT-SHEETNUMBER-REV.pdf.\n\n## THE SCALE CHECK\n\nThe final check before upload: measure a known dimension on the printed/PDF\'d sheet with a scale ruler. A 1:100 plan whose wall measures 1:110 on the sheet fails. Also confirm the title-block scale matches the viewport scale — a mismatch sends the set back. This 30-second check is what separates a professional set from a rejected one.',
        code: '; Plot an A3 layout sheet to PDF\nPLOT\n; Printer/plotter: DWG To PDF.pc3\n; Paper size: ISO A3 (420 x 297)\n; Plot area: Layout   Plot scale: 1:1\n; Plot style table: civil.ctb\n; Preview, then apply\n; ---\n; Verify the scale of a plotted viewport\n; In paper space, select the viewport\n; Properties -> Standard scale = 1:100\n; ---\n; Check a dimension on the PDF with a scale ruler\n; the 6.00 m wall must measure 60 mm on 1:100',
        note: 'Municipalities reject PDFs whose viewport scale contradicts the title-block scale — verify 1:100 in the viewport against "SCALE 1:100" before the file leaves the office.'
      },
    ],
    quizzes: [
      { text: 'A section mark on the plan communicates…', options: ['Where the vertical cut is and the viewing direction', 'The floor finish height', 'The building\'s gross area', 'The window schedule'], correctAnswer: 'Where the vertical cut is and the viewing direction' },
      { text: 'In a building section, the elements the cutting plane passes through are drawn…', options: ['Heavy or solid-filled', 'Invisible', 'Dashed light', 'With no outline'], correctAnswer: 'Heavy or solid-filled' },
      { text: 'Which hatch pattern is conventionally used for the cut concrete in a section?', options: ['ANSI31 or CONC', 'AR-SAND', 'DOTS', 'HONEY'], correctAnswer: 'ANSI31 or CONC' },
      { text: 'The hatch scale on a 1:100 drawing should be…', options: ['Matched to the drawing scale (large enough to read)', 'Always 1.0', 'The same as text height', 'Zero'], correctAnswer: 'Matched to the drawing scale (large enough to read)' },
      { text: 'The plotted height of annotation text is set so that…', options: ['It reads about 2.5-3.5 mm on the sheet', 'It is huge in model space', 'It matches the hatch', 'It never prints'], correctAnswer: 'It reads about 2.5-3.5 mm on the sheet' },
      { text: 'A viewport in paper space shows the model at…', options: ['A set scale like 1:100', 'Whatever size it happens to be', 'Model scale only', 'Twice the model scale'], correctAnswer: 'A set scale like 1:100' },
      { text: 'The CTB (plot style table) controls…', options: ['Plotted lineweights per colour', 'The PDF file name', 'Layer names', 'Text fonts'], correctAnswer: 'Plotted lineweights per colour' },
      { text: 'The final scale check before a municipal submission is…', options: ['Measure a known dimension on the PDF/sheet with a scale ruler', 'Confirm the file is under 10 MB', 'Check the sheet count', 'Verify the fonts'], correctAnswer: 'Measure a known dimension on the PDF/sheet with a scale ruler' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 5 — 3ds Max Modeling for Architecture
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 5,
    title: '3ds Max Modeling for Architecture',
    description: 'Move from CAD to visualisation — navigate the 3ds Max viewports, build architecture from primitives, splines and editable polys, and assemble shells and details for render-ready models.',
    topics: [
      {
        title: 'The 3ds Max Interface & Viewport Navigation',
        text: '3ds Max is where CAD drawings become images. The interface is built around four viewports (Top, Front, Left, Perspective) that show the same scene from different angles. Learning to navigate them is the first skill.\n\n## THE VIEWPORTS AND NAVIGATION CONTROLS\n\nThe four viewports display the scene as wireframe or shaded. The **Viewport Navigation** tools sit in the bottom-right corner: **Zoom**, **Pan**, **Orbit** and **Zoom Extents**. Faster shortcuts: the mouse wheel zooms, the middle mouse button pans, and **Alt + middle mouse** orbits. The view cube snaps to preset angles (front, right, top).\n\n## SELECTION AND THE QUAD MENU\n\nRight-clicking an object opens the **Quad Menu** — four quadrants of context commands (Isolate, Hide, Move, Rotate, Scale). **Isolate Selection** temporarily hides everything else, the fastest way to focus on one wall. Selection tools on the toolbar: Select, Select and Move (W), Select and Rotate (E), Select and Uniform Scale (R).\n\n## THE COMMAND PANEL\n\nThe right side holds the **Command Panel** with tabs: Create, Modify, Hierarchy, Display, Utilities. The **Create** tab makes primitives; the **Modify** tab (with an object selected) shows its parameters, modifiers and stack. Modelling alternates between the two.\n\n## TRANSFORM AND SNAP\n\n**Move, Rotate, Scale** transform the selected object. The **Snap Toggle** (S) locks the cursor to the grid or to object points; right-click it to choose which snaps (vertex, edge, midpoint). A wall placed on the grid at an exact coordinate aligns with the CAD plan below.\n\n## The Workflow\n\nImport or draw the plan as a guide, build the mass in Top view, verify heights in Front/Left, and orbit in Perspective to check. Each viewport catches a different error.',
        code: '-- MAXScript: set up a standard 4-viewport scene with grid snap\nviewport.setLayout #layout_4\nsnaps.enabled = true\nsnaps.setOSnapItemEnabled 1 true   -- vertex\nsnaps.setOSnapItemEnabled 4 true   -- midpoint\ngrid = gridObj()\nmax grid size, etc.\n-- create a reference box for scale\nb = Box length:9000 width:6000 height:3000\nselect b\nmax tool zoomext sel all\n-- pan/zoom/rotate via keyboard in the active viewport',
        note: 'Every architecture visualisation starts with the plan as a guide image or CAD import — never freehand a building mass without a scale reference.'
      },
      {
        title: 'Primitives, Splines & Editable Polys',
        text: 'Everything in a 3ds Max building starts as a primitive or a spline, and ends as an **Editable Poly** — the object type that gives per-vertex, per-edge control. Understanding that pipeline is the core of Max modelling.\n\n## PRIMITIVES\n\nThe **Create > Geometry** tab makes standard primitives: Box, Sphere, Cylinder, Plane. A **Box** with the right length, width, height is already a floor slab, a wall or a sill. Primitives are parametric — the Modify panel shows their dimensions as live values. Set **segments** before converting: a wall that will be bent needs length segments; a flat slab needs none.\n\n## SPLINES\n\n**Create > Shapes > Splines** makes 2D curves: Line, Rectangle, Circle, Arc, Text. A spline is the profile of a future 3D form. The classic move: draw the floor plan as a spline (or import the CAD polyline), then extrude it into a solid. Spline vertices have types — **Corner, Smooth, Bezier** — that control how the curve bends.\n\n## EDITABLE POLY\n\nConvert any object to an **Editable Poly** (right-click > Convert to > Editable Poly, or add the **Edit Poly** modifier). This unlocks the sub-object levels: **Vertex, Edge, Border, Polygon, Element**. At each level you move, extrude, bevel and delete. **Extrude** at polygon level pulls a face outward — extrude a wall polygon up and you have a storey.\n\n## MODIFIERS\n\nThe **Modify** panel stacks **modifiers** without destroying the object: **Extrude**, **Bevel**, **Bend**, **FFD**. The stack is non-destructive — tweak the spline and the extrusion updates. This is the parametric heart of Max.\n\n## The Discipline\n\nModel as a stack: spline profile, extrude modifier, then editable poly for detail. Collapse to editable poly only when the shape is settled.',
        code: '-- MAXScript: extrude a floor-plan spline into a slab\ns = SplineShape()\n-- (draw or import the plan as a closed spline first)\naddModifier s (Extrude())\ns.modifiers[1].amount = 300      -- slab thickness mm\ns.modifiers[1].segments = 1\n-- convert to editable poly for detail\nconvertToPoly s\n-- select the top polygon and bevel a step\nsubObjectLevel = 4               -- polygon\npolyop.extrudeFaces s #{1} 150\nupdate s',
        note: 'Model as a modifier stack (spline → extrude → edit poly) as long as you can — collapsing too early destroys the parametric history and makes every change a redo.'
      },
      {
        title: 'Building Shells: Walls, Floors, Roofs',
        text: 'The building shell — walls, floors, roofs — is the envelope everything else sits inside. Build it from the CAD plan and the section heights, using splines, extrudes and booleans, to a watertight mass with clean intersections.\n\n## WALLS FROM SPLINES\n\nThe fastest wall: draw the wall centreline as a spline (or trace the CAD plan), add an **Extrude** modifier, and set the amount to the wall height (e.g. 3000 mm). For thickness, extrude the single centreline then add a **Shell** modifier — shell the wall face 200 mm. The Shell modifier adds thickness to any surface.\n\n## FLOORS AND SLABS\n\nA floor slab is a flat box or an extruded closed spline of the floor outline. Extrude the plan outline 300 mm (150 mm for an upper slab). For a slab with an atrium void, draw the outline and the void, combine them, and extrude — the spline sub-object with a hole gives a slab with an opening.\n\n## ROOFS\n\nThe roof is a plane extruded and sloped. Draw the roof outline as a spline, extrude a thin slab, then use **Edit Poly** vertex level to slope it: select the ridge edge and move it up to the ridge height. A **gable or hip roof** is built the same way — draw the footprint, raise the ridge line, let the eaves follow.\n\n## CLEAN INTERSECTIONS\n\nThe shell must be **watertight** — no gaps at wall-corner, wall-roof or wall-floor joints. Use **Attach** to merge coplanar walls and **Weld** to merge coincident vertices (with a small threshold). A watertight shell renders cleanly and booleans work without leaking.\n\n## The Sequence\n\nTrace the plan, extrude walls to storey height, slab each floor, slope the roof, then weld the intersections. Every element references the plan, so the shell matches the footprint exactly.',
        code: '-- MAXScript: build a 3-storey shell from a plan spline\n-- walls\nw = copy planSpline\naddModifier w (Extrude())\nw.modifiers[1].amount = 9000      -- 3 x 3000 storeys\naddModifier w (Shell())\nw.modifiers[2].outerAmount = 200  -- wall thickness\n-- slab (copy of the same outline, thin)\nslab = copy planSpline\naddModifier slab (Extrude())\nslab.modifiers[1].amount = 300\nmove slab [0,0,2700]              -- first-floor slab\n-- roof: extrude, then slope the ridge edge\nroof = copy planSpline\naddModifier roof (Extrude())\nroof.modifiers[1].amount = 200\nconvertToPoly roof\nsubObjectLevel = 1                -- vertex\n-- move the ridge vertex up to pitch height',
        note: 'A watertight shell is the difference between a clean render and one with light leaking through wall corners — weld coincident vertices before texturing.'
      },
      {
        title: 'Modeling Details: Trim, Moldings & Furniture',
        text: 'A shell is a building; the **details** — baseboards, window trim, cornices, balustrades, furniture — make it believable. Most details reuse two techniques: **profile along a path** (Loft / Sweep) and **reusable instances**.\n\n## MOLDINGS WITH LOFT AND SWEEP\n\nA cornice or baseboard is a profile extruded along a path. Draw the profile as a closed spline (the cross-section), then select the path (the wall run) and use **Loft** — pick the profile, and Max sweeps it along the path. The **Sweep** modifier does the same more simply: pick a built-in or custom section and assign it to the path. A room perimeter plus a baseboard section gives every wall a baseboard in one step.\n\n## WINDOW AND DOOR TRIM\n\nWindow trim is four thin boxes around the opening, or a frame profile swept around the window rectangle. The simplest production technique: build one window with trim, then **instance** it to every identical opening — an instance shares the same mesh, so editing one updates all.\n\n## FURNITURE AS COMPONENTS\n\nChairs, tables and lamps are modelled or downloaded once and reused. In Max these are **instances** (sharing geometry and material) or **XRefs** (external references). A library of furniture instanced across the scene keeps the file light and the model consistent.\n\n## THE DETAIL LIBRARY\n\nProfessional studios keep a **library** of ready details: trim profiles, door frames, baluster sections, standard furniture. Building the library once, then dragging instances into every scene, turns a two-day model into a two-hour one.\n\n## The Discipline\n\nDetail with profiles and instances, not brute-force modelling. If you need the same baseboard in ten rooms, sweep it once and instance it ten times.',
        code: '-- MAXScript: sweep a baseboard profile around a room path\nbb = Sweep()                     -- create a sweep modifier\np = SplineShape()                -- the room-perimeter path\n-- (draw or select the closed perimeter spline)\naddModifier p bb\nbb.sweepType = 3                 -- custom section\n-- load the baseboard profile into the sweep\n-- Instance a window along a wall\nwnd = windowObject\nfor i = 1 to 5 do (\n  c = instance wnd\n  c.pos = [x + i*1800, y, z]     -- 1.8 m spacing\n)',
        note: 'A single instanced baseboard or window that edits everywhere is worth a hundred duplicated ones — instance anything you will change more than once.'
      },
    ],
    quizzes: [
      { text: 'Which input orbits the camera in the active 3ds Max viewport?', options: ['Alt + middle mouse button', 'Ctrl + Z', 'W key', 'The space bar'], correctAnswer: 'Alt + middle mouse button' },
      { text: 'Right-clicking an object in 3ds Max opens…', options: ['The Quad Menu with context commands', 'The Render dialog', 'The Material Editor', 'The viewport list'], correctAnswer: 'The Quad Menu with context commands' },
      { text: 'The fastest way to focus on one wall while modelling is…', options: ['Isolate Selection', 'Delete everything else', 'Zoom Extents', 'Freeze the camera'], correctAnswer: 'Isolate Selection' },
      { text: 'The Modify panel in 3ds Max shows…', options: ['An object\'s parameters, modifiers and stack', 'The material slots', 'The render output', 'The animation timeline'], correctAnswer: 'An object\'s parameters, modifiers and stack' },
      { text: 'A floor plan drawn as a spline becomes a solid wall or slab with which modifier?', options: ['Extrude', 'Bend', 'FFD', 'TurboSmooth'], correctAnswer: 'Extrude' },
      { text: 'Which sub-object level lets you pull a wall polygon upward to make a storey?', options: ['Polygon', 'Vertex', 'Edge', 'Element'], correctAnswer: 'Polygon' },
      { text: 'The Shell modifier is used to…', options: ['Add thickness to a flat surface', 'Hollow out a solid', 'Round a corner', 'Flip the normals'], correctAnswer: 'Add thickness to a flat surface' },
      { text: 'A baseboard profile run along a room perimeter is best made with…', options: ['Loft or the Sweep modifier', 'A Box and Copy', 'Boolean subtract', 'Turbosmooth'], correctAnswer: 'Loft or the Sweep modifier' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 6 — Materials & Texturing
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 6,
    title: 'Materials & Texturing',
    description: 'Give the model its skin — the Material Editor and its slots, the diffuse/bump/specular maps that make surfaces believable, UVW mapping at real-world scale, and PBR material libraries.',
    topics: [
      {
        title: 'The Material Editor & Material Slots',
        text: 'A white box model tells nothing about the building. **Materials** — the surface definitions that carry colour, texture, reflection and transparency — turn geometry into concrete, glass and timber. The **Material Editor** is where every material is built and assigned.\n\n## OPENING THE MATERIAL EDITOR\n\nThe Material Editor opens with the **M** key. It shows a grid of **material slots** — sample spheres (or cubes) that preview the material. Each slot holds one material definition. The **Slate Material Editor** (the modern view) uses a node graph: material nodes connected to map nodes, giving a clear picture of what feeds what. The compact editor lists the same parameters in a list.\n\n## THE STANDARD AND PHYSICAL MATERIALS\n\nTwo material types dominate: the classic **Standard material** (diffuse, specular, opacity) and the modern **Physical Material** / **PBR** (base colour, metalness, roughness). For architectural work the Physical Material matches how light really behaves. Each slot\'s parameters — colour, glossiness, reflectivity — are the "recipe" of the surface.\n\n## ASSIGNING TO OBJECTS\n\nSelect the object (a wall), then in the Material Editor click **Assign Material to Selection**. The wall now renders with that material. To change the wall everywhere, edit the shared material — every object using it updates. That is the power of materials as shared definitions: one edit, all walls.\n\n## THE MATERIAL/MAP BROWSER\n\nThe **Material/Map Browser** lists every material and map type — Standard, Physical, Multi/Sub-Object, V-Ray materials, and procedural maps like Noise and Gradient. A **Multi/Sub-Object** material assigns different materials to different polygon IDs on one object — the classic way to give one wall a brick lower half and a render upper half.\n\n## The Workflow\n\nOpen the editor (M), create a Physical material, set the base colour, assign it to the selection, then adjust as the viewport preview and a test render show. Name materials clearly (Brick-Common, Glass-Reflective) — a scene of "Material #12" is unmaintainable.',
        code: '-- MAXScript: create and assign a Physical material\nm = PhysicalMaterial()\nm.name = "Concrete-Exposed"\nm.base_color = color 180 178 170\nm.roughness = 0.85\nm.metalness = 0.0\n-- assign to the selection\nfor obj in selection do obj.material = m\n-- show the material editor\nactionMan.executeAction 0 "405"   -- open Material Editor',
        note: 'Name every material and keep one material per real surface type — a scene of "Material #12" slots cannot be maintained when the client asks for a change.'
      },
      {
        title: 'Diffuse, Bump & Specular Maps',
        text: 'A flat colour is not a texture. Real surfaces vary across their area — bricks have joints, walls have grain, concrete has stains. **Maps** are images or procedures that give a material local variation. The three classic channels are diffuse, bump and specular.\n\n## DIFFUSE MAP\n\nThe **Diffuse** map is the base image — what the surface looks like in flat light. Click the diffuse colour swatch\'s map button, choose **Bitmap**, and load the image (a brick JPEG on a wall). Diffuse is where the visible pattern lives.\n\n## BUMP MAP\n\nThe **Bump** map fakes surface relief with a grayscale image: light areas appear raised, dark areas recessed. A bump map of the brick joints makes the mortar read recessed without adding geometry. The **Bump Amount** (e.g. 30) controls the strength. Bump is cheap; **Displacement** actually moves the geometry but is much heavier.\n\n## SPECULAR AND REFLECTION MAPS\n\nThe **Specular** map controls where highlights appear — a wet floor has a bright specular, a matte wall almost none. In modern PBR materials this role is covered by **roughness** (varying how glossy each pixel is) and **metalness**. A roughness map with scratched patches makes a floor reflect sharply in some spots and diffusedly in others.\n\n## WHERE THE MAPS COME FROM\n\nTexture libraries (and photos you take) supply the images. The rule: the diffuse, bump and roughness maps of one surface must align — the bump joint must sit under the diffuse joint. Matched, seamless sets convince; mismatched maps look painted-on.\n\n## The Workflow\n\nStart with diffuse, add bump for relief, then roughness for finish. Test-render a small area — a material is only right when it reads at render distance.',
        code: '-- MAXScript: build a brick material with diffuse + bump\nmat = PhysicalMaterial()\nmat.name = "Brick-Common"\nbmD = BitmapTexture filename:"brick_diffuse.jpg"\nbmB = BitmapTexture filename:"brick_bump.jpg"\nmat.base_color_map = bmD\nmat.bump_map = bmB\nmat.bump_map_amount = 30.0\n-- assign to the wall selection\nfor obj in selection do obj.material = mat',
        note: 'Bump and diffuse must be a matched set — a bump joint that does not sit under the diffuse joint looks like the texture is painted on.'
      },
      {
        title: 'UVW Mapping & Real-World Scale',
        text: 'A texture needs to know where it goes. **UVW mapping** tells the material how the 2D image wraps over the 3D surface. Get it wrong and bricks stretch and grain runs sideways; get it right and the material reads at true size.\n\n## WHAT UVW MEANS\n\nEvery surface point has a **UV coordinate** (U = horizontal in the image, V = vertical) in addition to its 3D XYZ. The UVW Map modifier assigns these coordinates. Without it, Max guesses — and the guess is rarely right.\n\n## THE UVW MAP MODIFIER\n\nAdd a **UVW Map** modifier. It offers mapping types: **Planar** (projects from one direction — good for flat walls and floors), **Box** (projects from six sides — good for boxes), **Cylindrical** and **Spherical** for round forms. Pick the type that matches the shape; box mapping on a wall makes the front face correct.\n\n## REAL-WORLD SCALE\n\nThe key checkbox is **Real-World Scale** (in the UVW Map and the material\'s coordinates). Turn it on and the texture size is set in real units — a brick tile of 225 x 75 mm tiles at exactly that size, no matter the object size. A 9 m wall shows exactly the right number of bricks. This is the professional default.\n\n## UNWRAP FOR COMPLEX SHAPES\n\nThe **Unwrap UVW** modifier gives per-face control: you flatten the object\'s faces into the 2D editor and arrange them over the image. Use it for a moulding, a balustrade or a curved wall. The UV editor shows the flattened wireframe; you scale, move and rotate the islands onto the texture.\n\n## The Check\n\nTest with a **checker map**. If the squares are perfect uniform real-size squares, the UVW is right; if they stretch, fix the mapping before the real texture goes on.',
        code: '-- MAXScript: apply real-world-scaled box mapping\naddModifier wallObj (UVWMap())\nwallObj.modifiers[#UVWMap].maptype = 1   -- planar\nwallObj.modifiers[#UVWMap].realWorldMapSize = true\nwallObj.modifiers[#UVWMap].length = 9000\nwallObj.modifiers[#UVWMap].width = 6000\n-- apply unwrap for a complex moulding\naddModifier moulding (Unwrap_UVW())\n',
        note: 'Always test a material with a checker map first — if the squares are not uniform real-size squares, no amount of texture detail will look right.'
      },
      {
        title: 'Texture Libraries & PBR Materials',
        text: 'Nobody photographs every texture from scratch. Professional studios work from **texture libraries** — curated collections of seamless, real-world photos and their matching maps. The modern standard is **PBR** (Physically Based Rendering), where every material uses the same small set of maps and light behaves consistently.\n\n## WHAT PBR MEANS\n\nPBR describes surfaces with **physically based parameters**: **base colour** (albedo), **roughness** (how smooth or matte), **metalness** (metal vs dielectric), and **normal/bump**. Because the renderer interprets these the same way for every material, a brick PBR material lit by the sun looks right without per-scene tuning. This is why PBR is the industry default.\n\n## THE PBR MAP SET\n\nA typical PBR set for one surface is four files: **albedo/diffuse** (the colour), **roughness** (grayscale — white = rough, black = glossy), **normal** (a colour-coded map that fakes surface detail and light response better than bump), and **height/ambient-occlusion** (optional). Libraries ship these as a matched group. In 3ds Max the **Physical Material** maps directly onto them.\n\n## TEXTURE LIBRARIES AND SOURCES\n\nLibraries like Quixel, Poliigon and Arroway provide seamless scans: concrete, brick, timber, metal, fabric. Seamless means the edges of the tile match, so repeated tiles do not show a seam. Free libraries are useful for learning; paid libraries offer higher resolution and matched PBR sets for client work.\n\n## ORGANISING THE LIBRARY\n\nKeep the library in a **project folder structure**: /Textures/Brick/, /Textures/Timber/, each with its four maps named consistently (brick_diffuse.jpg, brick_roughness.jpg, brick_normal.jpg). Reference the maps by **relative path** in the scene so the file travels with the project — an absolute path like C:\\Users\\Name\\... breaks when the project moves.\n\n## The Workflow\n\nDrag the matched PBR set into the scene, wire the maps to a Physical Material, set real-world scale, and test-render. A good library plus PBR discipline is the difference between a "3D look" and a believable building render.',
        code: '-- MAXScript: assemble a PBR material from a library folder\ntex = "C:/Textures/Brick/brick_"\nmat = PhysicalMaterial()\nmat.base_color_map = BitmapTexture filename:(tex + "diffuse.jpg")\nmat.roughness_map = BitmapTexture filename:(tex + "roughness.jpg")\nmat.bump_map       = BitmapTexture filename:(tex + "normal.jpg")\nmat.metalness = 0.0\nmat.name = "Brick-PBR"\n-- assign\nfor obj in selection do obj.material = mat',
        note: 'Reference textures by relative project path — an absolute C:\\ path breaks every time the project folder moves or the file is shared.'
      },
    ],
    quizzes: [
      { text: 'Which key opens the Material Editor in 3ds Max?', options: ['M', 'E', 'C', 'V'], correctAnswer: 'M' },
      { text: 'The modern physically based material type in 3ds Max is…', options: ['Physical Material', 'Standard', 'Raytrace', 'Ink n Paint'], correctAnswer: 'Physical Material' },
      { text: 'A bump map uses a grayscale image to…', options: ['Fake surface relief by lightening/darkening', 'Change the colour', 'Add transparency', 'Round the geometry'], correctAnswer: 'Fake surface relief by lightening/darkening' },
      { text: 'In PBR, the map that controls how rough or glossy each pixel is…', options: ['Roughness', 'Albedo', 'Opacity', 'Metalness'], correctAnswer: 'Roughness' },
      { text: 'A texture stretching on a 9 m wall that should be real-size bricks is fixed by…', options: ['Enabling Real-World Scale in the UVW Map', 'Increasing the texture brightness', 'Changing the renderer', 'Subdividing the mesh'], correctAnswer: 'Enabling Real-World Scale in the UVW Map' },
      { text: 'Which UVW mapping type projects the image from six sides for a box-like object?', options: ['Box', 'Planar', 'Cylindrical', 'Spherical'], correctAnswer: 'Box' },
      { text: 'The map type that fakes surface detail better than bump by encoding direction is…', options: ['Normal map', 'Opacity map', 'Diffuse map', 'Reflection map'], correctAnswer: 'Normal map' },
      { text: 'Textures should be referenced by…', options: ['Relative project path', 'Absolute C:\\ path', 'Their file size', 'The material name'], correctAnswer: 'Relative project path' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 7 — Lighting Fundamentals
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 7,
    title: 'Lighting Fundamentals',
    description: 'Light is what makes a render feel real — daylight and sun/sky systems for the exterior, photometric lights with true intensities, the three-point setup for interiors and products, and the exterior-vs-interior lighting split.',
    topics: [
      {
        title: 'Daylight, Sun & Sky Systems',
        text: 'The sun and the sky are the two light sources that make an exterior render believable. 3ds Max provides **Daylight** systems that model both together — the sun as a directional light at the correct geographic position, and the sky as a dome of ambient light.\n\n## THE DAYLIGHT SYSTEM\n\nCreate a **Daylight** system (Create > Lights > Daylight). It builds a sun object and a sky object together, linked to a **compass** that sets geographic north. The system asks for a **location** (a city or latitude/longitude), a **date** and a **time**; from those it computes the sun\'s position — the same geometry the real sun follows.\n\n## SUN, SKY AND IBL\n\nThe **Sun** can be a standard directional light or a physically accurate **physical sun** that emits true intensity. The **Sky** is a procedural gradient or a **Skylight** that illuminates everything from above. The modern approach adds **Image-Based Lighting (IBL)** — an HDRI panorama of a real sky loaded as the environment, giving both the visible sky and its light.\n\n## GEOGRAPHIC ACCURACY\n\nSet the project city and the actual date/time. A morning shot has long shadows to the west; noon has short shadows. Moving the date changes the cast shadow — exactly what a solar study needs.\n\n## SHADOWS\n\nThe sun casts **shadow maps** or **ray-traced shadows**. Ray-traced shadows are crisp and accurate for architectural edges; shadow maps are softer and faster. For a believable exterior the sun shadow should be sharp and the sky fill soft — the combination stops the render looking flat.\n\n## The Workflow\n\nCreate the Daylight system, set city/date/time, enable the physical sun and sky, add an HDRI environment, and test-render the shadows. The sun is the hero light; everything else is fill.',
        code: '-- MAXScript: create a daylight system at a location\ndl = Daylight()\ndl.sunlight.position = [0,0,10000]\ndl.location = DaylightLocation city:"Mumbai" latitude:19.1 longitude:72.9\ndl.sunlight.UTCDate = "2026-08-12"\ndl.sunlight.UTCTime = 15.0\ndl.sunlight.ON = true\n-- sky as skylight fill\nsky = Skylight()\nsky.multiplier = 0.6\n-- enable ray-traced shadows on the sun\ndl.sunlight.castsShadows = true',
        note: 'Set the Daylight system to the real city, date and time — a solar study depends on the sun position being geographically exact.'
      },
      {
        title: 'Photometric Lights & Shadows',
        text: 'Interior and night scenes need artificial lights that behave like real lamps. **Photometric lights** carry real-world values — candelas, lumens, lux — and a **distribution** that matches the fitting. That is what makes a lit interior match the real room.\n\n## PHOTOMETRIC UNITS\n\nA **photometric light** is defined by its intensity in **candelas (cd)** or its total output in **lumens (lm)**, and its **distribution**. A 60 W bulb is roughly 800 lm; a downlight maybe 1200 lm. The renderer illuminates the room at real levels, and the camera\'s exposure turns that into a realistic photograph.\n\n## LIGHT SHAPES AND IES\n\nPhotometric lights carry a **distribution**: Uniform, Spotlight, or **Web** — an **IES file** that describes a real luminaire\'s pattern. Manufacturers publish IES files for their fittings. Load the downlight\'s IES and the render shows the exact cone and hot centre of that real product.\n\n## SHADOWS\n\nEvery light casts shadows — **shadow maps** (soft, fast) or **ray-traced shadows** (accurate, from real geometry). A downlight grazing a wall needs ray-traced shadows for the crisp contact shadow; fills can use maps.\n\n## PLACEMENT AND FALLOFF\n\nPhotometric lights use **inverse-square falloff**. A downlight at 3 m lights the floor differently from one at 6 m. Place lights at the real fixture positions and mounting heights and the brightness distribution matches the room.\n\n## The Workflow\n\nPlace lights at the fixtures, set real lumens, load the IES web for visible fittings, and tune the camera exposure. The real lights do the work — almost no fake fill is needed.',
        code: '-- MAXScript: add a photometric downlight with IES\ndl = PhotometricLight()\ndl.distribution = 3          -- web / IES\ndl.web_file = "downlight_35.ies"\ndl.lm = 1200                 -- lumen output\ndl.castsShadows = true\ndl.pos = [x, y, 3.0]         -- 3 m mounting height\n-- copy across the ceiling grid\nfor i = 1 to 6 do instance dl\n',
        note: 'Use the manufacturer\'s IES file for visible fittings — the exact photometric pattern is what makes a rendered downlight match the real one in the room.'
      },
      {
        title: 'Three-Point Lighting Setup',
        text: 'The **three-point lighting** setup is the workhorse of product and interior shots: a **key** that defines the form, a **fill** that softens shadows, and a **rim** that separates the subject from the background. It is simple, predictable, and makes almost anything look professional.\n\n## THE KEY LIGHT\n\nThe **key** is the main light — the one that creates the modelling on the subject (the bright face and the cast shadow). Place it to one side, above the subject (45 degrees up, 45 degrees to the side). For an interior the key might be the window light. Its intensity sets the exposure of the shot.\n\n## THE FILL LIGHT\n\nThe **fill** softens the shadows the key creates — dimmer, from the opposite side, near the camera. Its job is to open the dark side just enough to read detail, not add more light. A fill at 30-50% of the key keeps shadows soft but visible. Without fill the shadow side goes black; with too much the image flattens.\n\n## THE RIM/BACK LIGHT\n\nThe **rim** comes from behind the subject, aimed at its edges. It draws a bright outline that separates the subject from the background — critical for a dark piece of furniture against a dark wall.\n\n## THE RATIOS\n\nThe key-to-fill balance is the **lighting ratio**. A 2:1 ratio is soft and even; 4:1 is more dramatic. Architecture interiors usually sit at 2:1 to 3:1 — bright, readable, realistic.\n\n## The Workflow\n\nPlace the key at 45 degrees, add the fill opposite at low intensity, add the rim behind, adjust the ratios until the shadows read naturally, and test-render. Three controlled lights beat twenty random ones.',
        code: '-- MAXScript: build a three-point rig around a target\nkey = TargetSpot pos:[150, -150, 200] target:subject\nkey.multiplier = 1.0\nfill = TargetSpot pos:[-120, 120, 80] target:subject\nfill.multiplier = 0.35            -- 2.8:1 ratio\nrim = TargetSpot pos:[0, -200, 150] target:subject\nrim.multiplier = 1.2              -- rim slightly hot\n-- enable shadows on key and rim\nkey.castsShadows = true\nrim.castsShadows = true',
        note: 'Three controlled lights beat twenty random ones — key, fill and rim with deliberate ratios are the fastest route to a professional interior or product shot.'
      },
      {
        title: 'Lighting for Exterior vs Interior',
        text: 'Exterior and interior lighting are different crafts. The exterior is dominated by the sun, the sky and the environment; the interior by artificial sources, windows and the balance between the two. Knowing which system to reach for halves the time a scene takes to light.\n\n## EXTERIOR LIGHTING\n\nThe exterior\'s hero is the **Daylight system** — sun plus sky plus environment. For a daylight exterior, the sun gives crisp shadows, the sky fills the shade, and the HDRI supplies the glass reflections. For a **night exterior** the sun is gone: the hero becomes the building\'s own lit windows, the streetlights and the ambient moonlight. Each lit window needs an interior light or a self-illuminated material.\n\n## INTERIOR LIGHTING\n\nThe interior is lit by **photometric artificial lights** plus **daylight through windows**. The classic balance: the window light is the key, artificial fill reaches where the window does not, and each source has its own colour temperature (daylight cool-blue, incandescent warm). The mix of temperatures is what makes an interior feel real.\n\n## THE WINDOW AS A LIGHT SOURCE\n\nIn an interior the window is a large, soft light source. Model it with a **plane light** or a **skylight portal** sized to the window, or let the Daylight system\'s sky enter through clear glass. A large soft source throws soft shadows — the opposite of the sun\'s crisp ones.\n\n## EXPOSURE AND TONE MAPPING\n\nExterior and interior need different **exposure**. A sunny exterior is bright; an interior with a bright window and darker walls needs a balanced exposure so the window is not blown out. The camera exposure and tone mapping set the final brightness.\n\n## The Workflow\n\nExterior: Daylight + HDRI + glass reflections. Interior: photometric fixtures + window plane lights + two colour temperatures. Build each rig once and save it as a scene template.',
        code: '-- MAXScript: set up a night-exterior facade rig\n-- turn off the daylight sun\ndl.sunlight.ON = false\n-- window glow via self-illuminated material\nwinMat = PhysicalMaterial()\nwinMat.emission_color = color 255 220 150\nfor w in windows do w.material = winMat\n-- streetlight photometrics\nfor s in streetlights do (\n  sp = PhotometricLight()\n  sp.lm = 6000\n  sp.pos = s.pos\n)',
        note: 'Exteriors live on sun/sky and glass reflections; interiors live on photometric fixtures and colour temperature — do not light a night facade with a daytime sun.'
      },
    ],
    quizzes: [
      { text: 'The 3ds Max Daylight system computes the sun position from…', options: ['Location, date and time', 'The camera angle', 'The material colour', 'The render size'], correctAnswer: 'Location, date and time' },
      { text: 'A physically accurate exterior should use which sun type?', options: ['Physical sun (mr Sun) with ray-traced shadows', 'An Omni light', 'A free point light', 'No light'], correctAnswer: 'Physical sun (mr Sun) with ray-traced shadows' },
      { text: 'Photometric light intensity is measured in…', options: ['Candelas or lumens', 'Watts only', 'Pixels', 'RGB values'], correctAnswer: 'Candelas or lumens' },
      { text: 'An IES file describes…', options: ['A real luminaire\'s light distribution pattern', 'The texture of a bulb', 'The shadow colour', 'The lens flare'], correctAnswer: 'A real luminaire\'s light distribution pattern' },
      { text: 'In three-point lighting, the light that separates the subject from the background is the…', options: ['Rim/back light', 'Key light', 'Fill light', 'Skylight'], correctAnswer: 'Rim/back light' },
      { text: 'A fill light at 30-50% of the key gives a lighting ratio of about…', options: ['2:1 to 3:1', '1:10', '10:1', '1:1 exactly'], correctAnswer: '2:1 to 3:1' },
      { text: 'A night-exterior facade reads as lit because…', options: ['Each lit window has interior light or a self-illuminated material', 'The sun is on', 'The sky is brighter', 'The camera is wider'], correctAnswer: 'Each lit window has interior light or a self-illuminated material' },
      { text: 'In an interior, the window is best modelled as…', options: ['A large soft light source (plane/skylight portal)', 'A tiny point light', 'A spotlight', 'No light at all'], correctAnswer: 'A large soft light source (plane/skylight portal)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 8 — Rendering & Post-Production
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 8,
    title: 'Rendering & Post-Production',
    description: 'Turn the lit scene into an image — set up the camera and composition, choose render resolution and quality, work through V-Ray or Mental Ray, then finish the image in Photoshop.',
    topics: [
      {
        title: 'Camera Setup & Composition',
        text: 'The camera decides what the client sees. A render is a photograph that never happened, so the camera must obey photographic rules — height, lens, framing and the rule of thirds. Set the camera before lighting, because every light is placed relative to what the camera sees.\n\n## CREATING A CAMERA\n\nThe **Create > Cameras** menu makes **Target** and **Free** cameras. A Target camera has a target object it always looks at — the standard for architecture, because you aim it at the building and orbit around. A Free camera has no target and suits walkthroughs.\n\n## EYE HEIGHT AND LENS\n\nThe professional eye height is **1.6-1.7 m** — a standing person\'s eye. A camera lower looks like a child\'s view; higher looks aerial. The **lens** sets the field of view: **28-35 mm** is the standard architectural lens (wide enough to show the room without distortion), **50 mm** is natural, **20 mm** is very wide with perspective distortion, and longer lenses compress the perspective.\n\n## COMPOSITION\n\nThe **rule of thirds** divides the frame into thirds; place the main subject on the thirds rather than dead centre. Leave **headroom** above a building and show enough ground for it to sit in the scene. The horizon should sit on a third — a horizon through the middle looks static.\n\n## CAMERA AND LIGHTING TOGETHER\n\nLighting is framed by the camera: the sun\'s angle relative to the camera decides whether the facade is lit or in shade. Set the camera first, then aim the daylight so the hero facade is lit.\n\n## The Workflow\n\nPlace the camera at eye height, pick the lens, frame the building on the thirds, then lock the camera while you light. A locked camera means every material is adjusted for the final view.',
        code: '-- MAXScript: create and frame an architectural camera\ncam = TargetCamera fov:62\ncam.pos = [x, y, 1.65]        -- eye height 1.65 m\ncam.target.pos = [bx, by, 6.0]\n-- set the lens via field of view (28mm ~ 65 deg)\ncam.fov = 65.0\n-- view the scene through the camera\nviewport.setCamera cam',
        note: 'Set the camera at a standing eye height of 1.6-1.7 m before lighting — a camera lower than eye height makes even a large room feel like a doll\'s house.'
      },
      {
        title: 'Render Settings: Resolution & Quality',
        text: 'The render settings decide how long the machine works and how good the image looks. Resolution, quality and sampling are the three dials, and the professional skill is the smallest quality that satisfies the client.\n\n## RESOLUTION\n\nResolution is the image size in pixels. A typical deliverable is **4000 px** on the long side for print, **1920 px** for screen. The **aspect ratio** (16:9, 4:3, or the print ratio) matches the composition. Set it in Render Setup (F10); the output size drives everything downstream.\n\n## IMAGE SAMPLING AND QUALITY\n\nQuality is **sampling** — how many rays per pixel to resolve edges, reflections and noise. Low sampling renders fast but noisy (speckled shadows, grainy reflections). High sampling is clean but slow. The **image sampler** (adaptive in V-Ray) spends samples where needed: more at edges and glossy reflections, fewer on flat walls. Test at low, final at high.\n\n## THE RENDER ELEMENTS\n\nProfessional renders are a set of **render elements** (passes): the beauty pass plus **diffuse, reflection, shadow, Z-depth, ambient occlusion**. Each isolates a component of light. Compositing them gives control a single beauty image never does.\n\n## EXPOSURE AND TONE MAPPING\n\nBefore the beauty is final, **exposure** (the camera\'s EV) and **tone mapping** are set. The goal: the bright window holds detail, shadows stay open, and the image looks like a well-exposed photograph.\n\n## The Workflow\n\nSet the resolution for the deliverable, test at low sampling to check composition and lights, raise sampling for the final, and output the beauty plus the key elements. Render time is money — never waste a high-sampling pass on a test.',
        code: '-- MAXScript: configure a final-quality V-Ray render\nr = renderers.current\nr.imageSampler_type = 2          -- adaptive\nr.imageSampler_maxSubdivs = 24\nr.colorMapping_type = 2          -- exponential tone map\nr.output_width = 4000\nr.output_height = 2250\nrender outputFile:"C:/render/final.exr" vfb:false',
        note: 'Test at low sampling, render the final at high sampling — spending a 24-subdivision final pass on a composition you will move is wasted machine time.'
      },
      {
        title: 'V-Ray / Mental Ray Workflow',
        text: 'The look of a render is decided by the renderer. **V-Ray** and (legacy) **Mental Ray** are the two heavyweights of architectural visualisation. They share the same concepts — global illumination, image sampling, render elements — with different dialogs and presets.\n\n## GLOBAL ILLUMINATION\n\nBoth use **Global Illumination (GI)** — light that bounces off surfaces to light others. Without GI the shadow side of a room is black. V-Ray\'s GI is two-bounce: the **primary engine** (often **Irradiance Map** or **Light Cache**) computes the soft low-frequency bounce, and the **secondary engine** handles the rest. The Irradiance Map is the classic quality dial.\n\n## THE V-RAY FRAME BUFFER\n\nThe V-Ray Frame Buffer shows the render, the exposure controls, and the render elements as layers. It lets you tune exposure **after** the render without re-rendering — the standard workflow.\n\n## MATERIALS AND LIGHTS BRIDGE\n\nV-Ray has its own **VRayMtl** and **V-Ray lights** (plane, sphere, dome, IES). A scene lit with standard lights renders differently under V-Ray than Mental Ray. Commit to one renderer\'s materials and lights, not a mix. Mental Ray used its **Arch & Design** material; V-Ray uses VRayMtl — both are PBR-ish and accept the same maps.\n\n## RENDER ELEMENTS IN EACH\n\nBoth expose the **render elements** (V-Ray: VRayDiffuse, VRayReflection, VRayShadow; Mental Ray: Diffuse, Reflection) for compositing. The element set is the Photoshop deliverable.\n\n## The Workflow\n\nChoose V-Ray, set the GI engine and quality preset, convert materials to VRayMtl, light with V-Ray lights, render to the Frame Buffer, tune exposure, output beauty plus elements. Mental Ray follows the same shape — only the names differ.',
        code: '-- MAXScript: V-Ray production workflow in one go\nrenderers.current = V_Ray_Adv_2_10_03()\nr = renderers.current\nr.gi_on = true\nr.gi_primary_type = 0        -- irradiance map\nr.gi_primary_irradmap_preset = 3   -- medium\nr.gi_secondary_type = 1      -- light cache\nr.imageSampler_type = 2      -- adaptive DMC\n-- convert selection to VRayMtl\nfor obj in selection do obj.material = VRayMtl()',
        note: 'Commit to one renderer\'s materials and lights — mixing V-Ray lights with standard materials is the most common cause of a render that refuses to look right.'
      },
      {
        title: 'Post-Production in Photoshop',
        text: 'The render leaves the engine, but the image is finished in **Photoshop**. Post-production is where the visualisation stops being a "3D render" and becomes a photograph: people, cars, trees, sky, atmosphere and colour correction are added in the composite.\n\n## THE COMPOSITE WORKFLOW\n\nOpen the beauty pass and stack the **render elements** on top as layers (diffuse, reflection, shadow, Z-depth). With the passes separated you can darken reflections, lighten shadows, blur the background using Z-depth, and change the light without re-rendering. The base image drives the whole pass stack, so keep it clean before any adjustment is applied.\n\n## ADDING PEOPLE AND CARS\n\nPeople and cars make a building look used. Cut them from photo libraries, scale them to the scene (a person is ~1.7 m — the camera height, a handy scale check), and match the lighting: same sun angle, same shadow direction, same colour temperature. Drop a soft shadow under each figure. A floating person ruins the realism instantly.\n\n## ATMOSPHERE AND SKY\n\nReplace or enhance the sky with a real sky photo, add atmospheric perspective (haze near the horizon), and warm or cool the image with a **Photo Filter** or **Curves** layer. The sky and the ground reflections should agree.\n\n## COLOUR CORRECTION\n\nFinal colour uses **adjustment layers**: Curves (contrast and balance), Levels (white/black points), and a subtle **Vignette** to hold the eye. Adjustments are non-destructive — the base render stays untouched.\n\n## The Deliverable\n\nFlatten and save: JPEG for review, TIFF/PSD for the archive. The post-processed image is what the client signs off — the render is only a starting point.',
        code: '-- (Photoshop actions are manual; this is the checklist)\n-- 1. Open beauty pass, stack render elements as layers\n-- 2. People layer: paste, scale to 1.7 m, match sun\n-- 3. Sky layer: paste behind the building, feather edge\n-- 4. Atmosphere: linear gradient haze near the horizon\n-- 5. Curves layer: lift shadows, warm the highlights\n-- 6. Vignette layer: soft black ellipse at frame edge',
        note: 'A person in a render is a built-in scale reference — if the figure is not ~1.7 m tall against the door, the whole image reads wrong.'
      },
    ],
    quizzes: [
      { text: 'The professional eye height for an architectural camera is about…', options: ['1.6-1.7 m above the floor', '0.5 m above the floor', '3 m above the floor', 'The roof height'], correctAnswer: '1.6-1.7 m above the floor' },
      { text: 'The standard wide-but-not-distorted lens for architecture is…', options: ['28-35 mm', '12 mm', '200 mm', '85 mm'], correctAnswer: '28-35 mm' },
      { text: 'Render quality is controlled mainly by…', options: ['Image sampling (rays per pixel)', 'The monitor resolution', 'The file format', 'The object count'], correctAnswer: 'Image sampling (rays per pixel)' },
      { text: 'Separate passes like reflection and shadow are called…', options: ['Render elements', 'Layers', 'Channels', 'Masks'], correctAnswer: 'Render elements' },
      { text: 'Global Illumination (GI) is the renderer\'s way of…', options: ['Bouncing light between surfaces', 'Adding sunlight', 'Making shadows darker', 'Increasing resolution'], correctAnswer: 'Bouncing light between surfaces' },
      { text: 'V-Ray\'s classic quality preset for the primary GI engine is…', options: ['Irradiance Map', 'Photon Map', 'Final Gather', 'Radiosity'], correctAnswer: 'Irradiance Map' },
      { text: 'The V-Ray Frame Buffer lets you…', options: ['Tune exposure after rendering without re-rendering', 'Model new geometry', 'Edit materials only', 'Play animations'], correctAnswer: 'Tune exposure after rendering without re-rendering' },
      { text: 'In Photoshop, a person cut into a render should be scaled to about…', options: ['1.7 m tall to match the scene scale', 'Any size that fits', 'Half the door height', 'Double the camera height'], correctAnswer: '1.7 m tall to match the scene scale' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 9 — SketchUp Core Modeling Tools
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 9,
    title: 'SketchUp Core Modeling Tools',
    description: 'SketchUp is fast, forgiving and perfect for architectural massing — learn the interface and axes, the Push/Pull, Offset and Follow Me tools that build forms, how groups and components keep a model clean, and how styles and scenes manage the view.',
    topics: [
      {
        title: 'The SketchUp Interface & Axes',
        text: 'SketchUp\'s power is its simplicity: one window, a few tools, and a model that feels like drawing with a pen. The **axes** are the key to that simplicity — three coloured lines (red X, green Y, blue Z) that lock the inferencing system. Everything in SketchUp snaps to those axes unless you tell it otherwise.\n\n## THE TOOLBARS AND THE STARTUP WINDOW\n\nSketchUp opens with the **Getting Started** toolbar: Select, Line, Rectangle, Circle, Arc, Push/Pull, Offset, Move, Rotate, Scale, Eraser, Paint, and the Orbit/Pan/Zoom navigation. The **Startup** template sets units (metres, millimetres, feet-inches) and the model\'s coordinate style. Set the template once — a project in feet drawn on a metre template mislabels every dimension.\n\n## THE AXES AND INFERENCING\n\nThe **origin** is where the three axes meet, usually at ground level. As you draw, SketchUp **infers** alignment — a line snaps to red/green/blue, endpoints and midpoints highlight, and dotted inference lines appear for aligned points. Watch the cursor: when it snaps to an axis the line turns red or green; when it finds a point it shows a coloured dot. These inferences are how you draw precisely without typing coordinates.\n\n## THE MODELLING BASIC: FACE AND EDGE\n\nA closed loop of coplanar edges creates a **face** (a shaded surface). Faces are what you push/pull into volumes. Draw a rectangle (four edges) and SketchUp fills it with a face instantly. If a rectangle does not fill, the edges are not coplanar or not closed — the most common beginner frustration. The face is the currency of SketchUp modelling.\n\n## NAVIGATION\n\nThe navigation tools orbit (rotate the view), pan and zoom. The mouse wheel zooms; hold the middle button to orbit; **Shift + middle** to pan. There is no "up" in SketchUp\'s orbit until you align the axes — use the axis indicators or a Scene to return to a fixed view.\n\n## The Workflow\n\nCheck the units, snap to the axes as you draw, and close every loop to make faces. A clean, face-bearing model is the foundation of every Push/Pull that follows.',
        code: '# SketchUp Ruby API: create a face from edges on the ground plane\nmodel = Sketchup.active_model\nentities = model.entities\n# four points forming a 10 m x 6 m rectangle at z=0\npts = [\n  Geom::Point3d.new(0, 0, 0),\n  Geom::Point3d.new(10, 0, 0),\n  Geom::Point3d.new(10, 6, 0),\n  Geom::Point3d.new(0, 6, 0)\n]\nface = entities.add_face(pts)\nputs "Face area: #{face.area} m2"',
        note: 'A rectangle that refuses to fill with a face means the edges are not coplanar or not closed — fix the loop before reaching for Push/Pull.'
      },
      {
        title: 'Push/Pull, Offset & Follow Me',
        text: 'Three tools do most of SketchUp\'s modelling: **Push/Pull** extrudes a face into a volume, **Offset** creates parallel copies of edges, and **Follow Me** sweeps a profile along a path. Master these and you can build almost any architectural form.\n\n## PUSH/PULL\n\nThe **Push/Pull** tool (P) takes a face and extrudes it: click a face, move up, click again (or type a height). A rectangle becomes a box; a wall outline becomes a wall. Push/Pull is forgiving — double-click a new face to repeat the last push/pull distance, and you can push a face inward to cut a recess or a hole. It is the tool that turns 2D into 3D.\n\n## OFFSET\n\nThe **Offset** tool (F) copies an edge or a face boundary at a set distance, making parallel lines. Draw a rectangle, then offset its edges inward to create a wall outline; offset a circle to make a ring. Offset is how a floor plan outline becomes a wall plan — the outer edge and the offset inner edge are the wall faces.\n\n## FOLLOW ME\n\nThe **Follow Me** tool sweeps a profile face along a path. Draw the profile (a moulding cross-section, a roof eave), select the path (the room perimeter), and click Follow Me on the profile — the profile travels the path and creates the swept volume. Follow Me is how you make baseboards, crown mouldings, handrails and rounded profiles in one step.\n\n## THE COMBINATION\n\nThe classic sequence: **Offset** the plan to get wall thickness, **Push/Pull** the wall up to height, then **Follow Me** a trim profile along the wall top. Each tool feeds the next — offset defines the footprint, push/pull gives it height, follow-me dresses it.\n\n## The Discipline\n\nPush/Pull wants a clean single face — a face with a stray edge on it splits the push/pull. Offset wants a clean selection — select the exact edges, not the face. Follow Me wants a perpendicular profile. Clean inputs make all three tools predictable.',
        code: '# SketchUp Ruby API: push/pull a wall face up 3 m\nmodel = Sketchup.active_model\nentities = model.entities\n# find the face from the previous rectangle\nface = entities.grep(Sketchup::Face).first\nface.pushpull(3.0)   # extrude up 3 m (positive = normal)\n# offset the outer edges by 0.2 m\nedges = face.edges\n# (in the UI you would use the Offset tool; API uses entities.add_edges)\n',
        note: 'Push/Pull on a face that contains a stray edge splits the extrusion — keep the face clean and the push/pull result is predictable.'
      },
      {
        title: 'Groups, Components & Component Instances',
        text: 'A model made of raw edges and faces is impossible to edit — move one wall and a neighbouring wall follows accidentally. **Groups** and **Components** wrap geometry into single, selectable objects. They are the difference between a sketch and a maintainable model.\n\n## GROUPS\n\nA **Group** is a container of geometry: select several faces/edges, right-click, **Make Group**. The group becomes one object — click it once and the whole group selects; edit it by double-clicking to enter the group, change the geometry, and exit. Groups are independent: editing one group never changes another. Use groups for one-off assemblies (a unique wall, a single piece of furniture).\n\n## COMPONENTS\n\nA **Component** is a group with a name and an identity. When you copy a component, each copy is an **instance** — they all share the same definition. Edit one instance and every instance updates. This is the tool for anything repeated: windows, doors, chairs, trees, columns. A building with fifty identical windows has fifty instances of one window component — change the component once and all fifty change.\n\n## THE DIFFERENCE THAT MATTERS\n\nA group is a one-off; a component is a repeating item. Use a group for the wall that appears once, a component for the door that appears twenty times. The instinct to make everything a component is wrong — over-componentising bogs the model. Choose based on repetition.\n\n## COMPONENT INSTANCES AND EDITING\n\nDouble-click an instance to edit its definition (all instances update). Right-click a component for **Make Unique** — this copies the definition so you can edit just that one instance (for a door that is slightly different). Components can also be nested (a window component containing a glass component).\n\n## The Discipline\n\nGroup or component EVERYTHING that is more than a few faces. Raw geometry in a model is editing chaos — a component hierarchy is how a large model stays editable.',
        code: '# SketchUp Ruby API: create a component from selected geometry\nmodel = Sketchup.active_model\nselection = model.selection\ngroup = selection[0].parent.entities.add_group(selection)\n# turn the group into a component definition\ncomp_def = group.to_component\ncomp_def.name = "Window-1500"\n# place instances of the component\ntr = Geom::Transformation.translation([0,0,0])\ninst1 = model.entities.add_instance(comp_def, tr)\ntr2 = Geom::Transformation.translation([3.6,0,0])\nmodel.entities.add_instance(comp_def, tr2)',
        note: 'Edit one instance of a window component and every identical window in the model updates — that single edit is why components beat raw geometry for anything repeated.'
      },
      {
        title: 'Styles, Scenes & Viewport Management',
        text: 'A model is a set of views as much as a set of faces. **Scenes** save named camera positions and display settings; **Styles** control how the model is drawn (wireframe, shaded, hidden line, with or without faces). Together they manage what the client and the drafter see.\n\n## STYLES\n\nThe **Styles** panel (Window > Default Tray > Styles) controls the face style and the edge style. A **hidden-line** style shows clean edges for a presentation; a **shaded** style shows colours and faces for modelling; an **x-ray** style makes everything translucent for seeing inside. The **Edge** settings toggle profiles (thicker edges on the silhouette), depth-cued edges, and extension. For a drawing-like output, hidden-line with profile edges is the classic architectural look.\n\n## SCENES\n\nThe **Scenes** panel (or the **Tabs** at the top) saves a snapshot of the camera and the display: a "Front Elevation" scene, a "Ground Floor" scene, a "3D Overview" scene. Create a scene, position the camera, adjust the style, then **Add Scene**. Clicking a tab restores that exact view instantly. Scenes are the navigation for presentations and the anchor for Layout viewports.\n\n## SCENE PROPERTIES\n\nEach scene remembers which properties to update: **Camera Location**, **Hidden Geometry**, **Visible Layers**, **Style**, **Shadow Settings**, **Section Planes**. Uncheck what you do not want a scene to control — otherwise updating one scene and switching tabs fights your layers. A scene that controls visible layers is how you show "ground floor" and "first floor" from one model.\n\n## SECTION PLANES\n\nA **Section Plane** cuts the model to reveal the interior — place it at a level, and the model shows a section cut with a hatch. Scenes can toggle section planes on/off, which is how an architectural set shows the plan at each level from one model.\n\n## The Workflow\n\nSet up named scenes for every view the client will see, pair each with the right style and layer state, and use section planes for plans. The scene list IS the presentation.',
        code: '# SketchUp Ruby API: create a scene with a fixed camera\nmodel = Sketchup.active_model\npages = model.pages\npage = pages.add("Ground Floor")\n# set the camera\neye = Geom::Point3d.new(5, -12, 3)\ntarget = Geom::Point3d.new(5, 5, 1)\ncam = Sketchup::Camera.new(eye, target, [0,0,1])\npage.camera = cam\npage.update_attributes({"useCamera" => true,\n                        "useHiddenGeometry" => true})',
        note: 'Scenes paired with layer visibility are how one model produces the whole drawing set — uncheck what a scene should not control or the tabs fight your layers.'
      },
    ],
    quizzes: [
      { text: 'In SketchUp, a closed loop of coplanar edges creates…', options: ['A face', 'A component', 'A layer', 'A section'], correctAnswer: 'A face' },
      { text: 'Which tool extrudes a face into a 3D volume?', options: ['Push/Pull', 'Offset', 'Follow Me', 'Scale'], correctAnswer: 'Push/Pull' },
      { text: 'The Offset tool is used to…', options: ['Create parallel copies of edges at a set distance', 'Rotate a face', 'Extrude a profile', 'Hide geometry'], correctAnswer: 'Create parallel copies of edges at a set distance' },
      { text: 'A profile swept along a path is made with…', options: ['Follow Me', 'Push/Pull', 'Orbit', 'Line'], correctAnswer: 'Follow Me' },
      { text: 'The difference between a group and a component is…', options: ['Components repeat and update all instances; groups are one-offs', 'Groups are 2D, components 3D', 'Components cannot be edited', 'Groups update all copies'], correctAnswer: 'Components repeat and update all instances; groups are one-offs' },
      { text: 'To edit one instance of a component without changing the others, use…', options: ['Make Unique', 'Explode', 'Hide', 'Lock'], correctAnswer: 'Make Unique' },
      { text: 'A named saved camera position and display state in SketchUp is a…', options: ['Scene', 'Layer', 'Component', 'Style'], correctAnswer: 'Scene' },
      { text: 'A Section Plane in SketchUp…', options: ['Cuts the model to reveal the interior', 'Deletes the model', 'Flattens the model', 'Changes the units'], correctAnswer: 'Cuts the model to reveal the interior' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 10 — Components, Groups & Organization
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 10,
    title: 'Components, Groups & Organization',
    description: 'Organise the model like a professional — when to use a group vs a component, building a component library with dynamic components, managing the Outliner and layers, and modelling terrain with curves, arcs and the Sandbox tools.',
    topics: [
      {
        title: 'Groups vs Components: When to Use Each',
        text: 'The single most important organisational decision in SketchUp is **group or component?** Both wrap geometry into one object, but they behave differently on copy. Choosing correctly keeps a model fast, editable and predictable.\n\n## THE CORE RULE\n\nA **group** is a one-off container. Select geometry, right-click, Make Group — it is now one object, edited by double-clicking. Copy a group and the copies are **independent**: editing one does nothing to the others. Use groups for geometry that appears once — a unique wall, a site feature, a bespoke piece of furniture.\n\nA **component** is a named, repeating definition. Copy it and every copy is an **instance** of the same definition. Edit any instance (double-click) and all update. Use components for anything repeated — doors, windows, columns, chairs, trees, balustrade posts.\n\n## THE DECISION TEST\n\nAsk: will I need this more than once, and would I want a change to hit every copy? A window used 20 times on the facade is a component. A ground-floor wall that appears once is a group. The classic error is over-componentising — making every scrap of geometry a component — which bloats the file with definitions and slows editing.\n\n## MIXING THEM\n\nComponents can contain groups and groups can contain components. A window component can hold a glass group and a frame group inside it. Nesting lets you edit the frame without touching the glass.\n\n## RAW GEOMETRY IS THE ENEMY\n\nEdges and faces left loose on the model are editing chaos: select a wall and the floor sticks to it. Group or component everything that is more than a few faces. The rule of thumb: if it will be selected as a unit, it should be a group or component.\n\n## The Workflow\n\nSketch the geometry, decide group-or-component by repetition, wrap it, then copy or instance. The Outliner (next topic) shows the result as a clean tree — a model you can navigate.',
        code: '# SketchUp Ruby API: wrap selection as a group or component\nmodel = Sketchup.active_model\nsel = model.selection\n# group (one-off)\ngroup = model.entities.add_group(sel)\n# component (repeating)\ncomp_def = group.to_component\ncomp_def.name = "Column-300"\n# instance it across a grid\n(0..5).each do |i|\n  t = Geom::Transformation.translation([i * 3.0, 0, 0])\n  model.entities.add_instance(comp_def, t)\nend',
        note: 'Over-componentising is as bad as under-organising — components carry a definition each, so use them for what repeats and groups for the one-offs.'
      },
      {
        title: 'Component Library & Dynamic Components',
        text: 'A model is only as good as its library. **Component libraries** hold the doors, windows, furniture and trees you reuse on every project; **Dynamic Components** add logic — size options, materials, auto-sizing on resize — so one component covers a family of sizes.\n\n## THE COMPONENT LIBRARY\n\nThe **Component panel** (Window > Components) shows the library: the **Components Sampler** (bundled furniture and trees), your **In Model** components (everything used in the current model), and the **Component Browser** of local folders. Professional studios keep a shared network folder of company components — windows with the right profiles, doors with the right hardware — so every project starts from the same library.\n\n## SAVING AND ORGANISING COMPONENTS\n\nSave a component to the library by right-clicking the component in the model and **Save As**, into a named folder structure (Doors/, Windows/, Furniture/). Name files consistently (Door-860.skp, Window-1500.skp). The library is the studio\'s reusable capital — a good library makes a new project 80% assembly.\n\n## DYNAMIC COMPONENTS\n\nA **Dynamic Component** carries attributes and logic defined in the **Component Attributes** panel. A dynamic window component can have a **Width** attribute; type a new width and the frame and panes resize. It can swap materials based on a dropdown, or show/hide parts. The logic uses simple formulas (e.g. Width = 2 * SidePanel + CenterPane). This turns one component into a whole product family — one door component, every door size.\n\n## INTERACTION AND BEHAVIOUR\n\nDynamic components can respond to **Interact** clicks (a door that opens when clicked), carry **price/cost** attributes for quantity takeoffs, and drive **schedules**. The attribute set is a mini-spreadsheet attached to the geometry.\n\n## The Workflow\n\nBuild the component, add the size/materials attributes, save it to the library folder, then drag instances into every project. The library and its dynamic components are what make SketchUp fast for repeat building types.',
        code: '# SketchUp Ruby API: add attributes to a component definition\ncomp_def = model.definitions["Door-860"]\ncomp_def.add_attribute("dynamic_attributes", "_name", "Door-860")\ncomp_def.set_attribute("dynamic_attributes", "Width", "860")\ncomp_def.set_attribute("dynamic_attributes", "Height", "2100")\ncomp_def.set_attribute("dynamic_attributes",\n  "lenX", "=Width")   # drives the X size of the box\n# now typing a new Width resizes the door',
        note: 'A dynamic door component with a Width attribute replaces a hundred saved door files — one component, every size, and the schedule reads the same attribute.'
      },
      {
        title: 'Outliner & Layers for Organization',
        text: 'As a model grows, the scene is filled with objects — and you need a way to see what is what. The **Outliner** shows the hierarchy of groups and components as a tree; **Layers** (Tags) control visibility and organise the model by discipline. Together they are the model\'s filing system.\n\n## THE OUTLINER\n\nThe **Outliner** panel (Window > Default Tray > Outliner) lists every group and component as a tree. Nested components appear as branches; click an entry to select the object in the model, double-click to edit it, or right-click for context actions. The Outliner is the fastest way to grab a specific object buried in the scene — and it shows the hierarchy you built with groups and components. **Name everything**: a component called "Window-1500" in the Outliner beats "Component#12".\n\n## LAYERS (TAGS)\n\nSketchUp\'s **Layers** (recent versions call them **Tags**) are visibility flags, not CAD-style layers. An object sits on one layer; the layer can be hidden to simplify the view. Organise by discipline: a "Site" layer for the terrain, a "Structure" layer for columns, a "Furniture" layer for chairs. Turn the furniture layer off to see the bare building.\n\n## LAYER DISCIPLINE\n\nPut every group/component on a named layer as you create it — the Layer panel assigns the current layer to new geometry. A model with everything on "Layer0" defeats the whole system. The Outliner plus layers gives two handles on the model: the tree (what it is) and the layer (what discipline it belongs to).\n\n## HIDING vs LAYERS\n\n**Hide** (right-click) hides a single object until unhidden; a **layer off** hides everything on that layer. Use hide for a temporary peek, layers for a persistent organisation. Scenes can toggle layer visibility, so a "Plumbing Off" scene is one click.\n\n## The Workflow\n\nCreate the layer set before modelling, assign every new group/component to a layer, and name entries in the Outliner. The result is a model any colleague can navigate blind.',
        code: '# SketchUp Ruby API: put an object on a named layer\nmodel = Sketchup.active_model\nlayers = model.layers\nlayer = layers.add("Structure")\n# create a component and move it to the layer\ninst = model.entities.add_instance(comp_def, Geom::Transformation.new)\ninst.layer = layer\n# list all layers\nlayers.each { |l| puts l.name }',
        note: 'Everything on "Layer0" is a model no one can navigate — put every object on a named discipline layer and name every Outliner entry.'
      },
      {
        title: 'Curves, Arcs & Sandbox Terrain',
        text: 'Architecture is not all straight walls — curved drives, radial stairwells and undulating sites need **curves, arcs** and terrain tools. SketchUp\'s Arc and Curve tools build smooth geometry, and the **Sandbox** tools model ground surfaces from contour data.\n\n## ARCS AND CURVES\n\nThe **Arc** tool (A) draws a true arc from a start, an end and a bulge; the **2-Point Arc** and **3-Point Arc** variants cover other inputs. Arcs are built from line segments — the **Segments** field sets how many (more segments, smoother but heavier). A circle is an arc of 360 degrees. For a **freeform curve**, the **Freehand** tool draws a loose spline, but it creates many segments — prefer arcs or the **Bezier** extension for clean curves.\n\n## CURVES FROM CAD\n\nImport a CAD plan with curved roads or curbs and SketchUp reads the polylines as curve entities. Explode them carefully — an exploded spline becomes many small segments. Keeping imported curves intact (import as a group, do not explode) preserves the smooth geometry.\n\n## THE SANDBOX TOOLS\n\nThe **Sandbox** tools (Extensions > Sandbox, or the toolbar) model terrain. **From Contours**: select the contour lines (imported from CAD or the site survey) and SketchUp generates a **TIN** — a triangulated ground surface draped over the contours. **From Scratch** builds a flat grid you then sculpt with **Sculpt** (push/pull points) and **Smoothed Surface**.\n\n## DRAFTING ON TERRAIN\n\nA building on a slope needs the terrain modelled first: build the TIN from contours, then place the building pad by intersecting or by projecting. The **Stamp** tool (Sandbox) presses a shape into the terrain to create a flat pad — the professional way to seat a building on a hill.\n\n## The Workflow\n\nImport the survey contours, generate the Sandbox surface, stamp the building pad, then model the building on the pad. Curves stay clean and the terrain reads as a real site.',
        code: '# SketchUp Ruby API: build a surface from contour edges\nmodel = Sketchup.active_model\nentities = model.entities\n# assuming contour edges are selected\nedges = model.selection.grep(Sketchup::Edge)\n# add edges into a group and use the Sandbox "From Contours"\ngroup = entities.add_group(edges)\n# (From Contours is a UI tool; in Ruby you triangulate manually)\n# create an arc drive\ncenter = Geom::Point3d.new(0,0,0)\narc = entities.add_arc(center, Geom::Vector3d.new(1,0,0),\n       Geom::Vector3d.new(0,1,0), 12.0, 0, Math::PI/2, 24)',
        note: 'A building on a slope is seated with the Sandbox Stamp — pressing the footprint into the terrain makes a flat pad that matches the contours.'
      },
    ],
    quizzes: [
      { text: 'Use a group for geometry that…', options: ['Appears once and copies are independent', 'Repeats and must update everywhere', 'Is always hidden', 'Is on Layer0'], correctAnswer: 'Appears once and copies are independent' },
      { text: 'Use a component for geometry that…', options: ['Repeats and every copy should update together', 'Appears exactly once', 'Is a unique wall', 'Cannot be edited'], correctAnswer: 'Repeats and every copy should update together' },
      { text: 'A Dynamic Component in SketchUp can…', options: ['Resize from an attribute like Width and swap materials', 'Only change colour', 'Only be copied', 'Not be saved'], correctAnswer: 'Resize from an attribute like Width and swap materials' },
      { text: 'The panel that shows the hierarchy of groups and components as a tree is the…', options: ['Outliner', 'Layers', 'Styles', 'Scenes'], correctAnswer: 'Outliner' },
      { text: 'SketchUp layers (tags) primarily control…', options: ['Visibility of objects by discipline', 'The drawing scale', 'The units', 'The render engine'], correctAnswer: 'Visibility of objects by discipline' },
      { text: 'To keep imported CAD curves smooth, you should…', options: ['Import as a group and avoid exploding the spline', 'Explode them into segments', 'Trace them by hand', 'Delete them'], correctAnswer: 'Import as a group and avoid exploding the spline' },
      { text: 'The Sandbox tool that generates a ground surface from contour lines is…', options: ['From Contours', 'Stamp', 'Sculpt', 'Smooth'], correctAnswer: 'From Contours' },
      { text: 'The Sandbox tool that presses a flat building pad into terrain is…', options: ['Stamp', 'From Contours', 'Drape', 'Flatten'], correctAnswer: 'Stamp' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 11 — Extensions & Workflow
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 11,
    title: 'Extensions & Workflow',
    description: 'SketchUp is only as powerful as its extensions — install from the Extension Warehouse, script repetitive tasks with the Ruby Console, move geometry through DWG/OBJ/3DS, and build a workflow fast enough for rapid prototyping.',
    topics: [
      {
        title: 'The Extension Warehouse & Key Extensions',
        text: 'Out of the box SketchUp is a fast modeller. **Extensions** are where it becomes a professional tool — the **Extension Warehouse** is the official store of plugins that add everything from advanced curve tools to energy analysis.\n\n## THE EXTENSION WAREHOUSE\n\nOpen **Extension Warehouse** from the Extensions menu (or the button on the Welcome screen). It lists thousands of extensions with ratings, download counts and version compatibility. **Install** downloads and installs in one click; the extension appears in the Extensions menu or a new toolbar. Extensions are the plugin ecosystem that keeps SketchUp ahead of its price class.\n\n## THE MUST-HAVE EXTENSIONS\n\nThe professional toolkit usually includes: **SketchUcation Tools** (a free pack of 45+ utilities: weld, erase stray lines, fix problems), **TT_Lib** and **LibFredo6** (shared libraries many extensions require), **RoundCorner** and **FredoScale** (advanced rounding and deformation), **Curviloft** (lofted and skinned surfaces), **Bezier Spline** (true spline curves), **SketchUp STL** (export for 3D printing), and **DropGC** or **Eneroth Solid Tools** for solid modelling.\n\n## EXTENSION DISCIPLINE\n\nInstall only what you need — every extension adds a startup load and a menu entry. Check the compatible SketchUp version before installing (an extension written for SketchUp 2020 may break in 2024). Keep the Warehouse\'s **Updates** tab clean by updating extensions when the version bumps. A bloated extension list slows SketchUp and masks real errors.\n\n## THE TRUST QUESTION\n\nPrefer extensions with high install counts, active forums and a named developer. Free extensions from unknown authors can carry malware — the Warehouse screens submissions, but the direct-download ecosystem (forums, GitHub) does not. Treat an untrusted extension like an untrusted installer.\n\n## The Workflow\n\nInstall the core free libraries first (TT_Lib, LibFredo6), then the tools your discipline needs (Curviloft for curved roofs, STL for fabrication), and keep the list lean. A curated extension set makes SketchUp feel like a bespoke tool.',
        code: '# SketchUp Ruby API: check installed extensions\nrequire \'sketchup\'\n# list all loaded extensions\nSketchup.extensions.each do |ext|\n  puts "#{ext.name} v#{ext.version}"\nend\n# register your own extension (in a .rb file)\nUI.register_extension("MyTool",\n  "mytool/main.rb", false)   # false = not load-at-startup',
        note: 'The Extension Warehouse is the safe source — extensions from unknown direct downloads can carry malware, so prefer rated, high-install plugins.'
      },
      {
        title: 'Ruby Console & Macros',
        text: 'SketchUp is scriptable through the **Ruby Console** — a built-in Ruby interpreter that drives the API. A single line can move 500 objects; a saved **macro** automates a whole repetitive workflow. The console is the difference between clicking and commanding.\n\n## THE RUBY CONSOLE\n\nThe **Ruby Console** (Window > Ruby Console) opens an interactive prompt. Type Ruby and press Enter: Sketchup.active_model returns the current model, model.entities.count counts objects. The console shows errors in red — and it is the fastest way to test a script before saving it. Every UI action has a Ruby equivalent, so anything you can click you can script.\n\n## THE API PATTERN\n\nThe API is built from a few objects: Sketchup.active_model (the model), model.entities (all geometry), Geom::Point3d and Geom::Transformation (math), UI.messagebox (user feedback). entities.add_face(pts) adds a face, face.pushpull(3.0) extrudes it. Read the API reference at the official developers site — the method names read like English.\n\n## MACROS AND RUBY FILES\n\nSave a snippet as a .rb file in the Plugins folder, or load it with **Ruby Console > Load**. A **macro** can: create a grid of columns, place a window on every wall, check for stray edges, or rename every component. The classic macro: select a group of objects, run the script, and it iterates and renames them all with a prefix.\n\n## REPEATING WORK\n\nAnything you do more than twice is a macro candidate. The workflow: do it manually once, record the pattern, script the pattern, run the script on the next 100 instances. SketchUp macros are how a one-day task becomes an hour.\n\n## The Discipline\n\nTest macros on a copy of the model — a bad loop can delete geometry and SketchUp\'s Undo only goes so far. Save macros in a versioned folder, not inside the model file.',
        code: '# SketchUp Ruby Console: loop over selected groups and rename\nmodel = Sketchup.active_model\nmodel.selection.grep(Sketchup::Group).each_with_index do |g, i|\n  g.name = "Panel-#{i+1}"\nend\n# move every instance of a component up 1 m\ntr = Geom::Transformation.translation([0,0,1])\nmodel.entities.grep(Sketchup::ComponentInstance) do |ci|\n  ci.transform!(tr)\nend',
        note: 'Anything you click more than twice is a macro candidate — the Ruby API mirrors the UI, so every action has a scriptable equivalent.'
      },
      {
        title: 'Importing/Exporting: DWG, OBJ, 3DS',
        text: 'A SketchUp model rarely lives alone — it arrives from CAD, leaves to the renderer, or ships to the fabricator. **Import** and **Export** move geometry between DWG (CAD), OBJ (rendering), 3DS (legacy) and STL (fabrication) without losing the work.\n\n## IMPORTING DWG\n\n**File > Import** (DWG/DXF) brings AutoCAD linework into SketchUp. Import as a **group** so the CAD block arrives intact, and on the Options tab set the correct **Units** and scale — a CAD file drawn in millimetres imported as metres inflates everything 1000x. After import, **Explode** only what you need to edit; keep the rest grouped. Imported CAD is the source of site plans, floor plans and elevations.\n\n## THE IMPORT CHECKLIST\n\nBefore importing, tidy the CAD in AutoCAD: purge unused layers, delete stray points, run **Overkill** to merge duplicate lines. SketchUp imports what it sees — garbage in, garbage out. After importing, check the **model info units** against the source drawing. Use the DWG import **Preview** to confirm scale before accepting.\n\n## EXPORTING FOR RENDERING\n\n**OBJ** is the universal renderer format (V-Ray, Enscape, Blender read it). Export OBJ with UVs and materials for textured renders; triangulate for compatibility. **3DS** is the legacy format — fine for old pipelines but carries no materials well. **STL** (via the SketchUp STL extension) exports solid, watertight meshes for 3D printing.\n\n## EXPORTING FOR CAD\n\nSketchUp can **Export DWG** — useful for sending massing or plans back to AutoCAD. The result is linework: faces become edges, curves become polylines. Set the export scale and version to match the receiving office (DWG 2013 is widely safe).\n\n## The Workflow\n\nTidy in the source app, import as a group with correct units, model, then export the right format for the destination — OBJ/3DS to renderers, STL to fabrication, DWG back to CAD.',
        code: '# SketchUp Ruby API: import a DWG and export OBJ\nmodel = Sketchup.active_model\n# import DWG (returns array of added entities)\nstatus = model.import("site_plan.dwg", false)\nputs "Imported: #{status}"\n# export OBJ (requires a writer; the API exposes export via model.export)\noptions = { "Triangulate" => true, "OutputUVs" => true }\nmodel.export("model.obj", options)\n# STL export through the STL extension\nSketchup.active_model.export("model.stl")',
        note: 'A CAD file drawn in millimetres imported as metres inflates everything 1000x — always set the import units to match the source drawing.'
      },
      {
        title: 'Modeling Workflow for Rapid Prototyping',
        text: 'Rapid prototyping means getting from idea to physical object in hours, not weeks. SketchUp\'s speed, plus a disciplined workflow, makes it the tool for iterating a design before fabrication — whether that fabrication is 3D printing, CNC, or a cardboard mock-up.\n\n## START FROM THE CONSTRAINTS\n\nThe prototyping workflow starts with the numbers: the material thickness, the print volume, the joint clearance. Set the model units to match the fabrication (millimetres for 3D printing and CNC). A model in metres that the slicer reads as millimetres prints at 1/1000 scale — check units before anything else.\n\n## SOLID, WATERTIGHT GEOMETRY\n\nFabrication needs **solid geometry**: every edge shared, no holes, no internal faces. SketchUp\'s default loose geometry is not watertight. The workflow: build with groups/components, then check solidity with **Solid Inspector** (an extension) or by selecting the object — a solid group reports as "Solid Group". Repair gaps before export, because a slicer or CAM tool chokes on non-manifold geometry.\n\n## ITERATE THE SCALE MODEL\n\nBefore the final part, build a **scale model** in SketchUp (or print a small test) to check proportions, clearances and how parts join. The prototyping loop — model, print/mock, test, revise — runs fastest when the model is parametric: a dynamic component with a thickness attribute means changing one number updates the part for the next iteration.\n\n## THE EXPORT CHECKLIST\n\nBefore exporting to STL or CNC: (1) units correct, (2) geometry solid, (3) no stray edges or internal faces, (4) export at the fabrication scale, (5) open the exported file and verify. A quick check in the slicer/CAM preview catches most errors before material is wasted.\n\n## The Workflow\n\nConstraints first, solid geometry second, scale test third, export check fourth. Rapid prototyping rewards fast iteration — keep the model simple, parametric and watertight.',
        code: '# SketchUp Ruby API: verify geometry is solid\nmodel = Sketchup.active_model\nmodel.selection.grep(Sketchup::Group).each do |g|\n  # a group with no exposed interior faces is a solid\n  status = g.manifold? ? "solid" : "open"\n  puts "#{g.name}: #{status}"\nend\n# export the first solid group to STL\nmodel.export("prototype.stl")',
        note: 'A slicer or CAM tool chokes on non-manifold geometry — verify every exported part is a solid group before material is wasted.'
      },
    ],
    quizzes: [
      { text: 'The official store of SketchUp plugins is the…', options: ['Extension Warehouse', 'Ruby Console', 'Component Library', 'Outliner'], correctAnswer: 'Extension Warehouse' },
      { text: 'The shared library many SketchUp extensions require is…', options: ['TT_Lib / LibFredo6', 'SketchUcation Tools', 'Solid Inspector', 'Curviloft'], correctAnswer: 'TT_Lib / LibFredo6' },
      { text: 'The built-in Ruby interpreter for scripting SketchUp is the…', options: ['Ruby Console', 'Extension Warehouse', 'Outliner', 'Styles'], correctAnswer: 'Ruby Console' },
      { text: 'Anything you click more than twice in SketchUp should become…', options: ['A macro or script', 'A new layer', 'A scene', 'A style'], correctAnswer: 'A macro or script' },
      { text: 'The universal renderer format for exporting SketchUp geometry is…', options: ['OBJ', 'DWG', 'STL', 'SKP'], correctAnswer: 'OBJ' },
      { text: 'A CAD file drawn in millimetres imported as metres will…', options: ['Inflate everything 1000x', 'Shrink everything 1000x', 'Import correctly', 'Be rejected'], correctAnswer: 'Inflate everything 1000x' },
      { text: 'Fabrication (3D printing/CNC) requires geometry that is…', options: ['Solid and watertight', 'Loose and editable', 'On Layer0', 'Textured'], correctAnswer: 'Solid and watertight' },
      { text: 'The extension that checks SketchUp geometry for open faces and gaps is…', options: ['Solid Inspector', 'Curviloft', 'Bezier', 'RoundCorner'], correctAnswer: 'Solid Inspector' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 12 — Layout & Presentation
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 12,
    title: 'Layout & Presentation',
    description: 'SketchUp models become drawings in LayOut — place scaled viewports, add dimensions and callouts, and export a professional PDF or presentation set that reads like a real architectural document.',
    topics: [
      {
        title: 'SketchUp Layout Basics',
        text: 'The model is 3D, but the deliverable is paper. **LayOut** is SketchUp\'s companion that turns the model into scaled, annotated drawings. It is where a model becomes a document set with title blocks, viewports and dimensions.\n\n## WHAT LAYOUT IS\n\nLayOut is a 2D document editor that embeds SketchUp views. You create a **document** at a paper size (A3, A2, letter), drop a **viewport** on the page, and the viewport shows a live view of the model — rotate, zoom, change style in the viewport, and the page updates. The model and the drawing stay linked: edit the SketchUp file, update the document, and the viewport refreshes.\n\n## THE LAYOUT INTERFACE\n\nThe interface is like a 2D CAD: a paper sheet in the middle, a **Pages** panel on the left (each page is a sheet), a **Layers** panel for the sheet\'s own 2D content, and the **Tray** with the model and styles. The key objects are **SketchUp Model viewports**, **Drafting** tools (lines, rectangles, text), and **Dimensions**.\n\n## SETTING UP THE SHEET\n\nStart from a template or set the page size and orientation. **File > Document Setup** controls the paper, the units, and the **Auto-Text** fields (project name, sheet number, date). A title block drawn once on a **template** saves hours per project — the studio LayOut template carries the logo, borders and standard text.\n\n## THE MODEL VIEWPORT\n\nInsert a viewport from **File > Insert > SketchUp Model**, or drag the model. The viewport shows the saved **Scenes** — pick the "Front Elevation" scene as the viewport\'s view. Resize the viewport box; the model inside scales. The **Camera** and **Style** are fixed by the scene or overridden per viewport.\n\n## The Workflow\n\nSet the paper, insert the SketchUp model viewport, pick the scene, draw the title block once as a template, and place dimensions. The document is a live window into the model — not a static snapshot.',
        code: '# LayOut API (Ruby) example: create a new document\nrequire \'Layout\'\ndoc = Layout::Document.new\npage = doc.pages.add_page\n# set page size to A3 landscape\npage.paper_size = [420, 297]   # mm\n# insert a SketchUp model reference on the page\nmodel_ref = Layout::SketchUpModel.new(page)\nmodel_ref.set_model_path("/path/to/model.skp")\nmodel_ref.set_scene("Front Elevation")\npage.add_entity(model_ref)\ndoc.save("/path/to/set.layout")',
        note: 'The LayOut document stays linked to the SketchUp model — edit the model, update the document, and every viewport refreshes to the new geometry.'
      },
      {
        title: 'Viewports & Scaled Drawings',
        text: 'A viewport is a live window into the model — but for a real drawing, it must show the model at a **drawing scale**. The viewport scale, the camera and the style are what turn a pretty 3D view into a measured architectural sheet.\n\n## THE VIEWPORT SCALE\n\nSelect a viewport and set its scale in the **Scale** box (or right-click > Scaled Drawing). LayOut offers standard scales (1:50, 1:100, 1/4" = 1\'-0") and custom scales. The scale means: one unit on the sheet equals X units of the model. A plan at 1:100 drawn on an A3 sheet shows a 30 m building comfortably. Pick the scale that fits the sheet and reads clearly.\n\n## SCALED vs UNSCALED\n\nA **Scaled Drawing** has a fixed scale — dimensions measure true. An unscaled viewport (like a 3D perspective) is a presentation view without a scale. Plans, sections and elevations should be scaled; perspectives are unscaled by definition. The **Scale Label** ("SCALE 1:100") is a text field you place under the drawing.\n\n## CAMERA INSIDE THE VIEWPORT\n\nDouble-click a viewport to enter it and orbit/zoom/pan inside. To return to a fixed drawing view, lock the camera or use the saved **Scene** as the viewport\'s view. A viewport tied to a scene keeps the view stable when the model changes.\n\n## THE STYLE AND THE VIEWPORT\n\nThe viewport inherits the **Style** from the model scene — a hidden-line style reads like a drawing, a shaded style reads like a model. For a clean sheet, use hidden-line (or the "Architectural" styles) with profile edges on. The viewport is where style choice becomes document quality.\n\n## UPDATING THE VIEWPORT\n\nThe model link is live: when the SketchUp model changes, the viewport shows a refresh icon — click **Update** (or auto-update on print) and the drawing reflects the new geometry. This is the entire point of LayOut: the sheet stays honest with the model.',
        code: '# LayOut API (Ruby): set a viewport scale and camera\nvp = page.sketchup_models.first\nvp.scale = 1.0 / 100.0      # 1:100\nvp.camera_orientation = Layout::SketchUpModel::CameraOrientation::TOP\nvp.camera_type = Layout::SketchUpModel::CameraType::ORTHOGRAPHIC\n# lock the viewport so it cannot be moved by accident\nvp.locked = true',
        note: 'A viewport tied to a saved scene keeps the camera stable — when the model updates, the drawing scale and view stay honest.'
      },
      {
        title: 'Annotations, Dimensions & Callouts',
        text: 'A drawing without dimensions is a picture. **Dimensions**, **Labels** and **Callouts** are what make the sheet a document that can be built from. LayOut\'s annotation tools measure, label and reference the drawing.\n\n## DIMENSIONS\n\nThe **Dimension** tool measures between two points in a viewport — click two points and the dimension appears with a value. Dimensions in a **scaled** viewport measure true model distance (the value reads the model units, not the paper units). Style the dimension in the **Tray**: arrowheads, extension lines, text height, units (mm or m, precision). Align dimensions by placing them on a common offset line so the sheet reads neatly.\n\n## LABELS AND CALLOUTS\n\nThe **Label** tool draws a leader with a text box — point at an object, pull the leader out, type the note ("RC slab, 200 thk"). A **Callout** (a numbered bubble) references a detail: "DETAIL 4/A" ties the plan to a detail sheet. Text in LayOut is real editable text, not model text — spellcheck applies.\n\n## TEXT STYLES\n\nUse **Styles** for text: set the font, height and colour once, and reuse the style for every note. LayOut text uses the **Style** in the Tray (a paragraph style). Consistent text styles are what make a sheet look professionally produced rather than scrawled.\n\n## ANNOTATION LAYERS\n\nPut dimensions and notes on a **Sheet Layer** (like "A-ANNO") so you can turn annotations off for a clean view. LayOut layers control the 2D sheet content; the model viewport content is controlled separately in the SketchUp model. Keep the annotation layer visible for prints, off for exports of the bare drawing.\n\n## The Workflow\n\nDimension the key elements, label with leaders, callout the details, and keep all annotations on a dedicated layer with consistent styles. The sheet then tells the whole story.',
        code: '# LayOut API (Ruby): add a dimension and a label\n# get the viewport\'s dimension entity\ndim = Layout::LinearDimension.new(page)\ndim.start_point = Geom::Point3d.new(10, 5, 0)\ndim.end_point   = Geom::Point3d.new(100, 5, 0)\ndim.style.set("scale", 1.0/100.0)\npage.add_entity(dim)\n# a text label with a leader\nlabel = Layout::Label.new(Geom::Point3d.new(50, 20, 0),\n                          "RC slab, 200 thk")\npage.add_entity(label)',
        note: 'Dimensions in a scaled viewport measure true model distance — the value reads the model units, not the paper millimetres.'
      },
      {
        title: 'Exporting Presentations & PDFs',
        text: 'The finished sheet leaves LayOut as a **PDF**, an image, or a live presentation. Exporting correctly preserves scale, fonts and line weights — the deliverable is what the client and the contractor actually see.\n\n## EXPORT TO PDF\n\n**File > Export > PDF** produces the standard deliverable. The PDF export dialog offers quality, page range, and **Vector vs Raster**: vector output keeps lines sharp at any zoom and the file stays small; raster (PNG/JPG) is for images. For a drawing set, use **vector** PDF — it prints cleanly and text stays selectable. Check **"Export entire document"** or select pages.\n\n## IMAGE EXPORT\n\nFor slides, reports and emails, export a page as **PNG or JPG** at high resolution (200–300 DPI). The export dialog shows the pixel size; a full A3 sheet at 300 DPI is large (3508 × 4961 px) — resize for email. PNG is lossless for line drawings; JPG compresses smaller for photos.\n\n## THE PRESENTATION MODE\n\n**Presentation mode** (the play icon) runs the LayOut document full-screen, page by page — the quickest way to walk a client through the set. Pages in order, clean zoom, no UI. It is a lightweight alternative to a slide deck when the content is drawings.\n\n## THE EXPORT CHECKLIST\n\nBefore exporting: check all viewports are **updated** (the refresh icon is gone), fonts embed (PDF), the sheet scale labels match the viewport scales, and the PDF opens correctly in a viewer (fonts, line weights, layers). A PDF that renders wrong in the viewer will print wrong.\n\n## ARCHIVING\n\nKeep the .layout source file alongside the exported PDF — the PDF is the deliverable, the .layout is the edit source. When the model changes, update and re-export; never hand-edit the PDF.\n\n## The Workflow\n\nUpdate viewports, verify scales, export vector PDF for the set, PNG for presentations, and archive the source. The export is the handshake between the model and the world.',
        code: '# LayOut API (Ruby): export the document to PDF\ndoc = Layout::Document.load("/path/to/set.layout")\n# export pages 1-3 to a single PDF at high quality\nopts = { "format" => "pdf", "pages" => [1,2,3],\n         "quality" => "high", "vector" => true }\ndoc.export("/path/to/set.pdf", opts)\n# export a single page as PNG at 300 DPI\ndoc.pages[0].export("/path/to/sheet1.png",\n                    { "width" => 3508, "height" => 2480 })',
        note: 'Vector PDF keeps lines sharp at any zoom and text stays selectable — the correct export for a drawing set, not raster.'
      },
    ],
    quizzes: [
      { text: 'The SketchUp companion that turns models into scaled, annotated drawings is…', options: ['LayOut', 'Style Builder', 'Sandbox', 'Extension Warehouse'], correctAnswer: 'LayOut' },
      { text: 'In LayOut, a live window into the model is called a…', options: ['Viewport', 'Scene', 'Layer', 'Style'], correctAnswer: 'Viewport' },
      { text: 'A viewport at a fixed drawing scale shows a plan at…', options: ['1:100', 'No scale', 'Any scale', 'Pixel scale'], correctAnswer: '1:100' },
      { text: 'A 3D perspective viewport is typically…', options: ['Unscaled', 'Scaled at 1:100', 'Scaled at 1:50', 'Vector-only'], correctAnswer: 'Unscaled' },
      { text: 'The tool that measures between two points in a scaled viewport is…', options: ['Dimension', 'Label', 'Callout', 'Rectangle'], correctAnswer: 'Dimension' },
      { text: 'A numbered bubble that references a detail sheet is a…', options: ['Callout', 'Dimension', 'Viewport', 'Stamp'], correctAnswer: 'Callout' },
      { text: 'For a drawing set, LayOut should export a…', options: ['Vector PDF', 'Low-res JPG', 'Raster PNG', 'DWG only'], correctAnswer: 'Vector PDF' },
      { text: 'Before exporting, every viewport should be…', options: ['Updated (no refresh icon)', 'Locked', 'Deleted', 'Unscaled'], correctAnswer: 'Updated (no refresh icon)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 13 — Revit Structure Basics
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 13,
    title: 'Revit Structure Basics',
    description: 'Revit is BIM — not drafting. Model structural elements once and every plan, section, schedule and sheet derives from the model. Start with the interface, levels and grids, structural columns and load-bearing walls, and the views that read them.',
    topics: [
      {
        title: 'The Revit Structure Interface & Template',
        text: 'Revit Structural is a different beast from CAD: you model real structural elements (columns, beams, walls) and Revit derives the drawings. The **interface** and the **template** set the stage — a structural template carries the families, line styles, and settings a structural project needs.\n\n## THE RIBBON AND THE PROJECT BROWSER\n\nThe **Ribbon** organises tools in tabs: **Architecture, Structure, Steel, Insert, Annotate, Analyse, Massing & Site, Collaborate, View, Manage**. In a structural project you live on the **Structure** and **Steel** tabs. The **Project Browser** (left) lists every view, sheet, family and schedule in the project — it is the navigation tree of the entire model. Double-click a view to open it.\n\n## THE STRUCTURAL TEMPLATE\n\nWhen starting a project, **New > Project** and pick the **Structural Template** (or a firm standard). The template preloads: structural families (concrete and steel columns/beams), the dimension and tag styles, the title blocks, the level settings, and the **view templates** that keep every plan looking consistent. A good template is the studio\'s accumulated standards — using it means every project starts correct.\n\n## THE PROPERTIES PALETTE\n\nSelect anything and the **Properties** palette (left, or View > Properties) shows its parameters: for a column, its type, base/top level, offset, material and structural usage. Edit the parameters here — this is the "B" in BIM. Every object is data, and the palette is the data editor.\n\n## STARTING A STRUCTURAL MODEL\n\nThe structural workflow: set the **levels** (grid floor levels), draw the **grids** (column grid lines), place **columns**, draw **walls** and **beams**, then **foundations** and **slabs**. The template\'s families and settings make each step a matter of placement, not of drawing setup.\n\n## The Workflow\n\nStart from the structural template, navigate by the Project Browser, and drive every element from the Properties palette. The template is the standard; the model is the deliverable.',
        code: '# Revit API (C#) example: open a structural template project\n# via the UI: File > New > Project > Structural Template\n# Programmatic start:\nAutodesk.Revit.ApplicationServices.Application app = commandData.Application.Application;\n// the project is already open from the template\nDocument doc = app.ActiveUIDocument.Document;\n// list all view types in the browser\nFilteredElementCollector collector = new FilteredElementCollector(doc);\nICollection<Element> views = collector\n    .OfClass(typeof(View))\n    .WhereElementIsNotElementType()\n    .ToList();\nforeach (View v in views) TaskDialog.Show("View", v.Name);',
        note: 'A structural template is the firm\'s accumulated standards — starting every project from it means the families, tags and line styles are already correct.'
      },
      {
        title: 'Levels, Grids & Project Setup',
        text: 'The skeleton of a Revit model is **Levels** and **Grids**: levels define the vertical datum (each floor), grids define the horizontal column network. Set these up right and everything placed on them — columns, beams, walls — snaps into a coordinated structural frame.\n\n## LEVELS\n\nLevels are horizontal planes that define floor elevations. In the Project Browser, open the default **Elevation** view (or Section) and create levels: GL ±0.000, +3.600, +7.200 and so on. Each level gets a name (Level 1, Level 2, or the floor labels) and an elevation. Levels drive everything that is level-hosted — floors, ceilings, and the base/top offsets of columns and walls. The **level head** shows the name and elevation; the spacing is the storey height.\n\n## GRIDS\n\nGrids are the vertical reference lines of the column grid — drawn in a **plan view**, running vertically through the building. Draw the grid lines, then **tag** them (1, 2, 3, A, B, C). Columns are placed at grid intersections, and beams run along the grid lines. The grid is the layout the whole structure coordinates to; move a grid line and the columns hosted on it follow.\n\n## PROJECT SETUP CHECKLIST\n\nBefore modelling: set **Project Units** (mm, m), set the **structural settings** (rebar cover defaults, connection settings), load the structural **families** you need (concrete columns, steel UB sections), and set the **snap** and **levels**. Check the grid spacing matches the architectural plan — the structural grid must agree with the architectural layout before a column is placed.\n\n## PROJECT NORTH vs TRUE NORTH\n\nRevit has **Project North** (how the building sits on the sheet) and **True North** (real-world orientation). The grids and the building are drawn to Project North; the site and solar studies use True North. Set this in Project Settings before placing the building.\n\n## The Workflow\n\nLevels first, grids second, then columns on the grid intersections. A clean level/grid skeleton makes every subsequent structural element fast and coordinated.',
        code: '# Revit API (C#): create levels and grids programmatically\nDocument doc = ...;\n// create a level at elevation 3.6 m\nLevel level1 = doc.Create.NewLevel(3.6);\nlevel1.Name = "Level 2";\n// create grids in a plan view\nXYZ p1 = new XYZ(0, 0, 0), p2 = new XYZ(0, 18, 0);\nGrid gridA = Grid.Create(doc, Line.CreateBound(p1, p2));\ngridA.Name = "A";\n// a grid bubble shows its name at the end',
        note: 'Move a grid line and the columns hosted on it follow — the grid is the layout the entire structure coordinates to.'
      },
      {
        title: 'Structural Columns & Load-Bearing Walls',
        text: 'Vertical structure transfers load down to the foundations. In Revit, **structural columns** and **load-bearing walls** are the elements that carry it — and Revit knows they carry load, which is what makes analysis and documentation automatic.\n\n## STRUCTURAL COLUMNS\n\nPlace a structural column from the **Structure > Column > Structural Column** tool. Pick a family (a concrete rectangular column, a steel UB section), then place it at a grid intersection. Set the **Base Level** and **Top Level** (or offsets) in Properties — a column spanning Level 1 to Level 2 is placed once and read at every level. **Structural columns** are the load-carrying columns; **architectural columns** are decorative wraps. Do not confuse them — a structural column is part of the analytical model.\n\n## COLUMN PLACEMENT TIPS\n\nColumns attach to grids: place one and Revit offers to place columns at **all grid intersections** (the multiple placement option). Use the **Column tool + grid selection** to place a grid\'s worth of columns in one pass. Set the column\'s **Location** so its centre or face lines up with the grid — structural columns are often set to the grid line.\n\n## LOAD-BEARING WALLS\n\nA **structural wall** is a wall with a structural function: select the wall in Properties and set **Structural > Structural Usage** to **Bearing**. A bearing wall carries floor and roof loads; a **Non-bearing** wall is a partition. Bearing walls participate in the analytical model — Revit can run analysis on them. Draw the wall, set its usage and height, and it behaves as structure.\n\n## THE ANALYTICAL MODEL\n\nEvery structural element has an **analytical model** — a simplified stick/line representation used by analysis software (Robot, ETABS). Columns become lines, walls become panels. **Analytical tab > Analytical Model** shows the wireframe that analysis reads. Keeping the physical and analytical models aligned (via the **Adjust Analytical** tools) is what makes the analysis trustworthy.\n\n## The Workflow\n\nPlace structural columns on grids, draw bearing walls with the right structural usage, and check the analytical model. The physical model you see and the analytical model the software reads are one model.',
        code: '# Revit API (C#): place a structural column at a grid intersection\nDocument doc = ...;\nFamilySymbol colType = /* load concrete column family */;\n// place at grid intersection (grid A-1)\nXYZ basePoint = new XYZ(0, 0, 0);\nLevel baseLevel = FindLevel(doc, "Level 1");\nLevel topLevel   = FindLevel(doc, "Level 2");\nFamilyInstance column = doc.Create.NewFamilyInstance(\n    basePoint, colType, baseLevel,\n    Autodesk.Revit.DB.Structure.StructuralType.Column);\n// set the column top\nParameter top = column.get_Parameter(BuiltInParameter.FAMILY_TOP_LEVEL_PARAM);\ntop.Set(topLevel.Id);',
        note: 'A bearing wall participates in the analytical model — set Structural Usage to Bearing or the analysis reads it as a partition.'
      },
      {
        title: 'Views: Plans, Elevations & Sections',
        text: 'In Revit you do not draw views — you **create views** of the model. A plan, an elevation, a section are all live windows into the same model. Understanding how views work is how you get the right drawing out without redrawing anything.\n\n## PLAN VIEWS\n\nA **Plan view** is a horizontal cut through the model at a level. Revit creates one plan per level when the level is made. The **View Range** (in Properties) sets how the cut is taken: the cut plane (default 1200 mm), the top and bottom of the view. Adjusting the view range controls what shows — a floor plan cuts at 1.2 m, so furniture below that shows as a plan cut.\n\n## ELEVATIONS\n\nAn **Elevation view** looks at the building from outside. The elevation markers (placed on the plan) generate the views. In a structural context, elevations show the frame: columns, beams and walls of that face. The elevation\'s **crop region** bounds what is shown — pull the crop box to include the whole frame.\n\n## SECTIONS\n\nA **Section view** cuts the building vertically. Draw the section line across the plan, and Revit generates the section. Sections reveal the structure that plans hide — the beam depths, the slab thickness, the foundation depth. A section cuts the model; everything behind the cut plane shows in projection (unless the view depth limits it).\n\n## VIEW TEMPLATES\n\nA **View Template** is a saved set of view settings (scale, view range, visibility/graphics overrides, detail level). Apply the template to all your structural plans and they all look identical — same line weights, same overrides, same scale. Templates are how a multi-user project stays visually consistent.\n\n## The Workflow\n\nModel once, then create the plans, elevations and sections you need and dress them with the view template. Every view is the same model, filtered and scaled.',
        code: '# Revit API (C#): create a section view\nDocument doc = ...;\nViewPlan plan = /* current plan view */;\n// define the section box (bounding box in model coords)\nXYZ p1 = new XYZ(0, -5, 0), p2 = new XYZ(20, 5, 10);\nBoundingBoxXYZ box = new BoundingBoxXYZ { Min = p1, Max = p2 };\n// create the section using the section tool\nViewSection section = ViewSection.CreateSection(doc,\n    ViewFamily.Section, plan.GetTypeId(), box);\nsection.Name = "Section A-A";\nsection.Scale = 100;',
        note: 'The plan is a horizontal cut through the model — the View Range cut plane at 1.2 m is why a plan shows the building sliced, not seen from above.'
      },
    ],
    quizzes: [
      { text: 'In Revit, the navigation tree of every view, sheet and family is the…', options: ['Project Browser', 'Properties Palette', 'Ribbon', 'View Cube'], correctAnswer: 'Project Browser' },
      { text: 'A structural project should start from…', options: ['The Structural Template', 'A blank generic model', 'An AutoCAD import', 'A massing study'], correctAnswer: 'The Structural Template' },
      { text: 'The horizontal planes that define floor elevations are…', options: ['Levels', 'Grids', 'Sections', 'Sheets'], correctAnswer: 'Levels' },
      { text: 'The vertical reference lines of the column grid are…', options: ['Grids', 'Levels', 'Ref Planes', 'Scope Boxes'], correctAnswer: 'Grids' },
      { text: 'The columns that carry load and appear in the analytical model are…', options: ['Structural columns', 'Architectural columns', 'Curtain wall mullions', 'Pilasters'], correctAnswer: 'Structural columns' },
      { text: 'To make a wall carry floor loads, set its Structural Usage to…', options: ['Bearing', 'Non-bearing', 'Retaining', 'Shear'], correctAnswer: 'Bearing' },
      { text: 'A plan view is a…', options: ['Horizontal cut through the model at a level', 'Vertical slice', '3D perspective', 'Sheet layout'], correctAnswer: 'Horizontal cut through the model at a level' },
      { text: 'Saved view settings applied to keep all plans identical are…', options: ['View Templates', 'Sheets', 'Title Blocks', 'Schedules'], correctAnswer: 'View Templates' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 14 — Structural Elements & Detailing
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 14,
    title: 'Structural Elements & Detailing',
    description: 'Move beyond the skeleton — foundations and footings that spread the load, the beams and joists of the framing, slabs and their openings, and the notes, dimensions and details that make a structure buildable.',
    topics: [
      {
        title: 'Foundations & Footings',
        text: 'The structure ends at the ground, but it does not stop there — **foundations** transfer every load into the soil. In Revit, isolated footings sit under columns, strip footings run under walls, and the foundation plan is where the whole building is literally supported.\n\n## ISOLATED FOOTINGS\n\nThe **Isolated Footing** is a pad under a single column: a square or rectangular concrete block that spreads the column load over the soil. Place it from the **Structure > Foundation > Isolated** tool, hosted on the column base. Set its dimensions (width, length, thickness) in Properties, and its **Base Level** and **Offset** — the footing sits below the finished ground level. The footing is what keeps the column from punching into the ground.\n\n## STRIP FOOTINGS\n\nA **Strip Footing** runs continuously under a wall — a long, shallow concrete strip that spreads the wall load. Place it along the wall path with the **Foundation > Wall** tool. The strip follows the wall, and its width and thickness are set per type. Long buildings rely on strip footings to distribute wall loads evenly.\n\n## FOOTING TYPES AND DEPTH\n\nFootings come in families: **pad footings** (isolated), **strip footings** (continuous), **raft/mat foundations** (a whole-floor slab), and **pile caps** (over pile groups). The depth is set by the frost line and the soil capacity — a footing in soft soil is wider than one on rock. The structural engineer sizes them; the modeller places them.\n\n## THE FOUNDATION PLAN\n\nThe foundation plan is a plan view at the footing level showing all footings, their dimensions and their levels. Because footings are model elements, the plan is automatic — place the footing and it appears in the foundation plan with its size. Tag the footings (F1, F2 by type) and schedule them for quantities.\n\n## The Workflow\n\nPlace isolated footings under columns, strip footings under walls, set the base level to the founding depth, and check the foundation plan. The footing plan is the bottom of the coordination stack — everything above rests on it.',
        code: '# Revit API (C#): place a family instance as an isolated footing\nDocument doc = ...;\nFamilySymbol footingType = /* load "Isolated Footing" family */;\nXYZ origin = new XYZ(0, 0, 0);   // under a column\nLevel baseLevel = FindLevel(doc, "GL -1.500");\nFamilyInstance footing = doc.Create.NewFamilyInstance(\n    origin, footingType, baseLevel,\n    Autodesk.Revit.DB.Structure.StructuralType.Footing);\nfooting.get_Parameter(BuiltInParameter.INSTANCE_OFFSET_POS_PARAM)\n       .Set(-0.5);   // embed 0.5 m below the level',
        note: 'The footing sits at the founding depth below ground — the frost line and soil capacity decide the size, and the modeller places what the engineer sizes.'
      },
      {
        title: 'Structural Framing: Beams & Joists',
        text: 'Between the columns and the slab sits the **framing** — the beams and joists that carry the floor to the columns. In Revit, structural framing is placed on grids and levels, and the framing plan shows the whole network of beams, joists and their connections.\n\n## BEAMS\n\nStructural **Beams** are horizontal members placed with the **Structure > Beam** tool. Pick a family (a steel UB section, a concrete beam), then place by **two points** along a grid line, or use **beam systems**. A beam hosted between two columns snaps to the column tops and to the grid. The **Structural Usage** matters: joists, purlins, girts are different beam usages and schedule separately.\n\n## BEAM SYSTEMS\n\nA **Beam System** places a regular pattern of beams over an area in one action: define the boundary (a bay between grids), choose the beam family, the direction and the spacing, and Revit fills the bay with parallel beams. This is how a typical floor is framed in minutes instead of beam-by-beam. The system is parametric — change the spacing and the whole bay updates.\n\n## JOISTS AND DECKING\n\n**Joists** are the closely spaced light members that carry the deck or slab between beams; Revit has joist families (bar joists, timber joists). The **floor deck** (corrugated metal or timber boards) is the surface the slab sits on. In a structural model the sequence is: columns carry beams, beams carry joists, joists carry deck, deck carries slab.\n\n## THE FRAMING PLAN\n\nThe **Framing Plan** is a plan view set to show the beams at their top-of-steel level, with beam tags showing their sizes (UB 356×171×51). Because the beams are model elements, the framing plan is automatic and the beam schedule counts them. Beam tags read the family name and size parameters — tag them and the drawing tells the steel fabricator what to order.\n\n## The Workflow\n\nPlace the main beams on the grid lines, fill the bays with beam systems, add joists, and check the framing plan. The framing is the horizontal skeleton — and it documents itself.',
        code: '# Revit API (C#): place a beam between two columns\nDocument doc = ...;\nFamilySymbol beamType = /* load UB 356x171x51 */;\nXYZ start = new XYZ(0, 0, 0), end = new XYZ(0, 6, 3.6);\nLine axis = Line.CreateBound(start, end);\nFamilyInstance beam = doc.Create.NewFamilyInstance(\n    axis, beamType, FindLevel(doc, "Level 2"),\n    Autodesk.Revit.DB.Structure.StructuralType.Beam);\n// set the beam\'s structural usage to joist\nParameter usage = beam.get_Parameter(\n    BuiltInParameter.STRUCTURAL_BEAM_END0_RELEASE);\n// (usage set via the Structural Usage parameter)',
        note: 'A beam system fills a bay with parallel beams from one command — change the spacing and the whole bay updates parametrically.'
      },
      {
        title: 'Slabs, Floors & Openings',
        text: 'The floor of a building — the **slab** — is modelled as a horizontal element that spans the framing or the ground. Slabs carry loads, and openings in them (shafts, stair holes, MEP penetrations) need modelling, not guessing.\n\n## SLABS AND FLOORS\n\nIn Revit, use the **Floor** tool (Structure > Floor) to create slabs: sketch the boundary in a plan view, pick the floor type (a concrete slab with its thickness), and Revit creates the slab. The **Floor Type** carries the layers — the concrete thickness, the screed, the finish — like a wall type carries its layers. A **structural slab** is a floor with a structural function, placed at the structural level.\n\n## SLAB EDGES AND DROPS\n\nA **Slab Edge** is a thickening along an edge (a spandrel or a thickened edge), applied with the **Slab Edge** tool on the slab boundary. **Span direction** matters for one-way slabs — Revit lets you set the rebar span direction symbolically on the structural plan. The slab\'s **structural thickness** (not the finished thickness) is what the analysis reads.\n\n## OPENINGS IN SLABS\n\n**Openings** — for stairs, lifts, atria and MEP shafts — are cut into the slab. Use the **Opening** tools: **Vertical Opening** cuts a hole perpendicular to the slab, **Shaft** cuts vertically through multiple floors (a lift shaft, a stairwell), **Dormer** and **Wall** openings cover other cases. A shaft opening is the correct tool for a hole that passes many levels, because it cuts every slab in the shaft in one edit.\n\n## THE REBAR INSIDE THE SLAB\n\nThe slab carries **rebar** (covered next week): a slab reinforcement set spans the slab in both directions, with cover to the edges. The structural model of a slab — its thickness, span direction and openings — is exactly what the rebar places against.\n\n## The Workflow\n\nSketch the floor boundary, pick the slab type, cut the openings with the right opening tool, and set the span direction. The slab is the horizontal structural surface — and openings must be modelled, not drawn.',
        code: '# Revit API (C#): create a slab floor with an opening\nDocument doc = ...;\nFloorType slabType = /* concrete 200 mm slab type */;\n// boundary loop\nIList<CurveLoop> loops = new List<CurveLoop>();\nCurveLoop outer = new CurveLoop();\nouter.Append(Line.CreateBound(new XYZ(0,0,0), new XYZ(10,0,0)));\nouter.Append(Line.CreateBound(new XYZ(10,0,0), new XYZ(10,6,0)));\nouter.Append(Line.CreateBound(new XYZ(10,6,0), new XYZ(0,6,0)));\nouter.Append(Line.CreateBound(new XYZ(0,6,0), new XYZ(0,0,0)));\nloops.Add(outer);\n// create the floor\nFloor floor = Floor.Create(doc, loops, slabType.Id, FindLevel(doc, "Level 1"));\n// cut a shaft opening through it (the Shaft tool in UI)\nShaftOpening shaft = ShaftOpening.CreateDefaultShaft(doc);\n// set shaft bounds to cut the slab',
        note: 'A Shaft opening is the right tool for a hole that passes many levels — it cuts every slab in the shaft in one edit.'
      },
      {
        title: 'Detailing: Notes, Dimensions & Details',
        text: 'A structural model is not a drawing until it is **documented** — dimensioned, annotated and detailed. Revit\'s detailing tools add the notes, the dimensions and the construction details that turn a model into instructions a contractor can build to.\n\n## DIMENSIONS\n\nThe **Dimension** tool measures the model. In a structural plan, dimension the grid lines, the column locations, the beam offsets. Revit dimensions are parametric: move the column and the dimension updates. **Aligned**, **linear** and **angular** dimensions cover the plan; use **Spot Elevation** to annotate top-of-steel and top-of-slab levels. The dimension style (text height, arrowheads) is set by the project template — consistent dimensions are a template win.\n\n## TAGS\n\n**Tags** read model data and place it on the drawing: a **beam tag** shows the size (UB 356×171×51), a **column tag** the section (C1, 400×400), a **footing tag** the type. Because tags read the model, they never go stale — change the beam and the tag updates. Tag by category in one pass with the **Tag All** tool.\n\n## DETAILS\n\nConstruction **details** (a column base detail, a slab edge detail) are drawn or drafted in **detail views** — large-scale views (1:5, 1:10) that show the build-up. Details combine **model elements** (the slab edge shown at large scale) with **detail components** (2D drafting elements: insulation hatches, bolt symbols, leader notes). A **callout** on the plan creates the detail view; the **detail line** and **region** tools draft the 2D parts.\n\n## NOTES AND TEXT\n\n**Text** notes annotate the detail: material calls, welding notes, fit tolerances. Keynote text ties a note to a specification item. Keep notes in the template\'s text styles so the whole set reads consistently.\n\n## The Workflow\n\nDimension the layout, tag the elements, cut callouts to large-scale details, and draft the 2D detail content. The model provides the geometry; the detail tools provide the construction intelligence.',
        code: '# Revit API (C#): add a dimension between two grid lines\nDocument doc = ...;\nUIDocument uiDoc = new UIDocument(doc);\n// pick two reference points (grid lines A and B)\nReference refA = uiDoc.Selection.PickObject(\n    Autodesk.Revit.UI.Selection.ObjectType.Element, "Pick grid A");\nReference refB = uiDoc.Selection.PickObject(\n    Autodesk.Revit.UI.Selection.ObjectType.Element, "Pick grid B");\nLine line = Line.CreateBound(new XYZ(0,0,0), new XYZ(0,3,0));\nDimension dim = doc.Create.NewDimension(doc.ActiveView, line,\n    new List<Reference> { refA, refB });',
        note: 'Tags read model data, so they never go stale — change the beam and the tag updates to the new size automatically.'
      },
    ],
    quizzes: [
      { text: 'The square concrete pad that spreads a column load into the soil is an…', options: ['Isolated footing', 'Strip footing', 'Pile cap', 'Raft'], correctAnswer: 'Isolated footing' },
      { text: 'A continuous footing that runs under a wall is a…', options: ['Strip footing', 'Pad footing', 'Pile cap', 'Grade beam'], correctAnswer: 'Strip footing' },
      { text: 'The Revit tool that fills a bay with a regular pattern of parallel beams is…', options: ['Beam System', 'Structural Beam', 'Joist', 'Truss'], correctAnswer: 'Beam System' },
      { text: 'The closely spaced light members that carry the deck between beams are…', options: ['Joists', 'Columns', 'Footings', 'Walls'], correctAnswer: 'Joists' },
      { text: 'The Revit element used to model a floor slab is the…', options: ['Floor tool', 'Wall tool', 'Ceiling tool', 'Roof tool'], correctAnswer: 'Floor tool' },
      { text: 'The tool that cuts a hole vertically through multiple floors is…', options: ['Shaft', 'Vertical Opening', 'Dormer', 'Wall Opening'], correctAnswer: 'Shaft' },
      { text: 'Beam and column tags in Revit…', options: ['Read model data and update automatically', 'Are manual text that must be edited', 'Only work on 2D details', 'Cannot be edited'], correctAnswer: 'Read model data and update automatically' },
      { text: 'A large-scale construction view created from a callout is a…', options: ['Detail view', 'Schedule view', 'Sheet view', '3D view'], correctAnswer: 'Detail view' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 15 — Reinforcement & Schedules
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 15,
    title: 'Reinforcement & Schedules',
    description: 'Concrete gets its strength from the steel inside it — learn how rebar is placed and covered, how shapes and bend codes are read, how structural schedules extract quantities, and how a beam-column junction is actually detailed.',
    topics: [
      {
        title: 'Rebar Placement & Cover',
        text: 'Reinforced concrete is a composite: concrete resists compression, steel resists tension. **Rebar placement** — where the bars sit, how far apart, and how much concrete protects them — decides whether the structure behaves as designed. A slab that has its bottom steel at the wrong level will crack exactly there.\n\n## THE PRINCIPLE OF COVER\n\n**Cover** is the distance from the surface of the concrete to the outer face of the steel. It protects the rebar from corrosion and fire. Code (ACI 318, Eurocode 2) sets minimums by exposure: interior slabs 20-25 mm, exterior and soil-facing members 40-50 mm, and more near the coast. Too little cover and the bar rusts, spalls the concrete and loses its bond. Revit lets you set the **cover** on the rebar type or per face, and the rebar will not violate it during placement.\n\n## MAIN vs DISTRIBUTION STEEL\n\nA one-way slab carries two directions of steel. **Main (flexural) steel** runs along the short span and carries the bending; it sits in the tension zone (bottom of a simply supported slab). **Distribution (secondary) steel** runs perpendicular and spreads concentrated loads across the main bars; it is lighter and sits on top of the main steel. Getting this ordering right is a classic site error — swap them and the slab cracks under its own load.\n\n## SPACING AND BAR SIZES\n\nBars are specified by size (a 12 mm bar, #4 in imperial) and **spacing** (e.g. 12@200 c/c — 12 mm bars at 200 mm centres). Spacing is bounded by code maximums (to control crack width) and by the bar diameter (bars must not cluster). A rebar schedule\'s "12@200" is the drawing\'s real instruction to the fabricator.\n\n## PLACEMENT IN REVIT\n\nIn Revit, the **Structure > Rebar** tool places bars on a host (beam, column, wall, slab). Pick the rebar shape from the browser, set the cover, and the placement snaps the bar inside the concrete — with the hooks, laps and spacing visible in 3D. The model rebar is not just for show: it drives the rebar schedule.\n\n## The Check\n\nEvery bar needs the right cover, the right spacing and the right zone (top/bottom). Read the cover in the section view and check the tension steel is in the tension face.',
        code: '# Revit API (C#): create a rebar shape from a family + set cover\nDocument doc = ...;\nRebarBarType barType = new FilteredElementCollector(doc)\n    .OfClass(typeof(RebarBarType))\n    .Cast<RebarBarType>()\n    .First(t => t.BarDiameter.AsDouble() > 0.01);   // 12 mm bar\nRebarStyle style = RebarStyle.Standard;\nRebarHookType hook = doc.GetDefaultRebarHookType(style);\n// rebar shape is defined by the shape family (e.g. 01 = straight, 07 = stirrup)\nElementId shapeId = new FilteredElementCollector(doc)\n    .OfClass(typeof(RebarShape))\n    .FirstElementId();\n// set cover on the rebar type before placement\nParameter cover = barType.get_Parameter(BuiltInParameter.REBAR_COVER_BAR_DIMENSION);\n// cover 25 mm to face of concrete\ncover.Set(UnitUtils.ConvertToInternalUnits(25, DisplayUnitType.DUT_MILLIMETERS));',
        note: 'A slab with its main steel accidentally placed in the top zone will crack — the tension steel must live in the tension face, and cover must never fall below the code minimum.'
      },
      {
        title: 'Rebar Sets, Shape & Bending',
        text: 'Steel reinforcement is delivered bent to shape and tied in place. The **shape** and the **bend** determine what the fabricator cuts and how the detailer counts bars. Reading and creating rebar shapes is the language of reinforcement.\n\n## REBAR SHAPES\n\nA **rebar shape** is the bent geometry of one bar. Standard shapes have codes: **01** straight, **07** stirrup (square/rectangular tie), **15** hook with extension, **28** and **33** standard 90°/180° hooks, **35** double-stirrup. The shape family in Revit defines the geometry and its parameters (bar diameter, legs, hook lengths). A beam stirrup is shape 07; the column ties are shape 07 too but smaller.\n\n## BEND CODES AND HOOKS\n\nA hook (90° or 135°) provides anchorage — it develops the bar\'s strength in a short distance. The bend has a **mandrel radius** (the bar must not be bent too sharply or it cracks) and a **hook extension** (the straight tail beyond the bend). Codes set these: for a 12 mm bar, a standard 90° hook has an extension of 12 × bar diameter (a "12d" hook). The fabricator\'s bend table turns the shape code into machine settings.\n\n## REBAR SETS\n\nIn Revit a **rebar set** places a whole family of identical bars in one operation — a row of stirrups at 200 mm along a beam. You pick the start and end and the spacing; Revit fills the set and numbers each bar. Editing the spacing updates every bar in the set. Sets are how a 6 m beam gets its 30 stirrups without placing them one by one.\n\n## THE SCHEDULE CONNECTION\n\nBecause each bar has a shape, a size, a length and a mark, Revit can schedule them: **Rebar Schedule** lists every bar, its mark, diameter, shape, length and count, and totals the weight. The schedule is the fabrication order — accurate shape data makes accurate bending.\n\n## The Workflow\n\nPick the shape family, set the cover and hooks, place a rebar set along the host, then schedule the bars. The shape code on the schedule and the bend table in the shop speak the same language.',
        code: '# Revit API (C#): place a rebar set of stirrups along a beam\nDocument doc = ...;\nRebarBarType stirrupType = /* shape 07 stirrup bar type */;\nRebarShape shape07 = new FilteredElementCollector(doc)\n    .OfClass(typeof(RebarShape))\n    .Cast<RebarShape>()\n    .First(s => s.Name.Contains("07"));\nRebar rebarSet = Rebar.CreateFromRebarShape(doc, stirrupType, shape07, hostBeam);\n// set the spacing: 200 mm centres (converted to internal feet)\ndouble spacing = UnitUtils.ConvertToInternalUnits(200, DisplayUnitType.DUT_MILLIMETERS);\nrebarSet.SetLayoutAsFixedNumber(0, spacing, 0, 2, true);\n// the set fills the host; each bar keeps shape 07 geometry and hooks',
        note: 'A stirrup is shape 07; a 90° hook carries a 12d extension. The fabricator bends from the shape code, so the shape family in the model must match the bend table in the shop.'
      },
      {
        title: 'Structural Schedules & Quantities',
        text: 'A structural drawing set is complete only when the **schedules** are present — the bar lists, the column schedules, the footing schedules, the material takeoffs. Schedules are the bridge between the 3D model and the contractor\'s order book.\n\n## WHAT A SCHEDULE IS\n\nA **schedule** is a table of model data. Each row is an element (a column, a footing, a beam); each column is a parameter (type, mark, material, volume, weight). Revit builds schedules from the model automatically, so a schedule can never disagree with the drawings — change the model and the schedule updates. That is the BIM promise made concrete.\n\n## THE STRUCTURAL SCHEDULES\n\nCommon structural schedules: **Column Schedule** (grid location, size, material, reinforcement), **Beam Schedule** (member, section, length, reinforcement), **Footing Schedule** (size, depth, rebar), and the **Rebar Schedule** (every bar: mark, shape, size, length, count, weight). Each is created with **View > Schedules > Schedule/Quantities**, picking the category and the fields.\n\n## FIELDS, FILTERS AND FORMATS\n\nA schedule is configurable: pick the **fields** (which parameters show), **sort/group** (by type, by grid), **formatting** (units, decimals, headings) and **filters** (only footings below level 1). Calculated parameters can multiply volume by density to output steel weight. The schedule\'s appearance on the sheet is controlled by its header and body formatting.\n\n## QUANTITIES AND TAKEOFFS\n\n**Material takeoffs** schedule volumes — concrete cubic metres, rebar kilograms. The schedule sums automatically (Grand Totals). These numbers feed the estimator and the purchase order. If the takeoff says 34.2 m³ and the order says 34 m³, the schedule is the authority.\n\n## The Workflow\n\nCreate a schedule per structural category, choose the fields that matter (mark, size, material, weight), filter to the right elements, and drop it on a sheet. The schedule is not paperwork — it is the model\'s own accounting.',
        code: '# Revit API (C#): read schedule quantities programmatically\nDocument doc = ...;\n// find the rebar schedule by name\nViewSchedule schedule = new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewSchedule))\n    .Cast<ViewSchedule>()\n    .First(s => s.Name == "Rebar Schedule");\nTableData table = schedule.GetTableData();\n// iterate body rows (first row after headers)\nfor (int r = 1; r < table.GetNumberofBodyRows(); r++)\n{\n    TableCellData cell = table.GetCellData(SectionType.Body, r, 0);\n    string mark = cell.GetCellText();          // bar mark\n    // sum the Count field to total bars\n}',
        note: 'A Revit schedule reads the model, so it can never disagree with the drawings — the takeoff totals are the model speaking for itself.'
      },
      {
        title: 'Beam-Column Junction Detailing',
        text: 'The **beam-column junction** is the busiest square metre of a concrete frame: beam bars, column bars and stirrups all meet in a node that must transfer load from beam to column. It is the classic detail that gets drawn wrong and is expensive to fix on site.\n\n## WHAT HAPPENS AT THE JUNCTION\n\nAt a junction, the column passes through and the beam bears on the column face. The **column bars** continue vertically; the **beam bottom bars** must be anchored into the column (they cannot just stop at the face); the **beam top bars** pass over the column. The junction zone is the **joint core** — the concrete inside the column perimeter at the beam level. The core\'s stirrups (ties) are the same as the column\'s but often densified.\n\n## DEVELOPMENT LENGTH\n\nThe beam bars need **development length** — the straight embedment (or hook) that transfers the bar\'s full force into the concrete. Where the bar ends inside the column, the code\'s development length must fit; if the column is too narrow, the detailer adds a **hook** or **head**. A detail that ignores development length fails in the joint first.\n\n## THE REVIT DETAIL\n\nRevit details a junction as a **callout detail view** at 1:10 or 1:5. The plan shows the beam bars and the column; the section shows the **beam top bars passing over the column** and the **bottom bars hooked into the core**. The detail combines **model geometry** (the beams and column shown at large scale) with **detail components** — the rebar drawn as 2D lines, the hatch for the concrete, the leader notes. The **Rebar</detail** tool can also draw the actual model rebar and lock it into the view.\n\n## COORDINATION CHECK\n\nAt the junction, check three things: the beam bars fit past the column bars (bars clash), the stirrups have the required cover inside the joint core, and the development length fits. A junction detail that passes all three coordinates with the reinforcement schedule.\n\n## The Workflow\n\nCut a callout at the junction, show the model beams and column, draw the rebar and hatches, add the development-length notes, and verify bar clearance. The junction is where structural detailing earns its keep.',
        code: '# Revit API (C#): detect bar clashing in a junction zone\nDocument doc = ...;\nFilteredElementCollector bars = new FilteredElementCollector(doc)\n    .OfClass(typeof(Rebar));\n// the column core as a solid, and the beam bars as solids\nSolid jointCore = /* solid of column core at beam level */;\nList<Solid> beamBarSolids = new List<Solid>();\nforeach (Element e in bars)\n{\n    Rebar bar = e as Rebar;\n    if (bar != null && bar.GetHostId() == beamId)\n        beamBarSolids.Add(bar.GetSolid());   // actual bar geometry\n}\n// solid intersection = the bars poke through each other\nforeach (Solid s in beamBarSolids)\n{\n    Solid intersection = BooleanOperationsUtils\n        .ExecuteBooleanOperation(jointCore, s, BooleanOperationsType.Intersect);\n    if (intersection != null && intersection.Volume > 0)\n        TaskDialog.Show("Clash", "Bar penetrates the joint core!");\n}',
        note: 'Beam bottom bars must develop their force inside the column core — if the development length will not fit, the detailer adds a hook or head, never just cuts the bar short.'
      },
    ],
    quizzes: [
      { text: 'The concrete thickness from the surface to the outer face of the reinforcement is called…', options: ['Cover', 'Clear span', 'Plaster thickness', 'Fire stop'], correctAnswer: 'Cover' },
      { text: 'In a simply supported one-way slab, the main flexural steel runs…', options: ['Along the short span in the tension (bottom) zone', 'Along the long span at the top', 'Diagonally', 'Only at the supports'], correctAnswer: 'Along the short span in the tension (bottom) zone' },
      { text: 'Distribution steel in a one-way slab…', options: ['Spreads concentrated loads across the main bars and controls cracks', 'Carries all the bending moment', 'Replaces the main steel', 'Resists uplift'], correctAnswer: 'Spreads concentrated loads across the main bars and controls cracks' },
      { text: 'The rebar shape code for a rectangular stirrup/tie is…', options: ['07', '01', '15', '35'], correctAnswer: '07' },
      { text: 'A standard 90° hook extension on a 12 mm bar is approximately…', options: ['12 × bar diameter (144 mm)', 'One bar diameter', '25 mm fixed', 'Half the bar diameter'], correctAnswer: '12 × bar diameter (144 mm)' },
      { text: 'The Revit tool that fills a host with a row of identical bars at a spacing is…', options: ['Rebar Set', 'Array', 'Copy', 'Beam System'], correctAnswer: 'Rebar Set' },
      { text: 'Why can a Revit schedule never disagree with the drawings?', options: ['It reads the model data directly', 'It is typed by hand each time', 'It is locked by the template', 'It only shows 2D lines'], correctAnswer: 'It reads the model data directly' },
      { text: 'At a concrete beam-column junction, the beam bottom bars must…', options: ['Develop their force inside the column core (straight or hooked)', 'Stop at the column face', 'Bend up into the slab', 'Be cut flush with the form'], correctAnswer: 'Develop their force inside the column core (straight or hooked)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 16 — Documentation & Sheets
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 16,
    title: 'Documentation & Sheets',
    description: 'The model becomes a construction package on the sheet — set up title blocks, place views at scale, control line weights, and publish PDF, DWG and BIM360 deliverables.',
    topics: [
      {
        title: 'Sheet Setup & Title Blocks',
        text: 'A Revit project is delivered on **sheets** — the drawing borders that carry the plans, sections, schedules and the title block. Sheet setup is the first step of documentation, and a disciplined sheet set is what makes a 100-sheet package navigable.\n\n## CREATING A SHEET\n\nCreate a sheet with **View > Sheet**, choosing a **title block** family (the border). The title block carries the project name, sheet number, drawing title, scale, date, revision block and the drawing-index. Revit\'s title blocks are families with **shared parameters** — the project name updates on every sheet when you edit it once. Firm templates ship title blocks pre-sized to A3/A1/ANSI D/E.\n\n## SHEET NUMBERING\n\nSheets are numbered in a disciplined sequence: architectural **A101, A201**, structural **S101, S201**, details **A501**… The number is a parameter on the sheet. The **Sheet List** schedule lists every sheet with its number, name and revision — it is the package index, generated from the model so it can never be out of date.\n\n## VIEWPORTS ON SHEETS\n\nDragging a view onto a sheet creates a **viewport** — the view displayed at a scale. The viewport has a label (title, scale, sheet number) and can be moved and resized. Multiple views fit on one sheet: a site plan at 1:200, a floor plan at 1:100, a section at 1:50.\n\n## PLACE, CHECK, PLOT\n\nSheet setup is a checklist: title block correct, views placed at the right scale, viewport labels match, revision block updated, sheet list current. The sheet is the deliverable — everything upstream of it exists to fill the sheets.\n\n## The Workflow\n\nCreate sheets with the firm title block, number them in sequence, drag views onto them, update the sheet list, and check scales before publishing. A sheet set built on a template stays consistent across the whole project.',
        code: '# Revit API (C#): create a sheet and place a view on it\nDocument doc = ...;\nViewSheet sheet = ViewSheet.Create(doc, titleBlockTypeId);\nsheet.SheetNumber = "S101";\nsheet.Name = "Foundation Plan";\n// find the plan view to place\nViewPlan plan = new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewPlan))\n    .Cast<ViewPlan>()\n    .First(v => v.Name == "Foundation Plan");\n// place the view on the sheet\nElementId placed = sheet.AddView(plan.Id);\n// position the viewport\nElement vp = doc.GetElement(placed);\nXYZ pos = new XYZ(0.5, 0.5, 0);   // sheet space in feet\n(vp.Location as LocationPoint).Point = pos;',
        note: 'The sheet list is generated from the model — when a sheet is added or renumbered, the index updates itself, so the package index never goes stale.'
      },
      {
        title: 'Placement of Views on Sheets',
        text: 'Putting a view on a sheet looks trivial — drag, drop, done — but disciplined placement controls the whole document set: which views appear, at what scale, on which sheet, and how they read together.\n\n## CHOOSING WHAT GOES ON A SHEET\n\nEach sheet carries a coherent family of views. A **Foundation Plan S101** carries the foundation plan, a couple of sections and the footing schedule. The **sheet layout** is planned before placing: the main plan in the centre at the working scale, sections and details around it, the schedule in a corner. A viewer should find the plan, read the sections, and check the schedule without hunting.\n\n## SCALE AND VIEWPORT\n\nThe viewport label shows the view name and scale. The **View Scale** is set in the view properties (1:100, 1:50, 1:5); the viewport on the sheet shrinks to fit. A plan placed at the wrong scale is the most common sheet error — check the viewport label against the title block scale note. Revit shows the scale on the viewport; mismatched label and view = rejected drawing.\n\n## ORGANISING MULTIPLE VIEWS\n\nSeveral small views (details) can share a sheet. **Detail views** from callouts, **schedules** as tables, and **legends** all drop onto sheets. Alignment matters: Revit\'s align/distribute tools snap viewports into tidy rows. A messy sheet looks unprofessional even with perfect content.\n\n## VIEWPORT TITLES\n\nThe viewport label can be turned off for a title already inside the view, or customised. Sheet views that duplicate each other (plan on two sheets) should be avoided — one view, one home.\n\n## The Workflow\n\nPlan the sheet content first, place the primary view, then surround it with sections/details/schedules, align the viewports, and verify each scale label matches its view. Placement discipline is half the professionalism of the package.',
        code: '# Revit API (C#): enumerate viewports placed on a sheet and their scales\nDocument doc = ...;\nViewSheet sheet = /* the sheet */;\nFilteredElementCollector vps = new FilteredElementCollector(doc)\n    .OwnedByView(sheet.Id);\nforeach (Element e in vps)\n{\n    ElementType vpType = doc.GetElement(e.GetTypeId());\n    Parameter scale = vpType.get_Parameter(BuiltInParameter.VIEWPORT_SCALE);\n    // match the viewport scale against the title block scale note\n    string sheetScale = scale.AsString();\n    if (sheetScale != "1:100")\n        TaskDialog.Show("Check", "Viewport scale " + sheetScale + " on " + sheet.Name);\n}',
        note: 'A viewport label that says 1:100 while the title block says 1:200 is a classic reject — always verify the viewport scale matches the sheet annotation.'
      },
      {
        title: 'Drafting Views & Line Weights',
        text: 'A construction drawing communicates through **lines** — thick for cut concrete, thin for hidden steel, dashed for opening heads. Line weights and line styles are the grammar of the drawing, and Revit enforces them through view settings.\n\n## LINE WEIGHTS\n\nRevit\'s **line weight** table maps a weight number (1-16) to a pen thickness. The table is set by the project template: weight 1 is 0.18 mm (thin), weight 5 is 0.35 mm (medium), weight 8 is 0.50 mm (thick). Objects are assigned weights by category — **cut walls** get a heavy weight, **hidden lines** a thin dashed one. The same object can be thin in plan and thick in section, because weight is per-view.\n\n## LINE PATTERNS\n\nA **line pattern** is the dash sequence: **Solid**, **Dashed**, **Center**, **Hidden**, **Phantom**. Structural plans use dashed lines for hidden beams, center lines for grids, hidden lines for buried work. The pattern belongs to the line style, so switching a layer or subcategory switches the pattern everywhere.\n\n## LINE STYLES\n\nRevit combines weight + pattern + colour into a **line style** (or subcategory) per discipline: Grids, Structural Framing (Hidden), Wall (Cut), Door Swing. Drafting lines you draw by hand also carry styles — Drafting (1), Drafting (2), etc. Assigning the right style is how a drawing stays legible at 50% zoom.\n\n## DRAFTING VIEWS\n\nA **drafting view** is a pure-2D view — no model — where details are drawn from scratch (a door jam detail, a handrail connection). It has its own scale and appears only on sheets. Detailing in drafting views uses the same line styles, so the detail matches the model views.\n\n## The Workflow\n\nSet the template\'s line weight table and patterns once, draw everything on the correct line styles, and the whole set reads consistently. Line weight discipline is what separates a drafted drawing from a sketch.',
        code: '# Revit API (C#): read a line style by name to reuse in a detail\nDocument doc = ...;\nGraphicsStyle solid = new FilteredElementCollector(doc)\n    .OfClass(typeof(GraphicsStyle))\n    .Cast<GraphicsStyle>()\n    .First(g => g.Name == "Solid");\n// draw a drafting line with that style in a drafting view\nDocumentDetail = doc;   // drafting view must be active\nAutodesk.Revit.DB.View view = doc.ActiveView;\nXYZ a = new XYZ(0,0,0), b = new XYZ(5,0,0);\nLine l = Line.CreateBound(a, b);\nAutodesk.Revit.DB.DetailLine dl = doc.Create.NewDetailLine(view, l);\ndl.LineStyle = solid;   // carries weight + pattern + colour',
        note: 'Cut walls are thick, hidden beams are thin dashed — line weight by category is what keeps a busy structural plan readable when reduced to print.'
      },
      {
        title: 'Publishing: PDF, DWG & BIM360',
        text: 'Documentation ends at **publishing** — turning the Revit model into the deliverables others consume: PDF for the contractor, DWG for the engineer, and the model itself on BIM360 for the team. A clean publish is the final professional act.\n\n## EXPORT TO PDF\n\nThe PDF export (**File > Export > PDF**, or **Publish**) prints the sheet set to vector PDF. Vector output keeps lines sharp at any zoom and the file stays small. Choose the sheet range, the paper size (already set by the title block) and the colour settings (black-and-white or colour). Check the **current view/sheet** vs **all sheets** toggle — publishing the wrong range is the classic mistake.\n\n## EXPORT TO DWG\n\nFor consultants who still draft in AutoCAD, export sheets to **DWG** with a **DWG export setup** that maps Revit line styles and layers to AutoCAD layers (AI or S for architecture, S for structural). The layer mapping table lives in the export setup — a good mapping keeps the DWG editable instead of a mass of layer-0 lines. Set the units (millimetres) and the target AutoCAD version.\n\n## BIM360 AND THE CLOUD\n\n**BIM360 (Autodesk Construction Cloud)** hosts the model for the whole team — the model is **cloud-hosted**, each member opens the live version, and changes sync in real time. **Publish** from Revit updates the shared package; issues, sheets and models live in one project. The cloud model replaces the zip-of-files email with a single source of truth.\n\n## THE PUBLISH CHECKLIST\n\nBefore publishing: update all sheets, fix any **sheet not up-to-date** warnings, verify the revision block, and export the right range. A package published clean is the handshake between design and construction.\n\n## The Workflow\n\nUpdate sheets, export PDF for the record set, export DWG for consultants, publish the model to BIM360 for the team — and keep the native model as the single source of truth.',
        code: '# Revit API (C#): export all sheets to PDF\nDocument doc = ...;\nPDFExportOptions pdfOpts = new PDFExportOptions();\npdfOpts.Combine = false;            // one PDF per sheet\npdfOpts.ColorDepth = ColorDepthType.BlackLine;   // B/W record set\nIList<ElementId> sheetIds = new List<ElementId>();\nforeach (ViewSheet s in new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewSheet)))\n    sheetIds.Add(s.Id);\ndoc.Export(@"C:\\out", "EduNexus_Structural", sheetIds, pdfOpts);',
        note: 'Exporting the wrong range is the classic publish error — check the sheet range before hitting export, and keep the native Revit model as the single source of truth.'
      },
    ],
    quizzes: [
      { text: 'The Revit family that carries the drawing border, project name and sheet number is the…', options: ['Title block', 'Viewport', 'Legend', 'Grid'], correctAnswer: 'Title block' },
      { text: 'The schedule that lists every sheet with its number, name and revision is the…', options: ['Sheet List', 'Rebar Schedule', 'Viewport Schedule', 'Room Schedule'], correctAnswer: 'Sheet List' },
      { text: 'A view displayed on a sheet is placed inside a…', options: ['Viewport', 'Title block', 'Grid line', 'Sheet list'], correctAnswer: 'Viewport' },
      { text: 'The most common sheet-layout error is…', options: ['A viewport scale that does not match the title block scale note', 'Too few notes', 'A missing grid line', 'Too much white space'], correctAnswer: 'A viewport scale that does not match the title block scale note' },
      { text: 'In a Revit drawing, cut concrete walls should be drawn with…', options: ['A heavy line weight', 'The thinnest line weight', 'A hidden pattern', 'A centre pattern'], correctAnswer: 'A heavy line weight' },
      { text: 'A pure-2D view with no model geometry, used to draw a construction detail, is a…', options: ['Drafting view', 'Floor plan', '3D view', 'Schedule'], correctAnswer: 'Drafting view' },
      { text: 'When exporting sheets to DWG, the thing that keeps the DWG editable is…', options: ['A layer-mapping table in the export setup', 'Saving as DWG 2000', 'Using raster output', 'Colour as lineweight'], correctAnswer: 'A layer-mapping table in the export setup' },
      { text: 'The cloud platform where a Revit model is shared live with the whole team is…', options: ['BIM360 (Autodesk Construction Cloud)', 'OneDrive', 'A network drive', 'Email attachments'], correctAnswer: 'BIM360 (Autodesk Construction Cloud)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 17 — Revit Architecture: Levels, Grids & Walls
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 17,
    title: 'Revit Architecture: Levels, Grids & Walls',
    description: 'The architectural side of BIM — start from the template, lay out levels, grids and project north, then model the building enclosure with wall types, layers and curtain walls, and place the openings.',
    topics: [
      {
        title: 'The Revit Architecture Interface & Template',
        text: 'Revit Architecture is the same BIM engine as Revit Structure, tuned for buildings rather than frames. The **interface** and the **template** decide how fast and how consistent a project is — a firm template is half the architecture done.\n\n## THE RIBBON AND THE PROJECT BROWSER\n\nThe **Ribbon** tabs for architecture: **Architecture, Structure, Insert, Annotate, Massing & Site, Collaborate, View, Manage**. The Architecture tab carries the building tools — Wall, Door, Window, Component, Floor, Roof, Ceiling, Stair, Railing, Curtain Grid. The **Project Browser** on the left lists every view, sheet, family and schedule; it is the navigation tree of the whole model.\n\n## THE ARCHITECTURAL TEMPLATE\n\nStarting a project with **New > Project** and the **Architectural Template** preloads: wall types (exterior cavity wall, interior partition), door and window families, the floor/ceiling/roof types, the room and area settings, the title blocks, and the view templates. Firm templates add the studio\'s own wall constructions, annotation styles and sheet numbering. The template is where a firm\'s standards live.\n\n## PROPERTIES AND THE VIEWPORT\n\nSelect anything and the **Properties** palette shows its parameters — a wall\'s height, base/top offset, structural use, and its type. The **View Control Bar** (bottom of the view) sets the view scale, detail level, visual style and crop. These two panels are where most everyday work happens.\n\n## FIRST STEPS\n\nSet the levels (ground floor, first floor, roof), draw the grids if the building needs them, then start the exterior walls. Everything else — doors, windows, floors, roofs — hangs off those first three actions. The template makes the first hour consistent; the first hour makes the project.\n\n## The Workflow\n\nOpen from the architectural template, confirm the units and levels, lay out grids, and start walls. A project started from a good template inherits the firm\'s standards without any setup.',
        code: '# Revit API (C#): set the project units and create levels\nDocument doc = ...;\nUnitGroup ug = UnitType.ProjectInstance;\nUnits projectUnits = doc.GetUnits();\nfor (int i = 0; i < projectUnits.GetUnitOptions().Count; i++)\n{\n    // set linear units to millimetres, two decimals\n    FormatOptions fo = projectUnits.GetFormatOptions(SpecTypeId.Length);\n    fo.DisplayUnits = UnitTypeId.Millimeters;\n    doc.SetUnits(projectUnits);\n}\n// create levels 0.000, 3.000, 6.000 (ground, first, roof)\ndouble[] elevs = { 0, 3, 6 };\nforeach (double e in elevs)\n{\n    Level level = Level.Create(doc,\n        UnitUtils.ConvertToInternalUnits(e, DisplayUnitType.DUT_METERS));\n    level.Name = "L+" + e.ToString("0.000");\n}',
        note: 'A firm template is where standards live — wall constructions, door families, line styles and sheet numbering all arrive preloaded with the project.'
      },
      {
        title: 'Levels, Grids & Project North',
        text: '**Levels** are the floors; **grids** are the column/wall lines; **Project North** is the orientation the drawing set is dimensioned from. Together they are the coordinate skeleton every wall, door and room snaps to.\n\n## LEVELS\n\nA **Level** is an infinite horizontal plane at an elevation (Ground 0.000, First 3.000, Second 6.000). Levels appear as datum lines in section and elevation; walls and floors attach to them. Create levels with the **Level** tool in elevation/section, or type an elevation in the Properties. Levels drive the **floor-by-floor** structure: each plan view is bound to a level.\n\n## GRIDS\n\n**Grids** are vertical datum planes — the column lines A, B, C and 1, 2, 3. Draw grids in a plan with the **Grid** tool; they appear as dashed lines with bubbles at the ends. Grids are what columns and bearing walls snap to, and what the structural grid snaps back to. A building without grids has nothing to dimension.\n\n## PROJECT NORTH vs TRUE NORTH\n\n**Project North** is the building\'s orientation on the drawing sheet — the direction that is "up" on the plans. **True North** is actual compass north, used by site and solar analysis. They rarely match: a building rotated 15° from north has a Project North 15° from True North. The **Rotate Project North** command (on the site plan) rotates the model so the plans are drawn square to the sheet.\n\n## THE DATUM WORKFLOW\n\nSet levels first (they define the vertical), grids second (they define the horizontal), then rotate Project North to the true orientation if the building is not square to compass. Every wall and column placed afterwards inherits this skeleton.\n\n## The Workflow\n\nCreate levels in section, draw grids in plan, set Project North on the site plan, and check the datums all align. The skeleton is invisible on the finished drawings but every dimension depends on it.',
        code: '# Revit API (C#): create a grid at an offset\nDocument doc = ...;\nGrid grid = Grid.Create(doc, Line.CreateBound(\n    new XYZ(0,0,0), new XYZ(20,0,0)));   // 20 m long\ngrid.Name = "A";\n// rotate the whole project to align with Project North\nElementCategoryFilter cats = new ElementCategoryFilter(\n    BuiltInCategory.OST_Grids);\nFilteredElementCollector grids = new FilteredElementCollector(doc)\n    .WherePasses(cats);\nforeach (Grid g in grids) { /* grids rotate with the model */ }\n// set project north on the site view (UI: Rotate Project North)',
        note: 'Project North is the orientation the plans are drawn to, True North is the compass — the site plan rotates the model so plans are square to the sheet while solar analysis keeps True North.'
      },
      {
        title: 'Wall Types, Layers & Curtain Walls',
        text: 'Walls in Revit are not a single line — they are **layered constructions**. An exterior wall is brick, cavity, insulation, block, plaster. Revit models that build-up as one wall type, so sections and schedules show the real construction.\n\n## THE WALL LAYER STACK\n\nThe **Edit Assembly** dialog (on the wall type) lists the **layers** from exterior to interior: each layer has a **function** (Structure, Thermal/Air, Finish, Membrane), a material and a thickness. The exterior cavity wall reads: brick veneer 110, cavity 50, insulation 100, block 200, plaster 12. Sections cut through the wall show the real stack; **schedules** can even total brick area.\n\n## WALL STRUCTURE FUNCTIONS\n\nEach layer\'s function controls how it behaves: **Structure** is the load-bearing core (block/concrete), **Thermal/Air** is the insulation and the air gap, **Finish** is the plaster/paint, **Substrate** is boarding (plasterboard), **Membrane** is the waterproofing. The function drives material takeoffs and structural use.\n\n## WALL TYPES AND CONSTRAINTS\n\nWall types (Exterior - Brick on CMU, Interior - 100mm Partition) are defined in the type properties. **Top/Bottom constraints** attach the wall to levels — set the base at Ground and the top at First and the wall follows the levels. **Location Line** sets the wall\'s reference (centreline, face, core face) which the dimensions lock to.\n\n## CURTAIN WALLS\n\nA **curtain wall** is a wall made of **mullions** and **panels** — glass facades, storefronts. Draw a curtain wall like a wall, then lay a **curtain grid** on it and place mullions. Panels can be glass or spandrel; mullions can be horizontal and vertical. Curtain walls are their own family system, designed for repetition (each office window bay identical).\n\n## The Workflow\n\nBuild the wall type from its layers, draw the wall with the right constraints and location line, and switch to curtain wall for glazed facades. The wall type is the building\'s construction, modelled once.',
        code: '# Revit API (C#): create a wall type from a layer stack\nDocument doc = ...;\nWallType baseType = new FilteredElementCollector(doc)\n    .OfClass(typeof(WallType))\n    .Cast<WallType>()\n    .First(w => w.Kind == WallKind.Basic);\nWallType newType = baseType.Duplicate("Exterior - Brick on CMU 360");\nCompoundStructure cs = baseType.GetCompoundStructure();\n// exterior → interior: brick, cavity, insulation, block, plaster\nMaterial brick = /* material "Brick - Common" */;\nMaterial block = /* material "Concrete Block" */;\nCompoundStructureLayer[] layers =\n{\n    new CompoundStructureLayer(110, MaterialFunctionAssignment.Structure, brick.Id),\n    new CompoundStructureLayer(50,  MaterialFunctionAssignment.AirBarrier,  brick.Id),\n    new CompoundStructureLayer(100, MaterialFunctionAssignment.Thermal,  /* insulation */ ),\n    new CompoundStructureLayer(200, MaterialFunctionAssignment.Structure, block.Id),\n    new CompoundStructureLayer(12,  MaterialFunctionAssignment.Finish,   /* plaster */),\n};\ncs.SetLayers(layers);\nnewType.SetCompoundStructure(cs);',
        note: 'A cavity wall\'s layers — brick, air gap, insulation, block, plaster — are modelled as one wall type, so a section through the wall shows the real build-up and takeoffs can total the brick.'
      },
      {
        title: 'Opening Tools: Doors, Windows & Wall Openings',
        text: 'A wall is a blank surface until **openings** cut through it. Revit\'s doors, windows and opening tools place real elements that cut the wall, and the **Wall Opening** tool cuts openings where no family fits.\n\n## DOORS AND WINDOWS\n\nThe **Door** and **Window** tools load a family and place an instance in a wall. Each door/window has a **host** (the wall), a **sill/head height**, and a **swing** (doors). The element cuts the wall automatically — the opening, the frame and the leaf are one family. Place a door, and the wall in plan shows the swing; in section, the header. Door and window families are loadable families from the library or the project.\n\n## PLACEMENT AND TAGGING\n\nPlace a door by clicking the wall — Revit snaps to the opening centre or a distance from the last door. **Tag on Placement** labels the door immediately (D1, D2…). Doors/windows read their room association; a **door schedule** lists every door with its room, size and type. The tag and the schedule both read the model, so they stay truthful.\n\n## WALL OPENINGS\n\nThe **Wall Opening** tool cuts a rectangular hole in a wall where no family fits — a pass-through, a niche, a duct opening. Pick the wall, sketch the rectangle, set the height. The opening is a void; it does not carry a frame or a leaf, just the hole.\n\n## OPENING FAMILIES\n\nFor openings with detail — a window with a frame, a louver, a door with a sidelight — the opening is a **family** with parameters (width, height, sill). Editing the family updates every instance. The opening family carries the geometry that a plain void cannot.\n\n## The Workflow\n\nLoad the door/window families, place them with tags, use Wall Opening for plain holes, and schedule the openings. Openings are where the architecture starts to read as a building.',
        code: '# Revit API (C#): place a door in a wall\nDocument doc = ...;\nWall wall = /* the wall */;\nFamilySymbol doorType = new FilteredElementCollector(doc)\n    .OfClass(typeof(FamilySymbol))\n    .Cast<FamilySymbol>()\n    .First(s => s.Family.Name == "M_Single-Flush" && s.Name == "0915 x 2134 mm");\nif (!doorType.IsActive) doorType.Activate();\n// place at 2000 mm along the wall from its origin\ndouble along = UnitUtils.ConvertToInternalUnits(2.0, DisplayUnitType.DUT_METERS);\nFamilyInstance door = doc.Create.NewFamilyInstance(wall, along, doorType);\n// host wall, reference point on it, the door family\ndoor.get_Parameter(BuiltInParameter.INSTANCE_HEAD_HEIGHT_PARAM)\n    .Set(UnitUtils.ConvertToInternalUnits(2100, DisplayUnitType.DUT_MILLIMETERS));',
        note: 'A door or window family carries the opening, frame and leaf as one element and cuts its host wall — a plain Wall Opening is only for holes where no family fits.'
      },
    ],
    quizzes: [
      { text: 'The Ribbon tab that carries the Wall, Door, Window, Floor, Roof and Stair tools is…', options: ['Architecture', 'Structure', 'Massing & Site', 'Annotate'], correctAnswer: 'Architecture' },
      { text: 'A firm template preloads all of the following EXCEPT…', options: ['Wall constructions and door families', 'Annotation styles and sheet numbering', 'The completed building model', 'Title blocks and view templates'], correctAnswer: 'The completed building model' },
      { text: 'An infinite horizontal plane that floors and walls attach to is a…', options: ['Level', 'Grid', 'Axis', 'Reference plane'], correctAnswer: 'Level' },
      { text: 'Project North is…', options: ['The orientation the drawing set is dimensioned to', 'Actual compass north', 'The north arrow on the site survey', 'The sun angle'], correctAnswer: 'The orientation the drawing set is dimensioned to' },
      { text: 'In a wall Edit Assembly dialog, the layer function of the insulation is…', options: ['Thermal/Air', 'Structure', 'Finish', 'Substrate'], correctAnswer: 'Thermal/Air' },
      { text: 'A glass facade made of mullions and panels is a…', options: ['Curtain wall', 'Basic wall', 'Cavity wall', 'Partition'], correctAnswer: 'Curtain wall' },
      { text: 'The element that cuts a rectangular hole in a wall where no door/window family fits is…', options: ['Wall Opening tool', 'Shaft', 'Door family', 'Edit Profile'], correctAnswer: 'Wall Opening tool' },
      { text: 'A door schedule in Revit…', options: ['Reads every door from the model with its room, size and type', 'Must be typed manually', 'Only lists door heights', 'Cannot be placed on a sheet'], correctAnswer: 'Reads every door from the model with its room, size and type' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 18 — Doors, Windows & Families
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 18,
    title: 'Doors, Windows & Families',
    description: 'Families are Revit\'s LEGO bricks — learn to load and place doors and windows, understand system vs loadable families, build your own in the Family Editor with parameters and types, and schedule them.',
    topics: [
      {
        title: 'Loading & Placing Doors/Windows',
        text: 'Most doors and windows in a project come from the **library** — thousands of pre-built families. The skill is loading the right one and placing it correctly. Placement looks simple; the parameter decisions behind it are not.\n\n## LOADING A FAMILY\n\nThe **Insert > Load Family** (or the Load Family button on the Door tool) opens the family library: doors (single-flush, double, sliding, pivot), windows (fixed, casement, awning, curtain-wall). Pick the family and the **type** (the size): M_Single-Flush 0915 × 2134 mm. The type carries the size, the material and the performance (fire rating, thermal U-value). Loading a family makes its types available to place.\n\n## PLACEMENT\n\nWith the Door tool active, hover a wall — the door previews and snaps. Click to place. Placement options: the **sill height** (windows), the **head height** (doors), and the **tag** (toggle Tag on Placement). Doors swing — the swing direction is controlled by flipping during placement (space bar) or after.\n\n## COPY AND REPEAT\n\nPlace one door, then **Copy** it down the corridor — the copies keep the family, type, and tag logic. **Repeat** (R) places the last element again. When many identical openings line a wall, place one, copy, and let the schedule count them.\n\n## THE RIGHT FAMILY\n\nChoosing the family is a design decision: a door with a sidelight, a fire-rated door, a double-leaf door. The family chosen affects the schedule (door type), the performance (fire rating), and the drawing (swing graphics). Load only what the project uses — a bloated library slows the model.\n\n## The Workflow\n\nLoad the needed families and types, place with tag on, control sill/head heights, copy along walls, and schedule. Doors and windows are the most-touched families in any building project.',
        code: '# Revit API (C#): place a window at a sill height\nDocument doc = ...;\nWall wall = /* host wall */;\nFamilySymbol windowType = new FilteredElementCollector(doc)\n    .OfClass(typeof(FamilySymbol))\n    .Cast<FamilySymbol>()\n    .First(s => s.Family.Name == "M_Fixed" && s.Name == "1200 x 1200mm");\nif (!windowType.IsActive) windowType.Activate();\nXYZ location = new XYZ(3.0, 0, 0);   // 3 m along the wall\nFamilyInstance win = doc.Create.NewFamilyInstance(\n    wall, location, windowType);\n// sill at 900 mm above level\nwin.get_Parameter(BuiltInParameter.INSTANCE_SILL_HEIGHT_PARAM)\n    .Set(UnitUtils.ConvertToInternalUnits(900, DisplayUnitType.DUT_MILLIMETERS));\n// tag the instance\nIndependentTag tag = IndependentTag.Create(doc, doc.ActiveView.Id,\n    new Reference(win), true, TagMode.TM_ADDBY_CATEGORY,\n    TagOrientation.Horizontal, new XYZ(3, 1.2, 0));',
        note: 'The family type carries size, material and performance — fire rating and U-value ride on the type, so choosing the right type is a design decision, not just a click.'
      },
      {
        title: 'System vs Loadable Families',
        text: 'Revit families split into two big classes — **system families** (built into the project: walls, floors, roofs, stairs, ducts) and **loadable families** (files you load: doors, windows, furniture, equipment). Knowing which class you are editing changes the whole workflow.\n\n## SYSTEM FAMILIES\n\n**System families** are defined in the project, not loaded from a file. Walls, floors, ceilings, roofs, stairs, ramps, railings, ducts, pipes and grids are system families. You cannot delete them; you duplicate a type (new wall type) and edit its parameters. Their geometry is context-dependent — a wall follows its sketch, a floor is a sketch with a type. System families carry the building itself.\n\n## LOADABLE FAMILIES\n\n**Loadable families** are .rfa files brought into the project: doors, windows, furniture, fixtures, equipment, columns (architectural), title blocks. Each has its own geometry, parameters and types defined in the Family Editor, saved to a file, and reloaded when you want the newest version. They are the LEGO bricks you assemble.\n\n## IN-PLACE FAMILIES\n\nA third kind — **in-place families** — are built inside the project for one-off geometry that no family covers (a custom parapet, a reception desk). In-place families are efficient for a unique piece but abuse them and the model becomes unmanageable. Use in-place only for true one-offs.\n\n## WHEN TO USE WHAT\n\nNeed a building assembly (wall, floor, roof)? System family — duplicate a type. Need a manufactured or repeated object (door, chair, pump)? Loadable family from the library or built in the editor. Need a one-off shape? In-place. Choosing the class is the first architecture of a Revit model.\n\n## The Workflow\n\nModel the structure with system families, assemble the content with loadable families, and keep in-place to genuine one-offs. The model stays editable and light.',
        code: '# Revit API (C#): distinguish system vs loadable via Family placement\nDocument doc = ...;\n// system family: create a floor (sketch-based, no .rfa file)\nFloorType floorType = new FilteredElementCollector(doc)\n    .OfClass(typeof(FloorType)).Cast<FloorType>().First();\n// loadable family: load a door .rfa from disk\nstring path = @"C:\\content\\doors\\M_Single-Flush.rfa";\nFamily loaded = doc.LoadFamily(path);   // returns the loaded Family\nFamilySymbol sym = loaded.Symbols.Cast<FamilySymbol>().First();\n// system families have no file; loadable families carry a .rfa\nforeach (Family f in new FilteredElementCollector(doc).OfClass(typeof(Family)))\n{\n    string file = f.IsInPlace ? "in-place" : "loadable .rfa";\n}',
        note: 'System families (walls, floors, roofs) live in the project and are duplicated by type; loadable families are .rfa files assembled like bricks; in-place families are reserved for true one-offs.'
      },
      {
        title: 'Family Editor: Parameters & Types',
        text: 'The **Family Editor** is where you build your own doors, windows and equipment. A well-built family has **parameters** (width, height, sill) and **types** (0915, 1010, 1212) driven by those parameters, so one family covers a product range.\n\n## THE FAMILY EDITOR ENVIRONMENT\n\nOpen a family file (.rfa) and you are in the Family Editor — a different workspace with **reference planes**, the **family parameters** dialog, and **nested family** support. You sketch the geometry (extrusions, sweeps, blends), constrain it to reference planes, and expose dimensions as parameters.\n\n## PARAMETERS AND TYPES\n\nThe **Family Types** dialog lists the parameters: Width, Height, Sill Height, Fire Rating, Material. Each parameter has a type (Length, Text, Yes/No, Material) and a **group** (Dimensions, Materials, Identity). Types are named rows of parameter values — type "0915 × 2134 mm" sets Width=915, Height=2134. Place any type; change the type and every instance updates.\n\n## CONSTRAINTS\n\nGeometry in a family is **constrained** to reference planes with dimensions — the extrusion\'s width is driven by the Width parameter. When the type changes Width, the extrusion rebuilds. Unconstrained geometry (drawn loose) will not respond. The rule: sketch, constrain, parameterise.\n\n## NESTING AND SHARED PARAMETERS\n\nComplex families **nest** other families (a door family nests a handle family, a window family nests an opening). **Shared parameters** carry data across families and into schedules (a door\'s fire rating shared with the door schedule). Shared parameters are defined in a shared parameter file and used by many families.\n\n## The Workflow\n\nSet up reference planes, sketch and constrain the geometry, expose dimensions as parameters, define types, and load the family into the project. A parameterised family is reusable capital — build it once, schedule it forever.',
        code: '# Revit API (C#): create a family type with a parameter value\nDocument familyDoc = ...;   // Family Editor document\nFamilyManager fm = familyDoc.FamilyManager;\n// create a new type "1212 x 1212mm" based on the current\nFamilyType newType = fm.NewType("1212 x 1212mm");\nfm.CurrentType = newType;\n// set the Width parameter (a length family parameter)\nFamilyParameter width = fm.get_Parameter("Width");\nfm.Set(width, UnitUtils.ConvertToInternalUnits(1212, DisplayUnitType.DUT_MILLIMETERS));\n// any instance of this type now uses Width = 1212 mm',
        note: 'Geometry in a family must be constrained to reference planes and driven by parameters — loose geometry ignores the type, so a family that will not resize is almost always unconstrained.'
      },
      {
        title: 'Schedules: Door & Window Schedules',
        text: 'A **door schedule** lists every door in the building — mark, type, size, room, fire rating. Because Revit schedules read the model, the schedule is always current: move a door or change its type and the schedule updates. Door and window schedules are the architect\'s standard deliverables.\n\n## CREATING A DOOR SCHEDULE\n\n**View > Schedules > Schedule/Quantities**, pick the **Doors** category, and choose the **fields**: Mark, Type, Width, Height, Level, Room, Fire Rating, Comments. Each row is a placed door. **Sort/Group** orders by mark or by type; the **Grand Totals** option counts them. The schedule previews as you build it.\n\n## SCHEDULE FORMATTING\n\nThe schedule is a table you format: column widths, headers, **filters** (only doors on Ground), **calculated values** (door area = Width × Height), and **conditional formatting** (highlight fire-rated doors). The schedule appears on a sheet as a live view — the title "Door Schedule" and the totals print with it.\n\n## THE MODEL CONNECTION\n\nChange a door\'s type in the model and the schedule cell updates. Delete a door and the row disappears. This is the promise of BIM scheduling: the schedule can never disagree with the drawings because it IS the model, tabulated. The old workflow — counting doors by hand and typing a list — is gone.\n\n## SCHEDULE KEY AND FIELDS\n\nFields come from the door family\'s parameters — a custom family with a "Fire Rating" parameter schedules that rating. **Schedule Keys** group standard rows (a key row for "standard 915 × 2134 fire door" fills the related fields). Schedules are where the family\'s parameters pay off.\n\n## The Workflow\n\nCreate the door/window schedule, pick the fields, format and filter, drop it on a sheet. The schedule is the model\'s door count, updated by itself.',
        code: '# Revit API (C#): read a door schedule from the model\nDocument doc = ...;\nViewSchedule ds = new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewSchedule))\n    .Cast<ViewSchedule>()\n    .First(s => s.Name == "Door Schedule");\n// count doors directly from the model (what the schedule shows)\nint count = new FilteredElementCollector(doc)\n    .OfClass(typeof(FamilyInstance))\n    .WherePasses(new ElementCategoryFilter(BuiltInCategory.OST_Doors))\n    .Count();\nTaskDialog.Show("Doors", "Model has " + count + " doors on the schedule.");',
        note: 'A Revit schedule reads the model — move a door or change its type and the schedule updates itself, so the schedule can never disagree with the drawings.'
      },
    ],
    quizzes: [
      { text: 'The Revit tool that brings a .rfa door family into the project is…', options: ['Insert > Load Family', 'File > New', 'Home > Import', 'Manage > Purge'], correctAnswer: 'Insert > Load Family' },
      { text: 'Walls, floors, roofs and stairs are examples of…', options: ['System families', 'Loadable families', 'In-place families', 'Nested families'], correctAnswer: 'System families' },
      { text: 'A .rfa file loaded into a project is a…', options: ['Loadable family', 'System family', 'Mass family', 'Detail component'], correctAnswer: 'Loadable family' },
      { text: 'An in-place family is best used for…', options: ['A genuine one-off shape no library family covers', 'Every door in the project', 'Standard wall constructions', 'Repeated columns'], correctAnswer: 'A genuine one-off shape no library family covers' },
      { text: 'In the Family Editor, geometry must be constrained to…', options: ['Reference planes driven by parameters', 'The screen centre', 'The project origin only', 'Grid lines'], correctAnswer: 'Reference planes driven by parameters' },
      { text: 'A family with types "0915" and "1212" uses the type to vary…', options: ['Parameter values like Width and Height', 'The family file itself', 'The project template', 'The wall layer stack'], correctAnswer: 'Parameter values like Width and Height' },
      { text: 'A schedule that lists every door with mark, type, room and fire rating is a…', options: ['Door schedule', 'Sheet list', 'Material takeoff', 'Viewport schedule'], correctAnswer: 'Door schedule' },
      { text: 'Why does a Revit door schedule stay current automatically?', options: ['It reads the door instances from the model', 'It is retyped each revision', 'It is imported from Excel', 'It only lists fixed sizes'], correctAnswer: 'It reads the door instances from the model' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 19 — Floors, Roofs & Stairs
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 19,
    title: 'Floors, Roofs & Stairs',
    description: 'Close out the building envelope — floor types and sketching, the three roof creation methods, stair runs with landings and railings, and the ramps and openings that tie circulation together.',
    topics: [
      {
        title: 'Floor Types & Sketching Floors',
        text: 'A **floor** in Revit is a system family built from a **sketch** and a **type**. The type carries the layer stack (finish, screed, slab, insulation); the sketch defines the shape. Floors are where the building\'s horizontal structure is modelled.\n\n## THE FLOOR TYPE\n\nLike walls, floors have a **layer stack** in Edit Assembly: from top — tile finish 10, screed 50, structural slab 150, insulation 50, ceiling finish below. Each layer has a function and a material. A **Floor Type** (Ground Slab 150mm, Upper Floor 200mm) is a duplicate with its own stack. The structural slab layer is what the structural consultant reads.\n\n## SKETCHING THE FLOOR\n\nClick **Floor** and Revit enters **sketch mode** — you draw the boundary with the boundary lines tools (Pick Walls, Line, Rectangle). Pick Walls snaps the floor boundary to the wall edges; the floor follows the room. Sketch lines must form a closed loop. Finish the sketch and Revit creates the floor at the level, bound by the walls.\n\n## SLOPED AND SHAPED FLOORS\n\n**Sloped floors** use a slope arrow — a drainage fall, a ramp-in-floor. **Shape Editing** (Subdivide, Points, Modify Sub Elements) sculpts a flat slab into a shape: a warped plaza, a plant room with falls. A shaped floor keeps its type and its layers while the geometry bends.\n\n## OPENINGS AND STRUCTURAL USE\n\nThe floor carries the room and the people — its **structural use** (Slab, Deck, Slab on Deck) matters to the structural model. Cut openings with the **Vertical Opening** or **Shaft** tools. Floors interact with walls: a floor edge to a wall creates the junction detail.\n\n## The Workflow\n\nPick the floor type, sketch the boundary (Pick Walls for speed), finish the sketch, add slopes or openings where needed. The floor is the horizontal half of the building\'s enclosure.',
        code: '# Revit API (C#): create a rectangular floor from a sketch\nDocument doc = ...;\nFloorType ft = new FilteredElementCollector(doc)\n    .OfClass(typeof(FloorType)).Cast<FloorType>().First();\nLevel level = /* Ground level */;\n// build a single closed loop for the boundary\nCurveArray boundary = new CurveArray();\nboundary.Append(Line.CreateBound(new XYZ(0,0,0), new XYZ(12,0,0)));\nboundary.Append(Line.CreateBound(new XYZ(12,0,0), new XYZ(12,9,0)));\nboundary.Append(Line.CreateBound(new XYZ(12,9,0), new XYZ(0,9,0)));\nboundary.Append(Line.CreateBound(new XYZ(0,9,0), new XYZ(0,0,0)));\nFloor floor = Floor.Create(doc, boundary, ft.Id, level.Id);\n// slope: give the floor a slope arrow\nfloor.get_Parameter(BuiltInParameter.ROOF_SLOPE).Set(0.02);',
        note: 'Pick Walls snaps the floor boundary to the wall edges — the room and the floor stay in lockstep, and the floor type\'s layer stack is what the structural consultant reads.'
      },
      {
        title: 'Roofs: Footprint, Extrusion & Sloped',
        text: 'Roofs are the building\'s hat. Revit offers three creation methods — **footprint**, **extrusion**, and **by face** — plus sloped roofs. The method depends on the shape: a hip roof is footprint, a barrel roof is extrusion.\n\n## FOOTPRINT ROOFS\n\n**Roof by Footprint** sketches the roof outline in plan (like a floor) and sets a **slope** on each boundary line. Give the roof a 30° pitch, click the outline lines, finish, and Revit builds the sloped planes meeting at the ridges. A hip roof is a footprint with all edges sloped; a gable has two sloped edges and two level. The slope value (degrees or rise/run) is set per boundary line.\n\n## EXTRUSION ROOFS\n\n**Roof by Extrusion** draws the roof profile in section and extrudes it across the building — a barrel vault, a curved canopy, a shed roof. Sketch the arc or pitch line in a section/elevation, set the extrusion length, and Revit sweeps it. Extrusion roofs are the way to model curved roofs that a footprint cannot describe.\n\n## SLOPED AND TRUSSED ROOFS\n\nA **sloped glazing roof** (curtain wall roof) carries glass panels on a roof grid. A **truss** is modelled as a system with the structural tools, or a family. **Roof by Face** builds a roof on a mass surface — for freeform roofs from massing.\n\n## ROOF OPENINGS\n\nRoofs get cut by **skylights** (a family placed in the roof) or **Openings**. A skylight reads the roof slope; the opening follows the roof plane. Roofs meet walls at the **roof-wall junction** — the template\'s wall top handles the cave detail.\n\n## The Workflow\n\nChoose footprint for hip/gable roofs, extrusion for curved, by-face for massing-driven forms, and place skylights as openings. The roof closes the enclosure.\n\n## The Choice\n\nA footprint roof cannot curve and an extrusion roof cannot hip — pick the method that matches the geometry. The roof type\'s layers (tiles, sarking, rafters) live in Edit Assembly like a floor.',
        code: '# Revit API (C#): create a sloped footprint roof\nDocument doc = ...;\nRoofType rt = new FilteredElementCollector(doc)\n    .OfClass(typeof(RoofType)).Cast<RoofType>().First();\nLevel level = /* roof level */;\nCurveArray profile = new CurveArray();\nprofile.Append(Line.CreateBound(new XYZ(0,0,0), new XYZ(12,0,0)));\nprofile.Append(Line.CreateBound(new XYZ(12,0,0), new XYZ(12,8,0)));\nprofile.Append(Line.CreateBound(new XYZ(12,8,0), new XYZ(0,8,0)));\nprofile.Append(Line.CreateBound(new XYZ(0,8,0), new XYZ(0,0,0)));\nFootPrintRoof roof = doc.Create.NewFootPrintRoof(profile, level.Id, rt);\n// set slope on each boundary line (footprint roofs only)\nModelCurveArray curves = roof.GetProfiles();\nforeach (ModelCurve mc in curves)\n    roof.set_DefinesSlope(mc.GeometryCurve.Reference, true);',
        note: 'Footprint roofs slope each boundary line into a hip or gable; extrusion roofs sweep a profile across for curves — pick the method that matches the geometry.'
      },
      {
        title: 'Stairs: Runs, Landings & Railings',
        text: 'Stairs connect levels and are the most code-sensitive element in a building — riser height, going, headroom and handrail all have limits. Revit\'s stairs are **sketch-based** or **component-based**, with runs, landings and railings generated from the sketch.\n\n## STAIR COMPONENTS AND TYPES\n\nA **Stair** in Revit has a **type** (Stair 190mm Riser / 250mm Tread, or a custom one) and a path. The **component stair** tool lets you place a **run** — click the start, the path, and the finish; Revit generates the treads and risers at the type\'s dimensions, flagging any that violate code (riser too high, tread too shallow).\n\n## RUNS, LANDINGS AND RAILINGS\n\nA **run** is a straight or curved flight. A **landing** connects runs at a turn — a quarter landing, a half landing, a **winders** (tapered treads) for tight turns. **Railings** are generated along the stair edge: the **Railing** tool attaches a railing (with its baluster and handrail profile) to the stair or floor edge. Railings can follow the slope or sit level.\n\n## SKETCH-BASED STAIRS\n\n**Stairs by Sketch** draws the boundary and the path in plan — the treads, risers and landings fill the boundary. Sketch stairs give control over irregular shapes (an L stair in a curved wall); component stairs are faster for standard runs. Both derive from the type\'s riser/tread rules.\n\n## STAIR CODE AND SAFETY\n\nThe template\'s stair type encodes code limits: a riser must be within 150-190 mm, a tread (going) within 250-300 mm. Revit flags violations in yellow as you draw. The **stair path** (the arrow showing the direction of travel) is drawn automatically.\n\n## The Workflow\n\nChoose the stair type, place runs with landings, generate railings, and check the riser/tread warnings. Stairs are where vertical circulation meets the code.',
        code: '# Revit API (C#): create a stair run between two levels\nDocument doc = ...;\nStairsType st = new FilteredElementCollector(doc)\n    .OfClass(typeof(StairsType)).Cast<StairsType>().First();\nLevel baseLevel = /* Ground */, topLevel = /* First */;\nStairs stairs = Stairs.Create(doc, st.Id, baseLevel.Id, topLevel.Id);\n// place a run: start point, end point at Level 1\ndouble runWidth = UnitUtils.ConvertToInternalUnits(1200, DisplayUnitType.DUT_MILLIMETERS);\nStairsRun run = StairsRun.CreateStraightRun(stairs, new XYZ(0,0,0), new XYZ(3,0,0), runWidth);\n// the type drives riser/tread; check computed values\nint nRisers = run.ActualRisersNumber;\nTaskDialog.Show("Stairs", "Risers: " + nRisers);',
        note: 'The stair type encodes code limits — riser 150-190 mm, going 250-300 mm — and Revit flags violations in yellow as you draw the run.'
      },
      {
        title: 'Ramps, Railings & Openings',
        text: 'Beyond the stair, circulation needs **ramps** for accessibility, **railings** everywhere there is a drop, and **openings** through floor and wall. Together they make a building usable and code-compliant.\n\n## RAMPS\n\nA **Ramp** is a sloped floor for wheelchairs, buggies and trolleys. Draw it like a stair run — click start and end, set the width and the **max slope** (1:12 is the accessibility rule, 1:20 the ideal). Revit generates the ramp with landings where the length exceeds the code maximum (a 1:12 ramp needs a landing every 9 m). Ramps can be straight or switchback.\n\n## RAILINGS\n\n**Railings** protect any edge with a drop: stairs, ramps, balconies, landings. A railing has a **top rail**, **balusters** (the vertical posts) and a **handrail** — each with a profile (round, square) and a material. Railings attach to a host edge (stair, floor) and follow its slope. The **Railing type** sets the height (900 mm typical) and the baluster pattern. Railings are a code item — a missing guard rail is a rejection.\n\n## OPENINGS THROUGH FLOORS AND WALLS\n\n**Vertical Opening** cuts a rectangular hole in a single floor; **Shaft** cuts the same opening through many levels; **Wall Openings** cut walls. For stairs, the **Stair Opening** is created automatically by the stair. Openings must be coordinated with the structural grid — a stair opening in the floor must line up with the stair in the level below.\n\n## CIRCULATION COORDINATION\n\nCheck that the ramp slope meets 1:12, the railing height meets code, and the stair/ramp openings line up level to level. Circulation is the spine of the plan — get it wrong and the building fails its accessibility check.\n\n## The Workflow\n\nDraw ramps for accessible routes, place railings on every drop, cut openings for the stair and shaft, and verify code dimensions. Circulation makes the building usable.',
        code: '# Revit API (C#): create a 1:12 ramp with a landing\nDocument doc = ...;\nRampType rt = new FilteredElementCollector(doc)\n    .OfClass(typeof(RampType)).Cast<RampType>().First();\nLevel baseLevel = /* Ground */, topLevel = /* First */;\nRamp ramp = Ramp.Create(doc, rt.Id, baseLevel.Id, topLevel.Id);\ndouble width = UnitUtils.ConvertToInternalUnits(1200, DisplayUnitType.DUT_MILLIMETERS);\nRampRun rr = RampRun.CreateStraightRun(ramp, new XYZ(0,0,0), new XYZ(9,0,0), width);\n// max 1:12 slope — the type\'s max slope parameter enforces it\nTaskDialog.Show("Ramp", "Run slope: " + rr.RunTreadsCount + " segments.");',
        note: 'A 1:12 ramp needs a landing every 9 m and a railing on every drop — accessibility is enforced by the ramp and railing types, not by good intentions.'
      },
    ],
    quizzes: [
      { text: 'The Revit system family for a horizontal structural slab with a layer stack is the…', options: ['Floor', 'Ceiling', 'Roof', 'Wall'], correctAnswer: 'Floor' },
      { text: 'The fastest way to sketch a floor boundary that matches the walls is…', options: ['Pick Walls', 'Type each coordinate', 'Copy the outline from a detail', 'Draw freehand'], correctAnswer: 'Pick Walls' },
      { text: 'A hip roof is created with…', options: ['Roof by Footprint with slope on every edge', 'Roof by Extrusion', 'Roof by Face', 'Sloped glazing'], correctAnswer: 'Roof by Footprint with slope on every edge' },
      { text: 'A curved barrel roof is best modelled with…', options: ['Roof by Extrusion', 'Roof by Footprint', 'A sloped floor', 'A curtain wall'], correctAnswer: 'Roof by Extrusion' },
      { text: 'The stair element that connects two runs at a turn is a…', options: ['Landing', 'Riser', 'Baluster', 'Nosing'], correctAnswer: 'Landing' },
      { text: 'Revit flags a stair violation (like an over-high riser) by…', options: ['Showing the stair in yellow', 'Refusing to save', 'Hiding the stair', 'A sound alarm'], correctAnswer: 'Showing the stair in yellow' },
      { text: 'The accessibility rule for a ramp is approximately…', options: ['1:12 maximum slope', '1:1 slope', '45 degrees', 'No limit'], correctAnswer: '1:12 maximum slope' },
      { text: 'The element that cuts one opening through many levels is a…', options: ['Shaft', 'Vertical Opening', 'Wall Opening', 'Floor Edge'], correctAnswer: 'Shaft' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 20 — Schedules, Sheets & Coordination
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 20,
    title: 'Schedules, Sheets & Coordination',
    description: 'Deliver the BIM package — material takeoffs and room schedules, view and sheet drafting setup, coordination through links and clash/interference checks, and the final delivery and certification path.',
    topics: [
      {
        title: 'Material Takeoffs & Room Schedules',
        text: 'The model is not just geometry — it is a database. **Material takeoffs** and **room schedules** mine that database for the quantities and the programmes the building needs. They are the BIM deliverable that a drawing never was.\n\n## MATERIAL TAKEOFFS\n\nA **Material Takeoff** schedules materials rather than elements: every wall, floor and roof that uses "Concrete Block" contributes its volume/area. Create it with **View > Schedules > Material Takeoff**, pick the category (Walls), and choose the **Material** field. The schedule totals area/volume per material — the concrete cubic metres, the plaster square metres. These numbers feed the estimator directly.\n\n## ROOM SCHEDULES\n\nA **Room Schedule** lists every room — its name, number, area, level, department. Rooms are placed with the **Room** tool (the room tag), and the schedule reads them. The architect\'s area statement (GFA, net area, circulation) is a room schedule with a **calculated value** (net area = room area × occupancy factor). Room data drives the brief: how many square metres of what use.\n\n## CALCULATED VALUES AND KEY SCHEDULES\n\n**Calculated values** compute new fields from existing ones (area × height = volume; wall area × U-value = heat loss). **Schedule Keys** let one row define a standard room (a "standard office" key fills size and fit-out for every row that uses it). Calculated and keyed fields are where the schedule becomes analysis, not just a list.\n\n## THE DATABASE MINDSET\n\nThe takeoff and the room schedule prove the BIM promise: the model is a database with geometry attached. Change the model and the takeoff updates; the quantities the contractor orders and the areas the client pays for both read from the same model.\n\n## The Workflow\n\nPlace rooms and tag them, build the material takeoff and the room schedule, add calculated values for area and volume, and drop both on sheets. The database speaks.',
        code: '# Revit API (C#): total concrete volume from a material takeoff\nDocument doc = ...;\nMaterial mat = new FilteredElementCollector(doc)\n    .OfClass(typeof(Material))\n    .Cast<Material>()\n    .First(m => m.Name.Contains("Concrete"));\n// sum material volume across all walls\ndouble total = 0;\nforeach (Element e in new FilteredElementCollector(doc)\n    .OfClass(typeof(Wall)))\n{\n    Wall w = e as Wall;\n    if (w != null)\n    {\n        Material m = w.GetMaterialArea(true, true) > 0 ? mat : mat;\n        total += w.get_Volume();\n    }\n}\nTaskDialog.Show("Takeoff", "Concrete volume ~ " +\n    UnitUtils.ConvertFromInternalUnits(total, DisplayUnitType.DUT_CUBIC_METERS) + " m3");',
        note: 'A room schedule is the area statement — name, number, area, level — and a material takeoff is the estimate feed; both read the model, so the numbers stay current.'
      },
      {
        title: 'Views, Sheets & Drafting Setup',
        text: 'Before the package can be delivered, the **views** must be composed and the **sheets** laid out. Drafting setup is the quiet work — view templates, crop regions, scale and line weights — that makes 40 views read as one coherent set.\n\n## VIEW TEMPLATES\n\nA **View Template** is a saved set of view properties — scale, detail level, visual style, category overrides (structural hidden vs shown), and the crop. Apply the template to a view and it inherits the settings; update the template and every view using it updates. Plans, sections and details each get their template, so the whole set shares one look.\n\n## CROP REGIONS AND SCALE\n\n**Crop Region** (the green frame) trims a view to the area you want — a plan cropped to one wing, a section to one bay. The **View Scale** sets how the view fits the sheet. **Hide/Isolate** and **Categories** control what the view shows. A plan with a tidy crop and a clear scale reads instantly; a plan full of the whole model reads as noise.\n\n## SHEET LAYOUT\n\nSheets are where views land. The **Sheet List** index, the **title block**, the **viewports** placed at their scales — drafting setup is arranging these into a navigable package. Place the primary view, surround it with sections and details, and check the viewport scales match the title block. The sheet layout IS the deliverable.\n\n## LINE WEIGHTS AND PATTERNS\n\nLine weight and pattern discipline (thick cut, thin hidden, dashed grid) is enforced by the template. **Detail Level** (Coarse/Medium/Fine) controls the amount of detail a view shows — coarse for key plans, fine for details. Consistency across views is what makes the set professional.\n\n## The Workflow\n\nApply view templates to every view, set crops and scales, lay out sheets with aligned viewports, and let the template enforce the line weights. Drafting setup is invisible when done well.',
        code: '# Revit API (C#): apply a view template to a plan\nDocument doc = ...;\nViewPlan plan = new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewPlan)).Cast<ViewPlan>()\n    .First(v => v.Name == "Ground Floor Plan");\n// find the view template\nViewTemplate v = new FilteredElementCollector(doc)\n    .OfClass(typeof(View))\n    .Cast<View>()\n    .First(x => x.IsTemplate && x.Name == "Plan - Structural");\n// apply it — scale, detail level, categories, overrides\nplan.ViewTemplateId = v.Id;\n// set the crop region to a rectangle\nplan.CropBoxActive = true;\nplan.CropBox = new BoundingBoxXYZ { Min = new XYZ(-5,-5,0), Max = new XYZ(25,20,10) };',
        note: 'View templates keep 40 views reading as one set — update the template and every plan, section and detail that uses it inherits the change.'
      },
      {
        title: 'Coordination: Links, Clash & Interference',
        text: 'A building is not designed in one file. **Architectural, structural and MEP** models are separate files **linked** together, and the big question is whether they fit. Coordination is linking the models, finding the clashes, and resolving them before the site does.\n\n## LINKS\n\nA **Link** brings another Revit model (or a DWG, IFC, Navisworks file) into the current model. Link the structural model into the architectural model — the columns and beams appear in the arch plans, positioned by shared coordinates. Links can be **bound** (their elements usable in this model) or left as references. Links update when the source changes, keeping the coordination live.\n\n## COORDINATION AND INTERFERENCE\n\n**Interference Check** (Collaborate > Interference Check) tests two linked models for overlaps: does the structural column clash with the arch wall? Does the duct run through the beam? The check reports each clash with a pair of elements and a location. Running interference between arch and structural (and MEP) catches the collisions the drawings never showed.\n\n## CLASH TYPES\n\nClashes are **hard** (two solids occupy the same space — a duct through a beam) or **soft** (two elements too close to each other — a pipe 10 mm from a column, impossible to install or insulate). The interference check reports hard overlaps; soft clearance is checked with the **Clearance** settings.\n\n## RESOLVING CLASHES\n\nA clash is resolved by moving one element, re-routing the duct, or raising the beam — a design decision the whole team must agree. The check is re-run until the clash list is clean. A coordinated model delivered clash-free is the BIM endgame — the site finds nothing to fight about.\n\n## The Workflow\n\nLink the structural (and MEP) models with shared coordinates, run the interference check against the arch model, resolve the clashes, and re-check until clean. Coordination is where BIM earns its keep.',
        code: '# Revit API (C#): run an interference check between arch and linked structural\nDocument doc = ...;\n// gather the arch model elements and the linked structural model\nFilteredElementCollector arch = new FilteredElementCollector(doc);\nIList<Element> linkElems = new List<Element>();\nforeach (RevitLinkInstance link in new FilteredElementCollector(doc)\n    .OfClass(typeof(RevitLinkInstance)))\n{\n    Document linkDoc = link.GetLinkDocument();\n    if (linkDoc != null)\n        linkElems.AddRange(new FilteredElementCollector(linkDoc)\n            .OfClass(typeof(FamilyInstance)));\n}\n// built-in interference check (interactive in UI)\nIList<Element> elements1 = arch.ToElements();\nIList<Element> elements2 = linkElems;\nFailureMessage msg = new FailureMessage(FailureMessage.GetIds()[0]);\nTaskDialog.Show("Clash", elements1.Count + " arch vs " + elements2.Count + " structural elements checked.");',
        note: 'A hard clash is two solids in the same space — duct through a beam; a soft clash is elements too close to install. The interference check catches both before the site does.'
      },
      {
        title: 'Final BIM Delivery & Certification',
        text: 'The project ends at **delivery** — the coordinated, documented model handed over as a complete package, and the career step of **certification** that proves you can do it. This is the professional close of the 20-week BIM journey.\n\n## THE DELIVERY PACKAGE\n\nDelivery is more than the drawings: it is the **model** (with its links and sheets), the **PDF set**, the **schedules and takeoffs**, and the **coordination report**. A clean package is checked — sheets up to date, revisions recorded, clash report clean, schedules current. The transmittal lists every file and its revision.\n\n## THE REVISION AND ISSUE SYSTEM\n\nProfessional delivery uses **revisions**: every change is a new **revision** (Rev. A, Rev. B) recorded in the **Revision Table** on the sheets. A drawing set with a revision table tells the reader exactly what changed and when. Issuing a set without recording the revision is how drawings go wrong on site.\n\n## THE PORTFOLIO\n\nYour work across the 20 weeks is a **portfolio**: the civil site plan, the 3ds Max render, the SketchUp model and Layout sheet, the Revit structural and architectural models. A portfolio page per project — the plan, the model view, the schedule — shows the full BIM workflow. Employers hire the portfolio.\n\n## CERTIFICATION PATHS\n\nThe professional credentials for this stack: **Autodesk Certified Professional** (Revit Architecture, Revit Structure), **Autodesk Certified User**, the **SketchUp certification**, and BIM-specific courses (Autodesk Construction Cloud). For India, **NSDC/SSDC**-aligned CADDED certifications and the Autodesk Authorised Training Centre path. Certification is the credential that makes the portfolio employable.\n\n## The Workflow\n\nCheck and issue the package with revisions, compile the portfolio, and take the certification exams. The deliverable is the coordinated, documented model — and the certified skill to build the next one.',
        code: '# Revit API (C#): list every sheet in the delivery set with its revision\nDocument doc = ...;\n// the Sheet List schedule already enumerates sheets; here we read it\nforeach (ViewSheet s in new FilteredElementCollector(doc)\n    .OfClass(typeof(ViewSheet)))\n{\n    string num = s.SheetNumber;\n    // latest revision per sheet\n    string rev = s.GetAllRevisionIds().Count > 0\n        ? doc.GetElement(s.GetAllRevisionIds()[0]).Name : "none";\n    TaskDialog.Show("Delivery", num + " rev " + rev);\n}',
        note: 'Every change is a new revision recorded in the revision table — a set issued without its revision recorded is how drawings go wrong on site.'
      },
    ],
    quizzes: [
      { text: 'A schedule that totals material quantities (concrete volume, plaster area) from the model is a…', options: ['Material Takeoff', 'Room Schedule', 'Sheet List', 'Viewport Schedule'], correctAnswer: 'Material Takeoff' },
      { text: 'A Room Schedule lists each room with its name, number, area and…', options: ['Level', 'Door size', 'Colour', 'Family'], correctAnswer: 'Level' },
      { text: 'A saved set of view properties (scale, detail level, overrides) is a…', options: ['View Template', 'Crop Region', 'Title block', 'Schedule key'], correctAnswer: 'View Template' },
      { text: 'The green frame that trims a view to the area you want is the…', options: ['Crop Region', 'Viewport', 'Grid', 'Scope box'], correctAnswer: 'Crop Region' },
      { text: 'Another Revit model brought into the current model with shared coordinates is a…', options: ['Link', 'Import', 'Group', 'Insert'], correctAnswer: 'Link' },
      { text: 'A hard clash is…', options: ['Two solids occupying the same space', 'Two elements too close to install', 'A missing annotation', 'A wrong scale'], correctAnswer: 'Two solids occupying the same space' },
      { text: 'Every change in a professional set is recorded in the…', options: ['Revision table', 'Title block', 'Sheet list', 'Room schedule'], correctAnswer: 'Revision table' },
      { text: 'The professional credential for Revit Architecture/Structure skills is…', options: ['Autodesk Certified Professional', 'CSWP', 'GDTP', 'NIMS'], correctAnswer: 'Autodesk Certified Professional' },
    ],
  },
];

export const caddCivilFinalExam: CaddCivilFinalExamQuestion[] = [
  { text: 'A municipal plan-check rejects a site plan because the contours are drawn solid and bold. The professional fix is…', options: ['Put existing contours on C-TOPO-MAJR-EX as dashed/light and proposed solid', 'Make all lines heavier', 'Remove the contours', 'Draw contours only in the legend'], correctAnswer: 'Put existing contours on C-TOPO-MAJR-EX as dashed/light and proposed solid' },
  { text: 'A survey delivers points as Northing, Easting but the drafter types them X, Y. The result is…', options: ['The site rotated/mirrored 90 degrees', 'The site drawn at half scale', 'Nothing — order does not matter', 'The site perfectly correct'], correctAnswer: 'The site rotated/mirrored 90 degrees' },
  { text: 'The workflow that guarantees the two halves of a symmetric floor plan are bit-identical is…', options: ['Draw one half, then MIRROR it about the centre line', 'Redraw the second half by eye', 'Copy and rotate the first half', 'Offset the outline'], correctAnswer: 'Draw one half, then MIRROR it about the centre line' },
  { text: 'A setback line on a site plan is conventionally drawn…', options: ['Dashed, offset inward from the boundary by the ordinance distance', 'Solid and bold on layer 0', 'As a contour', 'With a leader note only'], correctAnswer: 'Dashed, offset inward from the boundary by the ordinance distance' },
  { text: 'In 3ds Max, the 1.0 diffuse value that lets a map show through is best described as…', options: ['A multiplier between texture and material', 'The render resolution', 'The camera exposure', 'The light intensity'], correctAnswer: 'A multiplier between texture and material' },
  { text: 'A render comes out flat with no depth. The lighting fix is…', options: ['A three-point lighting setup', 'More render passes', 'A higher resolution', 'A longer camera lens'], correctAnswer: 'A three-point lighting setup' },
  { text: 'A curved building facade in SketchUp is modelled fastest with…', options: ['Follow Me along a path', 'Push/Pull on a rectangle', 'The Eraser tool', 'Styles only'], correctAnswer: 'Follow Me along a path' },
  { text: 'A door you reuse in every project should be saved as…', options: ['A component in the library', 'A group drawn fresh each time', 'A section cut', 'A texture'], correctAnswer: 'A component in the library' },
  { text: 'In Revit Structure, a continuous footing running under a wall is a…', options: ['Strip footing', 'Isolated footing', 'Pile cap', 'Grade beam'], correctAnswer: 'Strip footing' },
  { text: 'The cover on a concrete face protects the rebar from…', options: ['Corrosion and fire', 'The concrete weight', 'The formwork', 'Expansion joints'], correctAnswer: 'Corrosion and fire' },
  { text: 'A Revit structural schedule that can never disagree with the drawings is…', options: ['The one that reads the model', 'The one typed by hand', 'The one imported from Excel', 'The one drawn in detail'], correctAnswer: 'The one that reads the model' },
  { text: 'Beam bottom bars at a junction must develop their force inside the…', options: ['Column core', 'Slab top', 'Formwork', 'Reinforcement table'], correctAnswer: 'Column core' },
  { text: 'A viewport label that says 1:100 while the sheet title says 1:200 is…', options: ['A classic sheet error that should be caught before publish', 'Correct', 'Unimportant', 'Fixed by the printer'], correctAnswer: 'A classic sheet error that should be caught before publish' },
  { text: 'The Revit element used to model a glass building facade with mullions is a…', options: ['Curtain wall', 'Basic wall', 'Cavity wall', 'Partition'], correctAnswer: 'Curtain wall' },
  { text: 'A hip roof is created by…', options: ['Roof by Footprint with a slope on every boundary edge', 'Roof by Extrusion', 'Roof by Face', 'Sloped glazing'], correctAnswer: 'Roof by Footprint with a slope on every boundary edge' },
  { text: 'The accessibility rule that a wheelchair ramp must not exceed is…', options: ['1:12', '1:1', '45 degrees', 'No limit'], correctAnswer: '1:12' },
  { text: 'Two solids occupying the same space between linked models is a…', options: ['Hard clash', 'Soft clash', 'Schedule error', 'View template'], correctAnswer: 'Hard clash' },
  { text: 'The final BIM deliverable package includes all of the following EXCEPT…', options: ['A rendered animation of the schedule table', 'The model and PDF set', 'Schedules and material takeoffs', 'The coordination/clash report'], correctAnswer: 'A rendered animation of the schedule table' },
];
