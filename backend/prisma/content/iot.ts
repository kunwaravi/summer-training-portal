// ============================================================================
// IoT & Smart Interfacing Solutions — Deep GfG-Style Curriculum (issue #96)
// ----------------------------------------------------------------------------
// 20 sections, each with 4 deep teaching topics (~250-300 words, original),
// a working code example, a real-world note, and 8 distinct chapter quizzes
// (4 options, exactly 1 correct). Plus an 18-question final exam.
//
// Per-topic quizzes live in iot_topic_quizzes.ts (keyed by EXACT topic title)
// and satisfy the frontend topic-lock flow.
// ============================================================================

export interface IotTopic {
  title: string;
  text: string;
  code: string;
  note: string;
}

export interface IotQuiz {
  text: string;
  options: string[];
  correctAnswer: string;
}

export interface IotSection {
  week: number;
  title: string;
  description: string;
  topics: IotTopic[];
  quizzes: IotQuiz[];
}

export interface IotFinalExamQuestion {
  text: string;
  options: string[];
  correctAnswer: string;
}

export const iotSections: IotSection[] = [
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 1 — Introduction to IoT & Smart Systems
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 1,
    title: 'Introduction to IoT & Smart Systems',
    description:
      'What the Internet of Things really is, how smart systems are built, and where they show up in daily life.',
    topics: [
      {
        title: 'What the Internet of Things Really Means',
        text: 'The **Internet of Things (IoT)** is the network of physical objects embedded with electronics, software, and sensors that exchange data over the internet — without a human typing every command. The phrase was coined by Kevin Ashton in 1999 while working on RFID supply chains. What makes IoT different from a plain embedded device is **connectivity plus autonomy**: a thermostat that just reads temperature is embedded; one that uploads your habits to the cloud and learns when you are away is IoT.\n\nAn IoT system has four essential parts. First, **sensing** — a temperature sensor, accelerometer, camera, or GPS reads the physical world. Second, **processing** — a microcontroller (ESP32, Arduino) or microprocessor runs firmware that interprets raw readings. Third, **communication** — WiFi, Bluetooth, LoRa, or cellular pushes data to the internet. Fourth, **actuation/action** — a relay, motor, or screen changes something in the physical world in response.\n\nThe value of IoT is not the sensors — it is the **decisions** enabled by aggregated data. A single fridge thermometer is useless; ten thousand of them across a grocery chain reveal which cold chain is failing, when, and why. This is why modern IoT architectures are built "data-first": collect broadly, then derive insight.\n\nIoT spans **consumer** (smart bulbs, wearables), **commercial** (smart buildings, vending machines), and **industrial** (IIoT) domains. Each has different reliability, latency, and security needs — an industrial valve actuator tolerates no downtime, while a smart bulb can retry for minutes.',
        code: '// Concept: an IoT device is "sense -> process -> connect -> act"\nstruct IotDevice {\n  float read_sensor();       // sensing\n  void run_firmware();       // processing\n  int  connect_to_cloud();   // communication\n  void trigger_action();     // actuation\n};\n\n// In Arduino-style code the loop() embodies this cycle:\n//   read() -> think() -> send() -> maybe act()\n//   delay(...);  // then repeat forever',
        note: 'IoT = physical sensing + internet connectivity + data-driven action. The value lives in the aggregated data, not the single sensor.',
      },
      {
        title: 'Smart Systems: Perception to Action',
        text: 'A **smart system** is an IoT device or network that perceives its environment, decides, and acts with minimal human involvement. The journey from raw signal to real action has a clear pipeline, and every good IoT engineer can name each stage.\n\n**1. Perception:** sensors translate physical phenomena (temperature, pressure, light, motion) into electrical signals. A thermistor changes resistance with heat; an accelerometer produces tiny voltages when it moves.\n\n**2. Conditioning:** raw sensor signals are noisy and weak. An op-amp amplifies a 10mV signal, an ADC samples the analog voltage into a digital number, and filters remove 50Hz mains hum. Getting this stage right determines data quality.\n\n**3. Interpretation:** firmware converts numbers into meaning — a raw ADC count of 512 becomes "25.4 °C" through calibration curves and linearization.\n\n**4. Decision:** the device or the cloud applies logic: thresholds ("if temperature > 60 °C, sound alarm"), machine learning, or business rules.\n\n**5. Action:** an actuator closes a valve, a relay cuts power, a message pops on a dashboard, or a notification reaches a phone.\n\n**6. Learning:** aggregated history tunes the thresholds — the system gets smarter over time. This feedback loop is what separates "smart" from merely "connected".\n\nThe classic mental model is the **control loop**: sense, compare against a desired setpoint, adjust output, repeat. A smart thermostat does this at home; a PID controller does it in a factory robot. The loop runs faster or slower depending on the application — 1Hz for weather, 1kHz for motor control.',
        code: '// The smart-system pipeline in one function\nfloat perceive() { return read_temperature(); }\nfloat condition(float raw) { return raw * 0.125 - 12.3; } // calibration\nbool  decide(float c) { return c > 60.0f; }              // threshold\nvoid  act(bool alarm) { digitalWrite(ALARM_PIN, alarm); }\n\nvoid loop() {\n  float raw = perceive();\n  float c = condition(raw);\n  act(decide(c));\n  delay(1000);\n}',
        note: 'Smart = perceive → condition → interpret → decide → act → learn. The feedback loop is what makes a system "smart".',
      },
      {
        title: 'IoT vs Traditional Embedded Systems',
        text: 'People use "embedded" and "IoT" loosely, but the engineering difference is real and affects every design decision.\n\nA **traditional embedded system** is a dedicated computer hidden inside a product — an ECU in a car, the controller in a washing machine, a pacemaker. It is optimised for **determinism, cost, and reliability**. It may never touch a network, runs the same firmware for a decade, and is designed with a hard power, memory, and price budget. Its job is to do one thing perfectly, forever.\n\nAn **IoT device** adds three burdens traditional embedded systems were never designed for: **connectivity**, **updateability**, and **cloud integration**. The device must join a network, authenticate, send telemetry, receive commands, and — critically — be **secure against remote attacks** for its whole life. Nobody attacks a washing machine through a cable; everyone attacks a smart bulb over the internet.\n\nPractically, this changes the stack. Traditional embedded runs bare-metal or a small RTOS. IoT devices usually run on top of WiFi/Bluetooth stacks and TLS, so they need **more RAM, flash, and CPU** — an ESP32 with 520KB of RAM and WiFi on-chip is typical, while a traditional MCU might have 2KB of RAM and no radio at all.\n\nSecurity posture differs too. A traditional device trusts its physical environment; an IoT device must assume hostile input on every packet. The result: IoT firmware is larger, updated over the air (OTA), and written with TLS, signed images, and secure boot in mind — concerns a washing-machine programmer rarely thought about.',
        code: '// Traditional: tiny, deterministic, no network\n//   8-bit MCU, 2 KB RAM, bare-metal loop\nwhile (1) {\n  if (button_pressed()) relay_on();\n  else relay_off();\n}\n\n// IoT: connected, updateable, secure\n//   ESP32 + WiFi + TLS + OTA\nvoid loop() {\n  publish_telemetry();   // connect & send\n  if (ota_update_available()) apply_ota();\n  handle_cloud_command();\n}',
        note: 'IoT devices must be connected, remotely updateable, and remotely secure — a different engineering contract than classic bare-metal embedded.',
      },
      {
        title: 'Real-World IoT Use Cases',
        text: 'The best way to learn IoT is to see where it already runs. Each use case stresses different parts of the architecture, and knowing the spread helps you pick what to build first.\n\n**Smart Home:** smart bulbs, thermostats, door locks, cameras. Connectivity: WiFi/Bluetooth/Zigbee. Priorities: ease of use, low cost, cloud app. Latency can be seconds — nobody notices a 300ms light delay.\n\n**Wearables & Health:** fitness bands, continuous glucose monitors, ECG patches. Priorities: ultra-low power (battery for weeks), small size, data privacy (health data is regulated). Bluetooth Low Energy dominates here.\n\n**Agriculture:** soil moisture probes, drone crop scans, smart irrigation. Priorities: LoRa/cellular long range (a field has no WiFi), battery life measured in seasons, ruggedness against dust and heat.\n\n**Smart City:** street lights that dim when empty, parking sensors, air-quality monitors, smart bins that call for pickup when full. Priorities: scale (thousands of nodes), low maintenance, secure municipal networks.\n\n**Industrial IoT (IIoT):** predictive maintenance on motors (vibration sensors catch bearing wear), production-line analytics, cold-chain monitoring for vaccines. Priorities: reliability, low latency, safety. A factory line cannot tolerate random 2-second drops.\n\n**Logistics & Supply Chain:** GPS trackers on shipping containers, temperature logging for perishables, RFID inventory. Priorities: cellular/LoRa coverage worldwide, tamper evidence, cheap data plans.\n\nPick a use case and let it define your hardware: battery life dictates power budget, range dictates radio, data volume dictates sampling rate. A fitness band that samples GPS at 10Hz would die in an hour — the use case sets every constraint.',
        code: '// Choosing a radio by use case\n//   Smart home / wearables -> WiFi, BLE     (short range, high BW)\n//   Agriculture / city     -> LoRa, NB-IoT  (long range, low BW)\n//   Logistics / tracking   -> 4G/5G LTE-M   (global, mobile)\n//   Industrial            -> Ethernet/TSN  (wired, deterministic)\n\n// Rule of thumb:\n//   range x bandwidth x power x cost  =  one trade-off triangle',
        note: 'Your use case fixes the radio, power budget, latency budget, and security needs. Design backwards from the application.',
      },
    ],
    quizzes: [
      { text: 'Who coined the phrase "Internet of Things" and in what context?', options: ['Kevin Ashton in 1999 while working on RFID supply chains', 'Bill Gates in 1995 in The Road Ahead', 'Steve Jobs in 2007 at the iPhone launch', 'Vint Cerf in 1974 while designing TCP'], correctAnswer: 'Kevin Ashton in 1999 while working on RFID supply chains' },
      { text: 'Which is the correct order of an IoT device cycle?', options: ['Act -> sense -> connect -> process', 'Sense -> process -> connect -> act', 'Connect -> act -> sense -> process', 'Process -> act -> connect -> sense'], correctAnswer: 'Sense -> process -> connect -> act' },
      { text: 'Where does the real value of IoT usually lie?', options: ['In the individual sensor hardware', 'In the aggregated data and the decisions it enables', 'In the physical enclosure design', 'In the length of the cables'], correctAnswer: 'In the aggregated data and the decisions it enables' },
      { text: 'What makes an IoT device different from a classic embedded system?', options: ['It uses a CPU', 'It is connected, updateable, and remotely secure', 'It runs firmware', 'It has a power supply'], correctAnswer: 'It is connected, updateable, and remotely secure' },
      { text: 'A smart irrigation system in a remote field is best served by…', options: ['WiFi from the farmhouse', 'LoRa or cellular long-range radio', 'Bluetooth beacons', 'A USB cable to a laptop'], correctAnswer: 'LoRa or cellular long-range radio' },
      { text: 'In the smart-system pipeline, what does "conditioning" mean?', options: ['Decorating the device', 'Amplifying, filtering, and digitising raw sensor signals', 'Connecting to WiFi', 'Writing the mobile app'], correctAnswer: 'Amplifying, filtering, and digitising raw sensor signals' },
      { text: 'Why do IoT devices typically need more RAM and flash than classic embedded MCUs?', options: ['Because they run WiFi/Bluetooth stacks and TLS', 'Because the firmware is always bigger for no reason', 'Because users demand more LEDs', 'Because they must render graphics'], correctAnswer: 'Because they run WiFi/Bluetooth stacks and TLS' },
      { text: 'A fitness tracker that samples GPS at 10 Hz would most likely…', options: ['Be more accurate forever', 'Drain the battery within hours', 'Need a bigger screen', 'Become cloud-incompatible'], correctAnswer: 'Drain the battery within hours' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 2 — IoT Architecture & Reference Model
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 2,
    title: 'IoT Architecture & Reference Model',
    description:
      'The layered IoT stack, edge vs fog vs cloud, protocols at each layer, and how to design a clean architecture.',
    topics: [
      {
        title: 'The Four-Layer IoT Stack',
        text: 'IoT systems are too complex to design as one blob. Engineers decompose them into **layers**, each with a clear responsibility and clean interface to its neighbours. The classic model has four layers.\n\n**1. Perception Layer (device):** sensors and actuators attached to a microcontroller. This is the physical world interface — reading temperature, driving a relay, capturing a frame. Its concerns are signal conditioning, power, and local logic.\n\n**2. Network Layer (communication):** transports data from the device to wherever it is processed. This includes WiFi, Bluetooth, LoRa, cellular, and the internet protocols on top (TCP/IP, MQTT, HTTP). Its concerns are range, bandwidth, latency, and reliability.\n\n**3. Middleware / Processing Layer (cloud/edge):** ingests, stores, and processes the data. Message brokers, databases, analytics engines, and application logic live here. Its concerns are scale, throughput, and fault tolerance — millions of devices reporting at once must not collapse the pipeline.\n\n**4. Application Layer:** the user-facing experience — dashboards, mobile apps, alerts, and business rules. Its concern is turning processed data into usable decisions.\n\nTwo things make layered design powerful. First, **each layer can be swapped** — you can change the radio without rewriting the dashboard. Second, **each layer has its own security concerns** — physical tampering at the device, encryption on the wire, access control in the cloud, and privacy in the app.\n\nWhen you design a project, draw these four boxes first and assign every component to one. If a component straddles two layers, that is usually a sign the design is muddled. A clean drawing predicts a clean build.',
        code: '// Mapping a smart-lock project onto the four layers\n//\n//  Perception : fingerprint sensor + solenoid + ESP32\n//  Network    : WiFi -> TLS -> MQTT over TCP\n//  Processing : broker + auth service + event store\n//  Application: phone app + admin dashboard\n//\n// Design rule: each layer talks ONLY to its neighbours.',
        note: 'Four layers: Perception → Network → Processing → Application. Assign every component to exactly one layer.',
      },
      {
        title: 'Edge vs Fog vs Cloud Computing',
        text: 'Where should the "brain" of an IoT system live? The answer splits into **cloud, fog, and edge**, and modern architectures use all three for different jobs.\n\n**Cloud** is centralised data centres — AWS, Azure, GCP. It offers nearly unlimited compute and storage, but data must travel the whole internet to reach it. Round-trip latency is tens to hundreds of milliseconds, and it needs connectivity. The cloud is ideal for long-term storage, heavy analytics, machine learning training, and business dashboards.\n\n**Edge** computing runs on or very near the device itself — right on the microcontroller, or on a gateway in the same room. Latency drops to microseconds-to-milliseconds, and it works even when the internet is down. Edge is where time-critical and privacy-sensitive logic belongs: a medical alarm that waits for the cloud would be unacceptable.\n\n**Fog** sits between the two — local servers or gateways on the network edge that pre-process data before it reaches the cloud. A factory floor gateway might aggregate 500 sensor readings, filter noise, compute hourly statistics, and forward only the summaries. This slashes bandwidth cost and lets a site keep running during an outage.\n\nThe rule of thumb: **time-critical → edge, bandwidth-heavy → fog, long-term/big-data → cloud.** A self-driving car makes braking decisions at the edge; a fleet-analytics dashboard lives in the cloud. As you build, ask of each computation: "how urgent is this, and how much data does it produce?" Then place it accordingly.\n\nA well-designed system degrades gracefully — edge keeps the essentials running even if the cloud vanishes.',
        code: '// Where does each job run?\n//   Braking / valve safety   -> EDGE  (microseconds, must survive offline)\n//   Telemetry aggregation    -> FOG   (gateway, reduce bandwidth)\n//   Trend analytics / ML     -> CLOUD (big data, heavy compute)\n\n// Fog gateway sketch:\n//   ingest 500 sensor msgs -> filter noise -> hourly summary -> send summary',
        note: 'Cloud = big data & heavy compute. Fog = local pre-processing. Edge = time-critical, offline-tolerant logic.',
      },
      {
        title: 'IoT Protocols by Layer',
        text: 'IoT speaks many protocols, and beginners drown in the alphabet soup (MQTT, CoAP, AMQP, HTTP, WebSocket, LoRaWAN, Zigbee…). The trick is to sort them by **layer**: transport protocols carry bytes, application protocols structure the conversation, and radio protocols move bits over the air.\n\n**Radio / PHY protocols** decide how far and fast data travels wirelessly: WiFi (fast, ~50m indoor), Bluetooth/BLE (short, low power), Zigbee (mesh, low power), LoRaWAN (kilometres, very low bandwidth), LTE-M/NB-IoT (cellular, global). Pick the radio by range × power × bandwidth.\n\n**Transport protocols** (TCP/UDP) sit inside the radio and deliver the data reliably (TCP) or at low overhead (UDP). MQTT rides on TCP; CoAP rides on UDP.\n\n**Application protocols** structure the payload: MQTT (publish/subscribe messaging), CoAP (lightweight request/response like HTTP for constrained devices), HTTP/REST (universal web), AMQP (enterprise messaging with routing). MQTT dominates IoT because it is lightweight, supports push (no polling), and brokers fan out to many subscribers.\n\nA concrete stack for an ESP32 project: **ESP32 → WiFi (radio) → TCP (transport) → MQTT (application) → broker → dashboard.** Each layer is replaceable — swap WiFi for LoRa and the MQTT application layer barely changes. That interchangeability is why layered thinking pays off.\n\nWhen choosing, ask: power budget? (BLE/LoRa for battery, WiFi for mains), range? (LoRa/cellular for wide area), real-time push? (MQTT/WebSocket), corporate integration? (AMQP). Almost every IoT project lands on MQTT over TCP for the app layer.',
        code: '// A typical ESP32 network stack, bottom to top\n//\n//   Application : MQTT  (pub/sub, QoS, retain)\n//   Transport   : TCP   (reliable byte stream)\n//   Network     : IP    (routing)\n//   Link        : WiFi / Ethernet\n//   Physical    : 2.4 GHz radio / cable\n//\n// Swap the bottom two layers for LoRa/cellular and\n// the MQTT application code stays the same.',
        note: 'Separate radio (PHY), transport (TCP/UDP), and application (MQTT/CoAP/HTTP). MQTT over TCP is the IoT default.',
      },
      {
        title: 'Designing a Simple IoT Architecture',
        text: 'Let us design a working architecture end-to-end — a small smart warehouse temperature monitor — so the theory becomes concrete. The goal: 30 ESP32 nodes report temperature, a web dashboard shows live values, and an alarm fires when any rack exceeds 28 °C.\n\n**Devices:** each ESP32 reads a DHT22 sensor, filters the reading, and publishes to MQTT topic `warehouse/rack/07/temp` every 30 seconds. Firmware: connect WiFi, connect MQTT, loop publish. That is it — devices stay dumb and cheap.\n\n**Connectivity:** WiFi to a local access point, then TLS-secured MQTT over TCP to a broker. On site, a Raspberry Pi runs an MQTT broker (Mosquitto) and a Node-RED/InfluxDB/Grafana stack. This is **fog** — processing stays local, latency is instant, and a cloud outage does not stop the alarm.\n\n**Processing:** the broker fans each message to three subscribers: a time-series database (InfluxDB) for history, a rule engine that fires the alarm when >28 °C, and an MQTT->WebSocket bridge for live UI updates. If the site wants remote access later, a small cloud instance mirrors a filtered summary.\n\n**Application:** Grafana dashboard showing rack temperatures in real time, plus a phone alert via the rule engine.\n\n**Security:** each node authenticates to the broker with unique credentials; TLS encrypts the wire; the dashboard sits behind a login. Alarms log to an audit file.\n\nThe whole design is four layers, each swappable: change the sensor, change the radio, change the dashboard — the plumbing stays. That is the payoff of drawing the architecture before writing code.',
        code: '// Architecture sketch for a 30-node warehouse monitor\n//\n//  [ESP32 + DHT22] x30 --WiFi/TLS--> [MQTT broker on Pi]\n//                                      |---> InfluxDB (history)\n//                                      |---> Rule engine (>28C alarm)\n//                                      |---> WebSocket -> Grafana UI\n//\n// Fog keeps it local; an optional cloud mirror adds remote access.',
        note: 'Design backwards from the use case: pick the layer per job, keep devices dumb, keep processing local (fog) when latency matters.',
      },
    ],
    quizzes: [
      { text: 'Which layer of the IoT stack contains sensors and actuators?', options: ['Network layer', 'Perception layer', 'Application layer', 'Middleware layer'], correctAnswer: 'Perception layer' },
      { text: 'A factory gateway that aggregates 500 sensor readings into hourly summaries is an example of…', options: ['Cloud computing', 'Fog computing', 'Quantum computing', 'Edge-only computing'], correctAnswer: 'Fog computing' },
      { text: 'Where should time-critical logic such as a medical alarm run?', options: ['In the cloud', 'At the edge, near the device', 'On a remote analytics server', 'In a spreadsheet'], correctAnswer: 'At the edge, near the device' },
      { text: 'Which application protocol dominates IoT because it is lightweight and supports push?', options: ['FTP', 'MQTT', 'SMTP', 'POP3'], correctAnswer: 'MQTT' },
      { text: 'MQTT rides on which transport protocol?', options: ['UDP', 'TCP', 'ICMP', 'ARP'], correctAnswer: 'TCP' },
      { text: 'A soil-moisture monitor spanning kilometres of farmland needs…', options: ['WiFi only', 'Bluetooth beacons', 'LoRa or cellular', 'A USB hub'], correctAnswer: 'LoRa or cellular' },
      { text: 'Why is the four-layer model useful when designing IoT systems?', options: ['Each layer can be swapped independently', 'It reduces the number of devices', 'It makes firmware run faster', 'It removes the need for security'], correctAnswer: 'Each layer can be swapped independently' },
      { text: 'What is the recommended placement for long-term analytics and machine learning in an IoT design?', options: ['Edge microcontrollers', 'Fog gateways only', 'The cloud', 'Nowhere — analytics are not needed'], correctAnswer: 'The cloud' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 3 — Sensors & Transducers
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 3,
    title: 'Sensors & Transducers',
    description:
      'How sensors convert physics into signals, digital vs analog readings, and the skill of choosing and wiring the right sensor.',
    topics: [
      {
        title: 'Sensors: From Physics to Electrical Signal',
        text: 'A **transducer** converts one form of energy into another. A **sensor** is a transducer that converts a physical quantity — temperature, pressure, light, force — into an **electrical signal** the microcontroller can read. Understanding the physics behind a sensor is what lets you debug it when readings go wrong.\n\nEvery sensor family works on a physical principle:\n\n- **Temperature:** a thermistor changes resistance with heat (NTC = negative temperature coefficient); a thermocouple produces a tiny voltage from two dissimilar metals; an LM35 outputs 10mV per °C linearly.\n- **Light:** a photodiode generates current from photons; an LDR changes resistance with light; a camera sensor counts photons per pixel.\n- **Motion:** an accelerometer measures proper acceleration via microscopic proof-masses on springs (MEMS); a gyroscope measures rotation via the Coriolis effect.\n- **Distance:** an ultrasonic sensor times a sound pulse reflection (HC-SR04); an IR distance sensor measures reflected light angle.\n- **Pressure/Force:** a strain gauge changes resistance when stretched; a load cell converts that into a measurable bridge voltage.\n\nKey specs to read before buying: **range** (minimum and maximum you can measure), **sensitivity** (output change per unit input), **accuracy vs precision** (how close to truth, how repeatable), **response time**, and **power consumption**.\n\nSensors output either **analog** (continuous voltage — needs an ADC) or **digital** (I2C/SPI/UART/1-Wire — reads a number directly). Modern digital sensors like the DHT22, BME280, and MPU6050 embed their own ADC and calibration, so firmware just reads a register. That is easier to use and far more accurate than raw analog — when a digital option exists, prefer it.',
        code: '// Reading a digital sensor (BME280 temp/humidity/pressure) over I2C\n#include <Wire.h>\n#include <Adafruit_Sensor.h>\n#include <Adafruit_BME280.h>\nAdafruit_BME280 bme;\n\nvoid setup() {\n  Serial.begin(115200);\n  bme.begin(0x76);                // I2C address of the sensor\n}\nvoid loop() {\n  Serial.printf("T=%.1fC H=%.1f%% P=%.1fhPa\\n",\n    bme.readTemperature(), bme.readHumidity(), bme.readPressure() / 100.0F);\n  delay(2000);\n}',
        note: 'A sensor turns physics into volts (analog) or registers (digital). Prefer digital sensors — they embed their own ADC and calibration.',
      },
      {
        title: 'Digital Sensors & Communication Interfaces',
        text: 'Digital sensors talk to the microcontroller over a **serial bus** and hand over calibrated numbers in registers. They carry their own ADC, temperature-compensation lookup tables, and sometimes filtering — the firmware reads a value rather than interpreting a voltage. This is why digital sensors are the modern default.\n\nThe three common buses, at a glance:\n\n- **I2C** — two wires (SCL clock, SDA data), each device addressed (7-bit address like 0x76), supports many devices on one bus. Moderate speed (100-400 kHz). Perfect for many low-speed sensors.\n- **SPI** — four wires (SCLK, MOSI, MISO, CS), one chip-select line per device, very fast (MHz), full-duplex. Ideal for high-rate sensors and displays.\n- **UART** — two wires (TX, RX), point-to-point, async, simple. Used by GPS modules, fingerprint scanners, and serial peripherals.\n- **1-Wire** — a single data line with parasitic powering; used by DS18B20 temperature probes that can be daisy-chained.\n\nReading a digital sensor is a standard dance: initialise the bus, verify the device responds (scan for its address), configure measurement registers, wait for the conversion to complete, read the data registers, and apply scaling.\n\nMost vendors publish **register maps** and **example libraries** (Adafruit, SparkFun, Seeed). Use them as the contract: the datasheet tells you what each register means, and the library hides the byte wrangling. But do not treat the library as a black box forever — read the datasheet too, because real-world problems (wrong address, sensor sleeping, mis-scaled units) are solved in the register map.\n\nBus debugging tip: if a sensor never responds, check wiring, then check for address conflicts (two devices at 0x76), then try slowing the clock. I2C address conflicts are the single most common "dead sensor" cause.',
        code: '// I2C scanner: find every device address on the bus\n#include <Wire.h>\nvoid setup() {\n  Serial.begin(115200); Wire.begin();\n  Serial.println("Scanning I2C bus...");\n  for (byte addr = 1; addr < 127; addr++) {\n    Wire.beginTransmission(addr);\n    if (Wire.endTransmission() == 0) {\n      Serial.printf("Device found at 0x%02X\\n", addr);\n    }\n  }\n}\nvoid loop() {}',
        note: 'Digital sensors hand over calibrated values over I2C/SPI/UART/1-Wire. When a device is silent, scan the bus and check for address conflicts.',
      },
      {
        title: 'Analog Sensors, ADCs & Voltage Dividers',
        text: 'Some of the best sensors are still **analog** — they output a voltage proportional to the measurement, and the microcontroller must digitise it with an **ADC (Analog-to-Digital Converter)**. Understanding the analog path is essential because the wiring decides the accuracy.\n\nAn ADC converts a continuous voltage into a binary number. The ESP32 ADC reads 0-3.3V and returns a 12-bit value (0-4095). The mapping is: `voltage = raw * 3.3 / 4095`. Your job is to get the sensor\'s output voltage into that 0-3.3V window.\n\nTwo common analog sensors:\n\n- **LDR (light-dependent resistor)** — resistance changes from ~1kΩ (bright) to ~1MΩ (dark). It cannot be read directly; you build a **voltage divider** with a fixed resistor and read the midpoint voltage.\n- **Potentiometer / joystick** — a voltage divider built in, output sweeps 0-Vcc with position.\n\n**Voltage divider math:** with sensor resistance R_s and fixed resistor R_f between Vcc and GND, the midpoint is `Vout = Vcc * R_f / (R_s + R_f)`. Choose R_f near the sensor\'s mid-range resistance so the output sweeps the most of the ADC window.\n\n**Danger point:** many analog sensors output more than 3.3V (a 5V LDR divider), and feeding >3.3V into a 3.3V ESP32 pin can damage it. Use a **voltage divider to scale down** or a level shifter, and always check the absolute maximum ratings in the datasheet.\n\nAlso remember: ADC readings are noisy. Take several samples and average them, or use a moving median, before trusting a value. A single raw ADC sample is a starting point, never a measurement.',
        code: '// Reading an analog sensor (e.g. LDR divider) on ESP32\n#define LDR_PIN 34        // ADC1 channel, analog pin\nvoid setup() { Serial.begin(115200); }\nvoid loop() {\n  int raw = analogRead(LDR_PIN);       // 0..4095\n  float v = raw * (3.3f / 4095.0f);    // voltage at the pin\n  // average several samples to kill noise\n  long sum = 0; for (int i = 0; i < 10; i++) sum += analogRead(LDR_PIN);\n  int avg = sum / 10;\n  Serial.printf("raw=%d avg=%d V=%.2f\\n", raw, avg, v);\n  delay(500);\n}',
        note: 'ADC turns 0-3.3V into a 0-4095 number. Use a voltage divider to fit the signal into the ADC window and never exceed 3.3V on the pin.',
      },
      {
        title: 'Choosing, Wiring & Calibrating Sensors',
        text: 'Great firmware cannot fix a badly chosen or badly wired sensor. Sensor selection and installation are 80% of measurement success. Here is the workflow engineers actually use.\n\n**1. Define the measurement.** Range (will it ever hit 80 °C?), accuracy (do we need ±0.1 °C or is ±2 °C fine?), response time, and update rate. A cheap DHT22 is great for room temperature and hopeless for an incubator.\n\n**2. Read the datasheet BEFORE buying.** Check supply voltage (3.3V vs 5V), output type (analog/I2C/SPI), accuracy spec, and wiring diagram. The "it says 5V on Amazon" surprise is the #1 cause of burnt sensors.\n\n**3. Wire it right.** Ground all grounds, never exceed the pin\'s max voltage, add a **pull-up** where the datasheet demands (I2C lines need 4.7kΩ pull-ups), and decouple the power rail with a 100nF capacitor near the sensor.\n\n**4. Verify the raw read.** Print the raw ADC count or register value FIRST, before any conversion math. If the raw value is sane, the conversion will be; if raw is garbage, no formula will save you.\n\n**5. Calibrate against a reference.** Compare against a trusted thermometer/multimeter at two points and derive a linear correction: `corrected = raw * gain + offset`. The ESP32 ADC is famously non-linear at the extremes — a two-point calibration fixes most drift.\n\n**6. Condition and filter in software.** Median-of-3 or exponential moving average kills spikes without adding lag. Reject absurd values (a 5 °C jump in one second on a slow thermal sensor is a glitch).\n\n**7. Document the calibration** — in the firmware comments and in a config file. The engineer who inherits your project needs to know exactly what each reading means.',
        code: '// Two-point calibration + spike rejection\nfloat corrected(float raw) {\n  // calibrated at 20.0C -> raw 19.4C and 50.0C -> raw 49.2C\n  float gain = (50.0 - 20.0) / (49.2 - 19.4);\n  float offset = 20.0 - gain * 19.4;\n  return raw * gain + offset;\n}\nfloat stable_read(float* prev) {\n  float r = corrected(read_sensor());\n  if (fabs(r - *prev) > 5.0f) return *prev; // reject glitch jump\n  *prev = 0.9f * (*prev) + 0.1f * r;        // exponential smoothing\n  return *prev;\n}',
        note: 'Read the datasheet, verify the raw value before converting, calibrate against a reference, and filter spikes in software.',
      },
    ],
    quizzes: [
      { text: 'What does a sensor fundamentally do?', options: ['It stores data', 'It converts a physical quantity into an electrical signal', 'It transmits radio waves', 'It powers the microcontroller'], correctAnswer: 'It converts a physical quantity into an electrical signal' },
      { text: 'Which property of an NTC thermistor changes with temperature?', options: ['Capacitance', 'Resistance', 'Inductance', 'Frequency'], correctAnswer: 'Resistance' },
      { text: 'An ESP32 ADC is 12-bit. What is its maximum raw reading?', options: ['255', '1023', '4095', '65535'], correctAnswer: '4095' },
      { text: 'How many wires does I2C need?', options: ['One', 'Two (SCL + SDA)', 'Four (SCLK, MOSI, MISO, CS)', 'Eight'], correctAnswer: 'Two (SCL + SDA)' },
      { text: 'Why do digital sensors usually beat raw analog ones for accuracy?', options: ['They are always cheaper', 'They embed their own ADC and calibration', 'They never fail', 'They need no power'], correctAnswer: 'They embed their own ADC and calibration' },
      { text: 'What is the most common cause of a "dead" I2C sensor?', options: ['Too much RAM', 'Address conflict on the bus', 'The code is too short', 'Using digital pins'], correctAnswer: 'Address conflict on the bus' },
      { text: 'Feeding a 5V analog sensor output into a 3.3V ESP32 pin will likely…', options: ['Work perfectly', 'Damage the pin', 'Improve accuracy', 'Change the sensor address'], correctAnswer: 'Damage the pin' },
      { text: 'What is the correct FIRST step when a sensor reads garbage?', options: ['Replace the sensor', 'Check the raw ADC/register value before any conversion math', 'Rewrite the whole firmware', 'Update the dashboard'], correctAnswer: 'Check the raw ADC/register value before any conversion math' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 4 — Signal Conditioning, ADC & DAC
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 4,
    title: 'Signal Conditioning, ADC & DAC',
    description:
      'Amplifying weak signals, digitising with ADC, generating analog output with PWM/DAC, and filtering noise.',
    topics: [
      {
        title: 'Signal Conditioning: Amplify, Level-Shift, Filter',
        text: 'Raw sensor signals are rarely ready for an ADC. **Signal conditioning** is the art of shaping a raw signal until it fits the ADC window cleanly. Three operations cover most cases: **amplify**, **level-shift**, and **filter**.\n\n**Amplify.** A thermocouple outputs millivolts; a load cell\'s bridge signal is a few mV. To digitise them you need gain. An **op-amp** in non-inverting configuration gives `Vout = Vin * (1 + Rf/Rg)` — pick the resistors for the gain you need. Dedicated instrumentation amplifiers (INA125, HX711 for load cells) package this with precision.\n\n**Level-shift.** An ADC wants 0-3.3V. If your signal swings -2 to +2V (audio, some accelerometers), you must offset it into the positive window — an op-amp summer adds a DC bias so the negative swing fits. This "biasing" is why you see `Vout = Vin + Vref/2` circuits in sensor boards.\n\n**Filter.** Power lines inject 50/60Hz hum; motors inject spikes. A simple **RC low-pass filter** (a resistor and capacitor) rolls off high frequencies: cutoff `f_c = 1 / (2πRC)`. For mains hum you pick R and C so f_c ≈ 10Hz. Digital filtering in firmware (moving average, median, low-pass) is often simpler and adjustable — but you cannot filter away a signal your ADC never saw, so a little analog filtering first helps.\n\n**The conditioning checklist:** Is the amplitude inside the ADC window? Is the DC bias right? Is there noise to remove? Is the impedance low enough to drive the ADC? Answer all four before wiring.\n\nRule of thumb: condition in analog (small, cheap, fast) and refine in digital (flexible, adjustable). Over-conditioning in analog is rigid; over-filtering in digital costs CPU. Balance the two.',
        code: '// Software low-pass filter (exponential moving average)\nfloat lowpass(float in, float* state, float alpha) {\n  *state = alpha * in + (1.0f - alpha) * (*state);\n  return *state;\n}\n// alpha small (0.1) = heavy smoothing, slow response\n// alpha large (0.8) = light smoothing, fast response',
        note: 'Condition in analog (amplify, level-shift, filter to fit the ADC), then refine in digital. Get the signal into the ADC window cleanly first.',
      },
      {
        title: 'ADC in Depth: Sampling, Resolution & Accuracy',
        text: 'The **ADC** is where the analog world becomes digital, and its specs decide measurement quality. The four specs that matter: **resolution**, **sampling rate**, **reference voltage**, and **accuracy**.\n\n**Resolution** is the number of bits: 10-bit → 1024 steps, 12-bit → 4096 steps, 16-bit → 65536 steps. The **step size** (LSB) is `Vref / 2^bits`. At 3.3V reference, a 12-bit ADC resolves about 0.8mV per step. More bits = finer granularity, but also more noise sensitivity.\n\n**Sampling rate** is how many conversions per second. The ESP32 ADC tops out around tens of kSPS — plenty for temperature (1Hz) but too slow for audio. Nyquist says you must sample at twice the signal\'s highest frequency to reconstruct it — sample audio at least 8kHz, not 1kHz.\n\n**Reference voltage (Vref)** sets the full scale. If Vref is noisy or drifts, every measurement drifts. The ESP32 ADC reference is not factory-calibrated, which is why the same voltage reads differently across boards — one reason calibrate-in-firmware is non-negotiable for real projects.\n\n**Accuracy pitfalls specific to ESP32:** the ADC is non-linear near 0 and Vref; the attenuation setting matters (`analogReadResolution`, `analogSetAttenuation(ADC_11db)` for 0-3.3V range); and WiFi transmission introduces noise onto readings taken concurrently. A common fix is to avoid sampling while the radio is transmitting, or to average many samples.\n\n**Best practice sequence:** (1) set attenuation and resolution explicitly, (2) take N samples, (3) drop outliers or take the median, (4) apply a two-point calibration, (5) average the rest. This routine turns a mediocre ADC into a usable measurement.',
        code: '// Reading the ESP32 ADC properly\nanalogReadResolution(12);                    // 0..4095\nanalogSetPinAttenuation(34, ADC_11db);       // full 0-3.3V range\n\nfloat read_stable(int pin) {\n  long sum = 0; int n = 16;\n  for (int i = 0; i < n; i++) { sum += analogRead(pin); delayMicroseconds(50); }\n  float raw = sum / (float)n;\n  return raw * (3.3f / 4095.0f);\n}',
        note: 'ADC quality = resolution × sampling rate × clean Vref. Set attenuation, average samples, and calibrate — especially on ESP32.',
      },
      {
        title: 'Analog Output: PWM, DAC & Servo Control',
        text: 'Actuators often need an **analog-style output**, and microcontrollers rarely have a true analog output. The two workhorses are **PWM** (pulse-width modulation) and, where available, a real **DAC**.\n\n**PWM** fakes an analog voltage by switching a pin between 0 and 3.3V very fast and varying the **duty cycle** (fraction of time HIGH). An LED sees the average brightness; a motor sees the average voltage; a servo reads the pulse width directly. ESP32 PWM channels let you set frequency and duty: `ledcSetup(channel, freq, resolution)`, `ledcAttachPin(pin, channel)`, `ledcWrite(channel, duty)`.\n\nThe duty-fraction → "voltage" math: `V_avg = Vcc * duty/2^res`. At 8-bit resolution, duty 128 → ~1.65V average. This is exactly how you dim an LED or set a fan speed.\n\n**DAC** is a true analog output. The ESP32 has two DAC channels (pins 25/26) that output a real 0-3.3V via `dacWrite(pin, value)` with 8-bit resolution. DACs are used for audio output, arbitrary waveforms, or driving an amplifier. They are rarer on MCUs than PWM, so treat them as a feature, not a given.\n\n**Servo control** is a special PWM application: hobby servos expect a 20ms frame with a 1-2ms high pulse. 1.0ms → 0°, 1.5ms → 90°, 2.0ms → 180°. The Servo library hides this: `servo.write(angle)`.\n\n**Current limits — the silent killer:** GPIO pins sink/source only ~20-40mA. You cannot drive a motor or relay directly from a pin. Use a **transistor/MOSFET**, an **H-bridge** (L298N, DRV8833) for bidirectional motors, or a **relay module** for mains loads. The pin only drives the switch\'s control input.',
        code: '// ESP32 PWM + servo\n#include <Servo.h>\nServo s;\nvoid setup() {\n  ledcSetup(0, 5000, 8);      // channel 0, 5kHz, 8-bit\n  ledcAttachPin(2, 0);        // LED on GPIO2\n  s.attach(13);               // servo on GPIO13\n}\nvoid loop() {\n  for (int d = 0; d <= 255; d++) { ledcWrite(0, d); delay(5); } // fade up\n  s.write(90); delay(1000); s.write(0); delay(1000);\n}',
        note: 'PWM fakes analog output via duty cycle; DAC gives true analog. GPIO pins can only signal — use transistors/H-bridges/relays for real loads.',
      },
      {
        title: 'Noise, Grounding & Robust Readings',
        text: 'Real projects are full of electrical noise, and a good measurement survives it. Noise enters a signal three ways: **conducted** (through wires), **radiated** (from motors, radios, mains), and **from the ADC itself** (reference drift, switching transients). Here is how to fight each.\n\n**Grounding discipline.** Star-ground everything: every sensor, every load, every supply returns to one point. Avoid "ground loops" where two grounds differ by even millivolts — they inject exactly that difference into your measurements. Keep the power ground and the signal ground separated as much as possible on the board.\n\n**Decoupling.** Place a 100nF ceramic capacitor right at each IC\'s power pin and a 10-100µF electrolytic near the main supply. These absorb the current spikes from switching that would otherwise corrupt adjacent ADC readings. On a breadboard this is the single highest-value habit.\n\n**Twisted/shielded wiring.** For long sensor leads, twist signal + ground together, or use shielded cable with the shield grounded at one end. This rejects magnetic pickup from nearby motor wires.\n\n**Software filtering.** Average multiple samples (reduces random noise by √N), use a median filter (kills single spikes), or a low-pass exponential filter (smooths without a big buffer). Reject impossible values outright.\n\n**Isolate the measurement time.** On ESP32, ADC readings taken during WiFi transmission are measurably noisier. Sample just after a transmit window or stop WiFi briefly during a critical measurement, then resume.\n\n**The golden rule:** fix the source, not the symptom. If a value is noisy, find and suppress the noise source (motor → add a flyback diode; PSU → add decoupling) before adding ever-stronger software filters. Software should be the last line of defence, not the only one.',
        code: '// Robust software read: median-of-3 then average\nfloat robust_analog(int pin) {\n  int a[5];\n  for (int i = 0; i < 5; i++) a[i] = analogRead(pin);\n  // simple bubble sort to find the median\n  for (int i = 0; i < 4; i++)\n    for (int j = i + 1; j < 5; j++)\n      if (a[i] > a[j]) { int t = a[i]; a[i] = a[j]; a[j] = t; }\n  return (a[1] + a[2] + a[3]) / 3.0f;  // drop min & max, average the rest\n}',
        note: 'Kill noise at the source (grounding, decoupling, shielding) and use software filters only as the final polish.',
      },
    ],
    quizzes: [
      { text: 'What is the formula for gain in a non-inverting op-amp?', options: ['Vout = Vin * (Rf/Rg)', 'Vout = Vin * (1 + Rf/Rg)', 'Vout = Vin / (1 + Rf)', 'Vout = Vin - Rf'], correctAnswer: 'Vout = Vin * (1 + Rf/Rg)' },
      { text: 'An RC low-pass filter\'s cutoff frequency is…', options: ['f = R*C', 'f = 1/(2πRC)', 'f = 2πRC', 'f = R/C'], correctAnswer: 'f = 1/(2πRC)' },
      { text: 'At 3.3V reference, what voltage does each step of a 12-bit ADC represent?', options: ['3.3V', 'About 0.8 mV', '1 mV', '10 mV'], correctAnswer: 'About 0.8 mV' },
      { text: 'The Nyquist rule says you must sample at least…', options: ['Twice the signal\'s highest frequency', 'Once per second', '100 times the signal\'s frequency', 'The signal\'s amplitude'], correctAnswer: 'Twice the signal\'s highest frequency' },
      { text: 'What does PWM vary to fake an analog voltage?', options: ['Frequency only', 'Duty cycle', 'Voltage level', 'Pin count'], correctAnswer: 'Duty cycle' },
      { text: 'Why can you NOT drive a motor directly from a GPIO pin?', options: ['The pin is too fast', 'The pin can only sink/source ~20-40 mA', 'The pin only outputs 1.8V', 'The pin is analog'], correctAnswer: 'The pin can only sink/source ~20-40 mA' },
      { text: 'What is the single highest-value breadboard habit for clean readings?', options: ['Using longer wires', 'A 100nF decoupling capacitor at each IC power pin', 'Running the ADC faster', 'Removing all capacitors'], correctAnswer: 'A 100nF decoupling capacitor at each IC power pin' },
      { text: 'When ESP32 ADC readings are noisier than expected, a likely culprit is…', options: ['WiFi transmission noise', 'Too much flash', 'A wrong I2C address', 'The servo\'s color'], correctAnswer: 'WiFi transmission noise' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 5 — Actuators & Output Control
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 5,
    title: 'Actuators & Output Control',
    description:
      'Turning digital decisions into physical action — motors, relays, solenoids, and safe high-power switching.',
    topics: [
      {
        title: 'Actuator Types & Principles',
        text: 'An **actuator** is the muscle of an IoT device — it turns an electrical signal into physical motion or action. Understanding the major families lets you pick the right one and know what its driver circuit needs.\n\n**DC motors** spin continuously. Speed follows voltage (or PWM duty), direction follows polarity (swap the leads, or drive through an H-bridge). They are the workhorse for fans, wheels, and pumps. Brushless motors (BLDC) are more efficient but need an electronic speed controller (ESC).\n\n**Servos** rotate to a commanded angle (0-180° typical) and hold it. They contain a small DC motor, gearbox, and feedback potentiometer. Control is a 50Hz PWM signal where pulse width 1-2ms sets the angle. Perfect for steering, robotic arms, and camera gimbals.\n\n**Stepper motors** move in exact discrete steps (e.g., 200 steps/revolution). They are open-loop position control — no feedback needed — driven by a stepper driver (A4988, DRV8825, ULN2003). Ideal for 3D printers, CNC, and precise positioning.\n\n**Solenoids** are linear electromagnetic plungers — energise to push or pull, release to spring back. Used for locks, valves, and push buttons.\n\n**Relays** are electromechanical switches that let a low-voltage signal control a completely separate high-voltage/current circuit. The coil is driven by a transistor (or relay module with one built in); the contacts switch the load. Relays handle mains (AC) safely but click audibly and switch slowly (ms).\n\n**Solid-state relays (SSR)** do the same with semiconductors — silent, fast, no moving parts, but need a heatsink for current.\n\nThe unifying rule: **the microcontroller never drives the load directly.** It always drives a switch — transistor, MOSFET, H-bridge, or relay module — which drives the load. Every actuator family has a driver stage that protects the MCU from the load\'s voltage, current, and inductive kickback.',
        code: '// Matching actuator to driver\n//   DC motor  -> MOSFET/transistor (one direction) or H-bridge (both)\n//   Servo     -> directly from a PWM-capable GPIO (low current)\n//   Stepper   -> driver board (A4988/ULN2003)\n//   Solenoid  -> MOSFET + flyback diode\n//   Relay     -> relay module driven by a transistor\n\n// None of these are "wire the actuator straight to the pin".',
        note: 'An actuator converts electrical signals into motion. The MCU always drives a switch/driver, never the load directly.',
      },
      {
        title: 'Driving DC Motors & H-Bridges',
        text: 'A **DC motor** is a classic inductive load — a coil of wire that draws a current surge when energised and produces a **voltage spike (flyback)** when switched off. Both demand a proper driver.\n\nFor **one-direction** control you need a single transistor or MOSFET. An N-channel MOSFET (e.g. IRFZ44N, or logic-level like IRLZ44N for 3.3V gate drive) switches the motor\'s ground side. Always place a **flyback diode** across the motor (cathode to +V, anode to ground) so the inductive spike has somewhere to go instead of the transistor.\n\nFor **bidirectional** control you need an **H-bridge** — four switches arranged in an H so the motor can be energised either polarity. You can build one from discrete transistors, but it is far saner to use an IC: **L298N** (classic, wasteful, drops 2V), **DRV8833** (nice for small motors, logic-level), or **TB6612** (efficient, common in robotics kits). The driver IC also handles the flyback internally, and exposes an ENABLE/PWM pin for speed control.\n\n**The H-bridge truth table:** A=HIGH, B=LOW → motor forward; A=LOW, B=HIGH → reverse; A=B → brake (both on) or coast (both off). Enable/PWM pin → speed via duty cycle.\n\n**Danger notes:**\n- Never drive both bridge inputs HIGH together — that shorts the supply (shoot-through).\n- Keep motor supply separate from logic supply on L298N; they share a common ground.\n- Use the **same ground** for MCU and driver, or the logic signal has no return path and behaves randomly.\n\n**Power budget:** a motor\'s stall current can be 5-10× its running current. Size the driver, wires, and supply for stall, not just running current.',
        code: '// TB6612 / DRV8833 H-bridge control from ESP32\n#define AIN1 25\n#define AIN2 26\n#define PWMA 27\nvoid setup() {\n  pinMode(AIN1, OUTPUT); pinMode(AIN2, OUTPUT);\n  ledcSetup(0, 1000, 8); ledcAttachPin(PWMA, 0);\n}\nvoid motor(int speed) {            // -255..+255\n  digitalWrite(AIN1, speed > 0);\n  digitalWrite(AIN2, speed <= 0);\n  ledcWrite(0, abs(speed));\n}',
        note: 'DC motors are inductive loads: use a MOSFET/H-bridge plus flyback diode. Never short both bridge inputs together.',
      },
      {
        title: 'Relays & Switching AC/Mains Loads',
        text: 'The moment an IoT project wants to switch a **mains** device — a lamp, a heater, a pump — you need a relay or solid-state relay. This is also the moment safety stops being optional.\n\nA **relay module** (the blue ones with a screw terminal and an optocoupler) is the beginner-friendly path: the module contains a transistor that drives the relay coil, so you only connect 3 pins — VCC, GND, and an IN signal (HIGH to activate, usually with a jumper for active-low).\n\nWiring a relay module:\n- VCC/GND from the MCU\'s 5V (most modules need 5V coil).\n- IN to a GPIO.\n- Load: **COM** (common) and **NO** (normally-open) terminals in series with the AC device. When the coil energises, the contacts close and the load switches on.\n\n**Mains safety rules (non-negotiable):**\n- Never work on live mains — switch the breaker off, verify with a tester.\n- Keep mains wiring on the **screw-terminal side** and low-voltage control on the GPIO side; never let the two touch.\n- Use a relay **rated above** your load: a 10A relay for a lamp, not a 5A one. Check whether the rating is for AC or DC — they differ.\n- For inductive AC loads (motors, fans), derate the relay further and consider a snubber.\n- Keep mains cables short and properly insulated; use strain relief.\n\n**Why not just use a transistor for mains?** Because the load is isolated from the control electronics — the relay\'s whole point is that the coil circuit and the contact circuit are electrically separate. That isolation protects you and your 3.3V electronics from the mains side.\n\nFor frequent switching (e.g. dimming, thousands of cycles), prefer an **SSR or triac** (no contacts to wear out). But for simple on/off, an electromechanical relay is cheap, simple, and reliable.',
        code: '// Switching a mains lamp with a relay module\n#define RELAY 14\nvoid setup() {\n  pinMode(RELAY, OUTPUT);\n  digitalWrite(RELAY, LOW);     // start OFF\n}\nvoid loop() {\n  if (digitalRead(BUTTON_PIN)) digitalWrite(RELAY, HIGH); // lamp on\n  else digitalWrite(RELAY, LOW);\n}',
        note: 'Relays isolate low-voltage control from high-voltage loads. Respect mains safety: rate the relay above the load and never mix the two sides.',
      },
      {
        title: 'PWM Motor Speed & Servo Position Control',
        text: 'Two of the most common actuator control tasks are **variable motor speed** and **servo positioning**. Both use PWM, but with different details.\n\n**Motor speed via PWM:** feeding a PWM signal to the driver\'s enable/speed pin sets the motor\'s average voltage. At 50% duty the motor runs at ~50% speed. Keep the PWM frequency high enough that the motor doesn\'t whine or cog: 1-20kHz is typical (ledcSetup at 1000-5000Hz on ESP32). The relationship between duty and speed is *not* perfectly linear — friction at low duty may stall a motor below ~20%. If you need accurate speed, add an encoder and a closed loop; for most projects, open-loop PWM is fine.\n\n**Servo via pulse width:** hobby servos expect a 50Hz (20ms) frame and a pulse of 1.0-2.0ms. The Servo library handles the timing — `servo.write(angle)` maps 0-180° to 1-2ms. For gimbals or continuous rotation servos, the pulse mapping differs, so read the servo\'s datasheet.\n\n**Beware power.** Several servos or a motor under load can draw more than a USB port provides. Brown-outs cause random resets — a classic "my project works until I add the second motor" failure. Power servos/motors from their own supply (5-6V for servos, separate motor battery) and share a common ground with the MCU.\n\n**Software ramp (soft start).** Jamming full speed instantly causes current spikes and mechanical shock. Ramp the duty up over 300-500ms: `for (d = 0; d < target; d++) { motor(d); delay(10); }`. Same idea slows servos to avoid slamming the mechanical stops.\n\n**Closed-loop upgrade path:** add an encoder (Hall or optical) and compare target vs actual speed, adjusting duty — a simple proportional controller. That is the bridge from "good enough" to "industrial".',
        code: '// Soft-start motor ramp (avoid inrush shock)\nvoid ramp_to(int target) {\n  int d = 0;\n  int step = target > 0 ? 3 : -3;\n  while (abs(d) < abs(target)) {\n    d += step;\n    motor(d);\n    delay(10);\n  }\n}\n// Then:  ramp_to(200);   delay(2000);  ramp_to(0);',
        note: 'Set motor speed via PWM duty (1-20kHz), servo position via 1-2ms pulse. Give loads their own supply and ramp speed to avoid brown-outs.',
      },
    ],
    quizzes: [
      { text: 'Which actuator moves in exact discrete steps without feedback?', options: ['DC motor', 'Stepper motor', 'Servo', 'Solenoid'], correctAnswer: 'Stepper motor' },
      { text: 'What does a flyback diode across a motor protect?', options: ['The motor\'s bearings', 'The switching transistor from the inductive spike', 'The PWM frequency', 'The wiring color'], correctAnswer: 'The switching transistor from the inductive spike' },
      { text: 'What happens if both H-bridge inputs are driven HIGH together?', options: ['The motor runs twice as fast', 'It shorts the supply (shoot-through)', 'The motor brakes gently', 'Nothing'], correctAnswer: 'It shorts the supply (shoot-through)' },
      { text: 'What is the primary reason to use a relay rather than a transistor for a mains load?', options: ['Relays are cheaper', 'Electrical isolation between control and load', 'Relays switch faster', 'Transistors cannot switch DC'], correctAnswer: 'Electrical isolation between control and load' },
      { text: 'A hobby servo\'s angle is set by…', options: ['The voltage level', 'The pulse width (1-2ms) at 50Hz', 'The number of steps', 'The current drawn'], correctAnswer: 'The pulse width (1-2ms) at 50Hz' },
      { text: 'Brown-out resets after adding a motor usually mean…', options: ['The code has a bug', 'The power supply cannot handle the current draw', 'The WiFi is weak', 'The motor is too big for the chassis'], correctAnswer: 'The power supply cannot handle the current draw' },
      { text: 'Why ramp motor duty up gradually instead of full speed instantly?', options: ['To reduce inrush current and mechanical shock', 'To save flash memory', 'To make the motor quieter forever', 'To comply with I2C rules'], correctAnswer: 'To reduce inrush current and mechanical shock' },
      { text: 'The relay\'s COM and NO terminals connect…', options: ['The MCU and the sensor', 'The load in series so the coil switches it on/off', 'Two GPIO pins', 'The ADC and ground'], correctAnswer: 'The load in series so the coil switches it on/off' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 6 — Microcontrollers & Development Boards
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 6,
    title: 'Microcontrollers & Development Boards',
    description:
      'MCU vs MPU, the Arduino/ESP8266/ESP32 board landscape, GPIO architecture, and choosing the right chip.',
    topics: [
      {
        title: 'Microcontroller vs Microprocessor',
        text: 'Every IoT device has a brain, and the two candidates are different creatures. A **microcontroller (MCU)** is a complete computer on one chip — CPU, RAM, flash, timers, ADCs, and I/O — designed to run one program forever with minimal power and cost. A **microprocessor (MPU)** is just the CPU; it expects external RAM, storage, and peripheral chips, and runs a full operating system.\n\nThe numbers tell the story. A classic 8-bit MCU (ATmega328) has 2KB of RAM and 32KB of flash — it boots in milliseconds and idles at a few milliamps. An ESP32 has 520KB of RAM, 4MB of flash, WiFi/BT on-chip, and dual cores. A Raspberry Pi (MPU) has gigabytes of RAM, runs Linux, and idles at hundreds of milliamps with a heatsink.\n\n**When to use which:**\n- **MCU** — when you need low power, low cost, determinism (hard real-time), and direct hardware control. Sensors, motors, radio nodes, wearables.\n- **MPU/Linux board** — when you need heavy compute (video, ML, a web framework), lots of storage, or complex user interfaces. Gateways, dashboards, AI cameras.\n\n**The dividing question:** does this device do one well-defined job (MCU) or does it run general-purpose software (MPU)? A smart doorbell\'s camera does ML → needs an MPU; its lock actuator is driven by a small MCU inside.\n\nThe modern sweet spot for IoT is a **WiFi/BT-enabled MCU** like the ESP32: MCU-level power and cost with enough grunt to run TLS and MQTT. That combination — MCU economics with connectivity — is exactly what made the ESP32 the default IoT prototyping chip.',
        code: '// Rough comparison table\n//               ATmega328    ESP32        Raspberry Pi 4\n// CPU            8-bit        2x Xtensa   4x Cortex-A72\n// RAM            2 KB         520 KB       4 GB\n// Flash          32 KB        4 MB (ext)   SD card\n// WiFi/BT        no           yes          yes\n// OS             bare/RTOS    bare/RTOS    Linux\n// Power          ~5 mA        ~100 mA      ~600 mA\n// Role           sensor node  IoT node     gateway/edge',
        note: 'MCU = one chip, one job, low power, deterministic. MPU = full OS, heavy compute. WiFi MCUs like ESP32 sit in between for IoT.',
      },
      {
        title: 'The Board Landscape: Arduino, ESP8266, ESP32',
        text: 'The development-board world revolves around three families, and each has a clear job. Knowing the differences prevents buying the wrong board for a project.\n\n**Arduino Uno/Nano (ATmega328):** the classic learning board. 16MHz, 2KB RAM, 5V logic, no radio. Perfect for first projects, huge ecosystem, thousands of tutorials. But it has no WiFi, so "IoT" means adding a separate module — which is clunky. It remains the best teacher.\n\n**ESP8266 (NodeMCU/Wemos D1):** the WiFi breakout champion. A cheap (~$3) chip with 802.11 b/g/n WiFi on board, 80-160MHz, 80KB RAM, 3.3V logic. It runs the Arduino environment, so Arduino skills transfer directly. Ideal for WiFi sensor nodes and simple HTTP/MQTT clients. Limitations: only one ADC pin, no Bluetooth, modest RAM for TLS.\n\n**ESP32 (DevKit/NodeMCU-32S):** the IoT workhorse. Dual-core 240MHz, 520KB RAM, 4MB flash, WiFi + Bluetooth (classic and BLE), two ADC channels with 18 usable ADC pins across both, DAC, touch sensors, timers, RTC. It runs Arduino, ESP-IDF (official SDK), MicroPython, and even FreeRTOS out of the box. If a project needs TLS, BLE, or several sensors, ESP32 is the default.\n\n**ESP32-S3 / C3:** newer variants — the S3 adds more RAM and AI acceleration; the C3 is a single-core RISC-V budget option with WiFi/BLE.\n\n**Decision heuristic:** learning/simple → Arduino Uno. Cheap WiFi sensor → ESP8266. Anything real with TLS, BLE, or multiple peripherals → ESP32. Heavy compute → add a Raspberry Pi or an MPU.\n\nThe practical upside: all three run the Arduino IDE/code, so skills and even much of the code port across with minimal changes. Buy an ESP32 DevKit as your daily driver and an Uno for pedagogy.',
        code: '// Porting Arduino code to ESP32 usually changes only the setup\nvoid setup() {\n  Serial.begin(115200);            // baud differs from Uno\'s 9600\n  pinMode(2, OUTPUT);              // LED_BUILTIN = 2 on many ESP32\n}\nvoid loop() {\n  digitalWrite(2, HIGH); delay(500); digitalWrite(2, LOW); delay(500);\n}',
        note: 'Arduino teaches, ESP8266 adds cheap WiFi, ESP32 is the full IoT workhorse (WiFi + BLE + dual core). Default to ESP32 for real projects.',
      },
      {
        title: 'GPIO Architecture: Pins, Pull-ups & Level Shifting',
        text: 'GPIO (General-Purpose Input/Output) pins are the microcontroller\'s hands. Understanding their electrical behaviour is what separates working circuits from smoke. The rules below apply to 3.3V boards like the ESP32 and ESP8266.\n\n**Pin modes.** A GPIO can be an input (read HIGH/LOW), an output (drive HIGH/LOW), or — on many pins — an analog input (ADC) or a PWM/timer output. Set the mode before use: `pinMode(pin, INPUT/OUTPUT/INPUT_PULLUP)`.\n\n**Pull-ups & floating pins.** An unconnected (floating) input reads random values from electrical noise. Add a **pull-up** (resistor to VCC) or **pull-down** (resistor to GND) so the pin has a defined idle state. Many MCUs have internal pull-ups: `pinMode(pin, INPUT_PULLUP)` — then a button wired to GND reads LOW when pressed. This is why `pinMode(BTN, INPUT_PULLUP)` appears in every button tutorial.\n\n**Open-drain vs push-pull.** Some pins (and I2C data lines) are **open-drain** — they can only pull low, relying on an external pull-up for the high state. This allows multiple devices to share a line safely. Know which pins on your board are open-drain.\n\n**Level shifting.** ESP32/ESP8266 are **3.3V logic**; many sensors and shields are 5V. Feeding 5V into a 3.3V pin can damage it. Convert between levels with a **level shifter** (bi-directional module) or a simple voltage divider on unidirectional lines. Never "just try it" — the datasheet\'s absolute maximum input is the law.\n\n**Current limits.** Each GPIO sources/sinks ~20-40mA max. The total across all pins is capped too (often ~200mA). Always drive loads through a transistor/relay as covered in the actuators section.\n\n**ADC pins are special.** On ESP32, only specific pins (ADC1: 32-39) are safe to use as ADCs; ADC2 pins share hardware with WiFi and produce noisy readings while WiFi is active.',
        code: '// GPIO checklist\npinMode(2, OUTPUT);                    // drive an LED\npinMode(13, INPUT_PULLUP);            // button to GND, reads LOW when pressed\nif (digitalRead(13) == LOW) digitalWrite(2, HIGH);\n\n// 5V sensor on 3.3V board? Use a level shifter, never direct.',
        note: 'Set pin modes explicitly, pull-up/pull-down floating inputs, level-shift 5V→3.3V, respect per-pin and total current limits.',
      },
      {
        title: 'Choosing the Right Microcontroller for Your IoT Project',
        text: 'Choosing an MCU is a trade-off exercise — no single chip wins everything. Work through these dimensions in order and the decision almost makes itself.\n\n**1. Connectivity.** Does the project need WiFi, Bluetooth, LoRa, or cellular? If WiFi/BLE, an ESP32 covers both. For battery-lifetime field nodes, LoRa (via a radio module) or an nRF52 (BLE) beats WiFi on power.\n\n**2. Power budget.** Battery-operated? Check sleep current. The ESP32 deep-sleep draws ~10µA — excellent. Always-on wall-powered? Power barely matters and you can afford a Raspberry Pi or a beefier chip.\n\n**3. RAM & flash.** TLS + MQTT + a few libraries can eat 40-100KB of RAM — the ESP8266\'s ~50KB usable RAM gets tight, the ESP32\'s 520KB is comfortable. If you want camera frames or ML, that\'s MPU territory.\n\n**4. Peripherals.** How many ADC pins, I2C buses, PWM channels, UARTs do you need? Count your sensors first. Two I2C buses and six analog sensors → a bigger chip.\n\n**5. Toolchain comfort.** Arduino environment for beginners; ESP-IDF or PlatformIO for professional firmware; MicroPython for fast prototyping. The toolchain you can actually ship in is worth real weight.\n\n**6. Price & availability.** For volume production, $1-3 chips (ESP32-C3, ATtiny) matter. For one-off prototypes, price is irrelevant.\n\n**7. Support & ecosystem.** An obscure chip with no libraries will eat your life. Community size, datasheet quality, and prebuilt driver libraries are decisive — which is why the ESP32\'s ecosystem makes it the safe default.\n\n**The pragmatic default:** if in doubt and the project is WiFi/BLE-based with a handful of sensors, buy an ESP32 DevKit and start. Re-evaluate only when power, cost, or scale demand it.',
        code: '// Selection mini-calculator (fill in, pick the winner)\n//   need =  WiFi+BLE, 3 ADC, 1 I2C, 1 UART, sleep mode, 520KB RAM\n//   ESP32      -> all met                       (WIN)\n//   ESP8266    -> no BLE, 1 ADC only            (NO)\n//   nRF52      -> BLE yes, WiFi no              (NO)\n//   Raspberry  -> overkill power, MPU            (NO for this job)',
        note: 'Choose by connectivity → power → RAM → peripherals → toolchain → price. When unsure, start with an ESP32.',
      },
    ],
    quizzes: [
      { text: 'What is the key difference between a microcontroller and a microprocessor?', options: ['MCUs are faster', 'An MCU is a complete computer on one chip; an MPU needs external RAM/storage', 'MPUs use less power', 'MCUs cannot do math'], correctAnswer: 'An MCU is a complete computer on one chip; an MPU needs external RAM/storage' },
      { text: 'Which chip has WiFi AND Bluetooth built in?', options: ['ATmega328', 'ESP8266', 'ESP32', 'ATtiny85'], correctAnswer: 'ESP32' },
      { text: 'Why does the ESP8266 struggle with TLS + MQTT + many libraries?', options: ['It is too fast', 'It has limited usable RAM (~50 KB)', 'It lacks a power pin', 'It cannot do analog reads at all'], correctAnswer: 'It has limited usable RAM (~50 KB)' },
      { text: 'A floating GPIO input reads…', options: ['A stable 0V', 'Random values from electrical noise', 'Always HIGH', 'The supply voltage'], correctAnswer: 'Random values from electrical noise' },
      { text: '`pinMode(13, INPUT_PULLUP)` with a button to GND reads LOW when…', options: ['The button is released', 'The button is pressed', 'The pin is disconnected', 'Never'], correctAnswer: 'The button is pressed' },
      { text: 'How should you interface a 5V sensor to a 3.3V ESP32?', options: ['Wire it directly', 'Use a level shifter or voltage divider', 'Add a resistor in parallel', 'Use a longer cable'], correctAnswer: 'Use a level shifter or voltage divider' },
      { text: 'Why are ADC2 pins noisy on ESP32?', options: ['They are too small', 'They share hardware with WiFi', 'They lack pull-ups', 'They are output-only'], correctAnswer: 'They share hardware with WiFi' },
      { text: 'A battery-powered field node that must last months should use…', options: ['A Raspberry Pi', 'An MCU with low sleep current (e.g. deep-sleep ESP32)', 'Always-on WiFi at 100% duty', 'A 4K display'], correctAnswer: 'An MCU with low sleep current (e.g. deep-sleep ESP32)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 7 — Arduino Programming Fundamentals
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 7,
    title: 'Arduino Programming Fundamentals',
    description:
      'The Arduino environment, sketch structure, digital and analog I/O, and disciplined coding habits.',
    topics: [
      {
        title: 'The Arduino Platform & Sketch Structure',
        text: 'Arduino is a **hardware platform plus a software framework** designed to make embedded programming approachable. You write a "sketch" — a C++ file with a standard shape — and the IDE compiles, uploads, and monitors it over USB.\n\nEvery sketch has exactly two mandatory functions:\n\n```cpp\nvoid setup() {\n  // runs ONCE when the board boots\n  // pin modes, serial, sensor init go here\n}\n\nvoid loop() {\n  // runs forever, as fast as possible\n}\n```\n\n`setup()` initialises the world — set pin directions, start `Serial`, initialise sensors. `loop()` is the endless working cycle: read, think, act, then the framework calls it again immediately. Because `loop()` spins as fast as the CPU allows, you add `delay()` calls to pace it — but blocking delays are a known trap for time-sensitive projects (covered next week).\n\nKey built-ins you will use constantly:\n- `pinMode(pin, MODE)` — set input/output.\n- `digitalWrite(pin, HIGH/LOW)` — drive a pin.\n- `digitalRead(pin)` — read a pin.\n- `analogRead(pin)` — read ADC (0-1023 on Uno, 0-4095 on ESP32).\n- `analogWrite(pin, duty)` — PWM output (on Uno; ESP32 uses `ledc*`).\n- `Serial.begin(baud)` / `Serial.print(...)` — text debug channel.\n\nThe IDE hides the boilerplate: `#include` and `main()` are injected for you, which is great for learning but means the file is not a standalone C++ program. For production, the same code runs under **PlatformIO** with explicit project structure — same syntax, more control.\n\n**Habit worth forming:** print the raw values first. `Serial.println(analogRead(pin))` before any maths. If the raw print is sane, your conversion will be; if it is garbage, no formula helps.',
        code: '// Minimal-but-complete Arduino sketch\nvoid setup() {\n  Serial.begin(115200);\n  pinMode(LED_BUILTIN, OUTPUT);\n  pinMode(2, INPUT_PULLUP);   // a button to GND\n}\nvoid loop() {\n  if (digitalRead(2) == LOW) digitalWrite(LED_BUILTIN, HIGH);\n  else digitalWrite(LED_BUILTIN, LOW);\n  Serial.println(analogRead(34));   // raw sensor value\n  delay(100);\n}',
        note: 'Every sketch = setup() once + loop() forever. Print raw values first, then convert. Use PlatformIO for production projects.',
      },
      {
        title: 'Digital I/O: Buttons, Debouncing & LEDs',
        text: 'Digital I/O — reading buttons and driving LEDs — is the first real skill, and it hides a classic pitfall: **bounce**.\n\n**Buttons.** Wire a button between a GPIO and GND, with the internal pull-up enabled: `pinMode(pin, INPUT_PULLUP)`. The pin reads HIGH normally and LOW when pressed. This avoids floating and needs no external resistor.\n\n**The bounce problem.** A mechanical button\'s contacts physically vibrate for 5-20ms when pressed, toggling the pin several times. Your code sees `LOW,HIGH,LOW,HIGH...` and may count one press as five. Fixes:\n\n- **Debounce in software:** sample, wait ~10-50ms, then sample again. Or track a timestamp: only accept a change if `millis() - lastChange > 50`.\n- The classic pattern: save `lastDebounceTime`, compare `reading != lastReading`, update state after the button is stable for `DEBOUNCE_DELAY` ms.\n\n**Rising/falling edge detection.** "Do something once when pressed" is an **edge** trigger, not a level. Detect the transition: remember `lastButtonState`; if current is LOW and previous was HIGH → the button *just* got pressed → fire once. Level checks (`if (digitalRead == LOW)`) fire every loop iteration while held.\n\n**LEDs & current.** An LED needs a current-limiting resistor (typically 220-330Ω) between the pin and the LED. Without it the LED draws far more than 20mA and both the pin and LED suffer. LED brightness is set with PWM `analogWrite`/`ledcWrite`.\n\n**The disciplined pattern** for any digital input: define pin constants at the top, use `INPUT_PULLUP`, debounce with timestamps, and react to edges not levels. It feels like ceremony, but it is the difference between a demo and a product.',
        code: '// Edge-triggered, debounced button\n#define BTN 2\nbool last = HIGH; unsigned long t = 0;\nvoid setup(){ pinMode(BTN, INPUT_PULLUP); Serial.begin(115200); }\nvoid loop(){\n  bool cur = digitalRead(BTN);\n  if (cur != last) { t = millis(); }        // state changed, start timer\n  if ((millis() - t) > 50) {                 // stable for 50ms\n    if (cur == LOW && last == HIGH) Serial.println("pressed once");\n    last = cur;\n  }\n}',
        note: 'Buttons bounce — debounce with a timestamp. React to edges, not levels, for "once per press". Always series-resistor your LEDs.',
      },
      {
        title: 'Analog Input & PWM Output Patterns',
        text: 'Reading analog sensors and producing dimmable output are the two analog skills you will use in nearly every project.\n\n**Analog input.** `analogRead(pin)` returns an ADC count. On the ESP32 you must first configure the channel: `analogReadResolution(12)` and `analogSetPinAttenuation(pin, ADC_11db)` for the full 0-3.3V range. The value maps to voltage via `V = count * (3.3 / 4095)`. Then apply your sensor\'s calibration curve (previous weeks).\n\n**A robust read pattern:** take N samples, average or median them, and convert once. Single raw reads are the #1 source of flaky readings.\n\n**PWM output.** Dim an LED or set a fan speed with pulse-width modulation. On ESP32, PWM is configured per channel: `ledcSetup(channel, freq, resolution)` then `ledcAttachPin(pin, channel)` then `ledcWrite(channel, duty)`. Resolution 8-bit → duty 0-255; 10-bit → 0-1023. Choose the frequency by load: 1-5kHz for LEDs and fans, up to 20kHz for motors (beyond audible whine).\n\n**The pot/joystick pattern** is the canonical combo: read a potentiometer (analog in) and map it to PWM (analog out) with `map(value, inMin, inMax, outMin, outMax)` — a brightness knob or motor speed dial in one sketch.\n\n**Mapping & constraining.** `map()` is linear and can return values outside the target range if input exceeds bounds — wrap it with `constrain()` to clamp. For smooth control, add an exponential moving average so the output doesn\'t jitter with ADC noise.\n\n**Real-world combo:** a light-dependent resistor (via voltage divider) + a lamp on PWM = automatic dimming street-lamp demo; a potentiometer + fan = manual speed control. Both are two small sketches away from IoT-ready.',
        code: '// Potentiometer -> LED brightness\nvoid setup(){\n  analogReadResolution(12);\n  analogSetPinAttenuation(34, ADC_11db);\n  ledcSetup(0, 5000, 8); ledcAttachPin(2, 0);\n}\nvoid loop(){\n  int raw = analogRead(34);            // 0..4095\n  int duty = map(raw, 0, 4095, 0, 255);\n  ledcWrite(0, constrain(duty, 0, 255));\n  delay(10);\n}',
        note: 'Configure ADC resolution+attenuation before reading; average samples; map+constrain into the PWM range. That is the whole analog pattern.',
      },
      {
        title: 'Serial Debugging & Structured Logging',
        text: 'The **Serial Monitor** is your window into the device. Debugging without it is blind coding; with it, you can watch every decision the firmware makes. Treat it as a professional tool, not a toy.\n\n**Setup.** `Serial.begin(115200)` in setup, and match the baud in the IDE. One baud mismatch = gibberish. On ESP32/ESP8266, remember many boards default to a different monitor baud (the ESP32 bootloader prints at 115200).\n\n**What to print.** The values that matter: raw sensor reads before conversion, converted values, key state changes ("button pressed", "WiFi connected", "MQTT published OK"), and error codes. Avoid printing entire buffers in hot loops — that slows timing-sensitive code drastically (Serial.print blocks until the byte is sent).\n\n**Structured logging pattern:** one-line records with a timestamp and a tag:\n\n```cpp\nSerial.printf("[%lu] SENSOR temp=%.1f raw=%d\\n\", millis(), t, raw);\n```\n\nThis format is greppable and parseable — you can log to a file and analyse later. Use distinct tags: `[BOOT]`, `[NET]`, `[MQTT]`, `[SENS]`, `[ERR]`.\n\n**Watch the log for the boot sequence.** ESP32 boot messages reveal crash causes: "Guru Meditation" dumps with backtraces and "rst:0x8 (SW_CPU_RESET)" — a stack trace + reset reason is the first clue for any crash. Decode it (online decoders exist) to find the offending function.\n\n**Beyond Serial:** for production, log to flash/SD or send logs to the cloud, and add **tagged log levels** (INFO/WARN/ERROR) that can be compiled out for release. Serial stays for development; structured remote logging carries you to field use.\n\n**The debugging loop that works:** change one thing → reproduce → read the log → change the next thing. Never change two variables between runs.',
        code: '// Structured logging with tags\n#define LOGTAG "[NET]"\nvoid loop(){\n  int rssi = WiFi.RSSI();\n  Serial.printf("%s rssi=%d dBm connected=%d\\n", LOGTAG, rssi, WiFi.isConnected());\n  delay(2000);\n}\n// Output: [NET] rssi=-54 dBm connected=1',
        note: 'Serial is your debug window: begin at the right baud, print tagged one-line records, read boot/crash dumps. Change one variable per test.',
      },
    ],
    quizzes: [
      { text: 'In an Arduino sketch, `setup()` runs…', options: ['Once at boot', 'Forever in a loop', 'Only when a button is pressed', 'Never'], correctAnswer: 'Once at boot' },
      { text: 'Why does a mechanical button "bounce"?', options: ['It is broken', 'Its contacts physically vibrate for a few ms when pressed', 'The pull-up is missing', 'The ADC is too slow'], correctAnswer: 'Its contacts physically vibrate for a few ms when pressed' },
      { text: 'How do you make a button press fire an action exactly once?', options: ['Check the level in loop()', 'Detect the HIGH→LOW edge with debouncing', 'Use analogRead on the button', 'Add a second button'], correctAnswer: 'Detect the HIGH→LOW edge with debouncing' },
      { text: 'Why does an LED need a series resistor?', options: ['To limit current to a safe level', 'To make it brighter', 'To change its color', 'To add delay'], correctAnswer: 'To limit current to a safe level' },
      { text: 'On ESP32, which calls must come before a reliable `analogRead`?', options: ['analogReadResolution and analogSetPinAttenuation', 'pinMode(INPUT) only', 'Serial.end()', 'digitalWrite(HIGH)'], correctAnswer: 'analogReadResolution and analogSetPinAttenuation' },
      { text: 'What is the primary risk of `Serial.print` inside a timing-critical loop?', options: ['It makes the code faster', 'It blocks until the byte is transmitted, slowing the loop', 'It corrupts flash', 'It turns off WiFi'], correctAnswer: 'It blocks until the byte is transmitted, slowing the loop' },
      { text: 'A "Guru Meditation" / crash dump in the ESP32 boot log usually indicates…', options: ['WiFi is off', 'A software crash with a backtrace to decode', 'A sensor calibration issue', 'Low battery'], correctAnswer: 'A software crash with a backtrace to decode' },
      { text: 'What does `map(raw, 0, 4095, 0, 255)` do?', options: ['Converts the raw ADC range to an 8-bit PWM duty range', 'Inverts the ADC', 'Delays the reading', 'Fixes baud rate'], correctAnswer: 'Converts the raw ADC range to an 8-bit PWM duty range' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 8 — ESP32 Deep Dive
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 8,
    title: 'ESP32 Deep Dive',
    description:
      'The ESP32\'s dual-core architecture, WiFi/BT radios, ADC/DAC, timers, and the low-power modes that make battery projects possible.',
    topics: [
      {
        title: 'ESP32 Architecture: Dual Cores, Memory & Peripherals',
        text: 'The ESP32 is not just "a faster Arduino" — it is a sophisticated SoC with architecture that rewards understanding.\n\n**Dual Xtensa cores.** Two LX6 cores run at 240MHz. Under Arduino, `loop()` runs on core 1 by default; WiFi and Bluetooth stacks run on core 0. You can pin tasks to cores with FreeRTOS (`xTaskCreatePinnedToCore`) — a common pattern is time-critical I/O on core 0 and network/MQTT on core 1 so they never stall each other. Arduino\'s `loop()` on one core while WiFi auto-handles on the other is why MQTT works smoothly in a single loop.\n\n**Memory map.** 520KB of SRAM in several banks — RTC fast memory survives deep sleep, PSRAM (if the board has it, 2-8MB) is external RAM for big buffers like camera frames. Flash (4MB typical) holds the firmware and can also store a small filesystem (SPIFFS/LittleFS). Budget: TLS needs ~40-50KB RAM; a raw JPEG frame from the camera can need 100KB+.\n\n**Peripherals on tap:** two 12-bit ADCs, two 8-bit DACs (pins 25/26), 16 PWM channels, 3 UARTs, 2 I2C, 4 SPI, touch sensors, Hall sensor, RTC, and hardware encryption for AES/SHA — the crypto block means TLS is fast and doesn\'t eat CPU.\n\n**The pin map matters.** Many GPIOs are dual-purpose: GPIO2 is often the onboard LED but also a boot-status pin; GPIO12 straps the flash voltage at boot; GPIO34-39 are input-only ADC pins. Read the board-specific pinout before wiring — "all pins are equal" is false on the ESP32.\n\n**Practical takeaway:** you rarely need the full architecture, but knowing "WiFi runs on core 0", "ADC2 is noisy with WiFi", "crypto is in hardware", and "deep sleep survives in RTC memory" resolves most ESP32 mysteries.',
        code: '// Pin a task to a specific core (FreeRTOS via Arduino)\n#include <Arduino.h>\nvoid networkTask(void*) { for(;;) { /* MQTT, OTA */ delay(100); } }\nvoid setup(){\n  xTaskCreatePinnedToCore(networkTask, "net", 8192, NULL, 1, NULL, 1); // core 1\n  // loop() runs on core 0 -> sensor timing never blocked by WiFi\n}\nvoid loop(){ /* time-critical sensor loop */ }',
        note: 'ESP32 = dual cores (WiFi on core 0, your loop on core 1), 520KB RAM, hardware crypto. Know your board\'s pinout — not all pins are equal.',
      },
      {
        title: 'WiFi & Bluetooth on ESP32',
        text: 'The ESP32\'s dual radios are its superpower: **WiFi** for internet, **Bluetooth Classic** for legacy audio/serial devices, and **BLE** for modern low-power peripherals. Getting each one set up cleanly saves hours.\n\n**WiFi modes.** `WIFI_STA` — connect to an existing router (the usual IoT case). `WIFI_AP` — create your own hotspot (for initial provisioning: the device broadcasts a setup network). `WIFI_AP_STA` — both (provision + connect to the real router simultaneously).\n\n**Connecting is asynchronous.** `WiFi.begin(ssid, pass)` returns immediately; connection completes in the background. A naive `while (WiFi.status() != WL_CONNECTED)` blocks for seconds and hard-crashes the watchdog. The robust pattern: attempt connect with a timeout, then check `WiFi.isConnected()` periodically. Reconnect logic belongs in the loop.\n\n**WiFi events.** Register event callbacks (`WIFI_EVENT_STA_GOT_IP`, `WIFI_EVENT_STA_DISCONNECTED`) so you know when you actually have an IP and when the link drops — then you can trigger reconnects instead of polling.\n\n**Bluetooth Classic (SPP):** pairs like a headset and gives a virtual serial link — handy for talking to phones or legacy BT peripherals. **BLE:** GATT-server peripherals advertise services/characteristics that a phone app reads/writes. BLE is the right choice for wearables because of its tiny power footprint.\n\n**Practical notes:**\n- WiFi radio draws ~100mA when transmitting — plan power accordingly.\n- RSSI of -60dBm or better = good signal; below -80 is marginal.\n- WiFi autoconnect + reconnect-on-drop is the difference between a device that "works" and one that silently dies when the router reboots.\n- For provisioning, save credentials in NVS (non-volatile storage) and offer a captive-portal AP mode once — the user enters WiFi once, forever.',
        code: '// Robust WiFi connect with timeout + reconnect\n#include <WiFi.h>\nvoid ensureWifi(){\n  if (WiFi.isConnected()) return;\n  WiFi.begin("mySSID", "myPass");\n  int tries = 0;\n  while (!WiFi.isConnected() && tries++ < 40) { delay(250); }  // 10s max\n}\nvoid loop(){ ensureWifi(); /* publish etc */ delay(5000); }',
        note: 'WiFi connect is asynchronous — use timeouts and reconnects, not blocking while-loops. BLE for low power, BT Classic for serial-style links.',
      },
      {
        title: 'ADC, DAC & Sensor Interfacing on ESP32',
        text: 'Interfacing sensors to the ESP32 means mastering its specific ADC and DAC behaviour — which differ from the Arduino Uno in ways that bite.\n\n**ADC quirks (the important part):**\n- **Resolution:** configure with `analogReadResolution(bits)` — default 12 (0-4095).\n- **Attenuation:** `analogSetPinAttenuation(pin, ADC_11db)` selects the input range (0-3.3V). With the default `ADC_0db` you only measure 0-~1V — a classic "why does my sensor max out at 4095 at 1V?" bug.\n- **Non-linearity:** the ESP32 ADC is not perfectly linear, especially near 0 and full-scale. A two-point calibration against a known voltage fixes most error.\n- **ADC2 noise:** pins 36/39 (ADC1) are the reliable ones; ADC2 pins (GPIO4, GPIO2…) share hardware with WiFi and go noisy/blocked while WiFi transmits. Use ADC1 for measurement.\n- **Impedance:** the ESP32 ADC is happier with low-impedance sources; very high source impedance reads low or erratic — a buffer or lower-value divider resistor helps.\n\n**DAC:** two true analog outputs (GPIO25, GPIO26), 8-bit (`dacWrite(pin, 0-255)` → 0-3.3V). Useful for audio or waveform generation; for most analog "output" needs, PWM is more flexible.\n\n**Sensor interfacing checklist:**\n1. Supply the sensor at its rated voltage (many want 3.3V — good; 5V ones need a regulator).\n2. Check the output type: analog (ADC) or digital (I2C/SPI/UART).\n3. For analog: level-fit into 0-3.3V, set attenuation, average samples.\n4. For digital: scan the bus, use the correct address, read registers.\n5. Decouple power near the sensor.\n\nThe "doesn\'t read right" debugging order on ESP32: attenuation → pin (ADC1 vs ADC2) → wiring → scaling. Nine times out of ten it is the attenuation or the wrong pin family.',
        code: '// Correct analog setup for a 0-3.3V sensor on GPIO36 (ADC1)\nanalogReadResolution(12);\nanalogSetPinAttenuation(36, ADC_11db);     // full range\nfloat v = analogRead(36) * (3.3f / 4095.0f);\nSerial.printf("V=%.2f\\n", v);\n// Use ADC1 pins (32-39) for accurate reads under WiFi.',
        note: 'ESP32 ADC needs explicit attenuation for the 0-3.3V range, is non-linear (calibrate), and ADC2 fights WiFi — measure on ADC1.',
      },
      {
        title: 'Timers, RTC & Low-Power Modes',
        text: 'Battery life is where IoT devices live or die, and the ESP32 gives you the tools: **timers** for precise periodic work, an **RTC** for wake scheduling, and **sleep modes** that drop power to microamps.\n\n**The sleep hierarchy (current at 3.3V):**\n- **Active:** ~100-240mA while transmitting, ~30-60mA idle.\n- **Modem sleep:** CPU runs, WiFi power-saving on — ~20-40mA.\n- **Light sleep:** CPU pauses, RAM retained, wake on timer/interrupt — ~1-5mA.\n- **Deep sleep:** CPU and most RAM off; only RTC + ULP coprocessor alive — ~10µA. Wake sources: timer, GPIO, touch, ULP.\n- **Hibernation:** RTC off too — ~2.5µA, but wakes only by reset.\n\n**The deep-sleep pattern** is the bread and butter of battery nodes:\n\n```cpp\n// measure -> publish -> sleep\nvoid setup(){\n  do_measurement();\n  esp_deep_sleep_start();   // never returns\n}\nvoid loop(){}               // unreachable\n```\n\nWake on a timer (`esp_sleep_enable_timer_wakeup(60 * 1e6)` = 60s) or on a GPIO edge (a PIR motion sensor). The duty cycle math: at 10µA sleeping and 100mA active for 2s every minute, average current is dominated by the active burst and sleep — that is how a coin cell runs a sensor for months.\n\n**RTC memory** survives deep sleep: store a counter or flags in `RTC_DATA_ATTR` variables so the device remembers state across reboots. This is how "send a report at most once per hour" logic persists.\n\n**Timers.** For precision without `delay()`, use `millis()` for uptime-based scheduling (non-blocking) or hardware timers (`hw_timer_t`) for exact microsecond work like pulse generation.\n\n**Practical schedule:** active burst of a few seconds, then deep sleep. Keep the radio off during measurement, transmit once, sleep long. That one design decision — "sleep instead of idle" — extends battery life by orders of magnitude.',
        code: '// Battery-friendly node: measure -> publish -> deep sleep 5 min\n#define uS_TO_S_FACTOR 1000000ULL\nvoid setup(){\n  float t = read_sensor();\n  publish(t);                       // connect, send, disconnect\n  esp_sleep_enable_timer_wakeup(5 * 60 * uS_TO_S_FACTOR);\n  esp_deep_sleep_start();           // ~10uA until wake\n}\nvoid loop(){}',
        note: 'The battery-life secret: deep sleep (~10µA) between short active bursts. Use RTC memory to persist state across wakes.',
      },
    ],
    quizzes: [
      { text: 'On a dual-core ESP32 under Arduino, the WiFi stack typically runs on…', options: ['Core 0', 'Core 1', 'Core 2', 'No core'], correctAnswer: 'Core 0' },
      { text: 'Which ESP32 ADC pins stay reliable while WiFi transmits?', options: ['ADC2 pins', 'ADC1 pins (32-39)', 'DAC pins', 'Touch pins only'], correctAnswer: 'ADC1 pins (32-39)' },
      { text: 'With the default ADC_0db attenuation, the ESP32 ADC measures…', options: ['0-3.3V', 'Only about 0-1V', '0-5V', '-5 to +5V'], correctAnswer: 'Only about 0-1V' },
      { text: 'Which ESP32 feature keeps state across a deep-sleep cycle?', options: ['Flash filesystem only', 'RTC memory (RTC_DATA_ATTR)', 'The DAC', 'The second UART'], correctAnswer: 'RTC memory (RTC_DATA_ATTR)' },
      { text: 'The power draw of deep sleep on the ESP32 is about…', options: ['100 mA', '10 µA', '1 A', '0 A (completely off)'], correctAnswer: '10 µA' },
      { text: 'Why is `while (WiFi.status() != WL_CONNECTED) { }` a bad idea?', options: ['It blocks for seconds and can trip the watchdog', 'It connects too fast', 'It uses too much RAM', 'It turns off Bluetooth'], correctAnswer: 'It blocks for seconds and can trip the watchdog' },
      { text: 'Which mode lets an ESP32 broadcast its own setup network for provisioning?', options: ['WIFI_STA', 'WIFI_AP', 'Deep sleep', 'Offline mode'], correctAnswer: 'WIFI_AP' },
      { text: 'A 2s active burst at 100mA plus 5 minutes deep sleep at 10µA produces an average current of roughly…', options: ['~1 mA', '~50 mA', '~100 mA', '~500 mA'], correctAnswer: '~1 mA' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 9 — Serial Communication: UART
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 9,
    title: 'Serial Communication: UART',
    description:
      'The UART protocol, wiring TX/RX, baud rate, and talking to GPS modules, fingerprint scanners, and serial sensors.',
    topics: [
      {
        title: 'UART Protocol Fundamentals',
        text: '**UART** (Universal Asynchronous Receiver/Transmitter) is the simplest way two devices exchange bytes — and the basis of GPS modules, fingerprint scanners, Bluetooth SPP, and every "serial monitor" you have ever used.\n\n**The wiring is two wires:**\n- **TX** (transmit) of device A → **RX** (receive) of device B.\n- **RX** of A ← **TX** of B.\n- Plus a **common ground** (mandatory — without it, the voltage reference differs and data corrupts).\n\n**Why "asynchronous"?** There is no clock line. Both sides agree on a **baud rate** (bits per second), and the receiver samples the line at that rate. A byte is framed with a **start bit** (line drops low), **8 data bits** (LSB first), an optional **parity bit**, and **1-2 stop bits** (line high). That framing lets the receiver resynchronise on every byte even without a shared clock.\n\n**Baud rates:** 9600 (old sensors), 115200 (the ESP32 default and most modules). Both ends MUST match exactly — a mismatch produces the classic stream of garbage characters. Standard bauds: 9600, 19200, 38400, 57600, 115200, 921600.\n\n**Voltage levels matter.** "TTL serial" is 3.3V/5V logic — safe to wire between boards. **RS-232** (±12V) needs a level converter (MAX3232). **USB** is neither — to talk to a PC you need a USB-to-UART bridge chip (the ESP32 DevKit has one built in, which is why the USB port gives you a serial console).\n\n**Full vs half duplex.** UART is full-duplex: TX and RX are independent lines, so both sides can talk simultaneously.\n\n**The mental model:** UART is a "virtual serial cable". Whatever you write to TX appears on the other side\'s RX. That simplicity is why it outlives fancier buses for point-to-point gadget links.',
        code: '// ESP32 has multiple UARTs (serial ports)\n//  Serial  = USB console (to your PC)\n//  Serial1 = GPIO pins, e.g. 9 (RX), 10 (TX)\nvoid setup(){\n  Serial.begin(115200);               // console\n  Serial1.begin(9600);                // GPS module at 9600 baud\n}\nvoid loop(){\n  while (Serial1.available()) {\n    char c = Serial1.read();\n    Serial.print(c);                  // echo GPS NMEA to console\n  }\n}',
        note: 'UART = two wires (TX→RX, RX→TX) + common ground, no clock, matching baud rates. It is the "virtual serial cable" of hardware.',
      },
      {
        title: 'Wiring UART Devices Correctly',
        text: 'UART wiring looks trivial — two wires — yet cross-connects and missing grounds cause most "serial doesn\'t work" nights. Here is the reliable procedure.\n\n**1. TX→RX, RX→TX.** The classic mistake is wiring TX to TX. Data must leave one device\'s transmit pin and enter the other\'s receive pin. Always cross: A.TX → B.RX and A.RX → B.TX.\n\n**2. Common ground.** Both devices need a shared reference, or the receiver cannot tell a 1 from a 0. Connect GND of both. This is non-negotiable.\n\n**3. Voltage compatibility.** 3.3V ↔ 3.3V: fine. 5V device → 3.3V ESP32 RX: risky (5V into a 3.3V pin can damage it). Use a level shifter or a divider on that one line. 3.3V TX → 5V RX: usually acceptable (5V reads 3.3V as HIGH), but a level shifter is cleaner.\n\n**4. Match baud rates.** If the module\'s datasheet says 9600, set 9600. ESP32 `Serial1.begin(9600)`. Wrong baud → garbage bytes.\n\n**5. No flow control surprises.** Some modules expect RTS/CTS handshake pins. For hobby modules you can usually ignore them (or ground/float per the datasheet). If you get data corruption at high baud with long wires, drop the baud — not every link does 115200 over 30cm of breadboard jumpers.\n\n**6. Verify with a loopback.** Short TX to RX on the same board and type — you should see your own output echo back. This isolates the port from the external device in one minute.\n\n**Debugging order for "no data":** ground connected? → wires crossed (TX/RX swapped)? → baud match? → voltage levels? → right pins (Serial1 not Serial)? → is the device actually sending (logic analyser/oscilloscope)? \n\n**Pro tip:** `Serial.available()` + `Serial.read()` is the correct consumption pattern; reading a fixed number of bytes blindly (`Serial.readString()`) will hang or mis-frame when the other side is slow.',
        code: '// Wiring to an ESP32 (module on Serial1, pins 9 RX / 10 TX)\n//   GPS.TX  -->  ESP32 RX (GPIO9)\n//   GPS.RX  -->  ESP32 TX (GPIO10)\n//   GPS.GND -->  ESP32 GND   (share ground!)\n//   GPS.VCC -->  3.3V or 5V per datasheet\n\nSerial1.begin(9600);   // must match GPS baud',
        note: 'Cross TX↔RX, share ground, match baud, mind voltage levels, and verify with a loopback before suspecting the device.',
      },
      {
        title: 'Parsing Serial Protocols: NMEA GPS & AT Commands',
        text: 'Once bytes flow, the real work is **parsing the protocol** the device speaks. Two patterns dominate the IoT world: **NMEA sentences** (GPS) and **AT commands** (old-school modems, GSM/SIM modules).\n\n**NMEA GPS:** a GPS module spits lines like:\n\n```\n$GPRMC,081347,A,2826.448,N,07701.355,E,0.00,77.52,130826,,,A*5B\n```\n\nEach `$`-line is a sentence; fields are comma-separated; the checksum follows `*`. $GPRMC gives time, status (A=valid), latitude, longitude, speed, date. **Parsing pattern:** buffer bytes until a newline, split on commas, decode the fields you need (note NMEA lat/lon are in `DDMM.mmmm` format — degrees and decimal minutes — convert to decimal degrees by `dd + mm/60`). Always validate the checksum before trusting a sentence.\n\n**AT commands:** a module in AT mode accepts human-readable commands and replies with OK / ERROR / +results:\n\n```\nAT+CSQ        ->  +CSQ: 19,99   (signal quality)\nAT+CMGS=\"...\" ->  send SMS, reply OK\n```\n\nThe pattern is **request → wait for response → parse** — read until `\r\n` lines or until an `OK`/`ERROR` terminator, with a timeout so a silent module doesn\'t hang your code.\n\n**Robust serial reading — the professional pattern:**\n- Read into a **line buffer** one char at a time until newline.\n- Strip `\r`/`\n` and trailing whitespace.\n- Handle partial lines (the device may split a sentence across calls).\n- Add a **watchdog timeout**: if nothing arrives for X ms, give up and reconnect.\n- Never block forever waiting for a response — a dead GPS should not freeze your loop.\n\n**SoftwareSerial caution:** bit-banged serial is CPU-heavy and glitchy at high baud; prefer the ESP32\'s hardware UARTs (Serial1/2) whenever possible.',
        code: '// Buffer one NMEA line, then split on commas\nString line = "";\nvoid loop(){\n  while (Serial1.available()) {\n    char c = Serial1.read();\n    if (c == \'\\n\') { parse(line); line = ""; }  // complete line\n    else if (c != \'\\r\') line += c;\n  }\n}\nvoid parse(String s){\n  if (!s.startsWith("$GPGGA")) return;\n  int i1 = s.indexOf(\',\'); int i2 = s.indexOf(\',\', i1+1);\n  String lat = s.substring(i1+1, i2);          // DDMM.mmmm\n  Serial.println("Lat raw: " + lat);\n}',
        note: 'Parse serial protocols by buffering to a line, splitting on delimiters, validating checksums, and never blocking on a dead device.',
      },
      {
        title: 'UART Debugging & Logic Analysis',
        text: 'When a UART link misbehaves, guessing wastes hours. A disciplined debugging toolkit — including a **logic analyser** — finds the fault in minutes.\n\n**1. Loopback test (isolate the board).** Short the board\'s TX to its RX. Whatever you `Serial.print` should echo back. If it does, the port works and the problem is external wiring or the remote device. If not, your pins/port config are wrong.\n\n**2. Loopback through the cable (isolate the wire).** Short the far end of the cable\'s TX to RX. Now the echo travels through the wiring too — proving the cable and connectors are intact.\n\n**3. Scope the levels.** Use a multimeter or oscilloscope: idle UART line sits HIGH (~3.3V); a transmission shows the line dropping to ~0V for each start bit. If the line never moves, the device isn\'t transmitting. If levels look wrong (stuck low, half voltage), suspect wiring/voltage mismatch.\n\n**4. Logic analyser (the power tool).** A $10-20 USB logic analyser (saleae-clone + PulseView) decodes the bus: set the decoder to "UART" at the expected baud and watch bytes appear. If bytes look like garbage, the **baud is wrong** — the analyser shows the actual bits so you can read the true speed. If the line is busy but your code sees nothing, your pins are wrong.\n\n**5. Check flow/parity settings.** Some devices use parity or 7 data bits. A mismatch yields subtly corrupt data that "looks close".\n\n**6. Electrical environment.** Long unshielded wires near motors/relays pick up noise — lower the baud, shorten the run, or use twisted/shielded cable.\n\n**7. Firmware-side traps.** `Serial1.begin()` before any use; `Serial.available()` correctly consumed; no blocking `readString` that waits forever; watchdog resets if the loop stalls.\n\nThe golden rule: **isolate the layer** — board, then cable, then levels, then protocol, then firmware. One variable at a time, always with evidence (scope or logic analyser), never by blind rewiring.',
        code: '// Loopback sanity check (TX shorted to RX on the same board)\nvoid setup(){ Serial.begin(115200); }\nvoid loop(){\n  if (Serial.available()) {\n    char c = Serial.read();\n    Serial.print(c);            // echoes what the port received\n  }\n}',
        note: 'Debug UART in layers: loopback the board, loopback the cable, then scope/analyser the levels and protocol. One variable at a time.',
      },
    ],
    quizzes: [
      { text: 'Which wires connect a UART link?', options: ['TX to TX, RX to RX', 'TX to RX, RX to TX, plus common ground', 'SCL and SDA only', 'Four wires with chip selects'], correctAnswer: 'TX to RX, RX to TX, plus common ground' },
      { text: 'Why is UART called "asynchronous"?', options: ['It is slow', 'There is no shared clock line — both sides agree on a baud rate', 'It uses a crystal oscillator', 'It only sends one bit'], correctAnswer: 'There is no shared clock line — both sides agree on a baud rate' },
      { text: 'A stream of garbage characters from a serial module most likely means…', options: ['The module is broken', 'The baud rates do not match', 'The power is too high', 'Too few wires connected'], correctAnswer: 'The baud rates do not match' },
      { text: 'The first isolation step when a UART link produces no data is…', options: ['Replace the module', 'Loopback test: short TX to RX and echo', 'Reboot the PC', 'Increase the baud'], correctAnswer: 'Loopback test: short TX to RX and echo' },
      { text: 'How are NMEA GPS sentences terminated and structured?', options: ['Tab-separated without newlines', 'Newline-terminated comma-separated fields with a *checksum', 'JSON objects', 'Fixed 8-byte binary frames'], correctAnswer: 'Newline-terminated comma-separated fields with a *checksum' },
      { text: 'What does the NMEA field format DDMM.mmmm mean?', options: ['Decimal degrees', 'Degrees and decimal minutes (convert with dd + mm/60)', 'Radians', 'UTM meters'], correctAnswer: 'Degrees and decimal minutes (convert with dd + mm/60)' },
      { text: 'Why should serial parsing never block forever waiting for a response?', options: ['It uses too much flash', 'A dead device would freeze the whole loop', 'Blocking is always faster', 'It corrupts the buffer'], correctAnswer: 'A dead device would freeze the whole loop' },
      { text: 'What does a logic analyser decode when set to "UART" at the expected baud?', options: ['The actual transmitted bytes', 'The temperature', 'The checksum only', 'The power supply voltage'], correctAnswer: 'The actual transmitted bytes' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 10 — Serial Communication: I2C
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 10,
    title: 'Serial Communication: I2C',
    description:
      'The two-wire I2C bus, addressing, clock stretching, and wiring many sensors to one pair of pins.',
    topics: [
      {
        title: 'I2C Protocol & the Two-Wire Bus',
        text: '**I2C** (Inter-Integrated Circuit) is the workhorse for connecting multiple low-speed sensors to one microcontroller using just **two wires**: **SCL** (clock) and **SDA** (data).\n\nThe microcontroller is the **master**; each sensor is a **slave** with a fixed 7-bit **address** (e.g. 0x76 for BME280, 0x27 for an LCD, 0x68 for MPU6050). The master generates the clock and addresses one slave at a time; every transaction starts with a START condition, sends the address + read/write bit, and ends with a STOP.\n\n**The transaction shape:**\n1. Master pulls SDA low while SCL is high → **START**.\n2. Master sends the 7-bit address + a R/W bit.\n3. The addressed slave pulls SDA low → **ACK**.\n4. Data bytes flow (master→slave for writes, slave→master for reads), each ACKed.\n5. A rising SDA while SCL is high → **STOP**.\n\n**Pull-ups are mandatory.** Both lines are **open-drain** — devices can only pull them low. The high level comes from external **pull-up resistors** (typically 4.7kΩ) to VCC. Without pull-ups the bus stays low and no device responds — the #1 I2C "it doesn\'t work" cause.\n\n**Speed classes:** standard 100kHz, fast 400kHz, fast-plus 1MHz. Slow devices (LCDs) are happy at 100kHz; sensors at 400kHz. If errors appear at high speed, drop the clock — the wires and capacitance may not support it.\n\n**Why it is everywhere:** two pins drive many devices (addresses allow up to ~128 slaves), wiring is trivial, and microcontrollers integrate the controller in hardware. For low-to-medium speed sensors, I2C is the default choice.\n\nThe mental model: I2C is a **bus of named mailboxes** — you call the address, send/read bytes, hang up. Keep that image and the protocol becomes intuitive.',
        code: '// Scan the I2C bus for every address (ESP32)\n#include <Wire.h>\nvoid setup(){\n  Serial.begin(115200);\n  Wire.begin(21, 22);            // SDA=21, SCL=22 (default ESP32)\n  Serial.println("Scanning...");\n  for (byte a = 1; a < 127; a++) {\n    Wire.beginTransmission(a);\n    if (Wire.endTransmission() == 0) Serial.printf("found 0x%02X\\n", a);\n  }\n}\nvoid loop(){}',
        note: 'I2C = SCL + SDA + mandatory pull-ups. Master addresses slaves by 7-bit address; open-drain lines need 4.7kΩ pull-ups.',
      },
      {
        title: 'I2C Addressing, Conflicts & Bus Sharing',
        text: 'Addressing is the I2C concept that trips up real projects. Every device has a **fixed or selectable 7-bit address**, and when two devices claim the same address on one bus, neither works reliably. Here is how to survive.\n\n**Where addresses come from.** The address is set in the chip\'s hardware: most parts offer one or two variants via an address pin (e.g. A0). The BME280 has two: 0x76 (default) and 0x77. The MPU6050: 0x68 or 0x69 via its AD0 pin. Check the datasheet for the "address" table — it tells you which pin selects which value.\n\n**The conflict.** Two 0x76 sensors on the same bus = both ACK the same address and the master talks to whichever answers — data garbles or the bus hangs. Solutions:\n- Buy parts with **selectable addresses** (address pin) and set them differently.\n- Put the conflicting device on a **second bus** (ESP32 has two I2C peripherals: Wire = GPIO21/22, Wire1 = any pair).\n- Use a **multiplexer** (TCA9548A) for many same-address sensors.\n\n**How the address shows up on the wire.** The 7-bit address is shifted left and the LSB becomes the R/W bit: to read address 0x76, the bus carries 0xEC; to write it, 0xED. Libraries hide this, but you\'ll see it in raw captures.\n\n**Bus sharing rules.** Many devices on one bus is the whole point — but mind the **capacitance**: each device adds pin/wire capacitance, and too much (≈400pF) distorts the clock. For a handful of sensors on short wires it is fine; for dozens, use a multiplexer or drop the clock speed.\n\n**The address-change trick:** when a device supports A0/A1 pins, wire them to GND/VCC to select among 2/4/8 addresses. Read the datasheet\'s address table and set the pins BEFORE power-on.\n\n**Diagnostic flow for I2C trouble:** run an **I2C scanner** (prints every address present) → if the expected address is missing, check wiring and pull-ups → if it appears but data is garbled, slow the clock → if TWO expected devices show the same address, you have a conflict → fix by pin/address or second bus.',
        code: '// Put a conflicting sensor on a second I2C bus\n#include <Wire.h>\nTwoWire Wire1 = TwoWire(1);          // second I2C peripheral\nvoid setup(){\n  Wire.begin(21, 22);                // bus 0: SDA=21 SCL=22\n  Wire1.begin(25, 26);               // bus 1: SDA=25 SCL=26\n  // now two 0x76 sensors can coexist, one per bus\n}',
        note: 'Same-address devices conflict. Solve with selectable address pins, a second I2C bus, or a multiplexer — never by luck.',
      },
      {
        title: 'Reading I2C Sensor Registers',
        text: 'Talking to an I2C sensor is just **register reads and writes**. Every sensor exposes a register map in its datasheet — the contract your firmware follows. Learn to read registers directly and no sensor is a black box.\n\n**The register read sequence (typical):**\n1. **Write** the register address you want (a small I2C write transaction).\n2. **Restart** or stop, then **read** N bytes from the sensor.\n3. Combine the bytes into a value (sensors send MSB-first or LSB-first — the datasheet says).\n\nFor example, the BMP180 pressure sensor\'s calibration is stored in registers 0xAA-0xBF; to read raw temperature you write 0xF4=0x2E, wait ~5ms for conversion, then read registers 0xF6-0xF7 (two bytes, MSB first) and combine into a 16-bit value.\n\n**The chip-id sanity check.** Nearly every sensor has an ID register (e.g. MPU6050 WHO_AM_I = 0x75 = 0x68). Reading it confirms the wiring, address, and bus all work BEFORE you trust any measurement. If `who_am_i` doesn\'t match, stop — you\'re talking to nothing or the wrong device.\n\n**Scaling.** Raw register values are rarely the physical unit. A 16-bit accelerometer at ±2g has a scale of `2g / 32768` per LSB; a temperature sensor might be `raw/100.0`. The datasheet\'s "register description" table always gives the scale. Apply it after combining bytes.\n\n**Reading multiple registers in one shot** (burst read) is faster and atomic: `Wire.requestFrom(addr, 6)` grabs 6 bytes starting from the previously-set register — many sensors (MPU6050, BME280) support burst reads, saving time in a tight loop.\n\n**The library question.** Libraries (Adafruit, SparkFun) wrap all this. Use them, but when a reading is wrong, go to the datasheet register map — that is where the real answer lives (wrong scale, wrong address, sensor sleeping).\n\nThe debugging order: `who_am_i` → raw register dump → scaling → units.',
        code: '// Reading two bytes from an I2C register (generic)\nuint16_t readReg16(uint8_t addr, uint8_t reg) {\n  Wire.beginTransmission(addr);\n  Wire.write(reg);\n  Wire.endTransmission(false);   // keep bus for the read (repeated start)\n  Wire.requestFrom(addr, 2);     // read 2 bytes\n  uint8_t hi = Wire.read();\n  uint8_t lo = Wire.read();\n  return (hi << 8) | lo;         // MSB first per datasheet\n}',
        note: 'I2C = register reads/writes. Verify with WHO_AM_I, combine bytes per datasheet, then apply the scale factor to get real units.',
      },
      {
        title: 'I2C Pull-ups, Speed & Real-World Pitfalls',
        text: 'I2C is robust — until it is not. The most common failures are mundane and all solvable by understanding the electrical reality of the two-wire bus.\n\n**Missing pull-ups (the classic).** With no 4.7kΩ resistors to VCC, SDA/SCL stay low and nothing answers. Some breakout boards include pull-ups, some do not. When in doubt, add them. Symptoms: scanner finds nothing, or reads corrupt.\n\n**Pull-ups in parallel.** If a breakout already has pull-ups and you add more, the combined resistance drops (e.g. two 4.7k → 2.35k), edges sharpen but drive current rises — usually fine, but if the lines look weak, remove duplicates.\n\n**Speed vs capacitance.** Every device and every centimetre of wire adds capacitance. At 400kHz a long bus may fail while 100kHz works. If data garbles intermittently, **drop the clock speed** first — a 3-line change that fixes most I2C mysteries.\n\n**Level shifting.** A 5V sensor on a 3.3V bus: both lines need a level shifter (bidirectional). Some 5V breakouts with onboard pull-ups to 5V pull SDA/SCL to 5V — that back-feeds the 3.3V pins. Use a shifter or pick 3.3V variants.\n\n**Wrong pins.** The ESP32\'s default I2C pins are GPIO21 (SDA) / GPIO22 (SCL), but many boards reassign them. If a scanner finds nothing, verify you\'re calling `Wire.begin(SDA, SCL)` with the right pins — and that those pins aren\'t ALSO used by something else (e.g. the onboard LED or strapping pins).\n\n**Bus lock-up.** A glitch (brown-out, an unpowered slave) can leave SDA held low forever (a stuck slave). Reset the bus: toggle the power of the stuck device, or momentarily ground SCL to release the slave — then scan again.\n\n**Clock stretching.** Some sensors (LCDs, some EEPROMs) hold SCL low to slow the master. ESP32 I2C supports it in hardware; if a device "freezes", check the library handles stretching.\n\nThe debugging ladder: pull-ups → pins → address/scanner → speed → level shifting → power glitch.',
        code: '// Robust I2C init on ESP32\n#include <Wire.h>\nvoid setup(){\n  Wire.begin(21, 22);            // SDA, SCL\n  Wire.setClock(100000);         // drop to 100kHz if flaky at 400kHz\n  // add 4.7k pull-ups on SDA and SCL to 3.3V if the board lacks them\n}',
        note: 'I2C fails in predictable ways: no pull-ups, wrong pins, too-fast clock, missing level shifter. Fix the ladder, not the sensor.',
      },
    ],
    quizzes: [
      { text: 'How many wires does I2C use?', options: ['One', 'Two (SCL clock + SDA data)', 'Four (with chip selects)', 'Eight'], correctAnswer: 'Two (SCL clock + SDA data)' },
      { text: 'Why do I2C lines need pull-up resistors?', options: ['To increase speed', 'Because the open-drain lines can only pull low — something must pull them high', 'To reduce power', 'To set the address'], correctAnswer: 'Because the open-drain lines can only pull low — something must pull them high' },
      { text: 'Two identical 0x76 sensors on one I2C bus will…', options: ['Work fine automatically', 'Conflict — both answer the same address and data garbles', 'Share the address round-robin', 'Double the speed'], correctAnswer: 'Conflict — both answer the same address and data garbles' },
      { text: 'What is the standard first sanity check when talking to an I2C sensor?', options: ['Read WHO_AM_I / chip-id register', 'Set the baud rate', 'Check the servo angle', 'Format the SD card'], correctAnswer: 'Read WHO_AM_I / chip-id register' },
      { text: 'An I2C scanner finds no devices at all. First suspects are…', options: ['Missing pull-ups or wrong pins', 'The battery is too big', 'The LCD backlight', 'Wrong servo PWM'], correctAnswer: 'Missing pull-ups or wrong pins' },
      { text: 'Intermittent I2C corruption at 400kHz that disappears at 100kHz usually means…', options: ['The sensor is broken', 'Bus capacitance/rise-time limits — drop the clock speed', 'The baud rate is wrong', 'The address changed'], correctAnswer: 'Bus capacitance/rise-time limits — drop the clock speed' },
      { text: 'How can two sensors with the same address coexist?', options: ['Use a second I2C bus or a multiplexer', 'Twist the wires', 'Raise the voltage', 'They cannot ever coexist'], correctAnswer: 'Use a second I2C bus or a multiplexer' },
      { text: 'Raw 16-bit register values from an accelerometer are…', options: ['Already in m/s²', 'Scaled per the datasheet (e.g. ±2g / 32768 per LSB)', 'Already in volts', 'Meaningless'], correctAnswer: 'Scaled per the datasheet (e.g. ±2g / 32768 per LSB)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 11 — Serial Communication: SPI
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 11,
    title: 'Serial Communication: SPI',
    description:
      'The high-speed SPI bus, MISO/MOSI/SCLK/chip-select, and when to choose SPI over I2C.',
    topics: [
      {
        title: 'SPI Protocol & the Four-Wire Bus',
        text: '**SPI** (Serial Peripheral Interface) is the fastest common serial bus — used for displays, SD cards, flash, and high-rate sensors. It needs **four wires** but delivers full-duplex speed and a clean, simple protocol.\n\nThe four lines:\n- **SCLK** — clock generated by the master.\n- **MOSI** (Master Out Slave In) — master→slave data.\n- **MISO** (Master In Slave Out) — slave→master data.\n- **CS/SS** (Chip Select, a.k.a. Slave Select) — one line PER slave, pulled low to talk to that slave.\n\nBecause there is a **dedicated chip-select line per device**, SPI needs no addresses — the master simply asserts the CS line of the target and the rest ignore the bus. That is why SPI wiring grows a wire per device while I2C stays at two.\n\n**How a transfer works:** master pulls the target\'s CS low, then toggles SCLK while shifting bits: each clock pulse sends one bit out on MOSI and simultaneously captures one bit on MISO. SPI is **full-duplex** — both directions transfer at once. After the transaction, CS goes high again.\n\n**The four SPI modes** (0-3) define clock polarity (CPOL) and clock phase (CPHA): mode 0 (CPOL=0, CPHA=0) is the most common default. Mismatched modes produce shifted or corrupted data — the datasheet always states the mode.\n\n**Speed.** SPI runs at MHz — hundreds of times faster than I2C\'s 100-400kHz. That is why screens and SD cards use it. The price: four-plus wires, one chip-select per slave, and no built-in acknowledgement (a sleeping slave just returns garbage — your problem to detect).\n\n**When SPI and when I2C:**\n- **SPI** — high data rates (displays, cameras, flash, SD), simple byte streaming.\n- **I2C** — many devices on few wires, low speed, multi-vendor sensor ecosystems, device discovery via addressing.\n\nThe mental model: SPI is a **shift register with a phone line per device** — you pull the right phone line, then clock data in and out.',
        code: '// SPI on ESP32 with the SD library (classic example)\n#include <SPI.h>\n#include <SD.h>\n#define CS_PIN 5\nvoid setup(){\n  SPI.begin(18, 19, 23);          // SCLK, MISO, MOSI\n  if (!SD.begin(CS_PIN)) {\n    Serial.println("SD card failed");\n    return;\n  }\n  Serial.println("SD card ready");\n}',
        note: 'SPI = SCLK + MOSI + MISO + one CS per device. Full-duplex and MHz-fast, at the cost of extra wires and no built-in ACK.',
      },
      {
        title: 'Chip Select & Sharing the SPI Bus',
        text: 'The **chip-select (CS)** line is how SPI addresses devices, and getting CS handling right is the difference between a bus that works and one that mysteriously garbles.\n\n**One CS per slave.** The master has one CS pin per device. To talk to device A, pull A\'s CS low; to device B, pull B\'s CS low. Never assert two CS lines at once — both slaves would drive MISO and the bus corrupts. (With SPI, "CS high = device ignores the bus", which is exactly the isolation you want.)\n\n**Active-low convention.** CS is active-low: LOW = selected, HIGH = deselected. Beginners invert this and wonder why nothing responds. Most libraries hide the polarity, but raw drivers expose it.\n\n**CS timing.** The CS line must settle (low) before the first clock edge and stay low for the whole transaction, then go high. Rushing the transitions is a classic SPI failure — give CS a short delay or use hardware SS control.\n\n**Hardware vs software CS.** Many MCUs have hardware SS pins that assert automatically; in practice it is often easier to treat CS as a plain GPIO you toggle manually around `SPI.transfer()` calls. This gives you control and avoids the classic "hardware SS went low and a slave latched garbage" surprise.\n\n**Multiple devices on one bus:**\n- Assign each device its own CS GPIO.\n- The shared lines (SCLK, MOSI, MISO) fan out to all.\n- Mind **drive strength / capacitance**: a long fan-out bus with many devices needs lower speed, just like I2C.\n- Some devices are **"3-state"** and float MISO when unselected — good. Others keep driving — a conflict waiting to happen if two are selected at once.\n\n**The golden rule:** only ONE chip select low at a time, and keep it low for the entire transaction. If your SPI bus reads garbage when you add a second device, you are almost certainly violating this rule.\n\n**Diagnostic ladder for SPI:** CS polarity/timing → mode match → wiring (MOSI/MISO swapped?) → speed too high for the wire run → shared-bus conflict.',
        code: '// Manual chip-select discipline\n#define CS_A 5\n#define CS_B 15\nvoid talk_to(uint8_t cs) {\n  digitalWrite(cs, LOW);            // select exactly one\n  SPI.transfer(0xAA);               // full transaction\n  digitalWrite(cs, HIGH);           // deselect when done\n}\n// Never: digitalWrite(CS_A, LOW); digitalWrite(CS_B, LOW);  // conflict!',
        note: 'Chip select is active-low, one per device, held low for the whole transaction. Only one CS low at a time — always.',
      },
      {
        title: 'SPI vs I2C: Choosing the Right Bus',
        text: 'Both SPI and I2C exist for a reason, and choosing wrong costs you wiring, speed, or sanity. This decision matrix settles it quickly.\n\n**Go SPI when:**\n- Data rates are high — displays, SD cards, cameras, audio, flash memory. An LCD refresh needs Mbps that I2C\'s 400kHz cannot dream of.\n- You stream bytes with simple framing and want maximum throughput.\n- You control the devices (chip selects) and do not need auto-discovery.\n- Full-duplex communication (data both ways on every clock) helps.\n\n**Go I2C when:**\n- You connect MANY sensors (temperature, humidity, IMU, pressure) — two wires handle dozens.\n- Devices have fixed addresses and you want easy bus sharing without extra CS wires.\n- Speed needs are modest (a sensor read every 100ms is nothing).\n- You want simpler wiring on a breadboard and lower pin usage.\n\n**Practical hybrid:** use I2C for the sensor cluster (2 wires) and SPI for the display/SD (4 wires + CS). This is the standard layout of real IoT boards: sensors on I2C, human interface and storage on SPI.\n\n**Speed comparison, concrete:** I2C 400kHz, 8-bit bytes, with overhead ≈ tens of KB/s. SPI 10-40MHz with 8-bit transfers ≈ hundreds of KB/s to MB/s — 10-100× faster.\n\n**Wiring comparison:** I2C = 2 wires + shared ground, same 2 wires for N devices. SPI = 3 shared + 1 CS per device; N devices = 3+N wires.\n\n**Robustness:** I2C has addressing, ACK, and clock stretching — devices tell you they exist. SPI is raw — a sleeping device just returns garbage and you must detect it yourself (e.g. check a status byte).\n\nThe rule of thumb that covers 90% of cases: **many slow sensors → I2C; few fast peripherals → SPI.**',
        code: '// Typical split on an IoT board\n//   I2C bus : BME280 (temp), MPU6050 (IMU), LCD (0x27)   [2 wires]\n//   SPI bus : SD card + TFT display                       [4 wires + 2 CS]\n//\n// Sensors are slow + numerous  -> I2C\n// Display/SD are fast + few    -> SPI',
        note: 'Many slow sensors → I2C. Few fast peripherals (display, SD) → SPI. Real boards run both.',
      },
      {
        title: 'Debugging SPI: Modes, Wiring & Timing',
        text: 'SPI debugging is usually one of four things: wrong mode, swapped wires, CS mistakes, or speed. Each has a signature and a fix.\n\n**1. Data is shifted but wrong (off-by-a-few-bits).** This is the classic **SPI mode mismatch**. The datasheet says "Mode 1" (CPOL=0, CPHA=1) and you\'re in Mode 0. Set `SPISettings(4000000, MSBFIRST, SPI_MODE1)`. Symptoms: bytes come back shifted or with a corrupted first/last bit.\n\n**2. Reads all 0xFF or all 0x00.** Usually **wiring** — MISO/MOSI swapped, or MISO not actually connected (you read a floating/unconnected line). Verify the pinout, and confirm the slave is powered and has its CS correctly asserted.\n\n**3. Works sometimes, garbles under load.** **Speed.** Long wires, fan-out, or a cheap breakout can\'t do 40MHz. Drop `SPISettings` to 1-8MHz and see if it stabilises.\n\n**4. First device works, second doesn\'t.** **CS discipline.** Both chip-selects low at once, or CS not held low for the transaction. Double-check each CS is a distinct GPIO and only one is ever low.\n\n**5. It worked on the Arduino but not the ESP32.** **Different pins.** ESP32 SPI pins are set via `SPI.begin(SCLK, MISO, MOSI, SS)` and must not collide with strapping/boot pins. Also ESP32\'s HSPI/VSPI bus choice matters if you use two SPI peripherals.\n\n**6. Nothing at all.** Power, ground, CS polarity (active-low!). A logic analyser at 16-24MHz sampling will show whether SCLK is toggling and whether CS actually asserts — the fastest way to separate "bus never moves" from "bus moves but wrong".\n\n**The disciplined SPI bring-up sequence:** (1) verify with a **loopback** (tie MISO to MOSI — transfer a byte, confirm it echoes), (2) then a known-response device (read a chip-id register), (3) then your actual peripheral. Loopback first isolates the master; the chip-id isolates the wiring.',
        code: '// SPI loopback test: tie MISO to MOSI, transfer, check echo\nvoid setup(){\n  Serial.begin(115200);\n  SPI.begin(18, 19, 23);          // SCLK, MISO, MOSI\n  SPI.beginTransaction(SPISettings(1000000, MSBFIRST, SPI_MODE0));\n  byte out = 0xA5;\n  byte back = SPI.transfer(out);\n  SPI.endTransaction();\n  Serial.printf("echo=0x%02X (expect 0xA5)\\n", back);\n}\nvoid loop(){}',
        note: 'SPI bugs are mode mismatch, swapped wires, CS errors, or speed. Loopback first, then a chip-id read, then the real device.',
      },
    ],
    quizzes: [
      { text: 'How many wires does SPI use for one slave?', options: ['Two', 'Four (SCLK, MOSI, MISO, CS)', 'Six', 'Eight'], correctAnswer: 'Four (SCLK, MOSI, MISO, CS)' },
      { text: 'How does SPI address a specific slave?', options: ['By 7-bit address', 'By asserting that slave\'s chip-select line low', 'By sending an ID byte', 'By clock speed'], correctAnswer: 'By asserting that slave\'s chip-select line low' },
      { text: 'What happens if two chip selects are asserted at once?', options: ['Both devices work in parallel', 'Both drive MISO and the bus corrupts', 'The bus speeds up', 'Nothing'], correctAnswer: 'Both drive MISO and the bus corrupts' },
      { text: 'Shifting-but-wrong data on SPI usually means…', options: ['Wrong SPI mode (CPOL/CPHA)', 'The battery is low', 'Too many pull-ups', 'The servo needs calibration'], correctAnswer: 'Wrong SPI mode (CPOL/CPHA)' },
      { text: 'SPI is roughly how much faster than I2C?', options: ['The same speed', '10-100× faster', 'Slower', '2× slower'], correctAnswer: '10-100× faster' },
      { text: 'A display and SD card on an IoT board are best served by…', options: ['I2C', 'SPI (high data rate)', 'UART', 'No bus'], correctAnswer: 'SPI (high data rate)' },
      { text: 'The first isolation test for an SPI master is…', options: ['Replace the slave', 'Loopback: tie MISO to MOSI and confirm an echo', 'Lower the voltage', 'Increase the baud'], correctAnswer: 'Loopback: tie MISO to MOSI and confirm an echo' },
      { text: 'CS lines on SPI are…', options: ['Active-high', 'Active-low (LOW selects the device)', 'Always floating', 'Not needed'], correctAnswer: 'Active-low (LOW selects the device)' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 12 — Networking & WiFi Connectivity
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 12,
    title: 'Networking & WiFi Connectivity',
    description:
      'IP basics, WiFi security, connecting and reconnecting reliably, and the TCP/IP stack on a microcontroller.',
    topics: [
      {
        title: 'IP, TCP & the Internet Stack on an MCU',
        text: 'Every IoT device you build participates in the same internet your laptop uses. Understanding the layers lets you reason about why things connect — or don\'t.\n\nThe **internet stack** on an ESP32, top to bottom:\n- **Application** — MQTT, HTTP, WebSocket (what your code speaks).\n- **Transport** — TCP (reliable) or UDP (lightweight).\n- **Internet** — IP: each device gets an **IP address**; routers forward packets.\n- **Link** — WiFi/Ethernet: moving frames over the air.\n\n**IP addresses.** Your device needs one. Usually the router\'s **DHCP** assigns it automatically (`WiFi.localIP()` shows it). A **static IP** is useful for servers or debugging. IPv4 (192.168.1.5) vs IPv6 — most home networks still use IPv4 internally.\n\n**TCP vs UDP — the practical difference:**\n- **TCP** guarantees delivery, ordering, and flow control. Every byte is acknowledged and retransmitted if lost. Perfect for MQTT, HTTP, file transfers. Costs: overhead and latency.\n- **UDP** is fire-and-forget — fast, connectionless, no guarantees. Perfect for telemetry where a dropped sample is acceptable, or real-time streams (audio, motion data) where retransmission would be too late.\n\n**Ports & connections.** A TCP connection is (IP, port) pair: your broker listens on 1883 (MQTT), web servers on 80/443. Each connection consumes a small socket on the MCU — the ESP32 handles a handful of concurrent sockets comfortably.\n\n**The ESP32 WiFi stack is software you don\'t see.** WiFi runs on core 0 with its own tasks. `WiFi.isConnected()` tells you the link is up; but "link up" ≠ "internet reachable" — the router may have internet while your DNS or uplink fails. Test reachability explicitly when it matters.\n\nThe mental model: **IP finds the machine, TCP/UDP carries the conversation, MQTT/HTTP structure the words.** Knowing which layer failed is half the debugging.',
        code: '// See your device on the network\nvoid setup(){\n  Serial.begin(115200);\n  WiFi.begin("mySSID", "myPass");\n  while (WiFi.status() != WL_CONNECTED) delay(500);\n  Serial.println(WiFi.localIP());          // e.g. 192.168.1.42\n  Serial.println(WiFi.subnetMask());\n  Serial.println(WiFi.gatewayIP());\n}\nvoid loop(){}',
        note: 'IP finds the machine, TCP guarantees the bytes, UDP trades reliability for speed, and MQTT/HTTP structure the conversation.',
      },
      {
        title: 'WiFi Security: WPA2/WPA3 & SSID Best Practice',
        text: 'Your IoT devices sit on your WiFi network, so WiFi security is device security. The weakest link in many smart homes is a misconfigured or legacy-secured network.\n\n**Encryption standards:**\n- **WEP** — broken in minutes. Never use.\n- **WPA/WPA2-PSK (WPA2-Personal)** — the standard for home networks. AES-CCMP encryption. Good enough with a strong passphrase.\n- **WPA3-Personal** — the modern upgrade: simultaneous authentication (SAE) resists offline dictionary attacks and gives forward secrecy. Support on the ESP32 is available in newer SDKs; the ESP32-C3/S3 handle WPA3.\n- **WPA2/WPA3-Enterprise (802.1X)** — per-user credentials via RADIUS (university/corporate networks). The ESP32 supports EAP-TLS/TTLS/PEAP — relevant when you deploy devices on institutional networks.\n\n**Choosing a network for your device:** if a router offers both WPA2 and WPA3, compatibility mode usually works; if a device only supports WPA2 and the router is set WPA3-only, the device will never join — a classic "worked at home, not at the office" failure.\n\n**SSID hygiene:**\n- Do NOT embed credentials in every device\'s code (`WiFi.begin("SSID","pass")` hardcoded) — when the password changes, you must reflash every device. Provision once into **NVS** (non-volatile storage) with a captive-portal setup mode.\n- Use a separate **IoT VLAN/guest network** if the router supports it, isolating your smart bulbs from your laptop.\n- Change default router passwords and disable WPS (a known attack vector).\n\n**Beyond WiFi itself:** even inside a well-secured WiFi, an IoT device is only as secure as its **firmware and TLS** — a device that phones home over plain HTTP can be eavesdropped on. WiFi encryption protects against neighbours; **TLS protects against the whole path** (next section).\n\nThe layered view: WiFi/WPA2 keeps outsiders off the LAN; TLS keeps data safe even on a hostile path; strong unique passwords keep devices safe from each other.',
        code: '// Provision credentials once, store in NVS\n#include <Preferences.h>\nPreferences prefs;\nvoid saveWifi(const char* ssid, const char* pass){\n  prefs.begin("wifi", false);\n  prefs.putString("ssid", ssid);\n  prefs.putString("pass", pass);\n  prefs.end();\n}\n// On boot: load from NVS, else enter setup mode',
        note: 'WPA2-AES at minimum, WPA3 where possible. Never hardcode credentials — provision into NVS. Isolate IoT on a guest/VLAN.',
      },
      {
        title: 'Reliable WiFi: Reconnects, Timeouts & Watchdogs',
        text: 'The most common real-world IoT failure is not a sensor bug — it is **WiFi dropping and the device silently dying**. Robust connectivity is a design requirement, not a nice-to-have.\n\n**The failure modes:**\n1. Router reboot → device never reconnects.\n2. Weak signal → intermittent drops.\n3. Sleep/wake → stale connection state.\n4. DHCP lease expiry → lost IP with no renewal.\n\n**The robust pattern — every loop checks the link:**\n```cpp\nvoid ensureWifi() {\n  if (WiFi.isConnected()) return;\n  WiFi.disconnect();\n  WiFi.begin(ssid, pass);\n  for (int i = 0; i < 20 && !WiFi.isConnected(); i++) delay(500); // 10s max\n  if (!WiFi.isConnected()) Serial.println("WiFi retry failed");\n}\n```\nCall `ensureWifi()` before any network operation. This heals most transient drops.\n\n**Timeouts everywhere.** Never block forever: every connect, every publish, every read gets a bounded wait. The ESP32 watchdog will reboot you if you block the loop too long — a timeout that reattempts is better than a crash-loop.\n\n**Reconnect events.** Register WiFi event callbacks (`WIFI_EVENT_STA_DISCONNECTED`, `WIFI_EVENT_STA_GOT_IP`) so you know the exact moment the link drops and can react immediately instead of discovering it 10 minutes later.\n\n**Persistent state with reboot recovery.** Store last-good connection state in NVS and, on boot, restore it before attempting a fresh connect — this dramatically shortens reconnect time after a power cycle.\n\n**Signal hygiene:** keep RSSI above about -70dBm for stability (print `WiFi.RSSI()` during bring-up). Improve with antenna orientation, closer AP, or a better-positioned device. A device at -85dBm will reconnect forever.\n\n**The 3-tier rule:** link-check in loop (fast heal) → timeout on every op (no hangs) → watchdog reboot (last resort). Layered like this, a device survives router reboots, DHCP churn, and weak-signal days without a human touching it.',
        code: '// Minimal robust loop\nvoid loop(){\n  ensureWifi();\n  if (WiFi.isConnected()) {\n    publish_sensor();      // each with its own timeout\n  }\n  delay(15000);            // 15s between attempts\n}\n// Add a watchdog: if the loop stalls >30s, ESP32 resets itself.',
        note: 'Check the link in every loop, bound every network op with a timeout, and let a watchdog reboot as the last resort.',
      },
      {
        title: 'Hostnames, mDNS & Finding Your Device',
        text: 'Hardcoding `192.168.1.42` in a dashboard is a ticking time bomb — DHCP can reassign it. The fix is **names**: mDNS and hostnames let you reach your device by a name it owns.\n\n**mDNS (Bonjour).** Multicast DNS lets a device advertise a name on the local network: `mydevice.local`. Any browser/tool that speaks mDNS (macOS, most Linux, Windows 10+) can reach it with no router config:\n\n```cpp\n#include <ESPmDNS.h>\nMDNS.begin("mydevice");   // now http://mydevice.local works\n```\n\n**How it works.** The device answers mDNS queries locally (multicast on 5353/udp), announcing its IP. No central DNS server needed — ideal for a LAN of IoT devices that have no fixed IP.\n\n**WiFi.hostname()** sets the DHCP client identifier so the router shows a friendly name (`ESP32-LivingRoom`) instead of `ESP-123ABC` in its client list.\n\n**HTTP on a name.** With mDNS active, a web UI served by the ESP32 (`WebServer` on port 80) is reachable at `http://mydevice.local` — no IP memorising, works after DHCP reassigns.\n\n**When mDNS fails:** some routers/firewalls block multicast; Windows sometimes needs the Bonjour service; and the device must be on the SAME LAN segment. For cross-network access you need a different mechanism: static IP, DDNS, or a cloud tunnel.\n\n**Finding devices at scale.** For many devices, add a **device discovery protocol**: devices broadcast a small UDP beacon (e.g. JSON with id + name + capabilities), and a gateway/app listens and builds the device list automatically. Zero-config onboarding is the professional touch.\n\n**Static IPs — when names are not enough.** For a device that must be reachable from outside the LAN, a static DHCP reservation (router-side, by MAC) or a device static IP keeps a known address — but a name + mDNS is simpler for most local projects.\n\nThe takeaway: give every device a name, advertise it with mDNS, and your dashboards stop breaking every time DHCP churns.',
        code: '// Advertise your device on the LAN\n#include <WiFi.h>\n#include <ESPmDNS.h>\nvoid setup(){\n  WiFi.hostname("smart-sensor-07");\n  WiFi.begin("mySSID", "myPass");\n  while (WiFi.status() != WL_CONNECTED) delay(500);\n  if (MDNS.begin("sensor7")) Serial.println("mDNS up");\n  Serial.printf("Reach me at http://sensor7.local (%s)\\n", WiFi.localIP().toString().c_str());\n}\nvoid loop(){}',
        note: 'Use mDNS so devices are reachable by name (mydevice.local), not by a DHCP IP that can change. Advertise + discover at scale.',
      },
    ],
    quizzes: [
      { text: 'Which transport protocol guarantees delivery and ordering?', options: ['UDP', 'TCP', 'ICMP', 'ARP'], correctAnswer: 'TCP' },
      { text: 'For a real-time motion stream where retransmission would be too late, you would use…', options: ['TCP', 'UDP', 'FTP', 'SMTP'], correctAnswer: 'UDP' },
      { text: 'Why is WEP unacceptable on a modern network?', options: ['It is slow', 'It is broken in minutes', 'It uses too much power', 'It is not supported by ESP32'], correctAnswer: 'It is broken in minutes' },
      { text: 'The most common real-world IoT failure is…', options: ['Sensor burnout', 'WiFi dropping and the device not reconnecting', 'Broken LEDs', 'Wrong baud rate'], correctAnswer: 'WiFi dropping and the device not reconnecting' },
      { text: 'What should you check before every network operation?', options: ['The battery voltage', 'Whether WiFi is still connected', 'The server time', 'The servo angle'], correctAnswer: 'Whether WiFi is still connected' },
      { text: 'mDNS lets you reach a device by…', options: ['Its MAC address', 'A name like mydevice.local', 'Its serial number', 'Its GPIO pin'], correctAnswer: 'A name like mydevice.local' },
      { text: 'A device at RSSI -85 dBm will likely…', options: ['Run faster', 'Have constant disconnects and reconnect forever', 'Use less battery', 'Block the network'], correctAnswer: 'Have constant disconnects and reconnect forever' },
      { text: 'Why is hardcoding an IP in your dashboard a problem?', options: ['IPs are hard to read', 'DHCP can reassign it, breaking the reference', 'IPs wear out', 'It uses flash'], correctAnswer: 'DHCP can reassign it, breaking the reference' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 13 — HTTP, REST & WebSockets
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 13,
    title: 'HTTP, REST & WebSockets',
    description:
      'Making HTTP requests from an MCU, REST APIs, JSON payloads, and real-time updates with WebSockets.',
    topics: [
      {
        title: 'HTTP from a Microcontroller',
        text: 'HTTP is the language of the web, and your ESP32 can speak it — as a **client** calling APIs or as a **server** hosting a control page. Both are simpler than the hype suggests.\n\n**HTTP basics.** A client sends a **request** — a method (GET/POST/PUT/DELETE), a path (`/api/temp`), headers (Content-Type, Authorization), and optionally a body. The server replies with a **status code** (200 OK, 201 Created, 400 Bad Request, 401 Unauthorized, 404 Not Found, 500 Server Error) and a body (often JSON).\n\n**As an HTTP client (the IoT case):** your device POSTs sensor data or GETs control commands. Using the `HTTPClient` library:\n\n```cpp\nHTTPClient http;\nhttp.begin("http://api.example.com/temp");\nhttp.addHeader("Content-Type", "application/json");\nint code = http.POST("{\"t\":25.4}");\nString body = http.getString();   // server response\nhttp.end();\n```\n\n**Key habits:**\n- **Always check the status code** — a 200 is success; a 400/500 needs handling, not ignoring.\n- **Bound the request with a timeout** so a dead server doesn\'t freeze your loop.\n- **Close the connection** (`http.end()`) — ESP32 has limited sockets.\n- **Don\'t hammer.** Add a delay between requests; many APIs rate-limit.\n\n**As an HTTP server (the smart-device case):** the `WebServer` library lets the ESP32 serve a control page:\n\n```cpp\nWebServer server(80);\nserver.on("/", handleRoot);        // serve HTML\nserver.on("/on", []{ relayOn(); server.send(200, "text/plain", "ok"); });\nserver.begin();\n```\nNow `http://device.local/on` turns the relay on — your device is an API endpoint. Pair with mDNS and you have a no-app smart device.\n\n**Security:** for anything beyond a hobby LAN, serve **HTTPS** (TLS) — the ESP32 supports it with certificates. Plain HTTP on the public internet is eavesdroppable; never send credentials or control commands over it.',
        code: '// ESP32 as HTTP client posting telemetry\n#include <HTTPClient.h>\nvoid postReading(float t){\n  if (!WiFi.isConnected()) return;\n  HTTPClient http;\n  http.setTimeout(5000);\n  http.begin("http://192.168.1.10:8080/api/telemetry");\n  http.addHeader("Content-Type", "application/json");\n  String body = String("{\\\"t\\\":") + t + "}";\n  int code = http.POST(body);\n  if (code != 200) Serial.printf("POST failed: %d\\n", code);\n  http.end();\n}',
        note: 'HTTP = request/response with status codes. Devices POST data and GET commands; always check the code, bound timeouts, and close connections.',
      },
      {
        title: 'REST APIs & JSON Payloads',
        text: 'Most cloud backends expose a **REST API** — a set of URL endpoints that map to operations on resources, with **JSON** bodies. Learning the REST+JSON pattern unlocks every dashboard, database, and cloud service.\n\n**REST resources & verbs.** A resource is a thing (a device, a reading, a user). The HTTP verbs act on it:\n- **GET** `/devices` → list devices.\n- **POST** `/devices` → create a device (body = JSON).\n- **GET** `/devices/7` → read one.\n- **PUT** `/devices/7` → replace it.\n- **PATCH** `/devices/7` → update part of it.\n- **DELETE** `/devices/7` → delete it.\n\n**Status codes are the contract:** 200 OK, 201 Created (POST success), 204 No Content, 400 invalid request, 401/403 auth failure, 404 missing, 500 server error. A good client logs the code with every call.\n\n**JSON on a microcontroller.** JSON is text: `{"device":"sensor7","temp":25.4,"unit":"C"}`. On the ESP32 use the `ArduinoJson` library — it both **serialises** (build) and **parses** (read) JSON with tiny RAM:\n\n```cpp\nStaticJsonDocument<256> doc;\ndoc["device"] = "sensor7";\ndoc["temp"] = 25.4;\nserializeJson(doc, serialized);   // -> JSON string\n\nStaticJsonDocument<256> in;\ndeserializeJson(in, payload);     // parse a response\nconst char* status = in["status"];\n```\n\n**API authentication** usually comes in one of two flavours for devices:\n- **API key** — a static token in a header: `X-Api-Key: abc123`.\n- **Bearer token / JWT** — obtained by logging in, then `Authorization: Bearer <token>`.\nStore these in NVS, never hardcoded.\n\n**The payload pattern:** build JSON → POST with Content-Type: application/json → check status → parse response JSON. Repeat every telemetry cycle. This one pattern covers ThingsBoard, custom Node/Python backends, and most cloud IoT APIs.\n\n**Pitfalls:** don\'t hand-concatenate JSON strings with user data (escaping bugs and injection) — always use a serialiser; keep payloads small (an MCU has limited RAM and upload bandwidth).',
        code: '// Build and parse JSON with ArduinoJson\n#include <ArduinoJson.h>\nvoid sendJson(){\n  StaticJsonDocument<200> doc;\n  doc["device"] = "sensor7";\n  doc["temp"] = 25.4;\n  doc["unit"] = "C";\n  char buf[128];\n  serializeJson(doc, buf);\n  Serial.println(buf);   // {"device":"sensor7","temp":25.4,"unit":"C"}\n}',
        note: 'REST = verbs on resources (GET/POST/PUT/DELETE). JSON = the payload format. Use a serialiser/parser library, never string concat.',
      },
      {
        title: 'WebSockets: Real-Time Two-Way Updates',
        text: 'HTTP is request/response — the device must ask every time. **WebSockets** give a persistent, **two-way** channel where the server can push to the device at any moment, and vice versa. For live dashboards and remote control, they are the right tool.\n\n**How it differs from HTTP.** A WebSocket starts as an HTTP `Upgrade` handshake, then both sides keep a single TCP connection open, exchanging frames with minimal overhead — no per-message request headers. The server can push instantly; the client just listens.\n\n**On the ESP32 client side** (`WebSocketsClient` library):\n\n```cpp\nWebSocketsClient ws;\nws.begin("192.168.1.10", 8080, "/ws");\nws.onEvent(wsEvent);       // handle connect/disconnect/message\nws.loop();                 // keep alive, in your main loop\n```\n\n**Why the onEvent pattern matters:** events arrive asynchronously, so your `loop()` stays free for sensing while incoming commands are handled by the callback. This is the big advantage over polling — instant push with no busy-wait.\n\n**Typical use cases on a device:**\n- **Dashboard** pushing live temperature to a browser page (MQTT→WebSocket bridge on the server).\n- **Remote control**: a phone sends `{"cmd":"on","channel":3}` and the relay fires within milliseconds.\n- **Command fan-out**: a server pushes config changes to 100 devices at once.\n\n**Robustness matters more than with HTTP:** a WebSocket is a *stateful* connection. Handle the DISCONNECTED event and reconnect with backoff; never assume the socket is alive (send pings or check periodically). The WebSocketsClient library handles pings automatically.\n\n**WebSocket vs MQTT?** They overlap — both push both ways. MQTT is built for machine-to-machine at scale (topics, brokers, QoS) and works with minimal RAM. WebSockets are natural when a **browser** is the other end (browsers speak WS natively, not MQTT). Many architectures: **MQTT between devices and broker, WebSocket between broker and browser.**\n\nThe mental model: HTTP is asking, WebSockets is a shared phone line — either side can speak anytime.',
        code: '// ESP32 WebSocket client, event-driven\n#include <WebSocketsClient.h>\nWebSocketsClient ws;\nvoid wsEvent(WStype_t type, uint8_t* payload, size_t len){\n  if (type == WStype_TEXT) {\n    Serial.printf("Got: %s\\n", (char*)payload);   // {"cmd":"on"}\n    // parse & act (e.g. relayOn())\n  }\n}\nvoid setup(){ ws.begin("192.168.1.10", 8080, "/ws"); ws.onEvent(wsEvent); }\nvoid loop(){ ws.loop(); }',
        note: 'WebSockets = persistent two-way push. Handle events in a callback, reconnect on disconnect — perfect for live dashboards and control.',
      },
      {
        title: 'Choosing the API Style for Your Device',
        text: 'HTTP polling, REST+JSON, WebSockets, or MQTT — which should your device use? The answer depends on data volume, latency, and who is on the other end. This decision framework settles it.\n\n**HTTP polling (simple, occasional):** device GETs/POSTs every few seconds. Zero infrastructure, trivially debuggable with curl. Great for low-frequency telemetry (temperature every minute) against a simple server. Costs: per-request overhead, no instant push, battery unfriendly if frequent.\n\n**REST + JSON (structured, cloud APIs):** the standard for talking to dashboards, databases, and platform APIs (ThingsBoard, custom backends). Clear semantics, easy to log, human-readable. Choose this when you integrate with an existing web service.\n\n**WebSockets (instant, bidirectional):** persistent push both ways. Choose when a browser or app needs live updates, or when the server must command the device instantly. Adds connection-state complexity.\n\n**MQTT (many-to-many, low bandwidth):** pub/sub with a broker — a hundred devices publish to topics and any number of subscribers consume. Efficient, push-capable, low overhead on constrained devices, built-in QoS. The IoT default for sensor fleets and when cloud platforms (AWS IoT, ThingsBoard, HiveMQ) are involved.\n\n**Decision guide:**\n- One device, one server, low rate → HTTP polling.\n- Integrating a platform/database → REST/JSON.\n- Live browser dashboard / instant control → WebSocket.\n- Fleet of devices, brokers, QoS, cloud IoT → MQTT.\n\n**Common hybrid:** device publishes MQTT → broker → Node-RED/dashboard (WebSocket to the browser) → REST API for the mobile app. Each hop uses the right protocol.\n\n**Don\'t overthink.** Start with the simplest that meets the latency and scale needs; you can always add a gateway that translates protocols later. Premature protocol complexity is a common beginner trap.',
        code: '// Quick decision matrix\n//   Low-rate single device + server   -> HTTP polling\n//   Cloud platform / DB integration   -> REST + JSON\n//   Live dashboard / instant control  -> WebSocket\n//   Fleet of devices, QoS, brokers    -> MQTT\n//\n// Typical: MQTT(device->broker) -> WebSocket(broker->browser)',
        note: 'Match the protocol to latency and scale: HTTP for occasional, REST for platforms, WebSocket for live, MQTT for fleets.',
      },
    ],
    quizzes: [
      { text: 'Which HTTP verb creates a new resource?', options: ['GET', 'POST', 'DELETE', 'HEAD'], correctAnswer: 'POST' },
      { text: 'A 401 status code means…', options: ['Success', 'Authentication failed', 'Not found', 'Server error'], correctAnswer: 'Authentication failed' },
      { text: 'Why use a JSON library instead of string concatenation on an MCU?', options: ['It is slower', 'It escapes properly and avoids injection/parsing bugs', 'It uses more RAM', 'It is required by the IDE'], correctAnswer: 'It escapes properly and avoids injection/parsing bugs' },
      { text: 'WebSockets give you…', options: ['A one-shot request/response', 'A persistent two-way channel with instant push', 'Email delivery', 'File transfer only'], correctAnswer: 'A persistent two-way channel with instant push' },
      { text: 'Which is the natural pairing for a live browser dashboard?', options: ['MQTT between devices and broker + WebSocket between broker and browser', 'FTP between everything', 'SMTP polling', 'DNS updates'], correctAnswer: 'MQTT between devices and broker + WebSocket between broker and browser' },
      { text: 'For a fleet of 100 sensors publishing to a cloud platform, the IoT default is…', options: ['HTTP polling', 'MQTT with a broker', 'WebSockets per sensor', 'ICMP pings'], correctAnswer: 'MQTT with a broker' },
      { text: 'A WebSocket is best handled on the device via…', options: ['A blocking read loop', 'An event callback (onEvent)', 'A cron job', 'Polling every hour'], correctAnswer: 'An event callback (onEvent)' },
      { text: 'The first thing to log on every HTTP call is…', options: ['The CPU temperature', 'The status code', 'The SSID', 'The GPIO state'], correctAnswer: 'The status code' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 14 — MQTT Protocol Deep Dive
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 14,
    title: 'MQTT Protocol Deep Dive',
    description:
      'The publish/subscribe model, topics, QoS levels, retained messages, and building a full MQTT device pipeline.',
    topics: [
      {
        title: 'MQTT Publish/Subscribe Fundamentals',
        text: '**MQTT** is the de facto IoT messaging protocol: lightweight, push-based, and built for constrained devices and unreliable networks. Understanding its publish/subscribe model unlocks nearly every cloud IoT platform.\n\n**The cast of characters:**\n- **Broker** — the central message router (Mosquitto, HiveMQ, AWS IoT, ThingsBoard). Devices never talk to each other directly; they talk to the broker.\n- **Publisher** — sends a message to a **topic** (e.g. `sensors/room1/temp`).\n- **Subscriber** — subscribes to a topic (or wildcard) and receives every message published there.\n\n**Decoupling is the magic.** The publisher does not know who (if anyone) subscribes. Add a second dashboard later? It just subscribes — the publisher is untouched. Remove a device? Subscribers keep working. This one-to-many decoupling is why MQTT scales to fleets.\n\n**Topics are hierarchical strings:** `home/livingroom/temperature` and `home/livingroom/humidity`. Slashes create levels. Subscribers use **wildcards**: `+` matches one level (`home/+/temp` = any room\'s temp), `#` matches the rest (`home/#` = everything under home). Wildcards let one dashboard subscribe to a whole fleet with a single topic string.\n\n**A minimal device flow:**\n\n```cpp\nclient.setServer("broker.local", 1883);\nclient.connect("sensor7");          // unique client ID\nclient.subscribe("home/+/cmd");     // listen for commands\nclient.publish("home/livingroom/temp", "25.4");  // send a reading\n```\n\n**Why MQTT fits IoT:** tiny packets (a few bytes of header), works over TCP, broker handles fan-out, clients stay asleep and only wake to send/receive. Compared to HTTP, MQTT is push-native (no polling) and dramatically lighter on battery and bandwidth.\n\nThe mental model: MQTT is a **message board** — you pin messages to topic boards (publish), and anyone who is watching a board (subscribed) sees them instantly. Nobody is ever blocked waiting for a specific peer.',
        code: '// Minimal PubSubClient flow (ESP32)\n#include <WiFi.h>\n#include <PubSubClient.h>\nWiFiClient net; PubSubClient mqtt(net);\n\nvoid callback(char* topic, byte* payload, unsigned int len) {\n  Serial.printf("[%s] %.*s\\n", topic, len, payload);\n}\nvoid setup(){\n  mqtt.setServer("192.168.1.10", 1883);\n  mqtt.setCallback(callback);\n  mqtt.connect("sensor7");\n  mqtt.subscribe("home/+/cmd");\n  mqtt.publish("home/livingroom/temp", "25.4");\n}\nvoid loop(){ mqtt.loop(); }',
        note: 'MQTT = pub/sub via a broker. Publishers and subscribers are fully decoupled through hierarchical topics.',
      },
      {
        title: 'MQTT Topics, Wildcards & Best Practices',
        text: 'Topic design is MQTT\'s architecture. Bad topics make systems unmaintainable; good topics make fleets self-documenting. Here is the convention that survives real deployments.\n\n**Structure: a strict hierarchy.**\n\n```\n<domain>/<location>/<device>/<metric>\n```\n\nExamples: `factory/line1/motor5/temp`, `home/livingroom/temp`, `fleet/vehicle7/gps/lat`. Slashes separate levels; every level is a namespace you can subscribe to with wildcards.\n\n**Design rules:**\n- **Order matters: put the broadest category first.** `sensors/factory/line1/motor5/temp` reads left-to-right from general to specific. This makes `sensors/factory/#` natural for a whole factory.\n- **Use `+` for "any one level", `#` for "this and everything below".** `factory/+/motor5/temp` = that motor across all lines. `factory/#` = everything.\n- **Never end a topic with `/`** (a trailing empty level is legal but confusing) and avoid spaces/special chars — keep topics URL-safe.\n- **Do NOT put timestamps or random values in the topic itself** — that creates unbounded topic growth. Keep IDs in the **payload**: `home/temp` with body `{"device":"lamp5","v":25.4}`.\n\n**Payload vs topic:** topics are the address, payload is the content. Prefer JSON payloads with a device id, timestamp, and value — one topic serves many devices and the payload is self-describing.\n\n**Topic permissions & separation:** at the broker, you can grant publish/subscribe per topic. Structure topics so ACLs can be simple: `devices/<id>/#` for per-device, `admin/#` for operators.\n\n**Retained & last-will (next topics), but topic hygiene first:** a well-named topic hierarchy makes dashboards, ACLs, and debugging trivial. Spend the same care naming topics as you would naming functions — future-you will thank you.\n\nThe pattern that scales: `<site>/<zone>/<device>/<metric>` with JSON payloads, `#` for monitoring, `+` for aggregation, and no volatile data in topic strings.',
        code: '// Subscribe examples\nclient.subscribe("factory/#");                // everything in the factory\nclient.subscribe("factory/+/motor5/temp");    // motor5 across all lines\nclient.subscribe("home/+/temp");              // all rooms, temps only\n\n// Publish\nclient.publish("home/livingroom/temp", "25.4");   // payload: value\nclient.publish("home/livingroom/temp", "{\\\"v\\\":25.4,\\\"id\\\":\\\"lamp5\\\"}"); // JSON',
        note: 'Design topics as a hierarchy (site/zone/device/metric). Use + and # for selective subscriptions; keep timestamps/ids in the payload.',
      },
      {
        title: 'QoS Levels, Retained Messages & Last Will',
        text: 'Three MQTT features separate "works on a bench" from "works on a flaky network": **QoS**, **retained messages**, and the **last will**. Master them and your devices behave gracefully under real-world conditions.\n\n**QoS (Quality of Service) — delivery guarantees:**\n- **QoS 0 (at most once):** fire-and-forget. Fast, no acknowledgement, may drop. Good for high-rate telemetry where a lost sample is fine.\n- **QoS 1 (at least once):** the broker acknowledges; the sender retries until ACK. Guarantees delivery but a duplicate may occur. The default for sensor readings that must arrive.\n- **QoS 2 (exactly once):** a four-way handshake prevents duplicates. Heaviest, for payments/exactly-once semantics — rare in IoT.\n\n**Retained messages.** A topic can hold a **retained** flag: the broker stores the last message and delivers it to any NEW subscriber immediately. Perfect for **state** topics — `home/livingroom/lamp/state` with retained `{"on":true}` means a freshly booted dashboard instantly knows the lamp is on, without waiting for the next publish. Without retain, a new subscriber only sees messages published *after* it subscribes.\n\n**Last Will & Testament (LWT).** The device tells the broker before it connects: "if I vanish without a clean disconnect, publish this message." On an abnormal drop (power cut, network death), the broker publishes the will. Subscribers use it to detect dead devices instantly — a gateway marks a device offline, or triggers a fallback action.\n\n```cpp\nclient.connect("sensor7", \"sensor7/status\", 1, true, "offline");\nclient.publish("sensor7/status", "online", true);  // retained\n```\n\nIf the device dies, the broker auto-publishes `offline` to `sensor7/status`.\n\n**The combination that powers production IoT:** every device publishes a **retained status** (online/offline) + an LWT (offline), and sends telemetry with **QoS 1**. Dashboards subscribe to `+/status` for presence and `+/data/#` for values. Devices that "just disappear" become visible instantly.\n\nPitfall: retained messages persist forever — publishing to a retained topic with a wrong value leaves stale state. Publish a clean `{"online":false}` or delete with a retained empty payload when a device goes away.',
        code: '// Retained status + LWT + QoS 1 telemetry\nclient.connect("sensor7", "sensor7/status", 1, true, "offline");\nclient.publish("sensor7/status", "online", true);          // retained\nclient.publish("sensor7/temp", "25.4", 1);                 // QoS 1\n// Broker publishes "offline" automatically if the device dies',
        note: 'QoS 0 = may drop, QoS 1 = at least once, QoS 2 = exactly once. Retained holds state; LWT announces death. Use them together.',
      },
      {
        title: 'Building a Full MQTT Device Pipeline',
        text: 'Let us wire a complete MQTT pipeline end-to-end — a sensor node publishing to a broker, with a dashboard and a command channel — so the pieces come together into one working system.\n\n**1. Broker.** Install Mosquitto locally (`sudo apt install mosquitto mosquitto-clients`) or use a cloud broker (ThingsBoard, AWS IoT, HiveMQ Cloud). For local dev, Mosquitto on localhost with default port 1883 works immediately. Test it with the CLI tools:\n\n```\nmosquitto_sub -t \'home/#\' -v          # watch everything\nmosquitto_pub -t \'home/test\' -m \'hi\'  # send a message\n```\n\n**2. Device publishes telemetry.** The ESP32 reads a sensor, publishes `home/<room>/temp` with QoS 1 every 30s. The PubSubClient handles the MQTT side; your loop keeps sensing and calls `mqtt.loop()` to pump incoming traffic.\n\n**3. Device subscribes to commands.** Subscribe to `home/<room>/cmd`. A retained empty-state plus LWT keeps presence visible. The callback parses `{"cmd":"relay_on"}` and fires the relay.\n\n**4. Dashboard subscribes.** Node-RED, a Python script, or a web page with a WebSocket bridge subscribes to `home/#`, renders live values, and publishes commands. The same broker fans data to every subscriber.\n\n**5. Presence & reliability.** Retained `status` + LWT `offline` let the dashboard show green/red per device. A reconnect loop (`ensureWifi` + `mqtt.reconnect()`) keeps the node alive through router reboots.\n\n**6. Security.** In production: unique credentials per device, TLS on port 8883 (encrypted), and topic ACLs so devices can only publish their own topics. In dev, plain 1883 on a private LAN is acceptable — never on the public internet.\n\n**The complete device loop:**\n\n```cpp\nvoid loop(){\n  ensureWifi();\n  if (!mqtt.connected()) reconnect();\n  mqtt.loop();\n  publishTelemetry();      // every 30s via millis()\n}\n```\n\nThat loop — link-check, reconnect, pump, publish — is the heart of every reliable MQTT node you will ever build. Everything else is detail.',
        code: '// The production-shaped MQTT loop\nvoid loop(){\n  ensureWifi();\n  if (!mqtt.connected()) reconnect();   // blocking, with timeout\n  mqtt.loop();\n  static unsigned long last = 0;\n  if (millis() - last > 30000) {        // every 30s\n    mqtt.publish("home/livingroom/temp", String(readTemp()).c_str(), 1);\n    last = millis();\n  }\n}',
        note: 'The pipeline: broker (Mosquitto/cloud) + device publish/subscribe + dashboard subscribe + presence (retain+LWT) + TLS in production.',
      },
    ],
    quizzes: [
      { text: 'In MQTT, devices communicate…', options: ['Directly with each other', 'Through a central broker', 'Via email', 'By broadcasting on UDP'], correctAnswer: 'Through a central broker' },
      { text: 'Which wildcard matches exactly ONE topic level?', options: ['#', '+', '*', '?'], correctAnswer: '+' },
      { text: 'A subscriber with `factory/#` receives…', options: ['Only one topic', 'Everything under factory (all levels)', 'Only QoS 2 messages', 'Nothing without retain'], correctAnswer: 'Everything under factory (all levels)' },
      { text: 'Which QoS level guarantees delivery but may duplicate?', options: ['QoS 0', 'QoS 1', 'QoS 2', 'QoS 3'], correctAnswer: 'QoS 1' },
      { text: 'Retained messages are used to…', options: ['Store the last state so new subscribers get it instantly', 'Keep the connection alive', 'Encrypt the payload', 'Reduce CPU usage'], correctAnswer: 'Store the last state so new subscribers get it instantly' },
      { text: 'The Last Will & Testament is published by the broker when…', options: ['The device publishes telemetry', 'The device disconnects abnormally', 'A subscriber joins', 'The topic is created'], correctAnswer: 'The device disconnects abnormally' },
      { text: 'Why keep timestamps OUT of topic names?', options: ['They are too long', 'They create unbounded topic growth; put them in the payload', 'They slow the broker', 'Wildcards cannot match them'], correctAnswer: 'They create unbounded topic growth; put them in the payload' },
      { text: 'For production MQTT over the internet you should use…', options: ['Plain 1883', 'TLS on port 8883 with unique credentials per device', 'UDP instead of TCP', 'No broker'], correctAnswer: 'TLS on port 8883 with unique credentials per device' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 15 — Cloud Platforms & Dashboards
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 15,
    title: 'Cloud Platforms & Dashboards',
    description:
      'Choosing and using an IoT cloud platform — ThingsBoard, AWS IoT Core — with device provisioning, telemetry, and dashboards.',
    topics: [
      {
        title: 'What an IoT Cloud Platform Does',
        text: 'An **IoT cloud platform** is the managed backend that absorbs your devices\' data and hands it to dashboards and apps — so you do not build the plumbing yourself. Choosing one early saves months of reinventing message queues and databases.\n\n**What every platform provides:**\n- **Device connectivity & protocol gateway** — MQTT/HTTPS/LoRaWAN ingestion.\n- **Device registry & provisioning** — a secure way to add, authenticate, and manage devices (each device gets credentials/certificates).\n- **Telemetry storage** — a time-series database for sensor history.\n- **Rules & alerts** — trigger emails/SMS/webhooks when values cross thresholds.\n- **Dashboards & visualisation** — live widgets (gauges, charts, maps) without frontend code.\n- **Command/control downlink** — send RPCs to devices (turn relay on).\n- **APIs & integrations** — REST/MQTT APIs to connect your own app or other systems.\n\n**The spectrum of platforms:**\n- **Full IoT platforms:** ThingsBoard, AWS IoT Core, Azure IoT Hub, Google Cloud IoT, Balena. End-to-end device-to-dashboard.\n- **Broker-plus tools:** a plain MQTT broker (Mosquitto) + InfluxDB + Grafana + Node-RED assembled yourself. More work, total control, zero vendor lock-in.\n- **PaaS data pipelines:** AWS Kinesis/Azure Event Hubs + functions for high-throughput custom pipelines.\n\n**ThingsBoard** is the sweet spot for learning and self-hosting: install it on a Pi/server, create devices, and it speaks MQTT out of the box, stores telemetry, and renders dashboards. The ESP32 publishes to `v1/devices/me/telemetry` with an access token, and ThingsBoard does the rest.\n\n**AWS IoT Core** is the cloud-scale choice: MQTT with X.509 certificate auth, rules engine that routes telemetry to DynamoDB/S3/Lambda, and device shadows for state.\n\nThe decision: if you want **control + learning + no cost**, self-host ThingsBoard. If you want **managed scale + AWS ecosystem**, use AWS IoT Core. If your project is just a handful of nodes, even a bare broker + Grafana is plenty.',
        code: '// ThingsBoard ingestion is just MQTT with an access token\n#include <PubSubClient.h>\nWiFiClient net; PubSubClient mqtt(net);\nconst char* TOKEN = "A1_TEST_DEVICE_TOKEN";\nvoid setup(){\n  mqtt.setServer("thingsboard.local", 1883);\n  mqtt.connect("ESP32-client", TOKEN);   // token = device credential\n}\nvoid sendTelemetry(float t){\n  char msg[64];\n  snprintf(msg, 64, "{\\\"temperature\\\":%.1f}", t);\n  mqtt.publish("v1/devices/me/telemetry", msg);\n}',
        note: 'A cloud platform gives you connectivity, telemetry storage, rules, and dashboards for free. ThingsBoard to self-host, AWS IoT Core for managed scale.',
      },
      {
        title: 'Device Provisioning & Authentication',
        text: 'Every real IoT platform requires devices to **prove who they are** before sending data. Provisioning — issuing that identity securely — is the security foundation of the whole fleet.\n\n**Authentication flavours used by platforms:**\n- **Access tokens** (ThingsBoard, simple brokers): a per-device string sent with the MQTT connect or in the topic. Easy for development; treat it as a password.\n- **Username/password** per device (Mosquitto with auth plugin): each device has its own credentials.\n- **X.509 certificates** (AWS IoT, Azure): the device holds a client certificate; the platform verifies it during the TLS handshake. Strongest and industry-standard at scale.\n\n**Provisioning models:**\n1. **Manual/static:** you create each device in the console, copy its token/cert into firmware, flash. Fine for tens of devices.\n2. **Claim/bulk provisioning:** the platform exposes a provisioning endpoint; a device with a shared "provisioning key" presents a unique serial and is auto-registered and issued its own credential. Necessary for hundreds of units — no human touches each one.\n3. **Zero-touch with certificates:** the device ships with a factory cert (PKI); on first boot it registers itself. The end state for manufacturing.\n\n**Best practice for firmware:**\n- Never hardcode production credentials in source; store them in NVS/secure element.\n- Separate **dev** and **prod** credentials.\n- On credential rotation, support re-provisioning without a full reflash.\n\n**What NOT to do:** shipping the same token in every unit (one leak = whole fleet compromised), storing plaintext credentials in public firmware, and letting devices authenticate anonymously.\n\n**The mental model:** provisioning is **issuing a driver\'s license** to each device. One shared secret bootstraps the fleet, then each device gets its own identity that can be revoked individually. A compromised single device should never expose the fleet.\n\nFor a class project, access tokens are perfectly fine; when you design for scale or production, move to per-device certificates and a proper provisioning flow.',
        code: '// ThingsBoard static token provisioning\nconst char* DEVICE_TOKEN = "A1_TEST_DEVICE_TOKEN";  // per-device, from console\nvoid connectMQTT(){\n  while (!mqtt.connected()) {\n    if (mqtt.connect("esp32-client", DEVICE_TOKEN)) {\n      Serial.println("MQTT connected");\n    } else { delay(2000); }\n  }\n}\n// For fleets: add a provisioning key + serial -> auto-register',
        note: 'Provisioning = issuing per-device identity. Tokens for dev, certificates for production; never share one credential across the fleet.',
      },
      {
        title: 'Telemetry Ingress: Device to Cloud',
        text: 'Getting telemetry from a device into the cloud is a pipeline with a few clean steps. Get each right and data flows reliably for years.\n\n**1. Packetise the reading.** Turn sensor values into a small self-describing payload. JSON is the default: `{"temperature":25.4,"humidity":60,"ts":1723432000}`. Include the device identity in the payload or the topic, and a timestamp if the platform doesn\'t add one.\n\n**2. Send over MQTT.** Connect, publish to the platform\'s ingest topic, disconnect or stay subscribed. QoS 1 for readings that must not be lost. Keep packets small — every byte travels over the air and costs battery.\n\n**3. Handle batching.** Sending one packet per reading at 1Hz is wasteful. **Batch:** accumulate readings for 30-60s and send them in one message (JSON array). This cuts radio-on time and extends battery life dramatically. Only latency-critical data must stream immediately.\n\n**4. Retry & ack.** If a publish fails (no broker, bad network), queue the reading in RAM or a small flash buffer and retry later. Dropping data silently is the cardinal sin of telemetry. A few KB of flash can buffer hours of readings.\n\n**5. Watch the units and schema.** The cloud stores what you send — inconsistent units (C vs F) or a changing schema creates garbage analytics. Define the schema once (field names, units, types) and keep every device compliant. A `temp_c` field always means Celsius.\n\n**6. Monitor ingress.** The platform should show "devices online / last seen / messages received". If ingress stops, it is usually: WiFi dropped (reconnect), MQTT disconnected (reconnect), or credentials rejected (re-provision).\n\nThe pipeline in one line: **read → JSON-ise → batch → MQTT publish (QoS 1) → buffer & retry on failure → consistent schema.** Design the schema first, then the code.',
        code: '// Batching telemetry before publish\nchar buf[512]; int n = 0;\nvoid queueReading(float t, float h){\n  n += snprintf(buf + n, sizeof(buf) - n,\n    "%s{\\\"temperature\\\":%.1f,\\\"humidity\\\":%.1f}", n ? "," : "[", t, h);\n  if (n > 200) flush();   // send when the batch is full\n}\nvoid flush(){ mqtt.publish("v1/devices/me/telemetry", buf); n = 0; buf[0] = 0; }',
        note: 'Telemetry = small JSON payloads, batched, sent via MQTT QoS 1, buffered and retried on failure, with a fixed schema.',
      },
      {
        title: 'Dashboards, Alerts & RPC Control',
        text: 'Data in the cloud is only useful when humans and systems act on it. Dashboards visualise, alerts react, and **RPC (remote procedure call)** lets you control devices back from the cloud.\n\n**Dashboards.** ThingsBoard and Grafana render live widgets from stored telemetry: gauges, line charts, tables, maps, and on/off switches. Because they subscribe to live MQTT/WebSocket feeds, values update in real time without refreshing. A good dashboard answers a question at a glance — "is rack 3 too hot?" — not just "here are numbers".\n\n**Alerts & rules.** The platform\'s rule engine watches telemetry: when temperature > 28°C for two consecutive readings, send an email/telegram/SMS or call a webhook. Design alerts to be **actionable and rate-limited** — alerting every second during a ramp-up just trains people to ignore you. Hysteresis (warn at 28, clear at 26) stops alert-flapping.\n\n**RPC / commands (downlink).** Beyond reading data, platforms push commands to devices: `switch`, `setpoint`, `reboot`. Two styles:\n- **Asynchronous commands:** the platform publishes to a topic the device subscribes to; the device acts when it gets one.\n- **Synchronous RPC:** the platform sends a request and waits for the device\'s response (with a timeout). ThingsBoard uses `v1/devices/me/rpc/request/+`; the device answers on `v1/devices/me/rpc/response/1`.\n\nThe device side is just MQTT subscribe + callback:\n\n```cpp\n// on RPC request topic -> parse cmd -> act -> publish response\nmqtt.subscribe("v1/devices/me/rpc/request/+");\n```\n\n**The closed loop in practice:** sensor reads temp → MQTT → cloud stores → rule engine sees 29°C → alert to operator → operator hits "cool it" in dashboard → RPC `{"cmd":"valve_open"}` → device opens valve → new reading confirms 24°C. Sense → cloud → decide → act → confirm. That loop is the entire point of IoT.\n\nStart simple: one dashboard, one alert, one RPC. Extend after the loop works end-to-end.',
        code: '// ESP32 handling an RPC (e.g. ThingsBoard)\nvoid callback(char* topic, byte* payload, unsigned int len){\n  String t = topic;\n  if (t.indexOf("rpc/request") >= 0) {\n    String id = t.substring(t.lastIndexOf(\'/\') + 1);\n    // payload = {"method":"relay_on","params":{}}\n    if (strstr((char*)payload, "relay_on")) digitalWrite(RELAY, HIGH);\n    String resp = "{\\\"ok\\\":true}";\n    mqtt.publish((\"v1/devices/me/rpc/response/\" + id).c_str(), resp.c_str());\n  }\n}',
        note: 'Dashboards visualise, rules alert, RPC controls. The full IoT loop: sense → cloud → decide → act → confirm.',
      },
    ],
    quizzes: [
      { text: 'What does an IoT cloud platform provide out of the box?', options: ['Only a database', 'Device registry, telemetry storage, rules, dashboards, and APIs', 'Only MQTT brokers', 'Physical hardware'], correctAnswer: 'Device registry, telemetry storage, rules, dashboards, and APIs' },
      { text: 'The industry-standard authentication for cloud IoT devices is…', options: ['The device\'s MAC address', 'X.509 certificates verified during TLS', 'A shared secret printed on the box', 'None'], correctAnswer: 'X.509 certificates verified during TLS' },
      { text: 'Why should you NOT share one credential across every device?', options: ['It is slow', 'One compromised device exposes the whole fleet', 'Credentials expire faster', 'The broker rejects duplicates'], correctAnswer: 'One compromised device exposes the whole fleet' },
      { text: 'Batching telemetry before sending helps…', options: ['Make the dashboard prettier', 'Cut radio-on time and extend battery life', 'Increase packet size', 'Bypass QoS'], correctAnswer: 'Cut radio-on time and extend battery life' },
      { text: 'Dropping readings silently on a failed publish is…', options: ['Acceptable', 'The cardinal sin of telemetry — buffer and retry instead', 'Required by MQTT', 'Faster and better'], correctAnswer: 'The cardinal sin of telemetry — buffer and retry instead' },
      { text: 'Alert flapping is prevented with…', options: ['Bigger dashboards', 'Hysteresis — warn at one threshold, clear at another', 'Faster sensors', 'Longer topic names'], correctAnswer: 'Hysteresis — warn at one threshold, clear at another' },
      { text: 'RPC downlink lets the platform…', options: ['Read the sensor', 'Send commands the device executes (e.g. relay_on)', 'Update the firmware silently', 'Change the SSID'], correctAnswer: 'Send commands the device executes (e.g. relay_on)' },
      { text: 'The complete IoT value loop is…', options: ['Sense → cloud → decide → act → confirm', 'Build → deploy → forget', 'Publish → sleep → repeat', 'Connect → disconnect → reconnect'], correctAnswer: 'Sense → cloud → decide → act → confirm' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 16 — Data Collection, Storage & Telemetry
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 16,
    title: 'Data Collection, Storage & Telemetry',
    description:
      'Designing the data layer — local storage on the device, time-series databases, and reliable end-to-end data pipelines.',
    topics: [
      {
        title: 'On-Device Data Storage: NVS, SPIFFS, LittleFS & SD',
        text: 'Devices do not always have connectivity. Storing data **on the device** — in flash or on an SD card — makes your system survive offline periods and keep history.\n\n**The storage tiers on an ESP32:**\n- **NVS (Non-Volatile Storage)** — tiny key-value store in flash for **configuration**: WiFi credentials, device id, calibration, counters. Survives reboots, tiny footprint. Use `Preferences` for this.\n- **SPIFFS / LittleFS** — a small filesystem in the ESP32\'s flash for **files**: config files, small logs, web assets served by the device. A few MB available.\n- **SD card (SPI)** — gigabytes of removable storage for **big data**: sensor logs, images, audio, offline buffering. The `SD` + `SPI` libraries handle it.\n\n**When to use which:**\n- Config that must survive reboot → **NVS**.\n- Small files, web UI assets, mini-logs → **LittleFS**.\n- High-volume telemetry logs, media, long offline buffering → **SD card**.\n\n**SD card basics:**\n```cpp\n#include <SPI.h>\n#include <SD.h>\nSD.begin(CS_PIN);\nFile f = SD.open("/log.csv", FILE_APPEND);\nif (f) { f.println("1723432000,25.4,60.2"); f.close(); }\n```\nCSV (comma-separated) is the perfect log format — human-readable, trivially imported into Excel/pandas later.\n\n**Logging discipline:**\n- **Write with timestamps** — a log line without a time is nearly useless. Use epoch seconds (millis()/1000 + boot time sync).\n- **Append, don\'t rewrite** — open with FILE_APPEND.\n- **Close files promptly** — a power cut during a write can corrupt the card.\n- **Rotate logs** — when a file exceeds a size, roll to `log-2.csv` and delete the oldest, so the card never fills.\n- **Buffer in RAM, flush periodically** — writing every reading to flash wears it out and slows the loop. Accumulate lines in a buffer, flush every 30-60s.\n\n**Flash wear is real.** Flash cells tolerate ~10k-100k writes. Design for **batched, infrequent writes** to flash/SD, never one write per millisecond. Your device will outlast the product otherwise.\n\nThe mental model: **NVS for config, filesystem for small files, SD for big data — and always timestamp, append, rotate, and flush in batches.**',
        code: '// Append a telemetry row to an SD log, buffered\nString buf;\nvoid logReading(float t, float h){\n  buf += String(millis() / 1000) + "," + String(t) + "," + String(h) + "\\n";\n  if (buf.length() > 500) flushLog();\n}\nvoid flushLog(){\n  File f = SD.open("/telemetry.csv", FILE_APPEND);\n  if (f) { f.print(buf); f.close(); }\n  buf = "";\n}',
        note: 'NVS for config, LittleFS for small files, SD for big data. Timestamp everything, append, rotate, and flush in batches to limit flash wear.',
      },
      {
        title: 'Time-Series Databases & Data Pipelines',
        text: 'Telemetry is **time-series data** — values indexed by time — and it demands the right storage. A regular database treats every row as equal; a time-series database (TSDB) is optimised for append-heavy, time-indexed queries, compression, and retention.\n\n**Why TSDBs exist:**\n- Telemetry is **append-mostly** (new readings only) with rare updates.\n- Queries are **time-windowed**: "average temperature for the last hour".\n- Compression matters: a year of 1Hz readings is millions of rows.\n- **Retention policies** — keep raw data 30 days, 1-minute averages a year — trim storage automatically.\n\n**The TSDB family:**\n- **InfluxDB** — the IoT/ops favourite. Has a simple write API: `curl -XPOST \'http://localhost:8086/write?db=mydb\' --data \'temperature,device=sensor7 value=25.4\'`. Queries in InfluxQL/Flux. Pairs perfectly with Grafana.\n- **TimescaleDB** — PostgreSQL with a time-series extension; good if you already live in SQL.\n- **Prometheus** — pull-based, metric-oriented, great for ops dashboards.\n- Managed: AWS Timestream, Azure Time Series Insights.\n\n**The data pipeline shape:**\n\n```\nsensors -> MQTT broker -> [collector] -> TSDB -> [dashboard / alerting]\n```\n\nThe **collector** (Node-RED, Telegraf, a tiny Python script) subscribes to MQTT topics, parses the JSON, and writes points to the TSDB. Because the broker decouples producers from storage, you can add databases, dashboards, or analytics without touching the devices.\n\n**Schema by example (InfluxDB):**\n```\ntemperature,device=sensor7,room=living value=25.4 1723432000\n^measurement  ^tags (indexed)        ^field  ^time\n```\nTags are indexed (use for device, room); fields are values (use for the reading). Tag cardinality matters — don\'t tag with ever-changing values.\n\n**End-to-end reliability:** the collector should survive broker restarts (reconnect), batch writes (TSDBs prefer bulk inserts), and retry on failure. A dead collector silently stops the whole pipeline — monitor IT as carefully as the sensors.',
        code: '// A Python collector: MQTT -> InfluxDB (concept)\n# import paho.mqtt.client as mqtt\n# import influxdb_client\n# on message:\n#   parse JSON {temperature:25.4, device:"sensor7"}\n#   point = Point("temperature").tag("device", d) \\\n#           .field("value", t).time(dt.utcnow(), "ms")\n#   write_api.write(bucket="iot", record=point)',
        note: 'Telemetry belongs in a TSDB (InfluxDB/TimescaleDB). Pipeline: sensors → broker → collector → TSDB → dashboard. Bulk-write and monitor the collector.',
      },
      {
        title: 'End-to-End Data Reliability & Ordering',
        text: 'Telemetry systems fail in predictable, embarrassing ways — gaps in the graph, duplicate points, out-of-order readings. A little design up front makes the data trustworthy.\n\n**The failure modes:**\n1. **Gaps** — WiFi dropped, publish failed, collector down.\n2. **Duplicates** — QoS 1 retry delivers twice.\n3. **Out-of-order** — two collectors write to one time series with different clocks.\n4. **Schema drift** — some devices send C, some send F.\n\n**Guaranteeing no silent gaps:**\n- **Buffer at the device** (RAM + flash) and retry until acked.\n- **Batch + acknowledge:** publish QoS 1 and treat a confirmed publish as "delivered". Keep a pending queue in flash for hard-offline periods.\n- **Sequence numbers** — attach an incrementing `seq` to each reading. The collector can detect missing points (a gap in seq) and request a catch-up or flag it.\n\n**Handling duplicates gracefully:**\n- **Idempotent writes.** A TSDB write by `(time, tagset)` is naturally idempotent if you overwrite on conflict. InfluxDB `?onConflict` semantics or a unique key make duplicates harmless.\n- **Or dedupe by seq** at the collector: drop a point whose seq ≤ the last seen from that device.\n\n**Ordering & clocks:**\n- **The server timestamp wins for storage.** Let the platform stamp on arrival (`ts = now()`) rather than trusting every device\'s clock. Devices with unsynced clocks produce a jagged chart.\n- **If you need device time** (offline periods), send `device_ts` as a separate field and let the dashboard choose.\n- **One writer per time series** avoids interleaved clocks entirely — single collector, single clock.\n\n**Validation at the edge:** reject absurd values at the device (temperature -200°C) AND at the collector. A bad point written once poisons averages forever unless you scrub it.\n\n**Monitoring the pipeline itself:** track `messages in`, `points written`, `duplicate rate`, `device last-seen`. If you cannot see the health of the pipeline, you will only discover failures when a dashboard looks "a bit off".\n\nThe rule: **no silent data loss, idempotent writes, one canonical clock, and validate twice.** That is what "trustworthy telemetry" means.',
        code: '// Device-side: sequence number + QoS 1 + flash-backed retry\nuint32_t seq = 0;\nvoid sendOrQueue(float t){\n  char msg[64];\n  snprintf(msg, 64, "{\\\"t\\\":%.1f,\\\"seq\\\":%lu}", t, ++seq);\n  if (!mqtt.publish("d/s7/temp", msg, 1)) {\n    queueToFlash(msg);   // retry later, never drop\n  }\n}',
        note: 'Trustworthy telemetry: no silent drops (queue+retry), idempotent writes (dupes harmless), server timestamp, one writer, validate twice.',
      },
      {
        title: 'Visualising Data: Graphs, Anomalies & Insights',
        text: 'A dashboard that just prints numbers wastes the data. The skill of turning telemetry into **decisions** — spotting trends, anomalies, and insights — is what separates a demo from a product.\n\n**Start with the right chart.**\n- **Time series line chart** — the default: value vs time, one line per device/metric. Great for trends and anomalies.\n- **Gauge/stat** — a single current value ("25.4 °C now") with thresholds colouring it green/amber/red.\n- **Histogram** — distribution of values over a day ("how many hours was the room 23-24 °C?").\n- **Scatter** — relationships between two metrics (temp vs humidity).\n- **Map** — geo-tagged devices.\n\n**Anomaly detection without ML:**\n- **Fixed thresholds** — the simplest and often enough: >28°C flags.\n- **Rate-of-change** — a temperature jumping 5°C in a minute is an anomaly even if it never crosses the absolute threshold.\n- **Rolling baseline** — compare a reading to the moving average ± 3σ of the last hour; deviations flag unusual behaviour.\n- **Cross-device comparison** — one sensor diverging from its siblings on the same rack is a strong anomaly signal.\n\n**The insight loop:**\n1. **Detect** an anomaly (threshold, rate, deviation).\n2. **Diagnose** via correlated data (temp spike + humidity spike → door left open).\n3. **Act** (alert, RPC).\n4. **Confirm** the fix in the next reading.\n\n**Dashboard hygiene:**\n- Show **recent** and **trend** together — a gauge for "now" plus a 24h sparkline.\n- Aggregate sensibly (1-min, 1-hour averages) so charts don\'t drown in raw points.\n- Add **annotations** — mark events (reboot, deploy) on the time axis so you can correlate chart changes with what you did.\n- Keep it to the questions that matter — a cluttered dashboard is ignored.\n\n**The pro move:** export a day of data and eyeball it in a notebook/Excel — the patterns you see by hand are the ones to encode into rules. The data tells you what to automate next.',
        code: '// InfluxQL-ish: a 5-minute average over the last hour\n//  SELECT mean("value") FROM "temperature"\n//  WHERE time > now() - 1h\n//  GROUP BY time(5m), "device"\n//\n//  + Grafana: threshold alert at mean > 28 -> webhook/SMS',
        note: 'Choose the right chart, detect anomalies by threshold/rate/deviation, then act and confirm. Dashboards answer questions; they do not print numbers.',
      },
    ],
    quizzes: [
      { text: 'Which ESP32 storage should hold WiFi credentials that must survive reboot?', options: ['SD card', 'NVS (via Preferences)', 'LittleFS', 'PSRAM'], correctAnswer: 'NVS (via Preferences)' },
      { text: 'Why write telemetry logs in batches instead of one write per reading?', options: ['It looks nicer', 'Flash cells wear out — batching limits writes', 'It is required by MQTT', 'It makes CSV invalid'], correctAnswer: 'Flash cells wear out — batching limits writes' },
      { text: 'Telemetry is best stored in…', options: ['A plain spreadsheet', 'A time-series database like InfluxDB', 'A JSON file on the device', 'The broker only'], correctAnswer: 'A time-series database like InfluxDB' },
      { text: 'A time-series database is optimised for…', options: ['Complex joins', 'Append-heavy, time-indexed queries with retention', 'Transactions', 'B-tree lookups by name'], correctAnswer: 'Append-heavy, time-indexed queries with retention' },
      { text: 'How do you make duplicate MQTT deliveries harmless?', options: ['Ignore them', 'Idempotent writes keyed by time/tagset', 'Use longer topics', 'Restart the broker'], correctAnswer: 'Idempotent writes keyed by time/tagset' },
      { text: 'Which timestamp should a TSDB trust for storage?', options: ['The device clock', 'The server/platform arrival time', 'The GPS time', 'No time at all'], correctAnswer: 'The server/platform arrival time' },
      { text: 'A temperature jump of 5°C in a minute that never crosses the absolute threshold is caught by…', options: ['A fixed threshold alert', 'Rate-of-change detection', 'A bigger gauge', 'Longer topic names'], correctAnswer: 'Rate-of-change detection' },
      { text: 'The best first step before writing anomaly rules is…', options: ['Buy more sensors', 'Export a day of data and eyeball it for patterns', 'Disable alerts', 'Switch brokers'], correctAnswer: 'Export a day of data and eyeball it for patterns' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 17 — IoT Security Fundamentals
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 17,
    title: 'IoT Security Fundamentals',
    description:
      'The threat model for connected devices, authentication, TLS, secure boot, OTA updates, and practical hardening.',
    topics: [
      {
        title: 'The IoT Threat Model',
        text: 'Security starts with a **threat model**: who is attacking, what they want, and how they could get it. For IoT, the honest answers are scarier than for most software — and the fixes are mostly hygiene.\n\n**Who attacks IoT devices and why:**\n- **Botnet recruiters** — the biggest threat. Compromised devices are enslaved into botnets (Mirai-style) and used for DDoS. They scan the entire internet for default passwords and open ports.\n- **Credential re-users** — default admin/admin or known tokens are tried on every device.\n- **Snoopers** — on shared networks, unencrypted traffic (plain MQTT, HTTP) is readable.\n- **Tamperers** — physical access to a device means the attacker can read flash, sniff pins, and clone credentials.\n- **Ransom/denial** — bricking a device by pushing bad firmware.\n\n**Attack surfaces on a typical device:**\n- **Network:** open MQTT/HTTP ports, weak credentials, plaintext protocols.\n- **Firmware:** lack of secure boot — replace the firmware by flashing.\n- **Storage:** credentials in plaintext flash, readable with a $10 adapter.\n- **OTA:** unsigned updates — push malware as an "update".\n- **Web UI:** if the device hosts a control page, XSS/CSRF if not hardened.\n- **Cloud credentials:** one leaked device token exposing a fleet.\n\n**The risk equation for IoT:** devices are often headless, unattended, on 24/7, with weak hardware and long life. That is a perfect botnet host. The industry consensus is blunt: **assume any internet-facing device will be attacked within minutes of exposure.**\n\n**The practical posture that follows:**\n- No open ports except what you need, and even those behind a firewall/VPN.\n- No default credentials — unique per device.\n- Encryption everywhere (TLS).\n- Signed and authenticated firmware updates.\n- Least privilege in the cloud (per-device tokens/ACLs).\n- Secrets out of source code, into secure storage.\n\nA secure system is not one that resists a genius; it is one that defeats the **automated mass attacks** — because that is what will hit it first. Fix the obvious, and you have already outrun 99% of the threat.',
        code: '// The security checklist every device must pass\n//   [ ] No default credentials (admin/admin)\n//   [ ] Unique secret per device, stored in NVS/secure element\n//   [ ] TLS on every connection (no plaintext MQTT/HTTP)\n//   [ ] Secure boot / signed firmware\n//   [ ] OTA updates authenticated\n//   [ ] No exposed debug ports on the internet\n//   [ ] Least-privilege cloud tokens',
        note: 'Assume internet-exposed devices get attacked in minutes. Botnets scan for defaults; defeat the automated mass attack first.',
      },
      {
        title: 'Authentication, Authorization & Secrets Management',
        text: 'IoT security at the device level is mostly about **who can talk and what they can do** — authentication (are you who you claim?) and authorization (may you do this?). Getting secrets right is most of the battle.\n\n**Authentication options, weakest to strongest:**\n- **No auth** — anyone can publish. Never, even on a LAN.\n- **Shared username/password** — one set for all devices. Weak: one leak compromises all.\n- **Per-device token/password** — each device has its own credential. Acceptable for small fleets; a leaked token compromises one device, not all.\n- **Per-device X.509 certificates** — strong mutual TLS; the platform verifies the cert in the handshake. Industry standard for production.\n- **Hardware-backed keys** — an ATECC608A secure element holds the private key so it can never be extracted from flash. The gold standard for tamper resistance.\n\n**Authorization: least privilege.** A device should only be able to publish its own topics and read what it needs:\n- Broker ACLs: device `sensor7` may publish `sensors/sensor7/#` only.\n- Platform IAM roles: a device role has `connect + publish telemetry`, nothing else.\n\n**Secrets management — the practical rules:**\n- **Never hardcode secrets in source code** — a public GitHub repo with your AWS keys is a weekly real-world disaster.\n- Store secrets in **NVS** (Preferences) or a **secure element**; flash at provisioning time.\n- **Rotate secrets** — support re-provisioning so a compromised credential can be replaced without reflashing 500 devices.\n- Keep **dev and prod credentials separate**.\n- Treat firmware as public — an attacker can read it, so assume any embedded secret is extractable and design accordingly (that is why certs + secure element beat tokens).\n\n**The trust boundary mindset:** split the system so that compromising one device does not expose the fleet or the cloud: per-device credentials, per-device ACLs, and cloud-side rate limiting and anomaly detection.\n\nThe golden rule: **each device gets its own revocable identity, the least privilege it needs, and secrets that live in protected storage — never in source.**',
        code: '// Provision a per-device token into NVS, never hardcoded\n#include <Preferences.h>\nPreferences prefs;\nvoid provision(const char* token){\n  prefs.begin("sec", false);\n  prefs.putString("token", token);\n  prefs.end();\n}\n// Boot: token = prefs.getString("token", "");\n// If empty -> enter provisioning mode',
        note: 'Auth = identity (token or cert), authz = least privilege per device. Secrets in NVS/secure element, never in source, always revocable.',
      },
      {
        title: 'TLS, Certificates & Encrypted Communication',
        text: 'Encryption on the wire is non-negotiable for any device that leaves a trusted LAN. **TLS** (the S in HTTPS, and the encryption layer under MQTTS on port 8883) protects data from eavesdropping and tampering. On an ESP32, it is both easy and heavy — get it right.\n\n**What TLS gives you:**\n- **Confidentiality** — nobody on the path can read the payload.\n- **Integrity** — nobody can alter it undetected.\n- **Authentication** — the client verifies the server\'s identity via its certificate.\n\n**How the ESP32 does TLS:**\n- `WiFiClientSecure` replaces `WiFiClient` in PubSubClient/HTTPClient.\n- The device verifies the **server certificate** against a **CA root certificate** (bundled into firmware via `setCACert`).\n- Optional but recommended for strong auth: the device presents its **client certificate** (mutual TLS), especially for AWS IoT.\n- `setInsecure()` disables verification — **never in production**; it makes the encryption pointless (a MITM just installs their own cert).\n\n```cpp\nWiFiClientSecure secure;\nsecure.setCACert(rootCA);        // verify the server\n// secure.setCertificate(clientCert); secure.setPrivateKey(key); // mTLS\nmqtt.setClient(secure);\nmqtt.setServer("broker.example.com", 8883);\n```\n\n**The resource cost is real.** TLS needs ~40-50KB RAM (ESP32 has 520KB — fine), and the handshake is CPU-heavy but the ESP32\'s hardware AES/SHA accelerates it. On an ESP8266 (limited RAM) TLS is tight but possible.\n\n**Certificate lifecycle headaches:**\n- **CA expiry** — the root CA in your firmware expires after ~years; devices need an OTA to update it. Track expiry dates!\n- **Fingerprint pinning** is a quick alternative for a single known server (hardcode its SHA256) but breaks when the cert rotates.\n- **Never disable verification** to "fix" a cert problem — fix the cert instead.\n\n**Beyond TLS:** the TLS handshake itself is the attack target — TLS clients on microcontrollers are sometimes simplified. Prefer maintained libraries (ESP-IDF mbedTLS) over hand-rolled crypto.\n\nThe rule: **TLS with server verification everywhere; mTLS for production fleets; never setInsecure(); and track cert expiry like a deadline.**',
        code: '// Secure MQTT on port 8883 with server verification\n#include <WiFiClientSecure.h>\n#include <PubSubClient.h>\nWiFiClientSecure secure;\nPubSubClient mqtt(secure);\nconst char* rootCA = "-----BEGIN CERTIFICATE-----\\n...\\n-----END CERTIFICATE-----";\nvoid setup(){\n  secure.setCACert(rootCA);          // verify the server, never insecure\n  mqtt.setServer("broker.example.com", 8883);\n  mqtt.connect("sensor7");\n}',
        note: 'TLS = confidentiality + integrity + server auth. Use WiFiClientSecure with setCACert, mTLS for fleets, and track cert expiry.',
      },
      {
        title: 'Secure Boot, OTA Updates & Hardening',
        text: 'The attack that lands is often **after** the device is deployed — via unsigned firmware or an open update channel. Securing the update path and boot chain closes the back door.\n\n**Secure boot.** On ESP32, **secure boot** cryptographically verifies the firmware at every boot: only a firmware signed by a key you hold will run. An attacker who gains physical flash access cannot boot a modified image. Combine with **flash encryption** (the firmware and NVS are encrypted on flash) so credentials are unreadable even with the chip in hand.\n\n**Why it matters:** without secure boot, "update" is just "flash whatever binary you want" — a captured device becomes a stepping stone into the fleet.\n\n**OTA (Over-The-Air) updates:** the ability to fix bugs and patch security without a USB cable. Rules for safe OTA:\n- **Sign the firmware** — verify an HMAC or signature before applying. Unsigned OTA lets an attacker push malware as an "update".\n- **Atomic install** — write the new image to the second slot (ESP32 uses dual OTA partitions), mark it active, reboot into it, and **roll back** to the last-good image if the new one crashes or fails a self-test.\n- **Authenticate the OTA server** (TLS), and the device → server too (mTLS) if sensitive.\n- **Rate-limit and throttle** updates to avoid bricking a whole fleet at once.\n\n**Practical hardening checklist:**\n- Remove/disable debug serial and JTAG in production builds.\n- Close unused ports; firewall off inbound except needed.\n- Disable Telnet/plain HTTP admin; use TLS.\n- Change every default password; enforce unique device keys.\n- Minimal attack surface: run the least code, expose the least.\n- **Log & monitor** — devices that report health make compromise visible.\n- Keep the toolchain and libraries patched (old mbedTLS = known CVEs).\n\n**The layered mindset:** depth-in-depth — even if one layer fails (a default password slips through), the next (TLS, per-device ACL, secure boot) still blocks the attacker. Security is a stack, not a single lock.\n\nThe rule: **secure boot + flash encryption to keep the device yours, signed+atomic OTA to keep it patched, and minimal surface + monitoring to stay ahead.**',
        code: '// OTA safety pattern (ESP32 ArduinoOTA, signed check concept)\n//   1. Download image over TLS\n//   2. Verify signature (HMAC/ECDSA) before touching active slot\n//   3. Write to inactive partition\n//   4. Mark active, reboot\n//   5. Self-test on boot; roll back to last-good on failure\n//\n// (The ESP-IDF provides all of this natively in the OTA APIs.)',
        note: 'Secure boot + flash encryption keep the device yours; signed, atomic OTA keeps it patched. Harden surfaces and monitor.',
      },
    ],
    quizzes: [
      { text: 'The biggest real-world IoT threat is…', options: ['Physical theft', 'Botnet recruitment of weak devices', 'Gamma radiation', 'Screen burn-in'], correctAnswer: 'Botnet recruitment of weak devices' },
      { text: 'The strongest practical authentication for production IoT devices is…', options: ['A shared password', 'Per-device X.509 certificates (mutual TLS)', 'The device MAC address', 'No authentication'], correctAnswer: 'Per-device X.509 certificates (mutual TLS)' },
      { text: 'Why must secrets never be hardcoded in source code?', options: ['They slow compilation', 'A public repo leaks them — treat firmware as readable', 'They use too much RAM', 'The IDE rejects them'], correctAnswer: 'A public repo leaks them — treat firmware as readable' },
      { text: 'What does `setInsecure()` do and why avoid it?', options: ['It speeds up TLS and is great', 'It disables server verification, making encryption bypassable', 'It sets a secure password', 'It turns off the radio'], correctAnswer: 'It disables server verification, making encryption bypassable' },
      { text: 'What does secure boot guarantee?', options: ['Faster boot', 'Only signed firmware runs', 'Longer battery', 'A wifi name change'], correctAnswer: 'Only signed firmware runs' },
      { text: 'Why must OTA firmware be signed?', options: ['To make it smaller', 'So an attacker cannot push malware as an "update"', 'To improve battery', 'To satisfy the broker'], correctAnswer: 'So an attacker cannot push malware as an "update"' },
      { text: 'Atomic OTA installs use…', options: ['One partition overwritten in place', 'Dual slots with rollback to last-good on failure', 'The SD card only', 'No partitions'], correctAnswer: 'Dual slots with rollback to last-good on failure' },
      { text: 'Security in depth means…', options: ['One very strong password', 'Layers: if one defence fails the next still blocks the attacker', 'Only physical locks', 'No security at all on the LAN'], correctAnswer: 'Layers: if one defence fails the next still blocks the attacker' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 18 — Smart Home Systems & Integration
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 18,
    title: 'Smart Home Systems & Integration',
    description:
      'Designing smart-home devices, integrating with assistants and ecosystems, and building a working home automation hub.',
    topics: [
      {
        title: 'Smart Home Architecture & the Hub Pattern',
        text: 'A smart home is a fleet of IoT devices orchestrated by a **hub or bridge** — one central brain that speaks many protocols and presents them to apps and assistants. Understanding the hub pattern explains how real smart homes stay manageable.\n\n**Why a hub?** Smart bulbs speak Zigbee, sensors speak Z-Wave or BLE, cameras speak WiFi, and an old IR-controlled AC speaks infrared. No phone app can talk to all of them directly. The **hub** (Home Assistant, a Raspberry Pi, SmartThings, Hubitat) translates: it has radios for each protocol, runs the automation logic, and exposes one clean interface to the user (app, voice assistant, web).\n\n**The architecture:**\n\n```\n[devices: Zigbee/WiFi/BLE/IR]\n      |\n   [HUB: Home Assistant / Pi]\n      |\\-- automations + dashboards\n      |\\-- cloud bridge to Alexa/Google/HomeKit\n      |\\-- MQTT for custom ESP32 devices\n```\n\n**Your ESP32 devices slot into the hub** via MQTT — publish `home/<room>/<device>/state` and subscribe to `home/<room>/<device>/cmd`. Home Assistant subscribes to the same topics, so an ESP32 relay becomes a first-class smart switch: controllable from the app, dashboards, and automations.\n\n**Local-first vs cloud:** a **local-first** hub (Home Assistant, Node-RED) keeps automations running when the internet drops and protects privacy. Cloud-dependent systems stop working in an outage and send your data through third parties. The professional default is local-first with a local MQTT broker (Mosquitto on the Pi).\n\n**The discovery pattern:** devices announce themselves (`homeassistant/<device>/config`) so the hub can auto-discover their capabilities — the MQTT Discovery convention. That is how a new ESP32 sensor appears in Home Assistant with no configuration.\n\n**Design rules for your own smart-home device:**\n- Speak **MQTT** with a clean topic schema.\n- Publish **state** (retained) and accept **commands**.\n- Advertise via **discovery** so the hub auto-adds it.\n- Implement **local fallback** — keep the relay state sane even if the hub is down.\n\nThe hub pattern is why a "smart home" is buildable piece by piece — every device plugs into the same brain.',
        code: '// An ESP32 switch that speaks the hub dialect\n//  state : home/kitchen/lamp/state   (retained)  {"on":true}\n//  cmd   : home/kitchen/lamp/cmd                {"on":true}\n\n// On boot: publish current state retained.\n// On cmd: act, then publish the new state (confirmation).\nvoid onCmd(const char* payload){\n  bool on = strstr(payload, "\\"on\\":true");\n  digitalWrite(RELAY, on);\n  mqtt.publish("home/kitchen/lamp/state", on ? "{\\\"on\\\":true}" : "{\\\"on\\\":false}", true);\n}',
        note: 'A hub (Home Assistant / Pi) translates protocols and runs automations. Make your devices speak MQTT with state/cmd topics + discovery.',
      },
      {
        title: 'Protocols in the Home: Zigbee, Z-Wave, BLE & WiFi',
        text: 'Different smart-home devices use different radios, and understanding the trade-offs tells you which to choose and how to integrate each. The big four:\n\n**Zigbee** — 2.4GHz mesh, low power, designed for home automation. Devices relay for each other (mesh), so coverage extends beyond router range. Best for battery sensors and bulbs. Requires a Zigbee coordinator (a USB dongle on the hub).\n\n**Z-Wave** — sub-GHz (868/915MHz) mesh, similar role to Zigbee but less congested (doesn\'t fight WiFi on 2.4GHz) and longer range. More common in the US/Europe professional installs.\n\n**Bluetooth/BLE** — short range, low power, ubiquitous in phones. Fine for wearables and beacons; BLE mesh exists but is newer. The ESP32\'s BLE lets it act as a BLE peripheral or central.\n\n**WiFi** — the universal and simplest for DIY: your ESP32 joins the existing network, no hub radio needed. Costs: higher power, and many WiFi devices can congest a router.\n\n**The integration reality:** a real smart home mixes them — Zigbee for battery sensors, WiFi for ESP32 devices and cameras, IR for legacy AC/TVs, BLE for trackers. The hub sits in the middle and translates all of them.\n\n**Protocol choice for YOUR DIY device:**\n- One ESP32 that must just work with your network → **WiFi + MQTT** (simplest).\n- A coin-cell sensor that must last months → **Zigbee/BLE** (but that means hub infrastructure).\n- Retrofit IR control of an old TV/AC → **IR transmitter** (ESP32 + IR LED).\n- Reading a smart plug or tracker → **BLE** (ESP32 as BLE central).\n\n**Interference note:** Zigbee, BLE, and WiFi all live in the 2.4GHz band — a busy WiFi network can degrade Zigbee. 5GHz WiFi and Z-Wave\'s sub-GHz avoid the pile-up.\n\nThe pragmatic path for most hobbyist smart-home builds: **WiFi ESP32 devices + MQTT into a local hub, plus a Zigbee dongle if you adopt Zigbee sensors.** Add radios as the device roster demands, not pre-emptively.',
        code: '// ESP32 as a BLE beacon (iBeacon-ish) for presence detection\n#include <BLEDevice.h>\n#include <BLEBeacon.h>\nvoid setup(){\n  BLEDevice::init("room-beacon");\n  BLEBeacon b;\n  b.setManufacturerId(0x004C);\n  b.setMajor(1); b.setMinor(7); b.setProximity(0xC5);\n  BLEAdvertising* adv = BLEDevice::getAdvertising();\n  adv->setScanResponse(false);\n  adv->setAdvertisementData(BLEAdvertisementData().setCompleteData(adv->getAdvertisementData().getPayload()));\n  BLEDevice::startAdvertising();\n}',
        note: 'Zigbee/Z-Wave mesh for battery sensors, WiFi for DIY ESP32, IR for legacy, BLE for trackers. The hub translates them all.',
      },
      {
        title: 'Voice Assistants, Apps & Ecosystem Integration',
        text: 'The finishing touch of a smart home is the **human interface**: voice assistants, mobile apps, and ecosystem integrations. The good news: you rarely write these from scratch — you **bridge** your device to existing platforms.\n\n**The integration patterns:**\n- **Voice assistant (Alexa, Google, HomeKit):** the hub (Home Assistant) exposes your MQTT device as a virtual switch/light/thermostat to the assistant ecosystem. You say "turn on kitchen lamp", the assistant tells the hub, the hub publishes MQTT, the ESP32 acts. You implement none of the voice side — you expose a standard entity.\n- **Mobile app:** Home Assistant/Node-RED provide apps; your device just needs the hub integration. For a standalone app, use the platform\'s REST/WebSocket API or build a tiny web UI on the ESP32 itself.\n- **IFTTT/Webhooks:** a rule that fires an HTTP request when something happens — glue between ecosystems.\n\n**The abstraction layer is the key:** bridge devices into a **standard entity model** — a switch has on/off, a light has brightness, a sensor has value. The assistant/app ecosystem understands entities; the hub maps your MQTT topics to entities. Design your device as a clean entity and every ecosystem can control it.\n\n**The MQTT Discovery convention** makes this automatic: publish a discovery payload `homeassistant/switch/kitchen_lamp/config` with the command/state topics, and the hub auto-creates the entity. No per-device configuration.\n\n**Practical pitfalls:**\n- **Cloud dependence** — if your bridge is cloud-only (Alexa), local control still works; voice stops during outages. Keep local automation on the hub.\n- **Latency** — voice → cloud → hub → MQTT → device adds ~1s. Acceptable for lights, not for safety.\n- **Naming** — name devices by room + function (kitchen_lamp), not serial numbers, so assistants understand you.\n- **State sync** — always publish state after acting (retained), so app/voice/hub agree with reality. A switch whose app says "on" but the relay is off is the #1 support complaint.\n\nThe pro pattern: **device speaks MQTT with clean state/cmd topics → hub auto-discovers it as an entity → assistants and apps control it through the hub.** Your work is the topic design and the state confirmation — the interfaces follow.',
        code: '// MQTT Discovery for Home Assistant (auto-adds a switch)\n// Publish to: homeassistant/switch/kitchen_lamp/config\n// {\n//   "name": "Kitchen Lamp",\n//   "command_topic": "home/kitchen/lamp/cmd",\n//   "state_topic": "home/kitchen/lamp/state",\n//   "payload_on": "{\\"on\\":true}",\n//   "payload_off": "{\\"on\\":false}",\n//   "unique_id": "esp32_kitchen_lamp"\n// }\n// Then it appears in the HA UI with zero config.',
        note: 'Expose your device as a standard entity via MQTT Discovery; hubs map it to assistants and apps. Always publish state back after acting.',
      },
      {
        title: 'Building a Home Automation Hub Project',
        text: 'Let us build the canonical capstone: a **home automation hub + ESP32 smart switch**, end-to-end, using only free software.\n\n**The stack:**\n- **Hub:** a Raspberry Pi (or any Linux box) running Home Assistant (or Node-RED + Mosquitto).\n- **Broker:** Mosquitto (MQTT) on the Pi.\n- **Devices:** ESP32 + relay(s), each publishing state and subscribing to commands.\n- **Interface:** Home Assistant dashboard + voice assistant bridge.\n\n**Step 1 — Bring up the broker.**\n```\nsudo apt install mosquitto mosquitto-clients\n# test:\nmosquitto_sub -t \'home/#\' -v\n```\n\n**Step 2 — The ESP32 smart switch.** Relay on a GPIO; firmware connects WiFi + MQTT, publishes `home/<room>/<device>/state` retained on every change, subscribes to `home/<room>/<device>/cmd`, and confirms by re-publishing state after acting. Add the MQTT Discovery config so the hub auto-adds it.\n\n**Step 3 — Configure Home Assistant.**\n- Install HAOS or Home Assistant Container on the Pi.\n- Add the **Mosquitto broker** integration (or point HA at your broker).\n- Watch your ESP32 appear automatically via MQTT Discovery.\n\n**Step 4 — Dashboard + automation.**\n- Build a dashboard with a switch card for the lamp.\n- Add an automation: "turn the lamp on at sunset" (sun sensor) or "if temperature > 30°C, turn on the fan". Automations live in the hub; the devices stay dumb.\n\n**Step 5 — Voice.** Connect HA to Alexa/Google; the lamp becomes voice-controllable with zero device changes.\n\n**Step 6 — Reliability & security.**\n- Reconnect loops in firmware; retained state so the UI is correct after reboots.\n- Broker authentication (per-device users), TLS if off-LAN.\n- Local-first: automations keep working during an internet outage.\n\n**What you learn by building it:** the hub pattern (protocol translation), MQTT topic design, entity discovery, automation logic, and the confirmation loop (state published after command). Every one of those skills transfers directly to industrial IoT — the same architecture, bigger hardware.\n\nThe hub project is the bridge from "I built a gadget" to "I built a system".',
        code: '// Firmware loop for the smart switch\nvoid loop(){\n  ensureWifi();\n  if (!mqtt.connected()) reconnect();\n  mqtt.loop();\n  bool cur = digitalRead(RELAY);\n  if (cur != lastState) {                 // publish state on every change\n    publishState(cur);                    // retained, so UI is always right\n    lastState = cur;\n  }\n  handleCommands();                       // callback-driven via mqtt.loop()\n}',
        note: 'Build the hub project: Pi (HA + Mosquitto) + ESP32 MQTT devices + discovery + automations + voice. Local-first and state-confirming.',
      },
    ],
    quizzes: [
      { text: 'Why do smart homes need a hub?', options: ['To make devices faster', 'Different devices speak different protocols; a hub translates', 'To charge batteries', 'To extend WiFi range only'], correctAnswer: 'Different devices speak different protocols; a hub translates' },
      { text: 'A coin-cell motion sensor that must last months is best on…', options: ['WiFi polling every second', 'Zigbee or BLE (low power)', 'Ethernet', 'USB power'], correctAnswer: 'Zigbee or BLE (low power)' },
      { text: 'Which band does Zigbee share with WiFi, causing possible interference?', options: ['5 GHz', '2.4 GHz', '900 MHz', '60 GHz'], correctAnswer: '2.4 GHz' },
      { text: 'How does a voice assistant actually control your ESP32 device?', options: ['It talks to the ESP32 directly via IR', 'It goes through the hub, which publishes MQTT', 'It reflashes the device', 'It cannot'], correctAnswer: 'It goes through the hub, which publishes MQTT' },
      { text: 'MQTT Discovery lets a hub…', options: ['Discover and auto-add a device as an entity with zero config', 'Encrypt all traffic', 'Update the firmware', 'Change the SSID'], correctAnswer: 'Discover and auto-add a device as an entity with zero config' },
      { text: 'The #1 smart-home support complaint is…', options: ['Slow WiFi', 'App and reality disagree on switch state', 'Batteries too big', 'Too many protocols'], correctAnswer: 'App and reality disagree on switch state' },
      { text: 'Why always publish state after acting on a command?', options: ['To use more bandwidth', 'So the app/voice/hub agree with the real relay position', 'To log it', 'To slow the loop'], correctAnswer: 'So the app/voice/hub agree with the real relay position' },
      { text: 'Local-first automations keep running…', options: ['Only with an internet connection', 'Even during an internet outage', 'Only at night', 'Never'], correctAnswer: 'Even during an internet outage' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 19 — Industrial IoT & Real-World Applications
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 19,
    title: 'Industrial IoT & Real-World Applications',
    description:
      'IIoT architecture, industrial protocols, predictive maintenance, and how the same skills scale to factories.',
    topics: [
      {
        title: 'What Makes IIoT Different from Consumer IoT',
        text: 'Industrial IoT (IIoT) is consumer IoT\'s serious older sibling: the same sensing-and-connecting skeleton, but with reliability, safety, and determinism requirements that change every design decision.\n\n**The defining differences:**\n- **Reliability.** A consumer smart bulb may drop off for a minute — nobody dies. A vibration monitor on a turbine must never miss an event; a factory line cannot tolerate random 2-second gaps. Requirements are often 99.9%+ uptime, which means redundant paths, deterministic networks, and rigorous testing.\n- **Latency & determinism.** Control loops (motor speed, valve position) need bounded latency in the milliseconds. "As fast as possible" is not a spec — the network must guarantee a maximum response time. This is why industrial plants use **wired, deterministic Ethernet (TSN, PROFINET)** alongside wireless.\n- **Safety.** Errors can injure people. Standards (IEC 61508, ISO 13849) govern functional safety — fail-safe behaviours, certified components, and verifiable logic.\n- **Environmental ruggedness.** Temperature, vibration, dust, humidity — industrial devices are housed and rated (IP ratings) for environments that would kill consumer hardware.\n- **Longevity.** Factories run 10-20+ year horizons; the electronics must be available and supportable that long (no trendy one-off chips).\n- **Security.** A hacked consumer camera is annoying; a hacked factory controller is a safety and supply-chain catastrophe. IIoT security is enforced by policy and standards (IEC 62443).\n\n**What stays the same:** the architecture. Sensors → connectivity → edge/cloud → dashboards → control is identical to the home version, just hardened and certified. The skills you learned (MQTT, telemetry, dashboards, security) are exactly the skills IIoT needs — the scale and stakes are larger.\n\nThe framing shift: **consumer IoT optimises for cost and convenience; IIoT optimises for availability, safety, and predictability.** Design for the requirement, not the gadget.',
        code: '// IIoT reliability in firmware = redundancy + health\n//   [x] Reconnect loops (WiFi + MQTT) with backoff\n//   [x] Watchdog on the main loop\n//   [x] Telemetry buffer for offline periods\n//   [x] Health/heartbeat topic ("alive", RSSI, free RAM)\n//   [x] Deterministic sampling with timers, not delay()\n//\n// A turbine monitor that misses one event is worthless.',
        note: 'IIoT = consumer architecture, hardened: determinism, safety, redundancy, ruggedness, longevity, and enforced security.',
      },
      {
        title: 'Industrial Protocols: Modbus, OPC-UA & MQTT',
        text: 'Industrial machinery speaks its own languages, and connecting IIoT means translating between the factory floor and the IT cloud. Three protocols cover most of the ground.\n\n**Modbus** — the 1979-vintage workhorse still everywhere: simple master/slave over serial (RTU) or TCP. Registers hold values; the master polls slaves. Cheap, ubiquitous, dead simple — PLCs, meters, and drives expose Modbus. Its limits: no encryption (security must come from the network), modest speed, no pub/sub.\n\n**OPC-UA** — the modern industrial standard: platform-independent, **secure by default** (TLS, certificates), and information-rich (models data as structured objects, not raw registers). Used where manufacturing systems need robust, audited integration. Heavier than MQTT — often run on gateways, not on a 520KB MCU.\n\n**MQTT** — crosses the IT/OT boundary: industrial gateways subscribe to plant data and publish MQTT to the cloud side. Sparkplug B is a spec that standardises MQTT for industrial telemetry (a named payload format with state, data, and command semantics).\n\n**The translation pattern:**\n\n```\n[PLC/sensor: Modbus] -> [gateway reads registers]\n      -> [gateway publishes MQTT: plant/line1/pressure]\n      -> [cloud / dashboards / analytics]\n      -> [commands back via MQTT] -> [gateway writes Modbus register]\n```\n\nThe **gateway** is the translator — it owns the Modbus polling, decodes the registers, and re-exposes everything as clean MQTT topics. This keeps the legacy machinery untouched while modernising the data plane.\n\n**Choosing:**\n- Legacy equipment already on Modbus → keep Modbus at the edge, translate up.\n- New industrial integration with security/audit → **OPC-UA**.\n- Telemetry up to the cloud → **MQTT (Sparkplug B)**.\n- Most DIY/small-scale → **MQTT end to end**.\n\nThe mental model: **Modbus is the factory floor, OPC-UA is the enterprise bus, MQTT is the cloud pipe.** A gateway bridges them, and your IoT skills (MQTT, telemetry, dashboards) cover the top of that stack.',
        code: '// Gateway concept: poll Modbus register -> publish MQTT\n//  (Python sketch)\n//   reg = modbus.read_holding(1, 40001)      # pressure value\n//   mqtt.publish("plant/line1/pressure", str(reg / 10.0))\n//\n//  Commands reverse: MQTT cmd -> write Modbus register',
        note: 'Modbus at the factory edge, OPC-UA for enterprise integration, MQTT to the cloud. Gateways translate between them.',
      },
      {
        title: 'Predictive Maintenance & Edge Analytics',
        text: 'The highest-value IIoT use case is **predictive maintenance**: using sensor data to predict failures *before* they happen, instead of fixing breakdowns after. It is where IoT data turns directly into money.\n\n**The maintenance ladder:**\n1. **Reactive** — fix when it breaks (most expensive: unplanned downtime).\n2. **Preventive** — service on a fixed schedule (cheaper, but wastes good parts and still misses failures).\n3. **Predictive** — monitor condition, predict failure, service exactly when needed. The IIoT sweet spot.\n4. **Prescriptive** — the system recommends and even schedules the optimal action.\n\n**What the sensors see:**\n- **Vibration** — bearings degrade with characteristic vibration signatures; imbalance and misalignment change spectra. An accelerometer on the motor housing, sampled at kHz, is the classic predictor.\n- **Temperature** — rising motor/bearing temperature predicts trouble.\n- **Current draw** — a pump drawing more current as its impeller wears.\n- **Acoustics/ultrasonics** — failing bearings emit ultrasound before audible noise.\n\n**The pipeline:**\n\n```\nsensors (vibration/temp/current) -> edge device\n  -> feature extraction (RMS, FFT peaks, trends)\n  -> [edge model or cloud model]\n  -> health score + alert when trend crosses threshold\n```\n\n**Edge vs cloud for analytics:**\n- **Edge:** compute RMS/FFT on the device or gateway — cheap, immediate, works offline. Sends only features (small) instead of raw waveforms (huge).\n- **Cloud:** train ML models on fleet data, fine-tune thresholds, and push updated models back down.\n\n**The practical model without ML:** baseline the healthy signature (e.g. RMS vibration at install), then alert when the metric drifts beyond a bound (RMS up 30% = bearing wear). This simple baseline+drift approach catches most real failures and is implementable on an ESP32-class device with a basic FFT.\n\n**Why it pays:** unplanned downtime costs thousands per hour; scheduled-but-unneeded maintenance wastes labour and parts. Predicting the *right* moment saves both. That is why predictive maintenance is the #1 IIoT ROI story.\n\nThe rule: **measure the right signal (vibration/temp/current), baseline healthy, track drift, alert early.** ML comes later — the baseline+drift first.',
        code: '// Edge vibration health: RMS drift vs baseline\nfloat baseline = 0.35f;      // RMS g, measured when healthy\nfloat rms = compute_rms(accel_samples, N);\nfloat drift = (rms - baseline) / baseline;\nif (drift > 0.3f) alert("bearing wear suspected", drift);\n// 30% drift over healthy = typical bearing-wear warning',
        note: 'Predictive maintenance: baseline the healthy signature, track drift (vibration/temp/current), alert early. Edge does features, cloud does ML.',
      },
      {
        title: 'Digital Twins, SCADA & the Industrial Stack',
        text: 'Beyond individual sensors, industry manages plants through systems: **SCADA** for real-time control, **digital twins** for simulation, and the modern edge/cloud stack that ties them together.\n\n**SCADA** (Supervisory Control and Data Acquisition) is the classic plant-control system: PLCs control machinery, a SCADA layer supervises, and operators watch HMI screens and set points. It is deterministic, certified, and has run factories for decades. Modern SCADA is being connected to IoT data platforms rather than replaced.\n\n**Digital twin** — a **living digital model of a physical asset**: its geometry, behaviour, and real-time sensor feed. You can simulate a valve\'s behaviour, test a new setpoint, or predict fatigue in software without touching the physical machine. Twins range from simple "simulate the physics with live input" to full 3D + ML models.\n\n**The industrial data stack (modern):**\n\n```\nPLC/RTU -> OPC-UA/Modbus -> [gateway]\n   -> MQTT/Timeseries DB (cloud)\n   -> analytics / ML / digital twin\n   -> dashboards / maintenance apps / ERP\n```\n\n**Edge processing** keeps time-critical loops on the floor (a valve response cannot wait for a cloud round trip), while the cloud handles the big-data analytics, fleet-wide learning, and business integration.\n\n**Where your IoT skills fit:** the gateway, MQTT telemetry, TSDB, dashboards, and alerting you already built ARE the top half of the industrial stack. The industrial layer adds certified hardware, deterministic control (PLC), and strict standards — but the data path you know carries the information.\n\n**The career-shaping insight:** "IoT for a factory" is not a different discipline — it is the same architecture with higher stakes and standards. Engineers who master MQTT/telemetry/dashboards/security on an ESP32 are exactly the engineers factories hire to modernise their data plane.\n\nThe whole stack in one sentence: **PLC runs the process, SCADA supervises, edge handles the real-time, the cloud stores and learns, and a digital twin simulates — all connected by the data pipeline you already know.**',
        code: '// Industrial stack, top to bottom\n//   ERP / business systems\n//   Cloud: TSDB + ML + digital twin\n//   MQTT / OPC-UA (data plane)\n//   Gateways (protocol translation + edge compute)\n//   PLC / RTU / field sensors   <- real-time control here\n//\n// Your IoT skills cover the MQTT->cloud->dashboard tier.',
        note: 'SCADA supervises, digital twins simulate, edge handles real-time, cloud learns. Same data pipeline you know, higher stakes.',
      },
    ],
    quizzes: [
      { text: 'The defining requirement that separates IIoT from consumer IoT is…', options: ['Cheaper sensors', 'Reliability, determinism, and safety', 'Smaller batteries', 'More LEDs'], correctAnswer: 'Reliability, determinism, and safety' },
      { text: 'Which protocol is the "factory floor" workhorse?', options: ['HTTP', 'Modbus', 'SMTP', 'DNS'], correctAnswer: 'Modbus' },
      { text: 'Which industrial standard is secure by default (TLS + certificates)?', options: ['Modbus RTU', 'OPC-UA', 'Plain MQTT', 'HTTP'], correctAnswer: 'OPC-UA' },
      { text: 'The best IIoT ROI use case is…', options: ['Remote LED control', 'Predictive maintenance', 'Faster WiFi', 'Prettier dashboards'], correctAnswer: 'Predictive maintenance' },
      { text: 'A bearing wearing out is best predicted by monitoring…', options: ['Its color', 'Vibration signature drift', 'Its serial number', 'The WiFi RSSI'], correctAnswer: 'Vibration signature drift' },
      { text: 'Why compute features (RMS/FFT) at the edge instead of streaming raw waveforms?', options: ['It is required by law', 'It sends small features instead of huge raw data and works offline', 'Edge is always slower', 'Cloud cannot compute'], correctAnswer: 'It sends small features instead of huge raw data and works offline' },
      { text: 'A digital twin is…', options: ['A second backup device', 'A living digital model of a physical asset fed by real data', 'A SCADA screen', 'A robot arm'], correctAnswer: 'A living digital model of a physical asset fed by real data' },
      { text: 'The gateway in an industrial stack mainly does…', options: ['Charging batteries', 'Protocol translation + edge compute', 'Running the ERP', 'Cooling the PLC'], correctAnswer: 'Protocol translation + edge compute' },
    ],
  },
  // ──────────────────────────────────────────────────────────────────────────
  // WEEK 20 — Building a Complete IoT Project + Certification
  // ──────────────────────────────────────────────────────────────────────────
  {
    week: 20,
    title: 'Building a Complete IoT Project + Certification',
    description:
      'From requirements to deployment — planning, hardware, firmware, cloud, documentation, and the final exam.',
    topics: [
      {
        title: 'From Requirements to a Working System',
        text: 'The capstone is not "connect a sensor to WiFi" — it is **engineering a complete system**: requirements, architecture, build, and verification. A structured start saves weeks of rework.\n\n**1. Write the requirements first.** One paragraph: what does the system DO, for whom, and when? Then make it testable:\n- "Monitors temperature of 3 fridge racks" → measurable: reports every 30s, within ±0.5°C.\n- "Alerts when too hot" → testable: sends an alert within 60s of crossing 8°C.\n- "Runs 30 days on battery" → testable: power budget measured.\n\n**2. Choose the architecture from requirements.** Requirements → radio (range/power), compute (edge/cloud split), storage, dashboard, security. Draw the four-layer diagram before writing code — it is the map that stops you getting lost.\n\n**3. Build in vertical slices, not horizontal piles.** Do NOT build "all the sensing", then "all the networking", then "all the UI". Build one end-to-end slice: **one sensor → one MQTT message → one dashboard reading**. Then a second sensor. Then the alert. Each slice proves the whole path works; the system grows by stacking verified slices.\n\n**4. Prototype, then harden.** A breadboard proof-of-concept is fine for validation; then move to a proper PCB/connection plan, add the watchdog, reconnects, and error handling. Hardening is a distinct phase, not an afterthought.\n\n**5. Verify against the requirements.** At the end, walk each requirement and prove it: measure the accuracy, time the alert, log 48h of uptime. The requirements are the test plan.\n\n**6. Document as you go** — a README with architecture diagram, wiring, API/topic schema, and how to run it. Future-you (and graders) will thank you.\n\nThe habit to internalise: **requirements → architecture → vertical slices → prototype → harden → verify.** Skipping the first two is where projects die; following them is where projects ship.',
        code: '// The capstone plan (fill in before coding)\n//   Goal     : <one sentence>\n//   Users    : <who>\n//   Slices   : 1) sense->MQTT->dashboard  2) alert  3) offline buffer\n//   Radio    : <WiFi/LoRa/BLE, why>\n//   Edge/cloud split : <what runs where, why>\n//   Security : <TLS, per-device keys, secure boot>\n//   Verify   : <each requirement, how proven>',
        note: 'Requirements first, architecture second, then vertical end-to-end slices. Prototype, harden, verify against the requirements, document.',
      },
      {
        title: 'Hardware Assembly & Wiring Best Practices',
        text: 'A stable system is built on stable hardware. Sloppy wiring causes the "works on the bench, fails in the field" classic. These habits eliminate most of it.\n\n**Power first.** A brown-out is the #1 intermittent-failure cause. Give the MCU a clean supply: a proper regulator (not a noisy USB rail), decoupling (100nF at the MCU, 10-100µF at the supply), and **separate supplies for loads** (motors, relays, servos) — they share a common ground with the MCU but not its power rail. A relay clicking should never dip your 3.3V.\n\n**Wiring discipline:**\n- **Color-code** and label: red = power, black = ground, distinct colors per bus. Labels pay off when you debug at 2am.\n- **Short, direct runs** for signals; twist power/ground pairs; keep signal wires away from motor/relay lines.\n- **Common ground for everything** — MCU, loads, sensors — but keep the high-current ground path physically separate from the signal ground path (star grounding).\n\n**Connectors & strain relief.** Breadboard jumpers are for prototypes. For anything that moves, use crimped connectors (DuPont/JST), add strain relief, and secure wires so a tug cannot pull a pin. A connector that survives 1000 flexes is worth the extra hour.\n\n**ESD & protection.** Touch the ground rail before touching chips; add a reverse-polarity diode and a fuse at the power input. A TVS diode on exposed lines (buttons, external sensors) absorbs ESD spikes that would otherwise reset or kill the MCU.\n\n**Test points & status LEDs.** Leave test points on the board (a header for GND + key signals) and a status LED with a heartbeat pattern. When the field calls "it\'s dead", the LED tells you if it\'s power, firmware, or network — in seconds.\n\n**Bring-up checklist:** power rails correct before the chip → decoupling present → all grounds common → loads on separate supply → connectors strain-relieved → protection in place → test points and heartbeat LED.\n\nThe rule: **power is the foundation, grounds are the return path, and a heartbeat LED is your first field instrument.** Get those three right and the field failures mostly vanish.',
        code: '// Power & wiring checklist\n//   [ ] Regulated MCU supply + 100nF decoupling\n//   [ ] Loads (motors/relays) on separate supply, shared ground\n//   [ ] Common ground everywhere, star the high-current paths\n//   [ ] Reverse-polarity diode + fuse at power input\n//   [ ] TVS on exposed lines\n//   [ ] Strain relief on all connectors\n//   [ ] Heartbeat LED + test points\n//   [ ] Color-coded, labelled wiring',
        note: 'Power is the foundation: clean regulated supply, decoupled, loads on their own rail, common ground, protection, and a heartbeat LED.',
      },
      {
        title: 'Firmware Architecture for a Complete Project',
        text: 'A capstone firmware is a small software system — and like any system, it needs structure. This architecture scales from one sensor to a fleet.\n\n**The module layout (one file per concern):**\n\n```\nmain.cpp       — setup/loop, wires modules together\nsensors.cpp    — reads + calibrates + filters\nnetwork.cpp    — WiFi connect/reconnect, mDNS\nmqtt.cpp       — topics, publish, callbacks\nstorage.cpp    — NVS config, flash/SD buffers\ndashboard.cpp  — web UI / RPC handling\nconfig.h       — pins, topics, constants\n```\n\n**The design rules:**\n- **No business logic in loop().** loop() is a scheduler: it pumps network, checks timers, delegates to modules. The "when to read, when to send" decisions live in clear functions.\n- **Use a state machine** for the device lifecycle: `BOOT → PROVISIONING → CONNECTING → RUNNING → SLEEP → ERROR`. Each state has entry logic, and transitions are explicit. A state machine makes "what happens if WiFi drops mid-publish" a question you can answer.\n- **Timer-driven, not delay-driven.** Replace `delay()` with `millis()`-based scheduling: `if (millis() - lastRead > interval) read()`. Non-blocking = your loop never freezes, watchdog never trips, and multiple periodic tasks coexist.\n- **Error handling with intent.** Define what each failure means and do: MQTT down → buffer to flash; sensor fail → publish `null` + log, don\'t crash; config corrupt → enter provisioning, don\'t brick.\n- **Config via config.h + NVS.** Pins and constants in one header; runtime-editable settings (interval, thresholds) in NVS so field changes need no reflash.\n- **Log with levels.** `LOG_I`, `LOG_W`, `LOG_E` macros that compile out in release.\n\n**The main loop (the shape you already know, made rigorous):**\n\n```cpp\nvoid loop() {\n  net.handle();        // reconnect if needed\n  mqtt.loop();         // pump callbacks\n  if (due(readTimer))  readSensors();\n  if (due(sendTimer))  publish();\n  heartBeat();\n  watchdog_feed();\n}\n```\n\nThe payoff: you can reason about the whole system, add features without breaking others, and debug by reading logs instead of sprinkling print statements. That is the difference between firmware and a program.',
        code: '// Non-blocking scheduling (no delay(), no hangs)\nunsigned long lastRead = 0;\nconst unsigned long READ_MS = 5000;\nvoid loop(){\n  if (millis() - lastRead >= READ_MS) {\n    readSensors();       // blocks only briefly, then returns\n    lastRead = millis();\n  }\n  mqtt.loop();           // network pumping is also non-blocking\n}',
        note: 'Structure firmware by module, drive it by a state machine, schedule with timers not delay(), and handle errors with intent.',
      },
      {
        title: 'Testing, Documentation & Shipping Your IoT Project',
        text: 'The final stretch is where good projects become great ones: **testing, documentation, and a clean handoff.** This is also what the certification evaluates.\n\n**Testing layers:**\n- **Unit** — test pure logic (calibration math, state transitions, JSON parsing) in isolation, even on your PC, with a test framework. Fast feedback, no hardware needed.\n- **Integration** — the device + broker + dashboard end to end: publish a known value, confirm it appears in the DB and UI.\n- **Field/soak** — run the real device for 48-72h and watch: memory leaks, reconnects, battery life, missed readings. Log uptime and free heap. This is where intermittent bugs surface.\n- **Failure drills** — deliberately kill WiFi, power, and the broker; verify recovery. A system that survives a router reboot is a system you can trust.\n\n**The test plan from requirements:** each requirement becomes a test ("reports every 30s ±0.5°C" → a test that checks interval and accuracy over an hour). Pass the plan and you have, by definition, met the requirements.\n\n**Documentation (the habit):**\n- **README:** what it does, architecture diagram, wiring, topic schema, how to run broker/device/dashboard, troubleshooting.\n- **Hardware:** BOM (bill of materials), pinout table, photos.\n- **Software:** how to build/flash, config options, known limitations.\n- **API/topics:** every MQTT topic and JSON field documented.\n\n**Shipping checklist:**\n- Enclosure + power safety (no exposed mains).\n- Firmware with auto-reconnect, watchdog, signed OTA ready.\n- Credentials provisioned per device, not hardcoded.\n- Backup of config and code.\n- A demo script: 3-minute walkthrough that proves each requirement.\n\n**For the certification:** present the system as an engineer would: requirements → architecture → what you built → how you verified it → what you would improve. Show the failures you found and fixed — that is the strongest evidence of real engineering.\n\nThe closing rule: **a project is not done when it works once; it is done when it works reliably, is documented, and someone else can run it.** Ship that and you are no longer building gadgets — you are building systems.',
        code: '// Soak-test summary (what to record over 48h)\n//   Uptime            : 48h 13m\n//   Free heap         : min 184 KB (no leak)\n//   Reconnects        : 3 (all recovered)\n//   Missed readings   : 0\n//   Battery (if any)  : 3.28V -> 3.19V (est 32 days)\n//   Alerts triggered  : 2 (both correct)',
        note: 'Test in layers (unit/integration/soak/failure drills), document for the next engineer, and prove every requirement. Ship a system, not a gadget.',
      },
    ],
    quizzes: [
      { text: 'The recommended build order for a capstone system is…', options: ['All hardware, then all software', 'Vertical end-to-end slices that each prove the full path', 'Only the dashboard first', 'Random components'], correctAnswer: 'Vertical end-to-end slices that each prove the full path' },
      { text: 'The #1 cause of intermittent "works on the bench, fails in the field" is…', options: ['Wrong WiFi password', 'Power brown-outs from loads sharing the MCU rail', 'Too many LEDs', 'Slow MQTT'], correctAnswer: 'Power brown-outs from loads sharing the MCU rail' },
      { text: 'Why keep loads (motors/relays) on a separate supply from the MCU?', options: ['They need more pins', 'A relay click must not dip the MCU voltage', 'It is cheaper', 'It is required by MQTT'], correctAnswer: 'A relay click must not dip the MCU voltage' },
      { text: 'Replacing `delay()` with `millis()`-based scheduling ensures…', options: ['Faster boot', 'A non-blocking loop that never freezes or trips the watchdog', 'Lower power always', 'Bigger logs'], correctAnswer: 'A non-blocking loop that never freezes or trips the watchdog' },
      { text: 'A device state machine helps you reason about…', options: ['The color scheme', 'What happens in every lifecycle state (e.g. WiFi drops mid-publish)', 'The PCB layout', 'The battery chemistry'], correctAnswer: 'What happens in every lifecycle state (e.g. WiFi drops mid-publish)' },
      { text: 'A soak test of 48 hours is designed to catch…', options: ['Syntax errors', 'Intermittent bugs: leaks, reconnects, missed readings', 'Wrong resistor values', 'Broker pricing'], correctAnswer: 'Intermittent bugs: leaks, reconnects, missed readings' },
      { text: 'Failure drills deliberately test…', options: ['New features', 'Recovery after killing WiFi, power, and broker', 'Dashboard aesthetics', 'Battery size'], correctAnswer: 'Recovery after killing WiFi, power, and broker' },
      { text: 'A project is "done" when…', options: ['It works once', 'It works reliably, is documented, and someone else can run it', 'The demo video is posted', 'The LED is on'], correctAnswer: 'It works reliably, is documented, and someone else can run it' },
    ],
  },
];

// ──────────────────────────────────────────────────────────────────────────────
// IoT FINAL EXAM — 18 distinct questions replacing the near-identical templates
// ──────────────────────────────────────────────────────────────────────────────
export const iotFinalExam: IotFinalExamQuestion[] = [
  { text: 'An IoT device performs: sense → process → connect → act. Which pair is the physical-world interface?', options: ['Sense and act', 'Process and connect', 'Connect and sense only', 'Process only'], correctAnswer: 'Sense and act' },
  { text: 'Which computing tier keeps time-critical logic working even when the internet is down?', options: ['Cloud', 'Edge', 'Fog only', 'The dashboard'], correctAnswer: 'Edge' },
  { text: 'A raw analog sensor reading is 2048 on a 12-bit ADC with a 3.3V reference. The pin voltage is…', options: ['3.3V', '1.65V', '0.8V', '2.5V'], correctAnswer: '1.65V' },
  { text: 'Which wiring is correct for UART?', options: ['TX to TX, RX to RX', 'TX to RX, RX to TX, shared ground', 'SCL to SCL, SDA to SDA', 'MOSI to MISO only'], correctAnswer: 'TX to RX, RX to TX, shared ground' },
  { text: 'An I2C scan finds no devices. The two most likely causes are…', options: ['Wrong baud rate and wrong voltage', 'Missing pull-ups and wrong pins', 'Too much flash and too little RAM', 'No LCD and no camera'], correctAnswer: 'Missing pull-ups and wrong pins' },
  { text: 'SPI is preferred over I2C when you need…', options: ['Many devices on two wires', 'High data rates (display, SD card)', 'Automatic device discovery', 'The lowest pin count'], correctAnswer: 'High data rates (display, SD card)' },
  { text: 'Which transport protocol should carry a real-time sensor stream where a dropped sample is acceptable?', options: ['TCP', 'UDP', 'FTP', 'SMTP'], correctAnswer: 'UDP' },
  { text: 'Why must firmware check `WiFi.isConnected()` before every network operation?', options: ['To save power', 'The link can drop; a check prevents failures and enables reconnect', 'It is required by law', 'To count bytes'], correctAnswer: 'The link can drop; a check prevents failures and enables reconnect' },
  { text: 'A REST POST returns 401. That means…', options: ['Success', 'Authentication failed', 'Resource not found', 'Server crashed'], correctAnswer: 'Authentication failed' },
  { text: 'In MQTT, `home/+/temp` subscribes to…', options: ['Exactly one topic', 'Temperature topics of every room at one level', 'Everything under home', 'Nothing'], correctAnswer: 'Temperature topics of every room at one level' },
  { text: 'QoS 1 guarantees…', options: ['Exactly once delivery', 'At-least-once delivery (may duplicate)', 'Fire-and-forget', 'No delivery'], correctAnswer: 'At-least-once delivery (may duplicate)' },
  { text: 'A retained message with the LWT pattern is used to…', options: ['Reduce bandwidth', 'Expose device presence/state to new subscribers and announce death', 'Encrypt traffic', 'Speed up boot'], correctAnswer: 'Expose device presence/state to new subscribers and announce death' },
  { text: 'Which is the strongest practical authentication for production cloud IoT?', options: ['A shared token', 'Per-device X.509 certificates via mutual TLS', 'The MAC address', 'None'], correctAnswer: 'Per-device X.509 certificates via mutual TLS' },
  { text: 'Why is `setInsecure()` on a TLS client unacceptable in production?', options: ['It is slow', 'It disables server verification, letting a MITM impersonate the server', 'It uses more RAM', 'It breaks the baud rate'], correctAnswer: 'It disables server verification, letting a MITM impersonate the server' },
  { text: 'A battery sensor node lasts longest by…', options: ['Polling WiFi constantly', 'Measuring, publishing, then deep sleeping', 'Using 5V logic', 'Leaving the LED on'], correctAnswer: 'Measuring, publishing, then deep sleeping' },
  { text: 'In IIoT, the factory-floor legacy protocol is usually…', options: ['Modbus', 'SMTP', 'DNS', 'WebSocket'], correctAnswer: 'Modbus' },
  { text: 'Predictive maintenance catches failures by monitoring…', options: ['The device color', 'Drift in vibration/temperature/current signatures from a healthy baseline', 'WiFi RSSI', 'The CPU serial number'], correctAnswer: 'Drift in vibration/temperature/current signatures from a healthy baseline' },
  { text: 'The capstone build order that avoids most rework is…', options: ['All hardware then all software', 'Requirements → architecture → vertical end-to-end slices', 'Dashboard first, then everything else', 'Random experimentation'], correctAnswer: 'Requirements → architecture → vertical end-to-end slices' },
];
