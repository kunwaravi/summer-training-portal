# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.

# --- API ---
VITE_API_URL=http://localhost:5000/api

# --- UPI payment (EnrollmentPanel) ---
# Production: set these to the live UPI ID / payee shown on the QR code.
VITE_UPI_ID=edunexuss@ptyes
VITE_UPI_PAYEE=Anjali Singh
